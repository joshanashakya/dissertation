import java.util.Arrays;import java.util.Collections;import java.util.HashSet;import java.util.LinkedList;import java.util.Queue;import java.util.Scanner;import java.util.Set; /** * * @author imac */public class NewMain2 {     /**     * @param args the command line arguments     */    public static void main(String[] args) {        // TODO code application logic here         Scanner s = new Scanner(System.in);                String str = s.next();                int n = str.length();                Queue<Character> q = new LinkedList<>();                for(char x : str.toCharArray()){            q.add(x);        }                Set<String> set = new HashSet<>();                for (int i = 0; i < n; i++) {            q.add(q.poll());            set.add(q.toString());        }         System.out.println(set.size());     } }
