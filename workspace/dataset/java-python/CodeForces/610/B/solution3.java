import java.util.ArrayList;import java.util.Scanner; public class SquarePainting {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        long[] a = new long[n];        int min = 0;         for (int i = 0; i < n; i++) {            a[i] = scanner.nextLong();            if(a[min] > a[i]) min = i;        }         ArrayList<Integer> minimum = new ArrayList<>();        for (int i = 0; i < n; i++) {            if(a[i] == a[min]){                minimum.add(i);            }        }         if(minimum.size() == 1){            System.out.println(n*a[min] + n-1);        }else {            int max = n - minimum.get(minimum.size()-1) + minimum.get(0);            for (int i = 0; i < minimum.size() - 1; i++) {                int diff = minimum.get(i+1) - minimum.get(i);                 if(max < diff) max = diff;            }            System.out.println(n*a[min] + max-1);        }    }}
