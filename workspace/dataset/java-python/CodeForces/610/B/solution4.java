import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(double a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            double temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }     public static void main(String[] args) throws IOException    {         Scanner input = new Scanner(System.in);        int n = input.nextInt();        long a[] = new long[n];        long ans=0;        long min = Long.MAX_VALUE;               for (int i = 0; i < n; i++) {            a[i] = input.nextLong();            if (min > a[i]) {                min = a[i];                           }        }        long highestRun = 0;        long run = 0;        for (int i = 0; i < n*2; i++) {                        if(a[i%n]==min)            {                                highestRun =Math.max(highestRun, run);                run=0;            }            else            {                run++;            }        }                       System.out.println(min*n+highestRun);     } }
