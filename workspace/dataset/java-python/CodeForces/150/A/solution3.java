import java.io.*;import java.util.ArrayList; public class Main {	static PrintWriter pr = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));	public static void main(String[] args) throws IOException {		long q = input.nextLong();		if (q == 1 || isPrime(q)){			pr.print("1\n" + 0);			pr.flush();			return;		}		ArrayList list = new ArrayList();		for (long i=2;i*i<=q;i++){			while (q%i == 0){				list.add(i);				q /= i;			}			if (list.size()>=2)				break;		}		if (q > 1)	list.add(q);		if (list.size() == 2){			pr.println(2);		}else{			pr.print("1\n" + Long.valueOf(String.valueOf(list.get(0)))*Long.valueOf(String.valueOf(list.get(1))));		}		pr.flush();	}	public static boolean isPrime(long x){		if (x <= 3){			return x > 1;		}		for (long i=2;i*i<=x;i++){			if (x%i == 0){				return false;			}		}		return true;	}		static class input{		static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		static StreamTokenizer in = new StreamTokenizer(br);		static String next() throws IOException{			return br.readLine();		}		static int nextInt() throws IOException{			in.nextToken();			return (int)in.nval;		}		static long nextLong() throws IOException{			in.nextToken();			return (long)in.nval;		}		static double nextDouble() throws IOException{			in.nextToken();			return in.nval;		}	}}  		 		   		  	 	 			 					   		
