import java.util.Scanner;public class Main {      public static void main(String[] args) {         Scanner s = new Scanner(System.in);        int n = Integer.parseInt(s.nextLine().trim());         int[] l = new int[n];         String[] st = s.nextLine().split(" ");        for(int i =0 ; i <n; i ++){            l[i]=Integer.parseInt(st[i]);        }         l=sort(l);         if(l[l.length-1]<=25)            System.out.println("0");        else{            System.out.println(l[l.length-1]-25);        }     }     public static int[] sort(int[] arr){         boolean fixed= false;         while(fixed == false){            fixed = true;            for(int i =0 ; i < arr.length-1 ; i++){                int temp =0;                if(arr[i]> arr[i+1]){                    temp = arr[i+1];                    arr[i+1] = arr[i];                    arr[i] = temp;                    fixed = false;                }            }        }         return arr;    } }
