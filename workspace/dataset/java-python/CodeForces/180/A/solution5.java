import java.util.ArrayList;import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner scan = new Scanner(System.in);        int n = scan.nextInt();        int m = scan.nextInt();        int empty = n + 1;        int[] cluster = new int[n];        ArrayList<String> ans = new ArrayList<>();        for (int i = 0; i < cluster.length; i++) {            cluster[i] = empty;        }        int counter = 0;        for (int i = 0; i < m; i++) {            int numClusters = scan.nextInt();            for (int j = 0; j < numClusters; j++) {                cluster[scan.nextInt() - 1] = counter++;            }        }//        System.out.println(Arrays.toString(cluster));        for (int i = 0; i < cluster.length; i++) {            if (cluster[i] != i) {                if (cluster[i] != empty) {                    for (int j = 0; j < cluster.length; j++) {                        if (cluster[j] == empty) {                            cluster[j] = cluster[i];                            ans.add(i+1 + " " + (j+1));                            break;                        }                    }                }                for (int j = i+1; j < cluster.length; j++) {                    if (cluster[j]==i) {                        cluster[i] = cluster[j];                        cluster[j] = empty;                        ans.add(j+1 + " " + (i+1));                    }                }            }        }        System.out.println(ans.size());        for (String s: ans) {            System.out.println(s);        }     }}
