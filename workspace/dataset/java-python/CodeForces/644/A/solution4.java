import java.util.*; public class Main {      public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int n = in.nextInt();        int rows = in.nextInt();        int cols = in.nextInt();         int cnt = 1;        if (n > rows * cols)            System.out.println(-1);        else            for (int r = 0; r < rows; r++)            {                String s = "";                for (int c = 0; c < cols; c++)                    if (r % 2 == 0)                    {                        if (cnt <= n)                            s += (cnt++ + " ");                        else                            s += "0 ";                    }                    else                    {                        if (cnt <= n)                            s = (cnt++ + " ") + s;                        else                            s = "0 " + s;                    }                System.out.println(s);            }     }  }
