import java.util.*;import static java.lang.Integer.*; public class Main {    public static Scanner scanner = new Scanner(System.in);    public static void main(String[] args) {        int a, b, c;        int ans;        a= scanner.nextInt();b= scanner.nextInt();c=scanner.nextInt();        ans = (a / 3) + (b / 3) + (c / 3);        for (int i = 0; i < min(3, min(a, min(b, c))); i++) {            int nw = (a - i) / 3 + (b - i) / 3 + (c - i) / 3 + i;            ans = max(ans, nw);        }        System.out.println(ans);    }}
