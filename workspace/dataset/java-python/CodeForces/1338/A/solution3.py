from math import logfor _ in range(int(input())):    n = int(input())    a = [int(x) for x in input().split()]    #b = [x ** 2 for x in range(100000)]    mx = 0    #mn = 100000000    #ans = 0    maxdif = 0    i = 1    start = a[0]    while i < n:        start = a[i - 1]        if a[i - 1] > a[i]:            #print(f'from {a[i - 1]}')            while i < n and start > a[i]:                maxdif = max(maxdif, start - a[i])                i += 1            #print(f'to {a[i - 1]}, maxdif = {maxdif}')        else:            i += 1      print(len(bin(maxdif)) - 2 if maxdif > 0 else 0)              #maxdif = max(a[i - 1] - a[i], maxdif)    #for i in range(10000000):    #    if 2 ** (i - 1) >= maxdif:    #        print(i)    #        break    #x = 0    #i = 0    #while x < maxdif:    #    x += 2 ** i    #    i += 1    #print(i)            #for j in range(len(b)):                #if a[i - 1] <= a[i] + 2 ** b[j]:                    #a[i] += 2 ** b[j]                    #mx = max(mx, j)                    #mn = min(mn, j)                    #b[j] = 0                    #break    #print(max(0, mx - mn))    #print(mx)    #print(a)
