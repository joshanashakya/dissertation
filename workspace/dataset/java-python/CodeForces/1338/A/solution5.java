import java.util.Scanner; public class Solution {    public static void main(String[] args) {        Scanner input = new Scanner(System.in);        int totalCases = input.nextInt();         for (int caseNumber = 1; caseNumber <= totalCases; caseNumber++) {            int arrayLength = input.nextInt();            int previousElement = Integer.MIN_VALUE;            int maximumDifference = 0;             for (int index = 0; index < arrayLength; index++) {                int currentElement = input.nextInt();                 if (previousElement > currentElement) {                    maximumDifference = Math.max(maximumDifference, previousElement - currentElement);                } else {                    previousElement = currentElement;                }            }             System.out.println((int) Math.ceil(Math.log(maximumDifference + 1) / Math.log(2)));        }    }}
