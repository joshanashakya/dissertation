from collections import defaultdict,dequefrom sys import *input = stdin.readline n = int(input())graph = defaultdict(list)deg = [0] * nfor _ in range(n-1):    a,b = map(int,input().split())    graph[a-1].append(b-1)    graph[b-1].append(a-1)    deg[a-1]+=1    deg[b-1]+=1 childLeaves = defaultdict(int)idx = 0for i in range(n):    if deg[i]==1:        idx=i        childLeaves[graph[i][0]]+=1 mAx = n-1 +(len(childLeaves))-sum(childLeaves.values()) dis = defaultdict(int)dis[idx]=0stack = deque()stack.append(idx)visited = [0]*nvisited[idx] = 1mIn = 1 while stack:    node = stack.pop()    if not visited[node]:        visited[node]=1    for i in graph[node]:        if not visited[i]:            dis[i] = dis[node]+1            stack.append(i)            if deg[i]==1 and dis[i]%2:                mIn=3 print(mIn,mAx)
