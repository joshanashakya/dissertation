n = int(input())divisors = [int(bruh) for bruh in input().split()]divvies = sorted(divisors)bignum = divvies[0] * divvies[-1] bignumdivvies = [] for div in range(1, bignum+1):    if bignum % div == 0:        bignumdivvies.append(div) bignumdivvies = list(set(bignumdivvies)) smallnumdivvies = [] for x in sorted(divisors):    for y in range(divisors.count(x) - bignumdivvies.count(x)):        smallnumdivvies.append(x) smallnum = smallnumdivvies[0] * smallnumdivvies[-1] print(bignum, smallnum)
