import sys def answer(n, d): #return list of ints    #create list of 2 ints: x and y. Pretend y is larger or equal to x.    y = max(d)    d.sort(reverse = True)    for i in range(n):        if y % d[i] != 0: # guaranteed to be x.            x = d[i]            break #no more looking.        elif d[i+1] == d[i]: # 2 elements the same, so one is there because of y and the other because of x.            x = d[i]            break     return [x, y]  def main():    n = int(input())    d = [int(i) for i in sys.stdin.readline().split()]    print(' '.join(map(str, answer(n, d))))      return main()
