import java.util.Scanner; public class B {    public static void main(String[] args) {        Scanner in=new Scanner(System.in);        int n=in.nextInt();        while (n-->0){            int l=in.nextInt(),m=in.nextInt();            char[][] chars=new char[l][m];            int[] row=new int[l],column=new int[m];            for (int i = 0; i < l; i++) {                String s=in.next();                for (int j = 0; j < m; j++) {                    if (s.charAt(j)=='*'){                        row[i]++;                        column[j]++;                    }                    chars[i][j]=s.charAt(j);                }            }            int ans=-1;            for (int i = 0; i < l; i++) {                for (int j = 0; j < m; j++) {                    if (ans==-1){                        ans=(l-row[i])+(m-column[j])-(chars[i][j]=='.'?1:0);                    }else {                        ans=Math.min(ans,(l-row[i])+(m-column[j])-(chars[i][j]=='.'?1:0));                    }                }            }            println(ans);        }    }    static void print(Object o) { System.out.print(o); }    static void println(Object o) { print(o);println(); }    static void println(){print('\n');}    static int max(int... m){        if (m.length==0){            return -1;        }        int s=m[0];        for (int max:m) {            s=Math.max(max,s);        }        return s;    }}
