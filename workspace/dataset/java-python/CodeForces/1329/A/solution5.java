import java.io.*;import java.util.*;public class Main {    public static void main(String args[])    {        FastReader input=new FastReader();        PrintWriter out=new PrintWriter(System.out);        int T=1;        while(T-->0)        {            int n=input.nextInt();            int m=input.nextInt();            int l[]=new int[m];            long sum=0;            int flag=0;            for(int i=0;i<m;i++)            {                l[i]=input.nextInt();                if(l[i]>n-i)                {                    flag=1;                }                sum+=l[i];            }            if(sum<n)            {                flag=1;            }            if(flag==1)            {                out.println(-1);            }            else            {                int r=n;                int index=-1;                for(int i=0;i<m;i++)                {                    if(sum<=r)                    {                        index=i;                        break;                    }                    sum-=l[i];                    r--;                }                for(int i=0;i<index;i++)                {                    out.print((i+1)+" ");                }                int x=n+1;                ArrayList<Integer> list=new ArrayList<>();                for(int i=m-1;i>=index;i--)                {                    x-=l[i];                    list.add(x);                }                for(int i=list.size()-1;i>=0;i--)                {                    out.print(list.get(i)+" ");                }            }        }        out.close();    }    static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {             return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    }}
