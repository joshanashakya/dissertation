import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*; import javafx.util.Pair; public class Main{        public static void main(String[] args)    {         FastScanner input = new FastScanner();       int n= input.nextInt();       String s = input.next();       boolean fcondition = true,scondition = true;       ArrayList<Character> firshalf = new ArrayList<>(),secondhalf =new ArrayList<>();        for (int i = 0; i < n; i++) {            firshalf.add(s.charAt(i));        }        for (int i = n; i <n*2; i++) {            secondhalf.add(s.charAt(i));        }        Collections.sort(firshalf);        Collections.sort(secondhalf);        for (int i = 0; i < n; i++) {            if((firshalf.get(i)-'0')<(secondhalf.get(i)-'0'))            {                continue;            }            else            {                fcondition= false;            }        }        for (int i = 0; i < n; i++) {            if((secondhalf.get(i)-'0')<(firshalf.get(i)-'0'))            {                continue;            }            else            {                scondition= false;            }        }        if(fcondition||scondition)        {            System.out.println("YES");        }        else            System.out.println("NO");                     }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
