import java.util.Arrays;import java.util.Scanner; public class JavaApplication1 {     public static void main(String[] args) {        Scanner input =new Scanner(System.in);        int n= input.nextInt();        int[] arr=new int [n];        int total=0;        for (int i = 0; i < n; i++) {            arr[i]=input.nextInt();            total+=arr[i];        }        Arrays.sort(arr);        int sumOfCoins=0;        int numOfCoins=0;        for (int i = n-1; i >= 0; i--) {            sumOfCoins+=arr[i];            numOfCoins++;            if (sumOfCoins > (total/2))                break;        }        System.out.println(numOfCoins);        input.close();    }}
