inp = input("").split('\n')nums = inp[0].split(' ') n = int(nums[0])a = int(nums[1])b = int(nums[2])k = int(nums[3]) st = str(input("").strip()) tot = 0mod = 10**9 + 9 def inv (a):    return pow (a, mod - 2, mod)    for i in range (0, k):    vl = 1    if (st[i] == '-'):        vl = -1        tot += vl * pow(a, n - i, mod) * pow(b, i, mod)    tot %= mod mult = (pow(b, k, mod) * inv(pow(a, k, mod))) % mod p = ((n + 1) // k) - 1 # tot + tot * mult + .... tot * mult^p if (mult == 1):    print((tot * (p + 1)) % mod)else:    num = (pow(mult, p + 1, mod) - 1) % mod    den = (mult - 1) % mod    num = (num * inv(den)) % mod        print((num * tot)%mod)      
