import java.util.Arrays;import java.util.Scanner; public class _26_Andrey_and_Problem { 	public static void main(String[] args) {		Scanner scn = new Scanner(System.in);		int n = scn.nextInt();		double[] arr = new double[n];		for (int i = 0; i < n; i++) {			arr[i] = scn.nextDouble(); 		}		Arrays.sort(arr);		double res = 1;		double add=0;		double max=0;		for (int i = n - 1; i >= 0; i--) {			double b = arr[i];						res = res * ((double)1 - b);			if(!(arr[i]<1.0)){				max=1.0;				break;			}			double bs = arr[i] / (1 - arr[i]);			add=add+bs;			if(res*(add)>max){				max=res*add;			}					}				System.out.printf("%.10f\n", max);}				}  
