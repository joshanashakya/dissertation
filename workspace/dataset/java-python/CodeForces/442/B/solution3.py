#   ==========     //\\       //||     ||====//||#       ||        //  \\        ||     ||   // ||#       ||       //====\\       ||     ||  //  ||#       ||      //      \\      ||     || //   ||#   ========== //        \\  ========  ||//====|| #  code def main():    n = int(input())    a = list(map(float, input().split()))    a.sort(reverse = True)    ans = 0    for i, v in enumerate(a):        tempans = 0        for j, v1 in enumerate(a[:i + 1]):            p = 1            for k, v3 in enumerate(a[:i + 1]):                if j == k:                    p *= v3                else:                    p *= (1 - v3)            tempans += p        ans = max(ans, tempans)    print(ans)    return if __name__ == "__main__":    main()
