import java.util.Scanner; public class DigitGame {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         String t = scanner.nextLine();        int tt = Integer.parseInt(t);         while (tt-- != 0) {            String n = scanner.nextLine();            int nn = Integer.parseInt(n);            String number = scanner.nextLine();            System.out.println(solve(nn, number));        }    }     public static int solve(int n, String number) {         if (n % 2 == 1) {            int counter = 0;            for (int i = 0; i < n; i++) {                if (Integer.parseInt(String.valueOf(number.charAt(i))) % 2 == 0) {                    counter++;                }                i++;            }            if (counter > (n - 1) / 2) {                return 2;            } else {                return 1;            }        } else {            int counter = 0;            for (int i = 1; i < n; i++) {                if (Integer.parseInt(String.valueOf(number.charAt(i))) % 2 == 1) {                    counter++;                }                i++;            }            if (counter > (n - 2) / 2) {                return 1;            } else {                return 2;            }        }     }}
