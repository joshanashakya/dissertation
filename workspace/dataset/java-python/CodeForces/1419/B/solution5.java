//package codeForces;import java.io.*; public class Stairs { 	public static void main (String[] args) throws IOException {				InputStreamReader ir = new  InputStreamReader(System.in);	        BufferedReader br = new BufferedReader(ir);	        int test = Integer.parseInt(br.readLine());	        for(int cas=1;cas<=test;cas++)	        {	           	             String[] line1 = br.readLine().split(" ");	            long n = Long.parseLong(line1[0]); 				long arr[] = new long[32];		long sum[] = new long[32]; 				for(int i =0;i<32;i++)		{			arr[i] = (long) ((long) (Math.pow(2, i+1)-1)*(Math.pow(2, i)));		}		sum[0] = 1;		for(int i =1;i<32;i++)		{		sum[i] = arr[i]+sum[i-1];		}			//	for(long i: arr)	//	System.out.print(i+" ");				//System.out.println();					  // for(long i: sum)		//	System.out.print(i+" ");				for(int i =1;i<32;i++)		{		if(n<sum[i])			{System.out.println(i);		    break;}		}			        }	} }
