import sys def ask(x, y):    print(f"? {x} {y}")    sys.stdin.flush()    ans = input()    if ans == "e":        exit()    return ans == "y"  while True:    s = input()    if s == "mistake" or s == "end":        exit()            lower = 0    while ask(lower, max(2*lower, 1)):        lower = max(1, 2*lower)        lo = lower    hi = max(1, lower*2)    while lo+1 < hi:        mid = (lo+hi)//2        if ask(lower, mid):            lo = mid        else:            hi = mid                print(f"! {hi}") 
