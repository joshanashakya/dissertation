import sys  def guess(x, y):    print('? %d %d' % (x, y))    sys.stdout.flush()    return input() line = ''while True:    line = input()    if line == 'end':        break    # Begin guessing    l = 0    r = 0    for i in range(0, 31):        r = 2 ** i        l = r // 2        if guess(l, r) == 'x':            break    # Binary search in [l + 1, r]    while l < r - 1:        mid = (l + r) // 2        if l == mid:            break        if guess(mid, r) == 'y':            r = mid        else:            l = mid    # Output    print('! %d' % r)
