import java.util.*;import java.lang.*;import java.io.*;public class Main{    public static int mod=(int)Math.pow(10,9) + 7;    // public static ArrayList<Integer> a=new ArrayList<>();	public static void main (String[] args) throws java.lang.Exception	{		try{            BufferedReader br=new BufferedReader(new InputStreamReader(System.in));            PrintWriter ot=new PrintWriter(System.out);            int t=1;//Integer.parseInt(br.readLine().trim());            while(t-->0)            {                String ss[]=br.readLine().trim().split(" ");                // double m[]=new double[5];                // int wr[]=new int[5];                String s[]=br.readLine().trim().split(" ");                double sum=0;                double x=500;                for(int i=0;i<5;i++)                {                    double m=Double.parseDouble(ss[i]);                    int w=Integer.parseInt(s[i]);                    sum+=Math.max(0.3*x, (1-m/250)*x-50*w);                    x+=500;                }                String r[]=br.readLine().trim().split(" ");                sum+=(100*Integer.parseInt(r[0]));                sum-=(50*Integer.parseInt(r[1]));                ot.println((int)sum);                                             }                    ot.close();            br.close();                    } catch(Exception e){            e.printStackTrace();            return;        }    }     }     
