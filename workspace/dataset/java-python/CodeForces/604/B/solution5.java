import java.util.*;  public class Main{ 		public static void main(String[] args) { 		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int x = sc.nextInt();		int[] a = new int[n];		int size = 0;		  for(int i = 0; i < n; i++){		    a[i] = sc.nextInt();		    size = Math.max(size, a[i]);		  }		  for(int i = 0; i < n - x; i++){		    size = Math.max(size, a[i] + a[2 * (n - x) - 1 - i]);		  }		  System.out.println(size);	}}
