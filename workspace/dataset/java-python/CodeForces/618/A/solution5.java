import com.sun.org.apache.bcel.internal.generic.AALOAD;import com.sun.org.apache.bcel.internal.generic.GOTO;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair;import static jdk.nashorn.internal.runtime.regexp.joni.Syntax.Java; public class Main{      public static void main(String[] args) throws IOException    {        Scanner input = new Scanner(System.in);       int n = input.nextInt();       Stack<Integer> st = new Stack<>();        for (int i = 0; i < n; i++) {            st.push(1);                        while(st.size()>1&&st.get(st.size()-1)==st.get(st.size()-2))            {                st.pop();                st.push(st.pop()+1);                            }                    }        for (Integer integer : st) {            System.out.print(integer+" ");        }        System.out.println("");		    }     }
