#include<iostream>#include<cstdio>#include<cstdlib>#include<cstring>#include<string>#include<stack>#include<map>#include<queue>#include<algorithm>#include<cmath>typedef long long LL;using namespace std; #define MAX 100000+10 int n,m;LL ans; LL a[MAX],b[MAX]; bool cmp(LL a,LL b){	return a>b;} int main(){	cin >> n >> m; 	for(int i=0;i<n;i++)		scanf("%lld",a+i);	for(int i=0;i<m;i++)		scanf("%lld",b+i); 	sort(a,a+n,cmp);	sort(b,b+m,cmp); 	if(a[0]>b[m-1])		cout << -1 <<endl;	else	{		int flag=0;		ans+=a[0];		for(int i=0;i<m-1;i++)			ans+=b[i]; 		if(b[m-1]==a[0])			flag=1; 		if(flag)		{			for(int i=1;i<n;i++)				ans+=a[i]*m;		}		else		{			for(int i=1;i<n;i++)				ans+=a[i]*m; 			ans+=b[m-1]-a[1];		} 		cout <<ans <<endl;	} 	return 0;}   	  		 		   			  		   	  	
