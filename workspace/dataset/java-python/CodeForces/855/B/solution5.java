import java.util.Scanner; public class MaxExpression {     public static void main(String[] args) {         Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        long p = sc.nextLong();        long q = sc.nextLong();        long r = sc.nextLong();         long[] arr = new long[n];        for (int i = 0; i < n; i++) {            arr[i] = sc.nextInt();        }         System.out.println(func(arr,p,q,r));    }    static public long func(long arr[], long p, long q, long r) {        int n = arr.length;         long pmax[] = new long[n];        pmax[0]=p*arr[0];        for (int i = 1; i < n; i++)        {            pmax[i]=Math.max(pmax[i-1],p*arr[i]);        }        long smax[] = new long[n];        smax[n-1]= r*arr[n-1];        for(int i=n-2;i>=0;i--)        {          smax[i]=Math.max(smax[i+1],r*arr[i]);        }        long ans=Long.MIN_VALUE;        for(int i=0;i<n;i++)        {            ans = Math.max(ans, (pmax[i]+q*arr[i]+smax[i]));        }        return ans;    }}
