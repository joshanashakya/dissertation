// package Practice3.CF237; import java.util.ArrayList;import java.util.Arrays;import java.util.Scanner; public class CF239B {    public static void main(String[] args) {        Scanner s = new Scanner(System.in);        int n = s.nextInt();        int q = s.nextInt();        String str = s.next();        while(q-->0) {            int[] count=new int[10];            StringBuilder sb=new StringBuilder("");            int l=s.nextInt()-1;            int r=s.nextInt()-1;            int pointer=0;            sb=sb.append(str.substring(l,r+1));            boolean flag1=false,flag2=false;            while(pointer>=0 && pointer<sb.length()) {                //System.out.println(sb);                if(sb.charAt(pointer)=='0'){                    count[0]++;                    sb.deleteCharAt(pointer);                    if(flag2)                        pointer-=1;                }                else if(sb.charAt(pointer)>='1'&&sb.charAt(pointer)<='9'){                    count[sb.charAt(pointer)-'0']++;                    sb.replace(pointer,pointer+1,Character.toString((char)((int)sb.charAt(pointer)-1)));                    if(flag2)                        pointer-=1;                    else                        pointer+=1;                }                else{                    int intial=pointer;                    if (sb.charAt(pointer) == '>') {                        flag1 = true;                        flag2 = false;                    } else {                        flag1=false;                        flag2 = true;                    }                    pointer += (flag1 ? 1 : -1);                    if(pointer>=0&&pointer<sb.length()&&(sb.charAt(pointer)=='>'||sb.charAt(pointer)=='<')){                        sb.deleteCharAt(intial);                        if(flag1)                            pointer-=1;                    }                }             }            for(int i=0;i<10;i++){                System.out.print(count[i]+" ");            }            System.out.println();         }    }} 
