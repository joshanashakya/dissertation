import java.util.*;   public class geek {     public static int gcd(int a, int b) {        if (b == 0) {            return a;        }        return gcd(b, a % b);    }    /*public static boolean prime(int x){        if(x==0||x==1){            return  false;        }        boolean b[]=new boolean[1000001];        Arrays.fill(b,true);        b[0]=false;        b[1]=false;        for(int i=2;i*i<=100000;i++){            if(b[i]){                for(int j=i*i;j<=100000;j+=i){                    b[j]=false;                }            }        }        if(b[x])return true;        return false;     }      */      public static void main(String[] args) {        try {            Scanner s = new Scanner(System.in);            int n = s.nextInt();                int m=s.nextInt();                int x=s.nextInt();                int y=s.nextInt();            List<Integer> a=new ArrayList<>();           List<Integer> b=new ArrayList<>();            for (int i = 0; i < n; i++) {                a.add(s.nextInt());            }            for (int i = 0; i < m; i++) {                b.add(s.nextInt());            }            List<Integer> l=new ArrayList<>();            List<Integer> k=new ArrayList<>();            Collections.sort(a);            //Collections.reverse(a);            Collections.sort(b);            //Collections.reverse(b);            for(int i=0;i<x;i++){                l.add(a.get(i));            }            for(int j=m-1;y>0&&j>=0;j--){                k.add(b.get(j));                y--;            }            boolean flag=true;            if(l.get(l.size()-1)>=k.get(k.size()-1)){                flag=false;                System.out.println("NO");            }            if(flag){                System.out.println("YES");            }        }        catch (Exception e){            System.out.println(e.getMessage());        }    }}                         
