import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class absolute {		static 	List<Integer> list;	public static boolean possible(int n)	{ 		for(int i=0;i<list.size();i++)		{			int st=0,end=list.size()-1;			while(st<=end)			{			int mid = st + (end-st)/2;			int sum = list.get(mid)+list.get(i);			if(sum == n)return true;			else if(sum > n)end=mid-1;			else st=mid+1;			}		}		return false;	}	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		list = new ArrayList<Integer>();		int i=1;		while((long)(i*(i+1))/2 <= 1000000000) 		{			list.add(i*(i+1)/2);i++;		}		int n = sc.nextInt();		System.out.println(possible(n)?"YES":"NO");	}	}
