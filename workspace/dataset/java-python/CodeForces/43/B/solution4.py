head =  input()text = input()head_m = {t:head.count(t) for t in head.strip().replace(' ','').replace('\t','') }IsValid = Truefor i in text.strip().replace(' ','').replace('\t',''):        # print(i)/////////////    if(i in head_m.keys()):        if head_m[i] > 0:            head_m[i] -= 1        else:            IsValid = False    else:        IsValid = Falseif(IsValid):    print("YES")else:    print("NO")
