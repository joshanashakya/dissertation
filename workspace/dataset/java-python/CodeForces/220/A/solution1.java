import java.util.Arrays;import java.util.HashMap;import java.util.Scanner; public class P220A {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt(), minSwaps = 0;        int[] a = new int[n], tmp = new int[n];        for (int i = 0; i < n; i++) {            int val = sc.nextInt();            a[i] = val;            tmp[i] = val;        }        Arrays.sort(tmp);        for (int i = 0; i < n; i++) {            if(a[i] != tmp[i]) {                minSwaps++;            }        }        System.out.println(minSwaps > 2 ? "NO" : "YES");    }}
