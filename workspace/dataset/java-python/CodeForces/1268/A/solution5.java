import java.util.*; public class Solution{       public static void main(String []args)       {              Scanner sc = new Scanner(System.in);             int n = sc.nextInt();             int k = sc.nextInt();             sc.nextLine();             String s = sc.nextLine();             char ans[] = new char[n];             System.out.println(n);            for(int i = 0 ; i < k ; i++)            {                   for(int j = i ; j < n ; j+= k)                   {                          ans[j] = s.charAt(i);                   }            }           boolean less = false;           int idx = -1;           char cc = ' ';           for(int i = 0 ; i < n ; i++)           {                  if(ans[i] > s.charAt(i))                  break;                                                      if(ans[i] < s.charAt(i))                  {                         less = true;                         idx = i;                                                 break;                  }           }                      if(!less)           System.out.println(ans);                                  else            {                  int min_idx = -1;                for(int i = 0 ; i < k ; i++)                {                       if(s.charAt(i) != '9')                       min_idx = i;                }                min_idx = Math.min(idx , min_idx);                                for(int i = min_idx ; i < k ; i++)                {                       for(int j = i ; j < n ; j += k)                       {                              if(i == min_idx)                              ans[j] = (char)(s.charAt(i) + 1);                                                            else                              ans[j] = '0';                       }                }                System.out.println(ans);            }       }}
