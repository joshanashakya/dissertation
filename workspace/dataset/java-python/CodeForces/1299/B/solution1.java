#include <iostream>#include <algorithm>#include <string>#include <vector>#include <stack>#include <queue>#include <deque>#include <math.h>#include <set>#include <unordered_map> #define max(a,b) ((a) > (b) ? (a) : (b))#define min(a,b) ((a) < (b) ? (a) : (b))#define int long long#define mod 1000003LL#define inf 10000000000LL#define double long double#define f(i,a,b) for(int i = (a); i < (b); i++)#define b(i,a,b) for(int i = (b) - 1; i >= (a); i--)#define vi vector<int>#define vvi vector<vi>#define pii pair<int,int>#define pb push_back#define all(x) (x).begin(), x.end()#define log 30 using namespace std;   void solve(){	int n;	cin >> n;	vector<pii> vals;	set<pii> valtree;	f(i, 0, n)	{		int x, y;		cin >> x >> y;		vals.pb(pii(x, y));		valtree.insert(pii(x, y));	} 	int xx = 0;	int yy = 0;	f(i, 0, n)	{		xx += vals[i].first;		yy += vals[i].second;	}	double xxx = xx / (double)n;	double yyy = yy / (double)n; 	f(i, 0, n)	{		int a = (int)round(2 * xxx - vals[i].first);		int b = (int)round(2 * yyy - vals[i].second);		if (valtree.find(pii(a, b)) == valtree.end())		{			cout << "NO";			return;		}	} 	cout << "YES";}  signed main(){	ios_base::sync_with_stdio(0);	cin.tie(0); 		solve(); 	return 0;}
