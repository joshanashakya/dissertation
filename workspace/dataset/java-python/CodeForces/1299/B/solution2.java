#include "vector"#include "set"#include "map"#include "unordered_set"#include "unordered_map"#include "string"#include "iostream"#include "algorithm"#include "queue" using namespace std; #define ll long long#define INF 2e9//#define MOD 1000000007#define MOD 998244353#define MAX_SIZE 1000000000 typedef vector<int> vi;typedef vector<ll> vll;typedef pair<int, int> pii;  template<typename T>ostream& operator<<(ostream& os, const vector<T>& v) { bool first = true; for (const auto& j: v) { if (!first) { os << " "; } first = false; os <<j; } os << "\n"; return os; }template<typename T>istream& operator>>(istream& is, vector<T>& v) { for (auto& elem : v) { is >> elem; } return is; }  void solve() {    int n;    cin >> n;     vector<pii> a(n);    for (int i=0; i<n; i++) {        cin >> a[i].first >> a[i].second;    }     if (n % 2) {        cout << "NO";        return;    }     n = n/2;    for (int i=0; i<n - 1; i++) {        ll x1 = a[i+1].first - a[i].first;        ll y1 = a[i+1].second - a[i].second;        ll x2 = a[n+i+1].first - a[n+i].first;        ll y2 = a[n+i+1].second - a[n+i].second;        if (x1 != -x2 || y1 != -y2) {            cout << "NO";            return;        }    }     cout << "YES";}  int main() {    //ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);     if (0) {        int tests;        cin >> tests;        for (int t = 1; t <= tests; t++) {            //cout << "Case #" << t << ": ";            solve();            cout << "\n";        }    } else {        solve();        cout << endl;    }     return 0;}
