import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.Scanner; public class Codeforces {     private static final Scanner sc = new Scanner(System.in);    private static final BufferedReader br = new BufferedReader(new InputStreamReader(System.in));    private static final long MOD = (long) (1e9 + 7);    private static PrintWriter out = new PrintWriter(System.out);     /** Optimal(Maximum,Minimum) Answers     * 1. Binary search     * 2. Prefix Suffix     * 3. Greedy (sorting searching)     * 4. DP     **/    public static void solve(int T) throws IOException {        long n = sc.nextLong();        int ans = 0;        for (int i=26000;i>=1 && n > 0;i--){            long x = i - 1;            long val = (3*x*(x+1)/2) + 2*(x+1);            if (val <= n){                ans += n/val;                n %= val;            }        }        System.out.println(ans);    }     public static void main(String[] args) throws IOException {        int t;        t = sc.nextInt();//        t = 1;        while (t-- > 0){            solve(t);        }        System.gc();    }}  
