import java.util.*; public class Question981B {    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int n=sc.nextInt();        int[][] arr=new int[n][2];        for(int i=0;i<n;i++){            for(int j=0;j<2;j++){                arr[i][j]=sc.nextInt();            }        }        int m=sc.nextInt();        int[][] arr1=new int[m][2];        for(int i=0;i<m;i++){            for(int j=0;j<2;j++){                arr1[i][j]=sc.nextInt();            }        }        Map<Integer,List<Integer>> map=new HashMap<>();        for(int i=0;i<arr.length;i++){            List<Integer> list=new ArrayList<>();            list.add(arr[i][1]);            map.put(arr[i][0],list);        }         for(int i=0;i<arr1.length;i++){            if(map.containsKey(arr1[i][0])){                List<Integer> list=map.get(arr1[i][0]);                list.add(arr1[i][1]);                map.put(arr1[i][0],list);            }else {                List<Integer> list=new ArrayList<>();                list.add(arr1[i][1]);                map.put(arr1[i][0],list);            }        }//        for (Map.Entry<Integer,List<Integer>> entry : map.entrySet())//            System.out.println("Key = " + entry.getKey() +//                    ", Value = " + entry.getValue());        long sum=0;        for (Map.Entry<Integer,List<Integer>> entry : map.entrySet()){            List<Integer> list=entry.getValue();            int x=Collections.max(list);            //System.out.println(x);            sum+=x;        }        System.out.println(sum);      }}
