import java.util.*;public class Test{     static int gcd(int a, int b)     { 	    if (a == 0) 		    return b; 	    return gcd(b % a, a);     }      static int lcm(int a, int b)     { 	    return (a*b)/gcd(a, b);     }      static void solve(int b,int g)    {            }                    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int n=sc.nextInt();        Hashtable<Integer,Integer> ht=new Hashtable<>();        for(int i=0;i<n;i++)        {            ht.put(sc.nextInt(), sc.nextInt());        }        int m=sc.nextInt();        for(int i=0;i<m;i++)        {            int x=sc.nextInt();            int y=sc.nextInt();            if(ht.containsKey(x))            {               if(ht.get(x)<y)               {                   ht.replace(x, ht.get(x), y);               }             }            else                ht.put(x, y);        }        long sum=0;        for(Map.Entry<Integer,Integer> e: ht.entrySet())        {            sum+=e.getValue();        }                        System.out.println(sum);    }}
