def solve (n,seq) :    outer = 0    while outer < n :        inner = 0        while inner < n - outer - 1  :                        if seq[inner] > seq[inner+1] :                seq[inner] , seq[inner+1] = seq[inner+1],seq[inner]                print (inner+1,inner+2)            inner += 1        outer += 1               n = int(input())seq = list(map(int,input().split()))  solve(n,seq)       
