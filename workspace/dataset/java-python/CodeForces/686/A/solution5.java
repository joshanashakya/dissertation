import java.util.Scanner; public class A686 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();               long x = scanner.nextLong(), distress = 0;        for (int i = 0; i < n; i++) {            String sym = scanner.next();            int d = scanner.nextInt();            if (sym.equals("+")) {                x += d;            }            if (sym.equals("-")) {                x -= d;                if (x < 0) {                    x += d;                    distress++;                }            }        }        System.out.println(x + " " + distress);    }}
