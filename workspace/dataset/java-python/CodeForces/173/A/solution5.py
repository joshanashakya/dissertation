def gcd(a, b):    if b == 0:        return a    return gcd(b, a % b)  def counter(s, t, r):    k, m = len(s), len(t)    p, q = 0, 0    for i in range(r):        if s[i % k] == 'P':            if t[i % m] == 'S':                q += 1            elif t[i % m] == 'R':                p += 1        elif s[i % k] == 'R':            if t[i % m] == 'S':                p += 1            elif t[i % m] == 'P':                q += 1        else:            if t[i % m] == 'P':                p += 1            elif t[i % m] == 'R':                q += 1    return p, q  n = int(input())s = input()t = input()k, m = len(s), len(t)r = k * m // gcd(k, m)p, q = counter(s, t, r)p1, q1 = counter(s, t, n % r)print(q * (n // r) + q1, p * (n // r) + p1)
