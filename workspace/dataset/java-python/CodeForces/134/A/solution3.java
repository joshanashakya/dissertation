/*package whatever //do not write package name here */ import java.io.*;import java.util.*;public class S {	public static void main (String[] args) {		Scanner in=new Scanner(System.in);		int n=in.nextInt();		long a[]=new long[n];		long sum=0;		for(int i=0;i<n;i++)		{		    a[i]=in.nextLong();		    sum=sum+a[i];		}		ArrayList<Long> al=new ArrayList<>();		for(int i=0;i<n;i++)		{		    sum-=a[i];		    if(sum%(n-1)==0 && sum/(n-1)==a[i])		    {		        al.add((long)i+1);		    }		    sum+=a[i];		}		System.out.println(al.size());		for(int i=0;i<al.size();i++)		{		    System.out.print(al.get(i)+" ");		}	}}
