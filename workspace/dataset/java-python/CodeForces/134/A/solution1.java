import java.util.Scanner; public class AverageNumbers { 	public static void main(String[] args) {				Scanner input = new Scanner(System.in);		int n = input.nextInt();		long arr[] = new long[n];		long arr2[] = new long[n];		long sum = 0;		int count = 0;				for (int i = 0; i<n; i++)  {			arr[i] = input.nextInt();			sum += arr[i];		}				for (int j = 0; j < n; j++) {			if ((sum - arr[j]) == (arr[j] * (n-1))) {				arr2[count] = j;				count++;			}		}		System.out.println(count);		if (count !=0) {			for (int k = 0; k < count; k++) {				System.out.print(arr2[k] + 1 + " ");			}		}		input.close();	} }
