import java.util.*; public class questionCF {		public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n=sc.nextInt();		int m=sc.nextInt();		String a[] = new String[n];		int num[] = new int[n];    				for(int i=0; i<n; i++) {			int count=0;			a[i]=sc.next();			for(int j=0; j<a[i].length(); j++) {				if(a[i].charAt(j)=='*') count++;			}			num[i]=count;		}				int mi=0, max=0, si=0, smax=0;		for(int i=0; i<n; i++) {			if(max<num[i]) {				max=num[i]; mi=i;			}		}		num[mi]=-1;		for(int i=0; i<n; i++) {			if(smax<num[i]) {				smax=num[i]; si=i;			}		}		String x = a[mi];		String y = a[si];				for(int i=0; i<m; i++) 			if(x.charAt(i)!=y.charAt(i)) {				System.out.println( (si+1)+" "+(i+1) );   break;			}	}}		
