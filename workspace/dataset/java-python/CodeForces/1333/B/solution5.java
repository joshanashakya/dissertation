import java.io.BufferedReader;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*; public class Main{     public static void main(String[] args) throws IOException    {         FastScanner input = new FastScanner();        int tc = input.nextInt();        work:    while (tc-- > 0) {            int n = input.nextInt();            int a[] = new int[n];             for (int i = 0; i < n; i++) {                a[i] = input.nextInt();             }            int b[] = new int[n];            for (int i = 0; i < n; i++) {                b[i] = input.nextInt();            }            int pos[] = new int[n];            int neg[] = new int[n];            if (a[0] == 1) {                pos[0]++;            } else if (a[0] == -1) {                neg[0]++;            }            for (int i = 1; i < n; i++) {                pos[i] += pos[i - 1];                if (a[i] == 1) {                    pos[i]++;                }            }            for (int i = 1; i < n; i++) {                neg[i] += neg[i - 1];                if (a[i] == -1) {                    neg[i]++;                }            }//            System.out.println(Arrays.toString(pos));//            System.out.println(Arrays.toString(neg));            if (a[0] != b[0]) {                System.out.println("NO");                continue work;            }            int i = 1;            while (i < n) {                 if (a[i] != b[i]) {                    int value = (b[i] - a[i]);                    if (value < 0) {                        if (neg[i - 1] == 0) {                            System.out.println("NO");                           continue work;                        }                    } else {                        if (pos[i - 1] == 0) {                            System.out.println("NO");                            continue work;                        }                    }                }                i++;            }            System.out.println("YES");        }    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
