import java.util.*; public class AprilFoolsDayPrank_A {    public static void main(String[] args) {        Scanner in=new Scanner(System.in);        String a = in.next(), b = in.next();        String A[] = new String[a.length() / 2];        String B[] = new String[b.length()/2];                int k = 0;        Arrays.fill(A, "");        Arrays.fill(B, "");                for(int i = 0 ; i < a.length() ; i++) {            A[k] += a.charAt(i);            B[k] += b.charAt(i);                        if(i % 2 != 0) k++;                    }                int As = 0, Bs = 0;        for(int i = 0 ; i < A.length ; i++) {            if(A[i].equals(B[i]))                continue;            else {                if(A[i].equals("()")) {                    if(B[i].equals("[]"))                        Bs++;                    else if(B[i].equals("8<"))                        As++;                                    }                else if(A[i].equals("[]")) {                    if(B[i].equals("()"))                        As++;                    else if(B[i].equals("8<"))                        Bs++;                                }                else if(A[i].equals("8<")) {                    if(B[i].equals("[]"))                        As++;                    else if(B[i].equals("()"))                        Bs++;                                }                            }                    }                if(As > Bs)            System.out.println("TEAM 1 WINS");        else if(Bs == As)            System.out.println("TIE");        else            System.out.println("TEAM 2 WINS");            }    }
