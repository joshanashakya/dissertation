import java.io.*;import java.util.ArrayList;import java.util.StringTokenizer; public class LuckySum {    public static void main(String[] args) {        FastReader in = new FastReader();        PrintWriter out = new PrintWriter(new BufferedOutputStream(System.out));         int l = in.nextInt();        int r = in.nextInt();         ArrayList<Long> nums = new ArrayList<>(2 * 517); // try without the initial capacity        nums.add(0L);        for (int i = 0; i < 515; i++) {            Long four = nums.get(i) * 10 + 4;            Long seven = nums.get(i) * 10 + 7;            nums.add(four);            nums.add(seven);        }        long res = 0;        for (Long num : nums) {            while (l <= num && l <= r) {                res += num;                l++;            }        }        out.println(res);        out.close();    }     private static class FastReader {        BufferedReader br;        StringTokenizer st;         FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }    }} 		         				   		 	  	     	
