import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Worms {					public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int T = sc.nextInt();		while(T-->0)		{			char[]  ch = sc.next().toCharArray();			List<Integer> indices = new ArrayList<Integer>();			String k = "";			int i=2,count=0,l=ch.length;			while(i<l)			{				if(ch[i-2]=='t' && ch[i-1]=='w' && ch[i]=='o')				{					if(i+2<l && ch[i+1]=='n' && ch[i+2]=='e')					{						ch[i]='@';						indices.add(i);						i+=2;					}					else					{						ch[i-1]='@';						indices.add(i-1);					}					count++;					i+=3;				}				else if(ch[i-2]=='o' && ch[i-1]=='n' & ch[i]=='e')				{					ch[i-1]='@';					indices.add(i-1);					count++;					i+=3;				}				else					i++;			}			//System.out.println(Arrays.toString(word));			System.out.println(indices.size());			for(int ind : indices)			{				System.out.print((ind+1)+" ");			}			System.out.println();		}			}}
