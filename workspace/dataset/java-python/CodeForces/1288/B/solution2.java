import java.util.*;import java.io.*; public class Main {    public static void main(String[] args) throws IOException {        FastScanner in = new FastScanner(System.in);        PrintWriter out = new PrintWriter(System.out);        int t = in.nextInt();        Main m = new Main();        while (t-- > 0) {            m.run(in, out);        }        out.close();    }     public static long mod = 17352642619633L;     void run(FastScanner in, PrintWriter out) {         // a*b + a + b = ab        // a*b + a = a * pow(10, len(b)-1)        // b + 1 = pow(10, len(b)-1)         // b = 99         // for any a, b is any number of 9s         // how many 9s below B         int numDigits = 0;        boolean allNines = true;        long A = in.nextInt();        long B = in.nextInt();         while (B > 0) {            numDigits++;            long digit = B%10;            allNines &= digit == 9L;            B /= 10;        }         int numValid = numDigits-1 + (allNines?1:0);        out.println(A*numValid);     }     static class FastScanner {        BufferedReader br;        StringTokenizer st;         public FastScanner(InputStream in) {            br = new BufferedReader(new InputStreamReader(in));            st = null;        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }    }}
