import java.io.*;import java.util.*; public class A { 	public static BufferedReader br;	public static PrintWriter out = new PrintWriter(new BufferedOutputStream(System.out)); 	public static void main(String[] args) throws IOException {		readInput();		out.close();	}	static long n,d;	static void solve() {		long min = Long.MAX_VALUE;		for (int i= 0; i * i <= d; i++) {			min = Long.min(min, i+(d+i)/(i+1));		}		if (min > n) out.println("NO");		else out.println("YES");	}	public static void readInput() throws IOException {		br = new BufferedReader(new InputStreamReader(System.in));		// br = new BufferedReader(new FileReader("in.in"));		int t = Integer.parseInt(br.readLine());		while (t-->0) {			StringTokenizer st = new StringTokenizer(br.readLine());			n = Integer.parseInt(st.nextToken());			d = Long.parseLong(st.nextToken());			solve();		}	}}
