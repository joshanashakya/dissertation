import java.util.Scanner; public class _0501SushiforTwo {		public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n=sc.nextInt();		int[] arr = new int[n];		for(int i=0;i<n;i++) {			arr[i]=sc.nextInt();		}		int max=-1;		int prev=-1;		int len=1;		for(int i=1;i<n;i++) {			if(arr[i]==arr[i-1]) {				len++;			}			else {				if(prev==-1) {					prev=len;					len=1;				}				else {						max=Math.max(max, Math.min(len,prev));						prev=len;						len=1;				}			}			max=Math.max(max, Math.min(len,prev)); //		/	System.out.println(len+" "+prev+" "+max);		}		System.out.println(max*2);			} }
