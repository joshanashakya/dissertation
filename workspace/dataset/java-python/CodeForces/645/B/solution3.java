import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;  public class CodeForce {			public static void main(String[] args) throws IOException {			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		// int t=Integer.parseInt(br.readLine());		// while(t-->0)String[] sr=br.readLine().split(" "); 		long n=Long.parseLong(sr[0]);		long k=Long.parseLong(sr[1]);		if(k>=n/2)		{			System.out.println(n*(n-1)/2);		}		else		{long total=0;			for(long i=0;i<k;i++)			{				total+=n-1-i;			}			for(long i=k;i<n-k;i++)			{				total+=k;			}			for(long i=n-k;i<n-1;i++)			{				total+=k-(i-(n-k-1));							}			System.out.println(total);					}			}							}  
