import java.util.*; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }     public static void main(String[] args)    {        Scanner input = new Scanner(System.in);        String s = input.next();                long ans = 0;        for (int i = 0; i < s.length(); i++) {            String sub = s.substring(i);            if(sub.contains("bear"))            {                ans+=(sub.length()-3-sub.indexOf("bear"));            }        }        System.out.println(ans);     } }
