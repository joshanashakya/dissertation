from collections import Counter t = int(input())ans = [] for _ in range(t):    n = int(input())    pins = [input() for _ in range(n)]    counter = Counter(pins)     arr = []    ch = 0    for p in pins:        counter[p] -= 1        if counter[p] == 0:             arr.append(p)            continue        ch += 1        pin = list(map(int, p))        while "".join(map(str, pin)) in counter:            pin[0] += 1            pin[0] %= 10                val = "".join(map(str, pin))        arr.append(val)        counter[val] = 1                            ans.append((ch, arr)) for ch, arr in ans:    print(ch)    for pin in arr:        print(pin)
