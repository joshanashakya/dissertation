import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; import java.util.*; public class Main{       public static void main(String args[])    {        FastScanner input = new FastScanner();        int tc = input.nextInt();        while (tc-- > 0) {          int n = input.nextInt();      ArrayList<String> s = new ArrayList<>();                        for (int i = 0; i < n; i++) {            s.add(input.next());        }                int count=0;        for (int k =0;k<n;k++) {            String temp =s.get(k);            s.remove(k);//            System.out.println(s.contains(temp));            if(s.contains(temp))            {                work:                for (int i = 0; i <4; i++) {                    char a[] = temp.toCharArray();                for (int j = 0; j <=9 ; j++) {                    char c = a[i];                    a[i]=(char)(j+'0');                     String h = new String(a);                    if(s.contains(h))                    {                        continue;                    }                    else                    {                        count++;                                               s.add(k,h);                        break work;                    }                }                }            }            else                s.add(k, temp);        }            System.out.println(count);            for (String string : s) {                System.out.println(string);            }          }    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
