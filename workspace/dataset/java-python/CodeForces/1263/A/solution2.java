import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int cases = scanner.nextInt();         for (int i = 0; i < cases; i++) {            int r = scanner.nextInt();            int g = scanner.nextInt();            int b = scanner.nextInt();            int sum = (r + g + b) / 2;            sum = Math.min(sum, r + g);           sum = Math.min(sum, r + b);           sum = Math.min(sum, g + b);             System.out.println(sum);        }    }}
