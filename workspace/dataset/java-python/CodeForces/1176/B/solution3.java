import java.util.ArrayList;import java.util.Scanner; public class MergeIt {    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int t=sc.nextInt();        while (t-->0) {            int n=sc.nextInt();            ArrayList<Long> list = new ArrayList<Long>();            long sum=0;            for (int i = 0; i < n; i++) {                Long temp=sc.nextLong();                if (temp%3==0) {                    list.add(temp);                } else {                    list.add(temp%3);                }                sum=sum+list.get(i);            }            // Collections.sort(list);            int count1=0,count2=0,count3=0;            for (int i = 0; i < list.size(); i++) {                // if (list.get(i)%3!=0) {                //     long start=1;                //     while (start*3<=sum) {                //         long num=(start*3)-list.get(i);                //         if (list.contains(num)) {                //             list.remove(i);                //             int index=list.indexOf(num);                //             list.remove(index);                //             list.add(start*3);                //             i--;                //             break;                //         }                //         start++;                //     }                // }                if (list.get(i)==1) {                    count1++;                }                else if(list.get(i)==2){                    count2++;                }                else if(list.get(i)%3==0){                    count3++;                }                            }            if (count1<=count2) {                System.out.println(count1+count3+((count2-count1)/3));            }            else{                System.out.println(count2+count3+((count1-count2)/3));            }            // int count=0;            // for (int i = 0; i < list.size(); i++) {            //     if (list.get(i)%3==0) {            //         count++;            //     }            // }            // System.out.println(list);            // System.out.println(count);        }    }}
