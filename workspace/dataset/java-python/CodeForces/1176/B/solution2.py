import sys def answer(n, a):    cnt = [0] * 3 #mod = 0, 1, or 2.    for e in a:        if e % 3 == 0:            cnt[0] += 1        elif e % 3 == 1:            cnt[1] += 1        else: #e % 3 == 2            cnt[2] += 1    total = cnt[0] #already div by 3.    common = min(cnt[2], cnt[1])    total += common #add a div 2 to a div 1, as many as the limiting factors permit.    cnt[2] -= common    cnt[1] -= common    #at this point, I have only 1s or 2s remaining.    total += (cnt[2] // 3)    total += (cnt[1] // 3)        return total def main():    t = int(input())    while t:        n = int(input())        a = [int(i) for i in sys.stdin.readline().split()]        print(answer(n, a))        t -= 1    returnmain()
