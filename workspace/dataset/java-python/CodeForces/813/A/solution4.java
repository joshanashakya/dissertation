import java.util.Scanner; public class A813 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int N = in.nextInt();        int total = 0;        for (int n=0; n<N; n++) {            total += in.nextInt();        }        int M = in.nextInt();        int answer = Integer.MAX_VALUE;        for (int m=0; m<M; m++) {            int L = in.nextInt();            int R = in.nextInt();            if (total <= R) {                answer = Math.min(answer, Math.max(L, total));            }        }        if (answer == Integer.MAX_VALUE) {            answer = -1;        }        System.out.println(answer);    } }
