from sys import stdinfrom collections import defaultdict  def get_prime_nums():  prime_nums = [2]  for i in range(3, 500):    is_prime = True    for num in prime_nums:      if i % num == 0:        is_prime = False        break    if is_prime:      prime_nums.append(i)  return prime_nums  n = int(stdin.buffer.readline()) input_nums = [int(_) for _ in stdin.buffer.readline().split()]prime_nums = get_prime_nums() prime_stats = defaultdict(lambda: [0] * 20) for input_num in input_nums:  for prime_num in prime_nums:    power = 1    while input_num % prime_num == 0:      prime_stats[prime_num][power] += 1      power += 1      input_num = input_num // prime_num  if input_num != 1:    prime_stats[input_num][1] += 1 ans = 1for prime_num, stat in prime_stats.items():  i = 0  while stat[i + 1] >= n - 1:    i += 1  ans *= prime_num ** i print (ans)
