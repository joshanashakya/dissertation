import java.io.File;import java.util.Scanner;import java.util.StringTokenizer; public class p026 {	public static void main(String args[]) throws Exception {		StringTokenizer stok = new StringTokenizer(new Scanner(System.in).useDelimiter("\\A").next());		StringBuilder sb = new StringBuilder();		int n = Integer.parseInt(stok.nextToken());		int m = Integer.parseInt(stok.nextToken());		int ta = Integer.parseInt(stok.nextToken());		int tb = Integer.parseInt(stok.nextToken());		int k = Integer.parseInt(stok.nextToken());		int[] a=new int[n],b=new int[m];		for(int i=0;i<n;i++)a[i]=Integer.parseInt(stok.nextToken());		for(int i=0;i<m;i++)b[i]=Integer.parseInt(stok.nextToken());		int id=-1;		if(k<n) {			int kc=k;			while(kc>=0) {				int val  = a[kc]+ta;				int lo=0,hi=m;				while(lo!=hi) {					int md=(lo+hi)/2;					if(b[md]<val) lo=md+1;else hi=md; 				}				id = Math.max(id, lo+k-kc);				kc--;			}		}		else id=m;		if(id>=m) System.out.println(-1);else System.out.println((b[id]+tb));	}}
