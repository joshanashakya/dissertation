import java.util.Scanner; /** * @author Mohammad Yasin Sadeghi */public class Nineteen {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String s;//        do {        s = scanner.nextLine();        System.out.println(enumerate(s));//        } while (!s.equalsIgnoreCase("quite"));    }     public static byte enumerate(String source) {        byte[] counter = new byte[]{-1, 0, 0, 0};        byte min;        for (char ch : source.toCharArray()) {            switch (ch) {                case 'n':                    counter[0]++;//                    if (counter[0] % 3 == 0) counter[0]++; //instead of rearranging the letters of the string                    break;                case 'i':                    counter[1]++;                    break;                case 'e':                    counter[2]++;                    break;                case 't':                    counter[3]++;                    break;            }        }         counter[0] /= 2;        counter[2] /= 3;         min = counter[0];        for (int i = 1; i < counter.length; i++) {            if (counter[i] < min)                min = counter[i];        }        return min;    }}
