import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Bouncing  { 	public static void main(String[] args) throws IOException {		Scanner sc = new Scanner(System.in);		long n = sc.nextLong();		long copy=n;		long bn = 0;		while(copy>=10)		{			bn *=10;			bn += 9;			copy/=10;			//System.out.println((long)copy+" "+(long)bn);		}				if(copy>1 && bn>0) {			String num = Long.toString(copy-1)+Long.toString(bn);			bn = Long.parseLong(num);		}				long sn = n-bn;				//System.out.println((long)bn +" "+(long)sn);		long sum=0;		while(bn>0)		{			sum += bn%10;			bn/=10;		}		while(sn>0)		{			sum += sn%10;			sn/=10;		}		System.out.println(sum);						}}
