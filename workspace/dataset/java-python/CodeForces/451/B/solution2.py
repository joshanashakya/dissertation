n = int(input())arr = [int(x) for x in input().split()]srted = Truecansort = Falsedescending = Falseidx1 = 0idx2 = 0 for i, val in enumerate(arr[:-1]):    if val > arr[i+1] and not descending:        if srted and not cansort:            cansort = True        else:            cansort = False            break        srted = False        descending = True        idx1 = i+1    elif val < arr[i+1] and descending:        descending = False        idx2 = i+1 if (not descending and n >= idx2+1 and arr[idx1-1] > arr[idx2]) or (idx1 >= 2 and arr[idx2-1] < arr[idx1-2]):    cansort = False if not cansort and not srted:    print('no')else:    print('yes')    if srted:        print('1 1')    elif descending:        print(f'{idx1} {n}')    else:        print(f'{idx1} {idx2}')
