import java.util.*;public class Saitama_Destroys_Hotel { 	public static void main(String[] args) {		// TODO Auto-generated method stub 		Scanner sc= new Scanner(System.in);		int n=sc.nextInt();		int s=sc.nextInt();		int f[][]=new int[n][2];		for(int i=0;i<n;i++)		{			f[i][0]=sc.nextInt();			f[i][1]=sc.nextInt();		}		Arrays.sort(f, java.util.Comparator.comparingDouble(a ->a[0]));		Collections.reverse(Arrays.asList(f));		int t=0;		for(int i=0;i<n;i++)		{			t+=s-f[i][0];			s=f[i][0];			if(t<f[i][1])				t=f[i][1];		}		t+=s;		System.out.println(t);	} }
