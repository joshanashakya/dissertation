import java.util.Scanner; public class SaitamaFuckedUp {    public static void main(String[] args) {        Scanner scan = new Scanner(System.in);         int n = scan.nextInt();        int s = scan.nextInt();        int[][] floor = new int[n][2];        int t = 0;         for(int i=0;i<n;i++){            floor[i][0] = scan.nextInt();            floor[i][1] = scan.nextInt();        }         for(int i=0;i<n;i++){            for(int j=i+1;j<n;j++){                if(floor[i][0] < floor[j][0]){                    int x = floor[i][0];                    floor[i][0] = floor[j][0];                    floor[j][0] = x;                     x = floor[i][1];                    floor[i][1] = floor[j][1];                    floor[j][1] = x;                }            }            t += s - floor[i][0];            s = floor[i][0];            if(floor[i][1] > t){                t = floor[i][1];            }        }        System.out.println(t + s);    }}
