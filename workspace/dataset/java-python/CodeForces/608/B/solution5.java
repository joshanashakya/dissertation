import java.util.*;import java.io.*; public class Main { 	public static void main(String[] args) throws IOException {		Scanner in = new Scanner(new BufferedReader(new InputStreamReader(System.in)));		String str1 = in.next();		String str2 = in.next();		int n1 = str1.length(), n2 = str2.length();		int count0 = 0, count1 = 0;		for(int x = 0; x <= n2 - n1; x++) {			if(str2.charAt(x) == '0')				count0++;			else 				count1++;		}		long ans = 0;		if(str1.charAt(0) == '0')			ans += count1;		else			ans += count0;		for(int x = n2 - n1 + 1; x < n2; x++) {			char ch = str1.charAt(x - n2 + n1);			char ch1 = str2.charAt(x), ch2 = str2.charAt(x - n2 + n1 - 1);			if(ch1 == '0') 				count0++;			else				count1++;			if(ch2 == '0') 				count0--;			else				count1--;			if(ch == '0')				ans += count1;			else				ans += count0;		}				System.out.println(ans);	} }
