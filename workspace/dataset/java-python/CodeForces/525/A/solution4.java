import java.io.*;import java.util.*;import java.text.*;import java.math.*; public class Solution {     public static void main(String[] args) throws IOException{        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		HashMap<Character, Integer> m = new HashMap<Character, Integer>();        int n = Integer.parseInt(br.readLine());        String s = br.readLine().trim();        int l = s.length();        int count = 0;        int ar[] = new int[26];        for(char ch: s.toCharArray()){            if(ch >= 97){                ar[ch - 97]++;            }            else if(ch < 97 && ar[ch - 65] > 0){                ar[ch - 65]--;            }            else{                count++;            }        }        // for(int i = 0; i < l; i += 2){        //     char ch1 = Character.toLowerCase(s.charAt(i + 1));        //     if(s.charAt(i) == ch1)        //         continue;        //     else if(m.containsKey(ch1)){        //         m.put(ch1, m.get(ch1) - 1);        //         if(m.get(ch1) == 0)        //             m.remove(ch1);        //     }        //     else{        //         char key = s.charAt(i);        //         count++;        //         m.put(key, m.getOrDefault(key, 0) + 1);        //     }        // }        System.out.print(count);    }}
