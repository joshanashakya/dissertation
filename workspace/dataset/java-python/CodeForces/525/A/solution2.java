import java.util.Scanner; public class Main {		static int[] hash = new int[100];		public static void main(String[] args) {		Scanner in = new Scanner(System.in);		int n = in.nextInt();		in.nextLine();		String s = in.nextLine();		char[] a = s.toCharArray();		int ans = 0;		for (int i = 0; i < s.length(); i ++) {			if (a[i] >= 'a' && a[i] <= 'z') {				hash[a[i] - 'a'] ++;			} else {				if (hash[a[i] - 'A'] > 0) {					hash[a[i] - 'A'] --;				} else {					ans ++;				}			}		}		System.out.println(ans);	} } 	  	 		   	  	    		  				 			
