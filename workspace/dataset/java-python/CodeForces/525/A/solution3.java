import java.io.*;import java.util.*;import java.text.*;import java.math.*; public class Solution {     public static void main(String[] args) throws IOException{        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		HashMap<Character, Integer> m = new HashMap<Character, Integer>();        int n = Integer.parseInt(br.readLine());        String s = br.readLine().trim();        int l = s.length();        int count = 0;        // int ar[] = new int[26];        // for(char ch: s.toCharArray()){        //     if(ch >= 97){        //         ar[ch - 97]++;        //     }        //     else if(ch < 97 && ar[ch - 65] > 0){        //         ar[ch - 65]--;        //     }        //     else{        //         count++;        //     }        // }        for(int i = 0; i < l; i ++){            char ch = s.charAt(i);            if(ch >= 97){                m.put(ch, m.getOrDefault(ch, 0) + 1);            }            else if(ch < 97 && m.containsKey((char)(ch + 32))){                m.put((char)(ch + 32), m.get((char)(ch + 32)) - 1);                if(m.get((char)(ch + 32)) == 0)                    m.remove((char)(ch + 32));            }            else{                count++;            }        }        System.out.print(count);    }}
