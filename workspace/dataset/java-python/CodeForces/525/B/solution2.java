import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;import static java.util.Arrays.sort;import javafx.util.Pair; public class Main{     public static void main(String[] args)    {         FastScanner input = new FastScanner();        char a[] = input.next().toCharArray();         int m = input.nextInt();        int store[] = new int[a.length];         for (int i = 0; i < m; i++) {            int start = input.nextInt() - 1;            store[start]++;        }        for (int i = 1; i <a.length/2; i++) {            store[i]+=store[i-1];        }//        System.out.println(Arrays.toString(store));        for (int i = 0; i < a.length/2; i++) {            if(store[i]%2!=0)            {                char temp = a[a.length-1-i];                a[a.length-1-i]=a[i];                a[i] = temp;            }        }         System.out.println(a);     }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
