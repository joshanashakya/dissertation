import java.util.Scanner;import java.lang.StringBuffer; public class birthday { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner s=new Scanner(System.in);          StringBuffer str=new StringBuffer(s.nextLine());                    int m=s.nextInt();          int n=str.length();                    int[] arr=new int[(n/2)+1];                    for(int i=0;i<m;i++)          {        	  int l=s.nextInt();        	  if(l<=n/2 && l>=1)        	  arr[l]++;        	            }                    for(int j=2;j<arr.length;j++)        	  arr[j]+=arr[j-1];                    for(int k=1;k<arr.length;k++)          {        	  if(arr[k]%2==0)        		  arr[k]=0;        	  else        		  arr[k]=1;        	            }                    for(int p=1;p<arr.length;p++)          {        	  if(arr[p]==1)        	  {        		        		  char ch=str.charAt(p-1);        		//  System.out.println(ch);        		  str.setCharAt(p-1,str.charAt(n-p));        		  str.setCharAt(n-p, ch);        	  }          }          System.out.println(str);   	  } }
