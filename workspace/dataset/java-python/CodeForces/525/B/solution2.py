from sys import *inp = lambda : stdin.readline() def main():    s = inp()[:-1]    n = int(inp())    a = [0 for i in range(len(s)+5)]    l = map(int,inp().split())    for b in l:         a[b-1] = (a[b-1] + 1)%2    temp = [c for c in s]    yet = 0    for i in range(len(s)//2):        yet = (yet+a[i])%2        if yet == 1:            temp[i],temp[len(s)-i-1] = temp[len(s)-i-1],temp[i]    print("".join(temp))  if __name__ == "__main__":    main()
