import mathtotalTime = 0n,d = input().split()n = int(n)d = int(d)songList = list(map(int, input().split()))initialJokeCount = 0for i in range(n):    if(i != n-1):        totalTime += songList[i] + 10        initialJokeCount += 2    else:        totalTime += songList[i] if(totalTime > d):    print(-1)else:    remainingTime = d - totalTime;    remainingJokeCount = math.floor(remainingTime/5)    print(initialJokeCount + remainingJokeCount)        
