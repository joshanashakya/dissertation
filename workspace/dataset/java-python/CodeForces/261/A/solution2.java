#include <bits/stdc++.h> const int MAXN = 1e5 + 5; int m, q[MAXN], n, a[MAXN], f[MAXN], exist[MAXN], min = 0x7fffffff;int sum[MAXN]; int main() {	scanf("%d", &m);	for(int i = 1; i <= m; i++) scanf("%d", q + i), min = std::min(min, q[i]);	scanf("%d", &n);	for(int i = 1; i <= n; i++) scanf("%d", a + i);	std::sort(a + 1, a + n + 1, [](int a, int b) -> bool {return a > b;});	std::sort(q + 1, q + m + 1);	for(int i = 1; i <= n; i++) sum[i] = sum[i - 1] + a[i];	f[0] = 0;	for(int i = 1; i <= n; i++) {		f[i] = f[i - 1] + a[i];		if(i - min - 1 >= 0) f[i] = std::min(f[i], f[i - min - 1] - sum[i - min - 1] + sum[i - 1]);		if(i - min - 2 >= 0) f[i] = std::min(f[i], f[i - min - 2] - sum[i - min - 2] + sum[i - 2]);	}	printf("%d\n", f[n]);	return 0;}		 				      	 	    	 		 		 	
