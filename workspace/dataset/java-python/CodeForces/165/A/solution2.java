import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}				static int gcd(int a, int b) {						if(b == 0) {				return a;			}						return gcd(b, a % b);		}			public static void main(String[] args) throws IOException {				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = io.nextInt();		int x[] = new int[n];		int y[] = new int[n];				for(int i = 0; i < n; i++) {			x[i] = io.nextInt();			y[i] = io.nextInt();		}				int count = 0;				for(int i = 0; i < n; i++) {			int xc = x[i];			int yc = y[i];						int l = 0;			int r = 0;			int u = 0;			int d = 0;						for(int j = 0; j < n; j++) {				if(j != i) {					if(x[j] < xc && y[j] == yc) {						l++;					}					else if(x[j] > xc && y[j] == yc) {						r++;					}					else if(x[j] == xc && y[j] > yc) {						u++;					}					else if(x[j] == xc && y[j] < yc) {						d++;					}				}			}						if(l >= 1 && r >= 1 && u >= 1 && d >= 1) {				count++;			}					}				System.out.println(count);	}}
