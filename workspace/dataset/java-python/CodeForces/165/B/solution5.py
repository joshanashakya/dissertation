n,k=[int(x) for x in input().split()]import mathdef solve(num,k):    divisor=k    ans=num    while True:        temp=math.floor(num/divisor)        if temp==0:            break        ans+=temp        divisor*=k    return ans def bs_first_greater(start,end,n):    if start>end:        return -1    mid=(start+end)//2    if solve(mid,k)>=n:        if mid==start:            return mid        elif solve(mid-1,k)<n:            return mid        else:            return bs_first_greater(start,mid-1,n)     else:        return bs_first_greater(mid+1,end,n) print(bs_first_greater(1,n,n))
