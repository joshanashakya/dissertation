import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}		public static void main(String[] args){				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = io.nextInt();		int m = io.nextInt();				int B[][] = new int[n][m];				for(int i = 0; i < n; i++) {			for(int j = 0; j < m; j++) {				B[i][j] = io.nextInt();			}		}				Set<Integer> r = new HashSet<>();		Set<Integer> c = new HashSet<>();				boolean isAnyOne = false;				for(int i = 0; i < n; i++) {			for(int j = 0; j < m; j++) {				if(B[i][j] == 0) {					r.add(i);					c.add(j);				}				if(B[i][j] == 1) {					isAnyOne = true;				}			}		}				if((r.size() == n || c.size() == m) && isAnyOne) {			System.out.println("NO");			return;		}				int A[][] = new int[n][m];				boolean isCorrect = true;				for(int i = 0; i < n; i++) {						for(int j = 0; j < m; j++) {								if(r.contains(i) && c.contains(j) && B[i][j] == 1) {					isCorrect = false;					break;				}								if(!r.contains(i) && !c.contains(j)) {					A[i][j] = 1;				}			}			if(!isCorrect) {				break;			}		}				if(isCorrect) {			System.out.println("YES");			for(int i = 0; i < n; i++) {				for(int j = 0; j < m; j++) {					System.out.print(A[i][j] + " ");				}				System.out.println();			}		}		else {			System.out.println("NO");		}	}}
