import java.util.Scanner; public class TempUnavailable {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int testCases = scanner.nextInt();         while (testCases-- != 0) {             int a = scanner.nextInt();            int b = scanner.nextInt();            int c = scanner.nextInt();            int r = scanner.nextInt();              solve(a, b, c, r);        }    }     public static void solve(int a, int b, int c, int r) {        int L = Math.max(Math.min(a, b), c - r);        int R = Math.min(Math.max(a, b), c + r);        System.out.println(Math.max(a, b) - Math.min(a, b) - Math.max(0, R - L));    }}
