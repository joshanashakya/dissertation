import java.util.Scanner;import java.util.Arrays; public class Polikarp {	public static void main(String[] args) {				Scanner input = new Scanner(System.in); 		int n = input.nextInt(); 		int[] array = new int[n];		int[] arr = new int[n];		long sum = 0; 		for (int i = 0; i < n; i++) { 			arr[i] = input.nextInt();		} 		for (int b = 0; b < n; b++) {			sum = arr[b] + sum;		} 		for (int h = 0; h < n; h++) {			array[h] = input.nextInt();		} 		Arrays.sort(array); 		if (array[n - 2] + array[n - 1] >= sum) {			System.out.println("YES");				} 		else {			System.out.println("NO");		}	}}
