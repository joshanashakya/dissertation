# include<bits/stdc++.h>using namespace std;using P  = pair<int,int>;#define int long long#define rep(i,n) for (int i = 0; i < n; i++)#define rep1(i,n) for(int i=1;i<=(n);i++)#define rrep(i,n) for(int i=(n)-1;i>=0;i--)#define rrep1(i, n) for (int i = (n); i > 0; i--)#define REP(i, a, b) for (int i = a; i < b; i++)template<class T> ostream& operator << (ostream &s, vector<T> &P){ for (int i = 0; i < P.size(); ++i) { if (i > 0) { s << " "; } s << P[i]; } return s; }template <class T>bool chmax(T &a, T b){if (a < b){a = b;return true;}return false;}template <class T>bool chmin(T &a, T b){if (a > b){a = b;return true;}return false;} const int MAXN = 5010;vector<vector<int>> dp(MAXN,vector<int>(MAXN)); void slv(){    int n,m;cin>>n>>m;    string S,T;cin>>S>>T;    int ans = 0;    rrep(i,n + 1)rrep(j,m+1){        if (i == n || j == m){continue;}        else{            if (S[i] == T[j]){                chmax(dp[i][j],dp[i+1][j+1]+2);            }            else{                chmax(dp[i][j],dp[i+1][j]-1);                chmax(dp[i][j],dp[i][j+1]-1);                chmax(dp[i][j],dp[i+1][j+1]-2);            }        }        chmax(ans,dp[i][j]);    }    cout << ans << endl;    return;} signed main(){    int t;t = 1;    while(t--){slv();}    return 0;}
