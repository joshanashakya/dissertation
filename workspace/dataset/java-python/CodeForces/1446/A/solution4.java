import java.util.*; public class Test {    public static void main(String[] args) {                Scanner input = new Scanner(System.in);        int t = input.nextInt();         loop: while (t-->0) {             int n = input.nextInt();            long w = input.nextLong();            ArrayList<int[]> a = new ArrayList<>(n);;            int singleSol = -1;            long c = (w + 1) / 2;             for (int i = 0; i < n; i++) {                int current = input.nextInt();                a.add(new int[]{current, i});                if (c <= current && current <= w) {                    singleSol = i + 1;                }             }                   if (singleSol != -1) {                System.out.println(1);                System.out.println(singleSol);                continue loop;            }            Collections.sort(a, new CPR());                        long weight = 0;                int start = 0, end = -1;            for (int i = 0; i < n; i++) {                                if (a.get(i)[0] > w) {                    start++;                    continue;                }                else if (weight + a.get(i)[0] <= w && weight < c) {                    weight += a.get(i)[0];                } else {                    end = i - 1;                    break;                }            }               end = (end == -1) ? n - 1 : end;            if (c <= weight && weight <= w) {                System.out.println(end - start + 1);                for (int i = start; i <= end; i++)                    System.out.print(a.get(i)[1] + 1 + " ");                System.out.println();            } else {                System.out.println(-1);            }        }       }     static class CPR implements Comparator<int[]> {        @Override        public int compare(int[] a, int[] b) {            if (a[0] > b[0]) {                return -1;            } else if (a[0] < b[0]) {                return 1;            }            return 0;        }    }}
