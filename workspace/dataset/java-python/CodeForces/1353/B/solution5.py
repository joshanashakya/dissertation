test=int(input())while test>0:    test-=1    arr=[int(x) for x in input().split()]    n=arr[0];k=arr[1]    a=[int(x) for x in input().split()]    b=[int(x) for x in input().split()]    a.sort();b.sort()    a_index=0;b_index=len(b)-1    while a_index<n and b_index>=0 and k>0:        if a[a_index]< b[b_index]:            # swap(a[a_index],b[b_index])            temp=a[a_index]            a[a_index]=b[b_index]            b[b_index]=temp            a_index+=1;b_index-=1        else:            break        k-=1    print(sum(a))        
