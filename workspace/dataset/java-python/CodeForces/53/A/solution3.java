import java.io.*;import java.util.*; import static java.lang.Integer.parseInt; public class Main {    public static void main(String[] args) throws IOException {        BufferedReader read = new BufferedReader(new InputStreamReader(System.in));        PrintWriter prnt = new PrintWriter(System.out);        String str = read.readLine();        int n = parseInt(read.readLine());        ArrayList<String> list = new ArrayList<>();        for (int i = 0; i < n; i++) {            String s = read.readLine();            int ind = s.indexOf(str);            if (ind == 0) {                list.add(s);            }        }        Collections.sort(list);        if (list.isEmpty()) {            prnt.println(str);        } else {            prnt.println(list.get(0));        }        prnt.close();    }}   		  	 	 						  	  		   	 	
