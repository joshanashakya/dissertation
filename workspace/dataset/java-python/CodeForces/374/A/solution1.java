import java.util.Scanner; public class innaandPinky { 	public static void main(String args[]) {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int m = sc.nextInt();		int i = sc.nextInt();		int j = sc.nextInt();		int a = sc.nextInt();		int b = sc.nextInt();		int ans = Integer.MAX_VALUE;		ans = Math.min(ans, sol(1, 1, i, j, a, b));		ans = Math.min(ans, sol(1, m, i, j, a, b));		ans = Math.min(ans, sol(n, 1, i, j, a, b));		ans = Math.min(ans, sol(n, m, i, j, a, b)); 		if (ans == Integer.MAX_VALUE || ((i - a < 1 && i + a > n )|| (j - b < 1 && j + b > m)) && ans != 0) {			System.out.println("Poor Inna and pony!");		} else {			System.out.println(ans);		}	} 	private static int sol(int n, int m, int i, int j, int a, int b) {		if (i == n && m == j)			return 0;		int x = Math.abs(n - i);		int y = Math.abs(m - j); 		if (x % a == 0 && y % b == 0) {			if ((x / a) % 2 == (y/b) % 2) {				x = x / a;				y = y / b;				return Math.max(x, y);			}		}		return Integer.MAX_VALUE;	}}
