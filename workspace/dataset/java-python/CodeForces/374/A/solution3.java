/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int m = sc.nextInt();		int i = sc.nextInt();		int j = sc.nextInt();		int a = sc.nextInt();		int b = sc.nextInt();				if((i==1&&j==m)||(i==1&&j==1)||(i==n&&j==1)||(i==n&&j==m))		{			System.out.println(0);			return;		}		int sol = Integer.MAX_VALUE;		//case1 1 n		int x = 1;		int y = m;				if(Math.abs(x-i)%a==0 && Math.abs(y-j)%b==0 && ((i-a)>0||(i+a)<=n) && ((j-b)>0||(j+b)<m))		{			int kk = Math.abs(x-i)/a;			int kk2 = Math.abs(y-j)/b;			if((kk%2==0 && kk2%2==0 )|| (kk%2==1 && kk2%2==1))			{				sol = Math.min(sol,Math.max(kk,kk2));				//System.out.println(sol+" "+x+" "+y);			}		}		x = 1;		y = 1;		if(Math.abs(x-i)%a==0 && Math.abs(y-j)%b==0 && ((i-a)>0||(i+a)<=n) && ((j-b)>0||(j+b)<m))		{			int kk = Math.abs(x-i)/a;			int kk2 = Math.abs(y-j)/b;			if((kk%2==0 && kk2%2==0 )|| (kk%2==1 && kk2%2==1))			{				sol = Math.min(sol,Math.max(kk,kk2));				//System.out.println(sol+" "+x+" "+y);			}		}		x = n;		y = 1;		if(Math.abs(x-i)%a==0 && Math.abs(y-j)%b==0 && ((i-a)>0||(i+a)<=n) && ((j-b)>0||(j+b)<m))		{			int kk = Math.abs(x-i)/a;			int kk2 = Math.abs(y-j)/b;			if((kk%2==0 && kk2%2==0 )|| (kk%2==1 && kk2%2==1))			{				sol = Math.min(sol,Math.max(kk,kk2));				//System.out.println(sol+" "+x+" "+y);			}		}		x = n;		y = m;		if(Math.abs(x-i)%a==0 && Math.abs(y-j)%b==0 && ((i-a)>0||(i+a)<=n) && ((j-b)>0||(j+b)<m))		{			int kk = Math.abs(x-i)/a;			int kk2 = Math.abs(y-j)/b;			if((kk%2==0 && kk2%2==0 )|| (kk%2==1 && kk2%2==1))			{				sol = Math.min(sol,Math.max(kk,kk2));				//System.out.println(sol+" "+x+" "+y);			}		}		if(sol==Integer.MAX_VALUE)			System.out.println("Poor Inna and pony!");		else		{			System.out.println(sol);		}	}}
