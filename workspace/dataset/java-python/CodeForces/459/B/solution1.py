from sys import stdin ,stdoutfrom os import pathrd = lambda:stdin.readline().strip()wr = stdout.writeif(path.exists('input.txt')):    stdin = open("input.txt","r")import time ,math#------------------------------------=x = int(rd())mylist = list(map(int,rd().split()))mydict = dict()for i in range(x):	 if mylist[i] not in mydict:	 	mydict[mylist[i]] = 1	 else:	 	mydict[mylist[i]] +=1 mx = max(mylist)mn = min(mylist) diff = mx - mncounter = 0if diff == 0 :	print(diff,(x*(x-1))//2)else:	for i in range(x):		if (mylist[i]+diff) in mydict:			counter+= mydict[mylist[i]+diff]	print(diff,counter)
