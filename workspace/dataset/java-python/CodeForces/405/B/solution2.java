import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*; import javafx.util.Pair; public class Main{     public static void main(String[] args)    {        FastScanner input = new FastScanner();         int n = input.nextInt();        String s = input.next();        int a[] = new int[n];        int  left = -1;        int right = -1;        int count=0;        int ans[] = new int[n];        Arrays.fill(ans, 1);        for (int i = 0; i < n; i++) {            if(s.charAt(i)=='L'&&right==-1)            {                for (int j = 0; j <=i; j++) {                    ans[j]= 0;                }                left =-1;                right =-1;            }            else if(s.charAt(i)=='L'&&right!=-1)            {                for (int j = right; j <=i; j++) {                    ans[j]=0;                }                if((i-right+1)%2!=0)                    ans[right]=1;                left =-1;                right =-1;            }            else if(s.charAt(i)=='R'&&right!=-1)            {               for (int j = right; j <n; j++) {                ans[j]=0;            }            }            else if(s.charAt(i)=='R')            {                right = i;            }                   }        if(right!=-1&&left==-1)        {            for (int i = right; i <n; i++) {                ans[i]=0;            }        }        for (int i = 0; i < n; i++) {            count+=ans[i];        }//        System.out.println(Arrays.toString(ans));        System.out.println(count);     }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
