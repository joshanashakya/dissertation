import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; public class NewSolution {    public static void main(String[] args) throws IOException {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        int n = Integer.parseInt(reader.readLine());        char[] arr = reader.readLine().toCharArray();        int[] res = new int[n];        char last = arr[0];        int j;        if (arr[0]!='L') {            res[0] = 1;            for (j=0; j<n-1; j++) {                if (arr[j]=='L') break;                if (arr[j]=='=') res[j+1] = Math.max(res[j+1], res[j]);                else res[j+1] = Math.max(res[j+1], res[j] + 1);            }        }        if (arr[n-2]!='R') {            res[n-1] = Math.max(res[n-1], 1);            for (j=n-2; j>-1; j--) {                if (arr[j]=='R') break;                if (arr[j]=='=') res[j] = Math.max(res[j], res[j+1]);                else res[j] = Math.max(res[j], res[j+1] + 1);            }        }        for (int i=1; i<n-1; i++) {            if (last=='L' && arr[i]=='R') {                res[i] = 1;                for (j=i-1; j>-1; j--) {                    if (arr[j]=='R') break;                    if (arr[j]=='=') res[j] = Math.max(res[j], res[j+1]);                    else res[j] = Math.max(res[j], res[j+1] + 1);                }                for (j=i; j<n-1; j++) {                    if (arr[j]=='L') break;                    if (arr[j]=='=') res[j+1] = Math.max(res[j+1], res[j]);                    else res[j+1] = Math.max(res[j+1], res[j] + 1);                }            }            if (arr[i]!='=') last = arr[i];        }        StringBuilder ans = new StringBuilder();        for (int elem:res) ans.append(elem).append(" ");        System.out.println(ans);    }}
