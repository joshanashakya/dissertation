import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.Scanner; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        AAPrank solver = new AAPrank();        solver.solve(1, in, out);        out.close();    }     static class AAPrank {        public void solve(int testNumber, Scanner in, PrintWriter out) {            int n = in.nextInt();            int[] arr = new int[n + 2];            for (int i = 0; i < n; i++) {                arr[i + 1] = in.nextInt();            }            arr[0] = 0;            arr[n + 1] = 1001;             int res = 0;            int curCount = 0;            for (int i = 1; i < arr.length; i++) {                if (arr[i] == arr[i - 1] + 1) {                    curCount++;                } else {                    if (curCount > 0)                        res = Math.max(curCount - 1, res);                    curCount = 0;                }            }            if (curCount > 0)                res = Math.max(curCount - 1, res);            out.println(res);        }     }} 
