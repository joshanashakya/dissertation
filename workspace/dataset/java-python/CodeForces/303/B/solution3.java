import java.io.BufferedReader;import java.io.IOException;import java.io.InputStream;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.StringTokenizer; /* * To change this license header, choose License Headers in Project Properties. * To change this template file, choose Tools | Templates74444444474747777777444444447474747777777 */ /** * * @author Andy Phan */public class b {    public static void main(String[] args) {        FS in = new FS(System.in);        PrintWriter out = new PrintWriter(System.out);         int n = in.nextInt();        int m = in.nextInt();        int x = in.nextInt();        int y = in.nextInt();        int a = in.nextInt();        int b = in.nextInt();                int gcd = gcd(a, b);        a /= gcd;        b /= gcd;        int tmp = Math.min(n/a, m/b);        a *= tmp;        b *= tmp;                int cornX = Math.max(0, Math.min(n, x-(a+1)/2));        int cornY = Math.max(0, Math.min(m, y-(b+1)/2));        int otherCornX = cornX+a;        int otherCornY = cornY+b;         cornX -= Math.max(0, otherCornX-n);        cornY -= Math.max(0, otherCornY-m);                        System.out.println(cornX + " " + cornY + " " + (cornX+a) + " " + (cornY+b));                out.close();    }        static int gcd(int a, int b) {        return b == 0 ? a : gcd(b, a%b);    }        static class FS {         BufferedReader in;        StringTokenizer token;                public FS(InputStream str) {            in = new BufferedReader(new InputStreamReader(str));        }                public String next() {            if (token == null || !token.hasMoreElements()) {                try {                    token = new StringTokenizer(in.readLine());                } catch (IOException ex) {                }                return next();            }            return token.nextToken();        }                public int nextInt() {            return Integer.parseInt(next());        }    }}
