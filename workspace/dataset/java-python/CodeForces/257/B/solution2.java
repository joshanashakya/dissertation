import java.util.Scanner; public class B257 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int N = in.nextInt();        int M = in.nextInt();        int score1 = solve(N-1, M, 0, false);        int score2 = solve(N, M-1, 1, false);        int score = Math.max(score1, score2);        System.out.println(score + " " + (N+M-1-score));    }     static int solve(int N, int M, int last, boolean petya) {        int next;        if (N == 0) {            if (M == 0) {                return 0;            } else {                next = 1;            }        } else {            if (M == 0) {                next = 0;            } else {                if (petya) {                    next = last;                } else {                    next = 1-last;                }            }        }        if (next == 0) {            N--;        } else {            M--;        }        int diff = (last == next) ? 1 : 0;        return diff + solve(N, M, next, !petya);    }    }
