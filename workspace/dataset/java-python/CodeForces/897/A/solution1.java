import java.util.Scanner; public class Singleton_Pattern {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int m = sc.nextInt();        sc.nextLine();        String s = sc.nextLine();        StringBuilder string = new StringBuilder(s);        for (int i = 0; i <m ; i++) {            int l = sc.nextInt();            int r = sc.nextInt();            char c1 = sc.next().charAt(0);            char c2 = sc.next().charAt(0);            sc.nextLine();            Character c3 = Character.valueOf(c1);            Character c4 = Character.valueOf(c2);              for (int j = l-1; j <=r-1 ; j++) {                 Character c5 = Character.valueOf(string.charAt(j));                boolean res = c3.equals(c5);                if (res==true)                    string.setCharAt(j,c2);            }         }        System.out.println(string);     }}
