import java.io.*;import java.util.*;import java.lang.*; public class Rextester{    public static void main(String[] args)throws IOException{        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer(br.readLine());        br.close();        int n = new Integer(st.nextToken());        int k = new Integer(st.nextToken());        int p=0;        long ans=0;        StringBuffer sb = new StringBuffer("11");        for(int a=0;a<n;a++){            long x = new Long(sb.toString());            //System.out.println(x);            int i;            ans+=x%k;            ans%=k;            for(i=sb.length()/2-1;i>=0;i--){                if(sb.charAt(i)<'9'){                    sb.setCharAt(i,(char)(sb.charAt(i)+1));                    sb.setCharAt(sb.length()-i-1,sb.charAt(i));                    for(int l=i+1;l<sb.length()-i-1;l++){                        sb.replace(l,l+1,"0");                    }                    break;                }            }            if(i==-1){                sb.replace(0,1,"1");                for(int j=1;j<sb.length();j++){                    sb.replace(j,j+1,"0");                }                sb.append("01");                       }        }        //System.out.println(k);        System.out.println(ans);    }}
