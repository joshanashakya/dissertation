import java.util.*; public class WhereDoITurn{ 	public static void main(String[] args)	{		Scanner scan = new Scanner(System.in);		Vec a = new Vec(scan.nextLong(), scan.nextLong());		Vec b = new Vec(scan.nextLong(), scan.nextLong());		Vec c = new Vec(scan.nextLong(), scan.nextLong());				Vec ab = b.sub(a);		Vec bc = c.sub(b);				long cp = ab.cprod(bc);		if(cp == 0) System.out.println("TOWARDS");		else if(cp > 0) System.out.println("LEFT");		else System.out.println("RIGHT");	}		static class Vec{				long x;		long y;				public Vec(long x, long y) {			this.x = x;			this.y = y;		}				public Vec sub(Vec v) {			return new Vec(x-v.x, y-v.y);		}				public long cprod(Vec v) {			return x*v.y-y*v.x;		}	} }
