import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;  public class CodeForce {		public static void main(String[] args) throws IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringBuilder sb = new StringBuilder();		// int t=Integer.parseInt(br.readLine());		// while(t-->0)		int n=Integer.parseInt(br.readLine());		long v=0;				long u=Integer.MIN_VALUE;			int[] map=new int[n];		String[] sr=br.readLine().split(" ");		int count=0;		for(int i=0;i<n;i++)		{map[i]=Integer.parseInt(sr[i]);		if(map[i]==0)count++;					}		Range[] arr=new Range[count+1];		int m=0;		for(int i=0;i<n;i++)		{				int x=map[i];			if(x==0)			{				v=i;				arr[m]=new Range(u,v);				m++;				u=v;			}					}		arr[count]=new Range(v,Integer.MAX_VALUE);		long k=0;		for(int i=0;i<count+1;i++)		{			for(;k<n;k++)			{				long x=arr[i].l;				long y=arr[i].r;				if(k>=x&&k<=y)				{					long p=(k-x)>(y-k)?(y-k):(k-x);					System.out.print(p+" ");				}				else break;							}		}  }} class Range{	long l,r;	Range(long a,long b){		l=a;r=b;	}}
