def maximal_binary_matrix(n, k):    matrix = [[0] * n for _ in range(n)]    for i in range(n):        for j in range(i, n):            if i == j and k >= 1:                matrix[i][j] = 1                k -= 1            elif k >= 2:                matrix[i][j] = 1                matrix[j][i] = 1                k -= 2    return matrix if k == 0 else None  n, k = map(int, input().split())m = maximal_binary_matrix(n, k)if m:    for row in m:        print(*row)else:    print(-1)
