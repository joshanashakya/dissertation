import java.io.*;import java.util.Arrays;import java.util.Iterator;import java.util.Scanner;public class Main{	    public static void main(String args[]) throws IOException {        PrintWriter out=new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));        StreamTokenizer sc=new StreamTokenizer(new BufferedReader(new InputStreamReader(System.in)));//        Scanner sc = new Scanner(System.in);//        while (sc.hasNext()) {        while (sc.nextToken()!=StreamTokenizer.TT_EOF) {        	int t=(int)sc.nval;        	        	while (t-->0) {        		sc.nextToken();        		int n=(int)sc.nval;        		sc.nextToken();        		int k=(int)sc.nval;        		        		int[] now=new int[n];        		for (int i = 0; i < now.length; i++) {        			sc.nextToken();					now[i]=(int)sc.nval;				}        		        		int[] sorted=now.clone();        		Arrays.sort(sorted);        		        		int[] control=new int[k];        		for (int i = 0; i < k; i++) {        			sc.nextToken();					control[i]=(int)sc.nval-1;				}        		int[] temp=now.clone();        		FI:while (true) {        			int count=0;            		for (int i = 0; i < control.length; i++) {    					if (temp[control[i]]>temp[control[i]+1]) {    						temp[control[i]]^=temp[control[i]+1];    						temp[control[i]+1]^=temp[control[i]];    						temp[control[i]]^=temp[control[i]+1];    						count++;    					}    				}            		if (count==0) {						break;					}				}        		boolean isok=true;        		for (int i = 0; i < temp.length; i++) {					if (temp[i]!=sorted[i]) {						isok=false;						break;					}				}        		out.println(isok?"YES":"NO");        		out.flush();			}        }    }}			 				 	 	  	   		 	 	  	  	
