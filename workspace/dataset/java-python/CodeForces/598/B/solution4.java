import java.io.*;import java.util.*;    public class Main {    public static Scanner in = new Scanner(new BufferedReader(new InputStreamReader(System.in), 640000));    public static PrintWriter out = new PrintWriter(System.out);      public static void rotate(char chrs[], int startIndex, int endIndex, int shiftOffset){        int length = (endIndex  - startIndex) + 1 ;        char aux[] = new char[length];                 for (int i = 0 ; i < aux.length ;++i)            aux[i] = chrs[startIndex + i] ;                for(int i = 0 ; i < aux.length ; ++i){            int newLocalIndex = (i + shiftOffset) % length ;             chrs[newLocalIndex + startIndex] = aux[i] ;         }     }      public static void solve(){        String s = in.next();          char chrs[] = s.toCharArray() ;         int m = in.nextInt();        for(int i = 0 ; i < m ; ++i){            int startIndex = in.nextInt() - 1;            int endIndex = in.nextInt() - 1;            int k = in.nextInt() ;             int shiftSize = (endIndex - startIndex)  + 1;            int finalShiftOffset = k % shiftSize ;             rotate(chrs, startIndex, endIndex, finalShiftOffset) ;            }        System.out.println(new String(chrs));    }     public static void main(String args[]) {        solve();        out.close();    }}     
