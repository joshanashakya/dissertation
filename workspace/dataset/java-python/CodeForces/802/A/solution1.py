import sysinput = sys.stdin.readlinefrom collections import deque, defaultdictn, k = map(int, input().split())A = list(map(int, input().split()))dic = defaultdict(deque)for i, a in enumerate(A):    dic[a].append(i)S = set()ans = 0for i, a in enumerate(A):    for d in dic:        while dic[d] and dic[d][0] <= i:            dic[d].popleft()    if a not in S:        if len(S) < k:            S.add(a)            ans += 1        else:            day = i            for s in S:                if s not in dic or not dic[s]:                    idx = s                    break                else:                    if dic[s][0] > day:                        day = dic[s][0]                        idx = s            S.remove(idx)            S.add(a)            ans += 1print(ans)
