import java.util.*;import java.io.*;  public class Task {     static BufferedReader s1;static BufferedWriter out;static String read() throws IOException{String line="";while(line.length()==0){line=s1.readLine();continue;}return line;}static int int_v (String s1){return Integer.parseInt(s1);}static int[] int_arr() throws IOException{String[] a=read().split(" ");int[] b=new int[a.length];for(int i=0;i<a.length;i++){b[i]=int_v(a[i]);}return b;}static void assign(){s1=new BufferedReader(new InputStreamReader(System.in));out=new BufferedWriter(new OutputStreamWriter(System.out));} static int cmp(int[] a, int[] b){    return b[1]-a[1];}  public static void main(String args[]) throws  IOException {        assign();        int[] n1=int_arr();        int n=n1[0],k=n1[1],res=0;        int[] b=int_arr();        int[] f=new int[n+1];        List<Integer>[] map=new ArrayList[n+1];        for(int i=0;i<=n;i++){map[i]=new ArrayList<>();}        for(int i=0;i<n;i++){            map[b[i]].add(i);        }        for(int i=0;i<n;i++){            map[b[i]].add(1000000);        }        boolean[] have=new boolean[n+1];        TreeSet<int[]> s1=new TreeSet<>((a,b1)-> cmp(a,b1));        for(int i=0;i<n;i++){             if(have[b[i]]){                s1.remove(new int[]{b[i],map[b[i]].get(f[b[i]])});            }            else{                res++;                if(s1.size()==k){have[s1.pollFirst()[0]]=false;}            }                       f[b[i]]++;            s1.add(new int[]{b[i],map[b[i]].get(f[b[i]])});            have[b[i]]=true;        }         out.write(res+"\n");             out.flush();    }           }
