import java.util.*; public class Main {    public static void main(String[] args) {        Scanner scn = new Scanner(System.in);        int t = scn.nextInt();        for (int c = 1; c <= t; c++) {            int n = scn.nextInt();// type            int m = scn.nextInt();// need             if (m % 2 != 0) {                for (int i = 0; i < n; i++) {                    int tl = scn.nextInt();                    int tr = scn.nextInt();                    int dl = scn.nextInt();                    int dr = scn.nextInt();                }                System.out.println("NO");                continue;            }            int req = (m / 2);            boolean sym = false;            for (int i = 0; i < n; i++) {                    int tl = scn.nextInt();                    int tr = scn.nextInt();                    int dl = scn.nextInt();                    int dr = scn.nextInt();                if (tr == dl) {                    sym = true;                }            }            if (sym) {                System.out.println("YES");            } else {                System.out.println("NO");            }        }     } }
