import java.util.Scanner; public class B1426 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int T = in.nextInt();        for (int t=0; t<T; t++) {            int N = in.nextInt();            int M = in.nextInt();            int[][][] tiles = new int[N][2][2];            for (int n=0; n<N; n++) {                for (int r=0; r<2; r++) {                    for (int c=0; c<2; c++) {                        tiles[n][r][c] = in.nextInt();                    }                }            }            boolean diagTile = false;            for (int[][] tile : tiles) {                if (tile[0][1] == tile[1][0]) {                    diagTile = true;                    break;                }            }            boolean nonDiagTile = false;            for (int[][] tile1 : tiles) {                for (int[][] tile2 : tiles) {                    boolean ok = true;                    for (int r=0; r<2; r++) {                        for (int c=0; c<2; c++) {                            if (tile1[r][c] != tile2[c][r]) {                                ok = false;                            }                        }                    }                    if (ok) {                        nonDiagTile = true;                    }                }            }            boolean possible;            if (M%2 == 1) {                possible = false;            } else if (M == 2) {                possible = diagTile;            } else {                possible = diagTile && nonDiagTile;            }            System.out.println(possible ? "YES" : "NO");        }    } }
