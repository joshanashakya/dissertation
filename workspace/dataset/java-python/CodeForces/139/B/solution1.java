import java.io.*;import java.util.*; public class CF139B {	public static void main(String[] args) throws IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		int n = Integer.parseInt(br.readLine());		int[] pp = new int[n];		int[] hh = new int[n];		for (int i = 0; i < n; i++) {			StringTokenizer st = new StringTokenizer(br.readLine());			int l = Integer.parseInt(st.nextToken());			int w = Integer.parseInt(st.nextToken());			int h = Integer.parseInt(st.nextToken());			pp[i] = (l + w) * 2;			hh[i] = h;		}		int m = Integer.parseInt(br.readLine());		int[] ll = new int[m];		int[] ww = new int[m];		int[] cc = new int[m];		for (int j = 0; j < m; j++) {			StringTokenizer st = new StringTokenizer(br.readLine());			ll[j] = Integer.parseInt(st.nextToken());			ww[j] = Integer.parseInt(st.nextToken());			cc[j] = Integer.parseInt(st.nextToken());		}		long sum = 0;		for (int i = 0; i < n; i++) {			int min = Integer.MAX_VALUE;			for (int j = 0; j < m; j++) {				int t = (pp[i] + ww[j] - 1) / ww[j];				int s = ll[j] / hh[i];				if (s > 0) {					int cost = (t + s - 1) / s * cc[j];					if (min > cost)						min = cost;				}			}			sum += min;		}		System.out.println(sum);	}}
