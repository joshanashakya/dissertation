n, m = map(int, input().strip().split())grid = [list(input().strip()) for _ in range(n)]valid = lambda x, y: x >= 0 and y >= 0 and x < n and y < mans = 0directions = [    [1, 0],    [0, 1],    [-1, 0],    [0, -1]]for i in range(n):    for j in range(m):        if grid[i][j] == 'W':            # print('!', i, j)            # grid[i][j] = '.'            for x, y in directions:                if valid(i + x, j + y):                    if grid[i + x][j + y] == 'P':                        ans += 1                        grid[i + x][j + y] = '.'                        break # print(*grid, sep='\n')print(ans)
