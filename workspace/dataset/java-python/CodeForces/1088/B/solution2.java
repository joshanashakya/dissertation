import java.util.Arrays;import java.util.Scanner; public class _0715Ehabandsubtraction { 	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n=sc.nextInt();		int k=sc.nextInt();		int[] arr = new int[n];		for(int i=0;i<n;i++) {			arr[i]=sc.nextInt();		}		Arrays.sort(arr);		int sum=0;		for(int i=0;i<n && k>0;i++) {			arr[i]=arr[i]-sum;			if(arr[i]==0) {				continue;			}			else {				System.out.println(arr[i]);				sum+=arr[i];				k--;			}		}		while(k>0) {			System.out.println(0);			k--;		}	} }
