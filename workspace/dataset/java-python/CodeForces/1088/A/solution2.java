import java.util.*;import java.util.stream.IntStream; public class twonos { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int x = sc.nextInt();		int a,b;		if(x == 1)		{			System.out.println(-1);			System.exit(0);		}		for(a=x;a<=x && a>=1;a--)		{			if(isPrime(a) == false)			{			for(b=1;b<a;b++)			{				if(a==x && b==1)				{					continue;				}				if(a%b ==0)				{					System.out.print(a +" " + b);					System.exit(0);				}				else					continue;							}			}			else			{				b = a;				System.out.println(a +" " + b);				System.exit(0);			}		} 	}	public static boolean isPrime(int num)	{		if ( num > 2 && num%2 == 0 ) {	        //System.out.println(num + " is not prime");	        return false;	    }	    int top = (int)Math.sqrt(num) + 1;	    for(int i = 3; i < top; i+=2){	        if(num % i == 0){	            //System.out.println(num + " is not prime");	            return false;	        }	    }	    //System.out.println(num + " is prime");	    return true; 			} }
