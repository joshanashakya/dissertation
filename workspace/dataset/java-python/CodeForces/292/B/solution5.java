import java.util.ArrayList;import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int m = sc.nextInt();        ArrayList<ArrayList<Integer>> graph = new ArrayList<>();        for(int i = 1; i <= n; i++){            graph.add(new ArrayList<Integer>());        }        for(int i = 0; i < m; i++){            int start = sc.nextInt();            int end = sc.nextInt();            graph.get(start-1).add(end-1);            graph.get(end-1).add(start-1);        }         int[] sizeCount = new int[n];        for(int i = 0; i < n; i++){            int count = graph.get(i).size();            sizeCount[count] = sizeCount[count] + 1;        }        if(sizeCount[2] == n - 2 && sizeCount[1] == 2) System.out.println("bus topology");        else if (sizeCount[2] == n) System.out.println("ring topology");        else if (sizeCount[n-1] == 1 && sizeCount[1] == n-1) System.out.println("star topology");        else System.out.println("unknown topology");    }}				 		 	  				 	 		  	  	  		
