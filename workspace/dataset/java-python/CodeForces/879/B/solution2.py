n, k = map(int, input().split()) p = list(map(int, input().split())) if k >= n - 1:    print(max(p))    exit(0) mx = -1 for i in range(n):    now = (i + 1) % n    num = 0    if mx < p[i] and mx != -1:        num = 1    for j in range(k):        if p[i] > p[now]:            num += 1            now = (now + 1) % n        else:            break    if num >= k:        print(p[i])        exit(0)    mx = max(mx, p[i])
