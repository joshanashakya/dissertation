# cook your dish herefrom collections import defaultdict def dfs(node,visited,graph):    visited[node]=1        for i in graph[node]:        if visited[i]==0:            dfs(i,visited,graph)                return    n,m = map(int,input().split())ans=0cnt=0graph = defaultdict(list)visited = [0]*(n+m+1)    for z in range(1,n+1):    lis = list(map(int,input().split()))    lang = lis[0]        if lang==0:        graph[z] = []        cnt+=1            else:        for i in lis[1:]:            graph[z].append(n+i)            graph[n+i].append(z)                    for i in range(1,n+1):    #print(visited)    if visited[i]==0:        dfs(i,visited,graph)        ans+=1 if cnt==n:    print(ans)    else:    print(ans-1)                            
