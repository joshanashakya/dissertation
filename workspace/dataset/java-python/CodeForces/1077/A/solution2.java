import java.util.Scanner; public class A1077 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        for (int i = 0; i < t; i++) {            long a = scanner.nextLong(), res = 0, b = scanner.nextLong(), k = scanner.nextLong();            if (k % 2 == 0) {                res += k / 2 * (a - b);            } else {                res += (k + 1) / 2 * a - k / 2 * b;            }            System.out.println(res);        }    }}
