from typing import Iterator  def get_num_input() -> Iterator[int]:    return map(int, input().split())  def main() -> None:    right: int    left: int    times: int    right, left, times = get_num_input()     print((right - left) * (times // 2) + right * (times % 2))  if __name__ == "__main__":    ONLY_ONCE: bool = False    for _ in range(1 if ONLY_ONCE else int(input())):        main()
