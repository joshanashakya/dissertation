import java.util.*;  public class Practise {     //public static int[][] dp;    // static int[] dp;     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int l = sc.nextInt();        int r = sc.nextInt();        if (l == 1) {            System.out.print(n + " ");        } else {            l = l - 1;            long ans = 0;            int c = 0;            for (int i = l; i >= 1; i--) {                ans += Math.pow(2, i);                c++;            }            ans += (n - c);            System.out.print(ans + " ");         }        if (r == 1) {            System.out.print(n + " ");        } else {            r = r - 1;            long ans = 0;            n = n - 1;            int c = 0;            for (int i = r; i >= 1; i--) {                ans += Math.pow(2, i);                c++;            }         //   System.out.println(c);            int remin=(n-c);            ans= (long) ((long)ans+(long)(remin)*(Math.pow(2,c)));            ans+=1;             System.out.println(ans);         }      }     private static long Gcd(long m, long n) {        if (n == 0)            return m;        else            return Gcd(n, m % n);     }     private static boolean checkPrime(int n) {        for (int i = 2; i * i <= n; i++) {            if (n % i == 0)                return false;        }        return true;     }}  					    	 	  		  	 				 	  	
