import java.io.BufferedReader;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigDecimal;import java.math.BigInteger;import java.util.StringTokenizer; public class Main {	public static void main(String[] args)// throws FileNotFoundException 	{//		System.setIn(new FileInputStream("A-small-practice.in"));//		System.setOut(new PrintStream(new FileOutputStream("out.txt")));		InputReader in = new InputReader();		PrintWriter out = new PrintWriter(System.out);//		System.out.println(Integer.MAX_VALUE);//		long[] JC=new long[13];//		JC[0]=JC[1]=1;//		for(int i=2;i<=12;i++)//			JC[i]=JC[i-1]*i;		while(in.hasNext())		{			int n=in.nextInt();			int num1=1, num2=1, num3=0;			for(int i=0;i<n;i++)			{				String s=in.next();				if(s.equals("UL") || s.equals("DR"))					num1++;				else if(s.equals("UR") || s.equals("DL"))					num2++;				else 					num3++;			}			out.println((num1+num3)*1L*(num2+num3));		}		out.close();	}} class InputReader {	BufferedReader buf;	StringTokenizer tok;	InputReader() 	{		buf = new BufferedReader(new InputStreamReader(System.in));	} 	boolean hasNext() 	{		while (tok == null || !tok.hasMoreElements()) 		{			try 			{				tok = new StringTokenizer(buf.readLine());			} 			catch (Exception e) 			{				return false;			}		}		return true;	} 	String next() 	{		if (hasNext())			return tok.nextToken();		return null;	} 	int nextInt() 	{		return Integer.parseInt(next());	} 	long nextLong() 	{		return Long.parseLong(next());	} 	double nextDouble() 	{		return Double.parseDouble(next());	} 	BigInteger nextBigInteger() 	{		return new BigInteger(next());	} 	BigDecimal nextBigDecimal() 	{		return new BigDecimal(next());	}}  			 	 		 		  		 		 	  					
