import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.OutputStreamWriter;import java.io.PrintWriter;import java.io.StreamTokenizer;import java.util.ArrayList; public class Main {	public static ArrayList<String> pre;	public static ArrayList<String> diff; 	public static void main(String[] args) throws IOException {		BufferedReader in = new BufferedReader(new InputStreamReader(System.in));		StreamTokenizer st = new StreamTokenizer(in);		PrintWriter pr = new PrintWriter(new OutputStreamWriter(System.out));		pre = new ArrayList<>();		diff = new ArrayList<>();		st.nextToken();		int n = (int) st.nval;		for (int i = 0; i < n; i++) {			st.nextToken();			pre.add(st.sval);		}		for (int i = 0; i < n; i++) {			st.nextToken();			String temp = st.sval;			if (!pre.remove(temp)) {				diff.add(temp);			}		}		pr.println(diff.size());		pr.flush();	}}	  		  			 			  	 										
