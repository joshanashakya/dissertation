import static java.lang.Math.max;import static java.lang.Math.min;import static java.lang.Math.abs;import java.util.*;import java.io.*; public class x1000A{    public static void main(String hi[]) throws Exception    {        BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer(infile.readLine());        int N = Integer.parseInt(st.nextToken());        String[] arr = new String[N];        for(int i=0; i < N; i++)            arr[i] = infile.readLine();        String[] brr = new String[N];        for(int i=0; i < N; i++)            brr[i] = infile.readLine();        int[] map = new int[420];        map['S'] = 1; map['M'] = 2;        int[][] freq1 = new int[3][5];        for(String s: arr)        {            int size = s.length();            char c = s.charAt(size-1);            freq1[map[c]][size]++;        }        int[][] freq2 = new int[3][5];        for(String s: brr)        {            int size = s.length();            char c = s.charAt(size-1);            freq2[map[c]][size]++;        }        int res = 0;        for(int size=1; size <= 4; size++)        {            int diff = 0;            for(int i=0; i < 3; i++)                diff = max(diff, abs(freq1[i][size]-freq2[i][size]));            res += diff;        }        System.out.println(res);    }    public static int[] readArr(int N, BufferedReader infile, StringTokenizer st) throws Exception    {        int[] arr = new int[N];        st = new StringTokenizer(infile.readLine());        for(int i=0; i < N; i++)            arr[i] = Integer.parseInt(st.nextToken());        return arr;    }}
