 import java.util.ArrayList;import java.util.Scanner; public class B { 	public static void main(String[] args) { 		Scanner nik = new Scanner(System.in);		int n = nik.nextInt();		int m = nik.nextInt(); 		int[] a = new int[n + 2]; 		int max = 0; 		for (int i = 1; i <= n; i++) {			a[i] = nik.nextInt();		}		a[0] = 0;		a[n + 1] = m; 		int off = 0;		int on = 0;		boolean b = false; // off ka count karoonga 		int gain = 0;		if (n % 2 == 0) {			b = true;		}		int idx = n+1;		int max1 = Integer.MIN_VALUE;		for (int i = n + 1; i > 0; i--) { 			int temp = a[i] - a[i - 1]; 			if (!b) {				off += temp;			} else {				on += temp;			} 			if (off > on) { 				int temp1 = off - on;				if (temp1 > max1) {					max1 = temp1;					gain = off - 1;					idx = i - 1;				}			}			b = !b;		}		int sum = gain + a[1];		for (int i = 2; i < idx; i += 2) {			sum += (a[i + 1] - a[i]);		}		System.out.println(sum); 	} }
