import java.util.*; public class question {			public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		String s = sc.next();				if(s.length() == 1) {			System.out.println(s); System.exit(0); }				StringBuilder ans = new StringBuilder();		int i=s.length()-1, j=0;		if( (s.length()&1)==0 ) {			for(int l=0; l<s.length()/2; l++) {				if( i==j ) {					ans.append(s.charAt(i));  break;				}				ans.append(s.charAt(i)); i--;				ans.append(s.charAt(j)); j++;			}			System.out.println(ans.reverse());		}		else {			int mid=s.length()/2;			int x=mid, y=mid;						System.out.print(s.charAt(mid));			for(int l=0; l<mid; l++) {				x++; System.out.print( s.charAt(x) );				y--; System.out.print( s.charAt(y) );			}		}			 					}}				  
