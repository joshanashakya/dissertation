import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }        public static void main(String[] args) throws IOException    {       Scanner input = new  Scanner(System.in);     int n = input.nextInt();     int m = input.nextInt();     int k = input.nextInt();     int row[] =new int[100002];     int col[] =new int[100002];     int num[] =new int[100002];        for (int i = 1; i <=k; i++) {            int check = input.nextInt();            int rc = input.nextInt();            num[i] = input.nextInt();            if(check==1)            {                row[rc-1]=i;                            }            else            {                col[rc-1]=i;            }        }        for (int i = 0; i < n; i++) {            for (int j = 0; j < m; j++) {                System.out.print(num[Math.max(row[i], col[j])]+" ");            }            System.out.println();        }    }           }
