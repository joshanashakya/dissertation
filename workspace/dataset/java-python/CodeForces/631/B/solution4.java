import java.util.Scanner; public class PrinterOverlap {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        int m = scanner.nextInt();        int k = scanner.nextInt();         int[][] row = new int[n][2];        int[][] col = new int[m][2];         for (int i = 0; i < k; i++) {            if(scanner.nextInt() == 1){                int a = scanner.nextInt() - 1;                 row[a][0] = scanner.nextInt();                row[a][1] = i+1;             }else {                int a = scanner.nextInt() - 1;                 col[a][0] = scanner.nextInt();                col[a][1] = i+1;            }        }         StringBuilder ans = new StringBuilder();         for (int i = 0; i < n; i++) {            for (int j = 0; j < m; j++) {                if(row[i][1] > col[j][1]) ans.append(row[i][0]).append(" ");                else ans.append(col[j][0]).append(" ");            }            ans.append("\n");        }         System.out.println(ans);    }}
