import java.util.*;import java.math.*;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer;  public class Main{    static  int n;    static int m;     static long k;    static Scanner sc;     static StringBuilder sb;      public static void solve(){         LinkedHashMap<String,Long> map = new LinkedHashMap<>();        for(long o = 0 ; o < k ; o++){            int l = sc.nextInt();            int r = sc.nextInt()-1;            long c = sc.nextLong();            if(map.containsKey(l+"#"+r))                map.remove(l+"#"+r);            map.put(l+"#"+r,c);        }         long[][] mat = new long[n][m];        for(String s : map.keySet()){             String[] str = s.split("#");            int l = Integer.valueOf(str[0]);            int index = Integer.valueOf(str[1]);            long col = map.get(s);            if(l == 1){                for(int j = 0 ; j < m ; j++)                    mat[index][j] = col;            }            else{                for(int i = 0 ; i < n ; i++)                    mat[i][index] = col;            }        }         sb = new StringBuilder();         for(int i = 0 ; i < n ; i++){            for(int j = 0 ; j < m ; j++){                sb.append(mat[i][j]+" ");            }            sb.append("\n");        }         System.out.print(sb);     }     public static void main(String[] args) {         sc = new Scanner(System.in);         int t = 1;         while(t > 0){               n = sc.nextInt();             m = sc.nextInt();             k = sc.nextLong();              solve();            t -= 1;        }     }   }
