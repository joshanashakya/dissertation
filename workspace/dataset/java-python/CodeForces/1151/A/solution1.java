import java.util.Scanner; public class _0732MaximandBiology {	static int countSteps(String s) {		String check="ACTG";		int steps=0;		for(int i=0;i<s.length();i++) {			int val=Math.abs(check.charAt(i)-s.charAt(i));			steps+=Math.min(val, 26-val);	}		return steps;	} 	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n=sc.nextInt();		sc.nextLine();		String s=sc.nextLine();		int minSteps=Integer.MAX_VALUE;		for(int i=0;i<s.length()-3;i++) {			minSteps=Math.min(minSteps,countSteps(s.substring(i,i+4)));		}		System.out.println(minSteps);	} }
