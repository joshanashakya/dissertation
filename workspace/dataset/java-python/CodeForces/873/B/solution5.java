/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Main{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc=new Scanner(System.in);		int n=sc.nextInt();		String str=sc.next();		HashMap<Integer,Integer> hm=new HashMap<>();		int sum=0;		int start=0;		int end=-1;		int maxlen=0;		int countOne=0;		int countZero=0;		int minus=-2;		for(int i=0;i<str.length();i++) {		    if(str.charAt(i)=='1')		    countOne++;		    if(str.charAt(i)=='0')		    countZero++;		}		if(countZero==0 || countOne==0) {		    System.out.println("0");		}		else {		for(int i=0;i<str.length();i++) {		    if(str.charAt(i)=='1') 		    minus=-1;		    else 		    minus=1;		    sum+=minus;		    if(sum==0) {		        start=1;		        end=i+1;		        maxlen=Math.max(maxlen,end-start+1);		    }		    if(hm.containsKey(sum)) {		        start=hm.get(sum)+2;		        end=i+1;		        maxlen=Math.max(maxlen,end-start+1);		    }		    		    if(!hm.containsKey(sum))		    hm.put(sum,i);		}		if(end==-1)		System.out.println("0");		else 		System.out.println(maxlen);		}	}}
