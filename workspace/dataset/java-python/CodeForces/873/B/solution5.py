input()instring = input()balance = [0] num0 = 0num1 = 0 for c in instring:    if c == "0":        num0 += 1      else:        num1 += 1     balance.append(num0 - num1) d = {} for i in range(len(balance)):    if balance[i] not in d:        d[balance[i]] = i ans = 0# minIndex = []for i in range(len(balance)):    # minIndex.append(i - d[balance[i]])    ans = max(ans, i - d[balance[i]]) # print(balance)# print(minIndex)print(ans)
