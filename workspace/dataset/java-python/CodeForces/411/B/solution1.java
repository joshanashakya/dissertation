import java.util.*;import java.lang.*;import java.io.*; public class Main {public static void main(String[] args) {    Scanner sc = new Scanner(System.in);    int n = sc.nextInt();    int m = sc.nextInt();    int k = sc.nextInt();    int[][] memoryAccess = new int[n][m];     for (int i = 0; i < n; ++i) {      for (int j = 0; j < m; ++j) {        memoryAccess[i][j] = sc.nextInt();      }    }    int[] deadlock = new int[n];    int[] blockedCores = new int[n];    for (int i = 0; i < m; ++i) {      int[] counter = new int[k + 1];       for (int j = 0; j < n; ++j) {        if (deadlock[j] > 0) {          memoryAccess[j][i] = deadlock[j];        }        ++counter[memoryAccess[j][i]];      }      for (int j = 0; j < n; ++j) {        if (memoryAccess[j][i] > 0 && counter[memoryAccess[j][i]] > 1 && deadlock[j] < 1) {          blockedCores[j] = i + 1;          deadlock[j] = memoryAccess[j][i];        }      }    }    for (int i = 0; i < n; ++i) {      System.out.println(blockedCores[i]);    }}}  				  		 		     	 		     	 	
