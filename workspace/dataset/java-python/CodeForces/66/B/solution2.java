import java.util.*;public class Main{    public static Scanner heba=new Scanner(System.in);	public static void main(String[] args) 	{	int n=heba.nextInt();	int [] clms=new int[n];	for(int i=0;i<n;i++)	clms[i]=heba.nextInt();	int[] cnts=new int[n];	//if(n==1)	//System.out.println(1);	//else	//{	for(int i=0;i<n;i++)	cnts[i]=1;	for(int x=0;x<n;x++)	{	   /* for(int i=1;i<n;i++)	    {        if(!(x-i<0))	        {	          if(clms[x-i]<=clms[x])	          cnts[x]++;	        }	        if(!(x+i>n))	       { 	           if(clms[x+i]<=clms[x])	           cnts[x]++;	       }	       if(x-i>0&&x+i>n)	        if(clms[x-i]>clms[x]||clms[x+1]>clms[x])	        break;*/	   // }	   for(int i=x;i<n-1;i++)	 {	     if(clms[i]>=clms[i+1])	    cnts[x]++;	   else	   break;	     	 }	   for(int j=x;j>0;j--)	 { 	     if(clms[j]>=clms[j-1])	   cnts[x]++;	   else 	   break;	 }	}	Arrays.sort(cnts);	System.out.println(cnts[n-1]);	//}	}} 
