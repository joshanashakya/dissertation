import java.io.*;import java.util.*; public class p38{    static class FastReader{        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    }     public static void main(String[] args){        FastReader sc = new FastReader();        long a = sc.nextLong();        long b = sc.nextLong();        char[] inp = sc.nextLine().toCharArray();        int n = inp.length;        long[][] arr = new long[n+1][2];        long x=0, y=0;        arr[0][0] = arr[0][1] = 0;        for(int i=0;i<n;i++){            if(inp[i]=='U'){                y++;            }            if(inp[i]=='D'){                y--;            }            if(inp[i]=='L'){                x--;            }            if(inp[i]=='R'){                x++;            }            arr[i+1][0] = x;            arr[i+1][1] = y;        }         for(int i=0;i<=n;i++){            long q = (x!=0?(a-arr[i][0])/x:0);            long w = (y!=0?(b-arr[i][1])/y:0);            long ra = arr[i][0] + q*x, rb = arr[i][1] + w*y;            if((q==w||x==0||y==0) && q>=0 && w>=0 && a==ra && b==rb){                System.out.println("Yes");                return;            }        }         System.out.println("No");    }}
