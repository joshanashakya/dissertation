import java.io.*;import java.util.*;public class Main { 	public static final int mod=1000000007;		static long[][] dp;	public static void main(String[] args) throws IOException {				//File filein = new File("input.txt");		//BufferedReader sc=new BufferedReader(new FileReader(filein));		//PrintWriter out=new PrintWriter(new FileWriter("output.txt"));		 		//Scanner sc=new Scanner(System.in);		BufferedReader sc=new BufferedReader(new InputStreamReader(System.in));		PrintWriter out=new PrintWriter(System.out);				//int tt=Integer.parseInt(sc.readLine());		int t=1;		while(t-->0){			out.flush();			String[] ip=sc.readLine().split(" ");			int n=Integer.parseInt(ip[0]);						int m=Integer.parseInt(ip[1]);						int h=Integer.parseInt(ip[2]);					int[] dep=new int[m+1];			ip=sc.readLine().split(" ");			int sum=0,hcnt=0;			for(int i=1;i<=m;i++){				dep[i]=Integer.parseInt(ip[i-1]);				sum+=dep[i];				if(i==h) hcnt=dep[i];			} 			if(sum<n){				out.println(-1);				continue;			}			if(hcnt==1){				out.println(0);				continue;			}			hcnt--;			sum--;			n--;			if((sum-hcnt)<n){				out.println(1);				continue;			} 			double res=1.0;			for(int i=0;i<n;i++){				double a=sum-hcnt-i;				double b=sum-i;				res=(res*a/b);				//out.println(res);			} 			out.println(1-res);		}		out.flush();		out.close();		sc.close();	} 	 }
