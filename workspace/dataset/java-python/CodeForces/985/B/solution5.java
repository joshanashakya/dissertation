import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.StringTokenizer;import java.io.IOException;import java.io.BufferedReader;import java.io.FileReader;import java.io.InputStreamReader;import java.io.InputStream; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        BSwitchesAndLamps solver = new BSwitchesAndLamps();        solver.solve(1, in, out);        out.close();    }     static class BSwitchesAndLamps {        public void solve(int testNumber, Scanner sc, PrintWriter pw) {            int n = sc.nextInt(), m = sc.nextInt();            int[] count = new int[m];            int[][] arr = new int[n][m];            for (int i = 0; i < n; i++) {                String s = sc.next();                for (int j = 0; j < m; j++) {                    arr[i][j] = s.charAt(j) - '0';                    if (arr[i][j] == 1)                        count[j]++;                }            }            for (int i = 0; i < n; i++) {                boolean f = true;                for (int j = 0; j < m; j++) {                    if (arr[i][j] == 1)                        count[j]--;                    if (count[j] <= 0) {                        f = false;                        break;                    }                }                if (f) {                    pw.println("YES");                    return;                }                for (int j = 0; j < m; j++)                    if (arr[i][j] == 1)                        count[j]++;            }            pw.println("NO");        }     }     static class Scanner {        StringTokenizer st;        BufferedReader br;         public Scanner(FileReader r) {            br = new BufferedReader(r);        }         public Scanner(InputStream s) {            br = new BufferedReader(new InputStreamReader(s));        }         public String next() {            while (st == null || !st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    throw new RuntimeException(e);                }            }            return st.nextToken();        }         public int nextInt() {            return Integer.parseInt(next());        }     }} 
