import sysfrom itertools import *from math import *def solve():    n = int(input())    tab = [list(map(int, input().split())) for _ in range(n)]    alex = list(map(int, input().split()))    has = list()    for row in range(n):        hasrow = list()        for val in alex:            if val <= row + 1:                hasrow.append(val)        has.append(hasrow)    for friend in range(1, n+1): #n + 1        bestindex = -1        bestpriority = n + 1        for time in range(n):            willsend = has[time][0] if has[time][0] != friend else  (-1 if len(has[time]) == 1 else has[time][1])            rating = n            for i in range(n - 1, -1, -1):                if tab[friend - 1][i] == willsend:                    rating = i            if rating < bestpriority:                bestpriority = rating                bestindex = time        print(bestindex + 1, end = ' ')       if sys.hexversion == 50594544 : sys.stdin = open("test.txt")solve()
