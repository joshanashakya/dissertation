import java.io.*;import java.util.*; public class Main {    static final int N = 3;    static int[] basis = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};    static String[] a = new String[N], b = new String[N], path = new String[N];    static boolean[] status = new boolean[N];    static ArrayList<String[]> list = new ArrayList<>();    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));     static void dfs(int u) {        if (u == N) {            list.add(path.clone());            return;        }        for (int i = 0; i < N; ++i) {            if (status[i]) {                continue;            }            path[u] = b[i];            status[i] = true;            dfs(u + 1);            status[i] = false;        }    }     public static void main(String[] args) throws IOException {        a = br.readLine().split("\\.");        b = br.readLine().split("\\.");        dfs(0);        int t1, t2, t3;        boolean flag = false;        for (String[] x : list) {            t1 = Integer.parseInt(a[2]);            t2 = Integer.parseInt(x[2]);            basis[2] = t2 % 4 == 0 ? 29 : 28;            if (t1 - t2 == 18) {                t1 = Integer.parseInt(a[1]);                t2 = Integer.parseInt(x[1]);                if (t2 == t1) {                    t1 = Integer.parseInt(a[0]);                    t2 = Integer.parseInt(x[0]);                    if (t2 <= t1) {                        flag = true;                        break;                    }                } else if (t2 > 0 && t2 < 13 && t2 < t1) {                    t3 = basis[t2];                    t2 = Integer.parseInt(x[0]);                    if (t2 <= t3) {                        flag = true;                        break;                    }                }            } else if (t1 - t2 > 18) {                t2 = Integer.parseInt(x[1]);                if (t2 < 13) {                    t3 = basis[t2];                    t2 = Integer.parseInt(x[0]);                    if (t2 <= t3) {                        flag = true;                        break;                    }                }            }        }        bw.write(flag ? "YES" : "NO");        bw.close();    }}
