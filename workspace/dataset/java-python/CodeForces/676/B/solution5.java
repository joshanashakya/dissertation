import java.util.*;public class pyramidOfGlass {    public static void main(String[] args) {        Scanner in = new Scanner(System.in);        if(in.hasNextLine()){            int n= in.nextInt();            //int time= in.nextInt();            double t= in.nextDouble();            double[][] arr = new double[n+1][n+1];            int value=0;            fn(arr, t, 0, 0);            for(int i=0; i<n; i++){                for(int j=0; j<=i; j++){                    if(arr[i][j]>=1.0){                        value++;                    }                }            }            System.out.println(value);        }    }     private static void fn(double[][] arr, double t, int i, int j) {        arr[i][j]+=t;        if(arr[i][j]<=1.0 || i>=arr.length-2){            return;        }        t=arr[i][j]-1.0;        arr[i][j]=1.0;        fn(arr, t/2, i+1, j);        fn(arr, t/2, i+1, j+1);     }}
