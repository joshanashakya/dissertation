import java.util.*;import java.io.*; public class aaaaaaaaaaaaaaaa {	public void run() throws Exception {		FastReader file = new FastReader();		int n = file.nextInt(); long x = file.nextLong(); long y = file.nextLong();		if (y < n) System.out.println(-1);		else if (y == n && x > (long)y) System.out.println(-1);		else {			long cur = (y - n + 1) * (y - n + 1);			if (cur + n - 1 >= x) {				System.out.println(y - n + 1);				for (int i = 0; i < n - 1; i++) System.out.println(1); 			}			else System.out.println(-1);		}	} 	public static void main(String[] args) throws Exception {		new aaaaaaaaaaaaaaaa().run();	} 	static class FastReader {		BufferedReader br;		StringTokenizer st; 		public FastReader() {			br = new BufferedReader(new InputStreamReader(System.in));		} 		String next() {			while (st == null || !st.hasMoreElements()) {				try {					st = new StringTokenizer(br.readLine());				} catch (IOException e) {					e.printStackTrace();				}			}			return st.nextToken();		} 		int nextInt() {			return Integer.parseInt(next());		} 		long nextLong() {			return Long.parseLong(next());		} 		double nextDouble() {			return Double.parseDouble(next());		} 		String nextLine() {			String str = "";			try {				str = br.readLine();			} catch (IOException e) {				e.printStackTrace();			}			return str;		}	}}
