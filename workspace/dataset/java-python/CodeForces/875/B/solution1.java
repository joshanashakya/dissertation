#include<bits/stdc++.h>using namespace std;typedef long long int ll;typedef long double ld;#define pb push_back#define pf push_front#define mp make_pair#define bns binary_search#define Lb lower_bound#define Ub upper_bound#define inf LLONG_MAX#define vll vector<ll>#define vvll vector<vll>#define vd vector<double>#define pii pair<ll,ll>#define fr first#define sc second#define ninf LLONG_MIN#define all(v) v.begin(),v.end()#define rall(v) v.rbegin(),v.rend()#define scin(s) cin.ignore();getline(cin,s);#define rep(i,a,b) for(ll i=a;i<=b;i++)#define rrep(i,a,b) for(ll i=a;i>=b;i--)#define SEIVE seive();#define PI 	3.14159265358979323846/*ll expo(ll x,ll n,ll m){	x=x%m;	ll res=1;	while(n){		if(n%2){			res=(res*x)%m;		}		x=(x*x)%m;		n/=2;	}	return res;}*//*ll maxn=1000000,c=0;vll minprime(maxn+1,1),sei(maxn+1,1),v; void seive(){	for(ll i=0;i<=maxn;i++)minprime[i]=i;	sei[0]=sei[1]=0;	for(ll i=2;i*i<=maxn;i++){		if(sei[i]==1){			for(ll j=i*i;j<=maxn;j+=i){				sei[j]=0;				minprime[j]=min(minprime[j],i);			}		}	}	/*rep(i,0,maxn){		if(sei[i]){			v.pb(i);		}	}		}*/void solve(){	ll n;	cin>>n;	vll p(n),a(n,0);	ll pos=n-1,on=0;	cout<<1<<" ";	rep(i,0,n-1){		cin>>p[i];		p[i]--;		a[p[i]]=1;		on++;		while(pos>=0 && a[pos] == 1){pos--;on--;}		cout<<on+1<<" ";	}}			int main(){	ios_base::sync_with_stdio(false);	cin.tie(NULL);	cout.tie(NULL);	//freopen("input.txt", "r", stdin);	//freopen("output.txt", "w", stdout);	ll t=1;	//cin>>t;	//SEIVE	while(t--){		solve();	}  }
