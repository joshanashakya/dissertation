import java.util.Scanner;import java.util.Arrays;import java.util.Vector;import java.util.Collections;   public class Problem_Solving1 {    public static int sumdigits(long n)    {     int sm = 0 ;        while (n > 0) {              sm += n % 10;              n/=10;                    }        return sm;    }           public static void main(String[] args) {        // TODO code application logic here        Scanner input = new Scanner(System.in);        long n = input.nextLong();        Vector<Long> v = new Vector<>();        for (int i = 1; i < 101; i++) {            if (i < n)            {              long x = n - i;              if (sumdigits(x) == i)              {               v.add(x);              }                        }        }                if (v.size()==0)            System.out.println(0);        else        {            System.out.println(v.size());            Collections.sort(v);            for (int i = 0; i < v.size(); i++) {                System.out.println(v.get(i));            }        }              }}
