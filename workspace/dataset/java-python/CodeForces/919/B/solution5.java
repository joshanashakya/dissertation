import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class absolute {	static int k;	static 	String ans;	static void process(char[] ch , int cur, int pos)	{		//System.out.println(Arrays.toString(ch)+" pos = "+pos+" remain = "+cur+" k = "+k);				if(k==0 && ans.equals("")) {			ans=new String(ch);			return;		}		if(pos>=ch.length)return;				if(pos==ch.length-1)		{			ch[pos]=(char)(cur+'0');			k--;			process(ch,0,pos+1);			return;		}				int st=(pos==0)?1:0;		for(int i=st;i<=9 && ans.length()==0;i++)		{			if(i<=cur)			{				ch[pos]=(char)(i+'0');				process(ch,cur-i,pos+1);			}		}			}	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		k = sc.nextInt();		ans="";		int nog=2;		while(k>0)		{			char[] temp = new char[nog];			process(temp,10,0);			nog++;		}		System.out.println(ans);	}	}
