import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }      public static void main(String[] args) throws IOException    {       Scanner input = new  Scanner(System.in);      StringBuilder s = new StringBuilder(input.next()+" ");    int i=0;              while(i<s.length())      {          StringBuilder newS = new StringBuilder(s);          for (char j = 'a'; j <='z'; j++) {              newS.insert(i, j);              String send = newS.toString().trim();              boolean ch = check(send);//              System.out.println(newS);              if(ch)              {                  System.out.println(send);                  return;              }             newS = new StringBuilder(s);          }          i++;      }                     System.out.println("NA");                   }     private static boolean check(String s)    {        StringBuilder newS = new StringBuilder(s);        String rev =newS.reverse().toString();        if(s.equals(rev))        {            return true;        }        return false;    }           }
