import math def dist(p1, p2):    return math.sqrt((p1[0] - p2[0]) ** 2 + (p1[1] - p2[1]) ** 2) def cross(p1, p2):    return p1[0] * p2[1] - p1[1] * p2[0] def parse_point(line):    tokens = line.split()    return int(tokens[0]), int(tokens[1]) n = int(input())pt = [parse_point(input())  for _ in range(n)] res = 1e100pr1, pr2 = None, Nonefor i in range(len(pt)):    pr1 = pt[i - 2]    pr2 = pt[i - 1]    cur = pt[i]    p1 = (pr1[0] - pr2[0], pr1[1] - pr2[1])    p2 = (cur[0] - pr2[0], cur[1] - pr2[1])    c = dist(pr1, cur)     if abs(cross(p1, p2) / 2) / c < res:        res = abs(cross(p1, p2) / 2) / c print(res)
