#include <stdio.h>#include <math.h>typedef long long ll;struct Node{    ll x,y;}node[1005];double jiaodu(ll x,ll y,ll x2,ll y2){    return (double)(x*x2+y*y2)/(sqrt((double)(x*x+y*y))*(sqrt((double)(x2*x2+y2*y2))));}double min(double a,double b){    return a<b?a:b;}double fabss(double a){    return a>0?a:-a;}ll chacheng(ll x1,ll y1,ll x2,ll y2){    return x1*y2-y1*x2;}int main(){    int n;    scanf("%d",&n);    for (int i=0;i<n;i++)    {        scanf("%lld%lld",&node[i].x,&node[i].y);    }    double ans=0;    for (int i=0;i<n;i++)    {        int k=(i-1+n)%n;        int p=(i+1)%n;        ll x1=node[i].x-node[k].x;        ll y1=node[i].y-node[k].y;        ll x2=node[p].x-node[k].x;        ll y2=node[p].y-node[k].y;        ll cc=chacheng(x1,y1,x2,y2);        double tt=fabss((double)cc/sqrt((double)(x2*x2+y2*y2)));        if(i==0)            ans=tt;        else            ans=min(ans,tt);    }    printf("%.10lf\n",ans/2);}    	  	 		 	 		 			  				  	
