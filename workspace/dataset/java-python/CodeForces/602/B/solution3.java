import java.util.Scanner; public class ConstantRange {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        Integer[] range = new Integer[n];         for (int i = 0; i < n; i++) range[i] = scanner.nextInt();         int len = 1;        int maxLen = 1;        int max = 0;        int min = 0;        int latestMin = 0;        int latestMax = 0;         for (int i = 1; i < n; i++) {            int change = i;            int newmin = min;            int newmax = max;            if(range[max] < range[i]) {                latestMax = i;                if(max == min) {                    newmax = i;                } else {                    newmax = i;                    change = latestMin;                    newmin = change + 1;                    latestMin++;                    len = -1;                }            } else if(range[max].equals(range[i])){                latestMax = i;            }             if(range[min] > range[i]){                latestMin = i;                if(max == min) {                    newmin = i;                } else {                    newmin = i;                     change = latestMax;                    newmax = change + 1;                    latestMax++;                    len = -1;                }            } else if(range[min].equals(range[i])) {                latestMin = i;            }             i = change;            len++;             min = newmin;            max = newmax;             if(maxLen < len) maxLen = len;        }         System.out.println(maxLen);    }}
