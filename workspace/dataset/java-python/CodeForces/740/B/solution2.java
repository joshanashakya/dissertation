import java.util.*;import java.*; public class Solution{    public static void main(String[] args){        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int[] arr = new int[n];        int m = sc.nextInt();        //reading the Array        for(int i=0;i<n;i++){            arr[i] = sc.nextInt();        }        int[] sumArr = new int[n];        sumArr[0] = arr[0];        for(int i=1;i<n;i++){            sumArr[i] = arr[i] + sumArr[i-1];        }        int mood = 0;        while(m>0){            int startIndex = sc.nextInt();            int endIndex = sc.nextInt();            int currentMood = 0;            if(startIndex==1){                currentMood = sumArr[endIndex-1];            }else if((startIndex != endIndex) && (startIndex!=1)){                currentMood = sumArr[endIndex-1] - sumArr[startIndex-2];            }else if(startIndex == endIndex){                currentMood = arr[endIndex-1];            }                        if(currentMood>0){                mood += currentMood;            }            m--;        }        System.out.println(mood);    }}
