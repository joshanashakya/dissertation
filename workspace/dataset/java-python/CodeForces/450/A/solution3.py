n, m = map(int,input().split())lst = map(lambda x: (x[0]+1, int(x[1])), enumerate(input().split())) max_val = float("-inf")max_idx = last_valid = start = 0 for idx,v in lst:    if v >= max_val:        max_val = v        max_idx = last_valid = idx        start = (max_val//m)*m if max_val % m else max_val - m     elif start<v<=max_val:        last_valid = idx    if last_valid != max_idx:    print(last_valid)else:    print(max_idx)
