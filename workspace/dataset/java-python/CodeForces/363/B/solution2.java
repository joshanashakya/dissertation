import java.util.Scanner; public class n363B {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        int k = scanner.nextInt();        int[] fence = new int[n];        int a;        for (int i = 0; i < n; i++) {            a = scanner.nextInt();            if (i == 0)                fence[i] = a;            else                fence[i] = fence[i - 1] + a;        }        int min = fence[k - 1];        int index = 1;        int result;        for (int i = 1; i < n - k + 1; i++) {            result = fence[i + k - 1] - fence[i - 1];            if (result < min) {                min = result;                index = i + 1;            }        }        System.out.println(index);    }}
