import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}		public static void main(String[] args){				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				String s1 = io.next();		String s2 = io.next();				int pre[] = new int[26];		int req[] = new int[26];				for(int i = 0; i < s1.length(); i++) {			pre[s1.charAt(i) - 'a']++;		}				for(int i = 0; i < s2.length(); i++) {			req[s2.charAt(i) - 'a']++;		}				int count = 0;				for(int i = 0; i < 26; i++) {			if(req[i] != 0 && pre[i] == 0) {				System.out.println(-1);				return;			}			else if(req[i] != 0){				count += (req[i] <= pre[i]) ? req[i] : pre[i]; 			}		}				System.out.println(count);	}}
