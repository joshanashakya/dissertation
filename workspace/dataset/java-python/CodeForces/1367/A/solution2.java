import java.util.*;import java.io.*;public class PartialReplacement {      public static void main(String[] args) throws java.lang.Exception {        BufferedReader br=new BufferedReader(new InputStreamReader(System.in));        int t=Integer.parseInt(br.readLine());        while(t-- >0){            String s=br.readLine();            StringBuilder sb=new StringBuilder();            sb.append(s.charAt(0));            if(s.charAt(0)==s.charAt(1)){                s=s.substring(1);            }            for(int i=0;i<s.length()-1;i++){                if(s.charAt(i)==s.charAt(i+1)){                    sb.append(s.charAt(i));                    i++;                }            }            sb.append(s.charAt(s.length()-1));            System.out.println(sb.toString());        }    }    }
