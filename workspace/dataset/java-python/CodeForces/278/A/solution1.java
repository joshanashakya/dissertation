import java.util.Scanner; public class practice {	public static void main(String[] args)  {				Scanner input = new Scanner (System.in);				int n = input.nextInt();		int []distances = new int [n+1];				for (int i = 1; i<= n; i++)		{			distances[i] = input.nextInt() + distances[i-1];		}				int s = input.nextInt();		int t = input.nextInt();						if (s > t)		{			int temp = s;			s = t;			t = temp;		}				int dis = Math.min(distances[t-1] - distances[s-1], distances[n] - distances[t-1] + distances[s-1]);		System.out.println(dis);							}	 }
