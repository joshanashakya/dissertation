import java.util.Scanner; public class Main { 	static int count = 0;	static int n, m;	static int q = 0;	static boolean bl; 	public static void main(String[] args) {		Scanner scan = new Scanner(System.in);		n = scan.nextInt();		m = scan.nextInt();		scan.nextLine();				char[][] arr = new char[n][m];		boolean[][] visited = new boolean[n][m];		String str = "";		for (int i = 0; i < n; i++) {			str = scan.nextLine();			for (int j = 0; j < m; j++) {				arr[i][j] = str.charAt(j);			}		}				f ( arr,visited, 0, 0, false );				for ( int i = 0; i < n; ++i )		{			for ( int j = 0; j < m; ++j )			{				System.out.print(arr[i][j]);			}			System.out.println();		}			} 	private static void f(char[][] arr, boolean[][] visited, int i, int j, boolean flag) {		if ( i == n || j == m )  //碰到边界，返回		{			return;		}				if ( visited[i][j] == true )		{			return;		}				if ( arr[i][j] == '-' )		{			visited[i][j] = true;			f ( arr,visited, i, j+1, !flag );			f ( arr,visited, i+1, j, !flag );		}				if ( arr[i][j] ==46 && !flag)		{			arr[i][j] = 'B';			visited[i][j] = true;			f ( arr,visited, i, j+1, !flag);			f ( arr,visited, i+1, j, !flag );		}		if ( arr[i][j] ==46 && flag )		{			arr[i][j] = 'W';			visited[i][j] = true;			f ( arr,visited, i, j+1, !flag );			f ( arr,visited, i+1, j, !flag);		}			}	}
