import java.util.Scanner; public class Main{ 	public static char[][] array;	public static char[][] pos = new char[][] {{'B','W'},{'W','B'}};		public static void main(String[] args) {		// TODO 自动生成的方法存根		Scanner input = new Scanner(System.in);				int row = input.nextInt();		int column = input.nextInt();		input.nextLine();				array = new char[row][];				for(int i=0;i<row;i++)		{			String temp = input.nextLine();			array[i] = temp.toCharArray();		}				for(int i=0;i<row;i++)		{			for(int l=0;l<column;l++)			{				if(array[i][l]=='.')				{					System.out.print(pos[i%2][l%2]);				}				else				{					System.out.print('-');				}			}			System.out.println();		}					}		public static void f(int row,int column,boolean bool)	{			if(row==-1||column==-1)		{			return;		}		if(array[row][column]=='B'||array[row][column]=='W'||array[row][column]=='!')		{			return;		}		if(array[row][column]==46)		{			array[row][column] = bool?'B':'W';			f(row-1,column,!bool);			f(row,column-1,!bool);		}		else		{			array[row][column]='!';			f(row-1,column,!bool);			f(row,column-1,!bool);		}	} }
