v = [int(i) for i in input().split()] a = v[0] - 1b = v[1] - 1x = v[2] ans = ['0', '1']dif = 1 while a > 0 and b > 0 and dif < x:    # adiciona uma diferenca a direita    ans += ['0', '1']    a -= 1    b -= 1        dif += 2 # "0XXXX1", com dif = x-1, x ou x+1if dif == x: # preenche os extremos (extensão)    ans = list('0'*a) + ans + list('1'*b)elif dif == x+1: # retira a diferença da esquerda e extende    ans[0] = '1' + '1'*b    ans[1] = '0' + '0'*aelse: # uma diferenca a menos    if a > 0: # diferenca no fim        ans = ans + list('1'*b + '0'*a)    else: # coloca diferenca no inicio        ans = list('1'*b + '0'*a) + ans        print(''.join(ans))
