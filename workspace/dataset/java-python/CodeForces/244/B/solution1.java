import java.math.BigInteger;import java.util.HashSet;import java.util.Scanner; public class Main {	static HashSet set = new HashSet<>();	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int num = sc.nextInt(); 		for(int x=0; x<=9; x++)		{			for(int y=x+1; y<=9; y++)			{				makeup(num,x,y,0,0);			}		}		System.out.println(set.size());  	}	static void makeup(int n,int x, int y,long res,int count)	{		if(res>n||(res==0&&count>0))		{			return;		}		if(res>0)		{			set.add(res);		}		makeup(n, x, y, res*10+x, count+1);		makeup(n, x, y, res*10+y, count+1);			} }    		  		      		 			  		 	  	
