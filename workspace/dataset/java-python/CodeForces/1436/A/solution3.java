import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer; public class main { 	public static void main(String[] args) throws IOException {		// creo el lector		BufferedReader lect = new BufferedReader(new InputStreamReader(System.in));		// leemos y comprobamos		int x = Integer.parseInt(lect.readLine());		//		for (int z = 0; z < x; z++) {			// dividimos la cadena leida			StringTokenizer divC = new StringTokenizer(lect.readLine());			// inicializamos variables y devolvemos el string			int n = Integer.parseInt(divC.nextToken());			int m = Integer.parseInt(divC.nextToken());			int sum = 0;			StringTokenizer divC2 = new StringTokenizer(lect.readLine());			// miramos is en la cadena hay mas tokens			while (divC2.hasMoreTokens())				sum += Integer.parseInt(divC2.nextToken());			// si el caracter es igual			if (sum == m)                System.out.println("YES");			else				System.out.println("NO");		}	} }
