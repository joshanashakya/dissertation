import java.util.Scanner; public class B1436 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int T = in.nextInt();        for (int t=0; t<T; t++) {            int N = in.nextInt();            // Find prime P such that P-(N-1) is not prime            int P = 1;            while (!(isPrime(P) && !isPrime(P-N+1))) P++;            int diag = P-N+1;            StringBuilder output = new StringBuilder();            for (int r=0; r<N; r++) {                for (int c=0; c<N; c++) {                    output.append((c == r) ? diag : 1).append(' ');                }                output.append('\n');            }            System.out.print(output);        }    }     private static boolean isPrime(int p) {        for (int d=2; d<p; d++) {            if (p%d == 0) {                return false;            }        }        return (p != 1);    } }
