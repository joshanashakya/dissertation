import java.util.*; public class Test {		static Scanner sc;	static Set<Integer> set;	public static void main(String[] args) {				sc = new Scanner(System.in); 		set = new HashSet<>();		set.add(0); set.add(1); set.add(2); set.add(5); set.add(8);		int t = Integer.parseInt(sc.next()); 		while(t-->0) solve(); 	} 	public static void solve(){			int n = sc.nextInt();		double k = sc.nextDouble(); 		double[] p = new double[n];		for(int i = 0; i < n; i++){			p[i] = sc.nextDouble();		} 		long ans = 0; 		double per, x=0;		for(int i = 1; i < n; i++){ 			per = p[i]*100/p[i-1];			if(per > k){ 				x = Math.ceil(p[i]*100/k - p[i-1]);				ans += x;				p[i-1] += x;			} 			p[i] += p[i-1];		} 		System.out.println(ans);	}	}
