import java.util.Scanner; public class B1476 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int T = in.nextInt();        for (int t=0; t<T; t++) {            int N = in.nextInt();            int K = in.nextInt();            int[] P = new int[N];            for (int n=0; n<N; n++) {                P[n] = in.nextInt();            }            long minPrice = 0;            for (int n=N-1; n>0; n--) {                //nextMinPrice >= minPrice - P[n];                //(100*P[n]+(K-1))/K <= nextMinPrice                minPrice = Math.max(minPrice - P[n], (100L*P[n]+(K-1))/K);            }            long answer = Math.max(0, minPrice-P[0]);            System.out.println(answer);        }    } }
