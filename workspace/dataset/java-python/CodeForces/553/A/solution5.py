from sys import stdin,stdoutfrom collections import Counternmbr = lambda: int(stdin.readline())lst = lambda: list(map(int,stdin.readline().split()))M=10**9+7fact=[1]for i in range(1,1005):    fact+=[(fact[-1]*i)%M]def ncr(n,r):    num=fact[n]    den=(fact[r]*fact[n-r])%M    ans=(num*pow(den,M-2,M))%M    return ansfor _ in range(1):#nmbr()):    n=nmbr()    a=[nmbr() for _ in range(n)]    dp=[0]*(1+n)    dp[1]=1    spaces=0    sm=a[0]    for i in range(2,n+1):        spaces=sm+a[i-1]-1        dp[i]=(dp[i-1]*(ncr(spaces,a[i-1]-1)))%M        sm+=a[i-1]    print(dp[n]%M)
