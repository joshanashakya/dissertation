import java.util.*;import java.io.*;import java.math.*;public class Solution{    public static void main(String[] args)throws IOException{        BufferedReader br =  new BufferedReader(new InputStreamReader(System.in));        PrintWriter out = new PrintWriter(System.out);        StringTokenizer st = new StringTokenizer(br.readLine());        int n = Integer.parseInt(st.nextToken());        long k = Long.parseLong(st.nextToken());        if(n==1) out.println("1");        else{            long[] tab = new long[n];            tab[0] = (long)0;            tab[1] = (long)0;            for(int i=2;i<n;i++){                //for(int j=2;j<i;j++) tab[i] += tab[j];                tab[i] = tab[i-1];                tab[i] += tab[i-2]+(long)1;                //out.print(tab[i]+" ");            }            //for(int i=1;i<n;i++){            //    tab[i]+= tab[i-1];            //    out.print(tab[i]+" ");            //}            int[] a = new int[n];            for(int i=0;i<n;i++) a[i] = i+1;            for(int i=0;i<n-1;i++){                long cnt = tab[n-i-1]+1;                //out.println(i+" "+(n-i-1)+" "+cnt+" "+k);                if(cnt<k){                    k -= cnt;                    int d = a[i];                    a[i] = a[i+1];                    a[i+1] = d;                    i++;                    if(k==1) break;                    continue;                }                if(k==1) break;            }            for(int i=0;i<n-1;i++) out.print(a[i]+" ");            out.println(a[n-1]);        }        out.flush();   }}
