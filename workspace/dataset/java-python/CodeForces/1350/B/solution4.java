import java.util.*; public class OracAndModels {     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();         for(int i = 0; i<t; i++) {            solve(sc);        }     }     public static void solve(Scanner sc) {        int n = sc.nextInt();        int s[] = new int[n];         for (int i = 0; i<n; i++) {            s[i] = sc.nextInt();        }         int dp[] = new int[n];        int max = 1;        dp[0] = 1;        for (int i = 1; i<n; i++) {            int r = 1;            for (Integer c : getDiv(i+1)) {                if (s[c-1]<s[i] && dp[c-1] + 1>r)                    r = dp[c-1] + 1;            }            dp[i] = r;            if (dp[i]>max) max=dp[i];        }        System.out.println(max);    }     public static List<Integer> getDiv(int n) {        List<Integer> l = new LinkedList<>();        l.add(1);        for (int i = 2; i<=(int)Math.ceil(Math.sqrt(n)); i++) {            if (n%i==0){                l.add(i);                if (i!=n/i)                 l.add(n/i);            }        }        return l;    }}
