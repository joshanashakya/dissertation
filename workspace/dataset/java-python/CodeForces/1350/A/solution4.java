import java.util.*;import java.io.*;import java.math.BigInteger; public class cp{    public static void main(String[] args)throws IOException{         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        int times = Integer.parseInt(br.readLine());         while(times > 0 ){            String input = br.readLine();            StringTokenizer st = new StringTokenizer(input);            int n = Integer.parseInt(st.nextToken());            int k = Integer.parseInt(st.nextToken());             // System.out.println(times);             // for (int m=0; m<k; m++){            //     int i = 2 ;            //     while (i <= n){            //         if (n % i == 0) break;            //         i++;            //     }            //     n = n + i;            // }             int i = 2 ;            while (i <= n){                if (n % i == 0) break;                i++;            }             if (n % 2 == 0){                System.out.println(n + 2*k);            }            else{                System.out.println(n + 2*(k-1) + i);            }            times--;        }     }}
