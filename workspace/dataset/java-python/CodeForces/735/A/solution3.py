import math n, k = map(int, input().split())cells = input() def findPath(n,k,cells):    line = []    grasshoper_pos = None    target_pos = None    counter = 0    for char in cells:        line.append(char)        if char == "G":            grasshoper_pos = counter        elif char == "T":            target_pos = counter        counter += 1     if abs(target_pos - grasshoper_pos) % k == 0:        if target_pos > grasshoper_pos:            for x in range(grasshoper_pos, target_pos, k):                if line[x] == "#":                    return "NO"        else:            for x in reversed(range(grasshoper_pos, target_pos, -k)):                if line[x] == "#":                    return "NO"        return "YES"    else:        return "NO" print(findPath(n,k,cells))
