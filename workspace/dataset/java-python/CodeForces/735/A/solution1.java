import java.util.Scanner;import java.util.StringTokenizer; public class Ostap {      public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String line = scanner.nextLine();         StringTokenizer stringTokenizer = new StringTokenizer(line);        int n = Integer.parseInt(stringTokenizer.nextToken());        int k = Integer.parseInt(stringTokenizer.nextToken());         String route = scanner.nextLine().trim();         int gPos = -1, iPos = -1;        for (int i = 0; i < route.length(); i++) {            if (route.charAt(i) == 'G') {                gPos = i;            } else if (route.charAt(i) == 'T') {                iPos = i;            }        }         if (gPos > iPos) {            for (int i = gPos - k; i >= 0; i = i - k) {                if (route.charAt(i) == 'T') {                    System.out.println("YES");                    return;                } else if (route.charAt(i) == '#') {                    System.out.println("NO");                    return;                }            }            System.out.println("NO");        }else{            for (int i = gPos + k; i < n; i = i + k) {                if (route.charAt(i) == 'T') {                    System.out.println("YES");                    return;                } else if (route.charAt(i) == '#') {                    System.out.println("NO");                    return;                }            }            System.out.println("NO");        }    }  }
