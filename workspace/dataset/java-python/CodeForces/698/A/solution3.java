import java.util.Scanner;public class GymContest {    static int[][] mem;	public static void main(String[] args) {				Scanner s=new Scanner(System.in);		  int n=s.nextInt();		  mem=new int[n][3];		  int[] arr=new int[n];		  		  for(int i=0; i<n; i++)			  arr[i]=s.nextInt();		  System.out.println(dp(arr,0,0));           //R-0, C-1, G-2	} 	private static int  dp(int[] arr, int i, int j) {				if(i==arr.length)			return 0;				if(mem[i][j]!=0)			return mem[i][j];				if(arr[i]==0)			return mem[i][j]=1+dp(arr,i+1,0);				if(arr[i]==1) {			if(j==1)				return mem[i][j]=1+dp(arr,i+1,0);						return mem[i][j]=dp(arr,i+1,1);		}				if(arr[i]==2) {			if(j==2)				return mem[i][j]=1+dp(arr,i+1,0);						return mem[i][j]=dp(arr,i+1,2);		}				if(j==0)			return mem[i][j]=Math.min(dp(arr,i+1,1),dp(arr,i+1,2));				if(j==1)			return mem[i][j]=dp(arr,i+1,2);				return mem[i][j]=dp(arr,i+1,1);	}}
