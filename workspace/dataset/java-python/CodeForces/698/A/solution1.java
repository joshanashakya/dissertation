import java.util.*; public class Test {     static Scanner sc;    static Map<String, Integer> dp;         public static void main(String[] args) {        sc = new Scanner(System.in);        dp = new HashMap<>();               int t = 1;        while(t-->0) solve();    }     static void solve(){                    int n = sc.nextInt();        int[] arr = new int[n];         for(int i = 0; i < n; i++)            arr[i] = sc.nextInt();         int ans = 0;        ans = rest(0, 0, arr);         System.out.println(ans);     }     static int rest(int pvs, int curr_ind, int[] arr){        int ans = 0;         if(curr_ind >= arr.length)            return ans;         String key = pvs+"-"+curr_ind;        if(dp.containsKey(key))            return dp.get(key);         if(arr[curr_ind] == 0){            ans = rest(0, curr_ind+1, arr) + 1;        }        else if(arr[curr_ind] == 3){            if(pvs == 1){                ans = Math.min(1+rest(0, curr_ind+1, arr), rest(2, curr_ind+1, arr));            }            else if(pvs == 2)                ans = Math.min(rest(1, curr_ind+1, arr), 1 + rest(0, curr_ind+1, arr));            else                ans = Math.min(rest(1, curr_ind+1, arr), rest(2, curr_ind+1, arr));         } else {            if(pvs == arr[curr_ind])                ans = 1 + rest(0, curr_ind+1, arr);            else                ans = rest(arr[curr_ind], curr_ind+1, arr);        }         dp.put(key, ans);         return ans;     }    }
