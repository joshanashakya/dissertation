#include<iostream>#include<iomanip>#include<vector>#include<math.h>#include<string>#include<sstream>#include<stdlib.h>#include<algorithm>#include<unordered_map>#include<unordered_set>#include<cstdlib>#include<climits>#include<cstring>#include<cmath>#include<map>#include<set>#include<stack>#include<queue> #define     fi(x,y) for(size_t i(x); i<y; i++)      //fl(0,n)#define     fj(x,y) for(size_t j(x); j<y; j++)#define     fk(x,y) for(size_t k(x); k<y; k++)#define     fa(x) for(auto i:x)                     //range based#define     fri(x,y) for(size_t i(x); i>=y; i--)    //reverse for loop#define     frj(x,y) for(size_t j(x); j>=y; j--)#define     frk(x,y) for(size_t k(x); k>=y; k--) #define     ll              long long#define     ld              long double#define     ull             unsigned long long#define     pq              priority_queue#define     pb              push_back#define     umap            unordered_map#define     uset            unordered_set#define     mod             ((ull)pow(10,9)+7)#define     pll             pair<ll,ll>#define     pii             pair<int,int>using namespace std; ull max(ull a[], ull b[]){    ull r(*a);    ull *k=a;    while(k!=b){        r = (r>(*k))?r:(*k);        k++;    }    return r;} ull min(ull a[], ull b[]){    ull r(*a);    ull *k=a;    while(k!=b){        r = (r<(*k))?r:(*k);        k++;    }    return r;} void sieve(bool prime[], int n){    memset(prime, true, sizeof(bool)*n);    for (int p = 2; p * p <= n; p++)    {        if (prime[p] == true)         {            for (int i = p * p; i <= n; i += p)                prime[i] = false;        }    }} template<typename T>bool compair(pair<T,T> a, pair<T,T> b){    // return a.second>b.second;   //desc    return a.second<b.second;   //asc} bool isPal(string a){    if(a.size() == 0)return true;    fi(0,a.size()/2+1){        if(a[i] != a[a.size()-1-i])            return false;    }    return true;} void solve(){    string s;    cin>>s;    fi(0,s.size()){        switch(s[i]){            case 'A':            case 'H':            case 'I':            case 'M':            case 'O':            // case 'S':            case 'T':            case 'U':            case 'V':            case 'W':            case 'X':            case 'Y':                continue;            default:{                cout<<"NO";                return;            }        }    }    if(isPal(s)){        cout<<"YES";    }    else{        cout<<"NO";    }} int main() {    ios_base::sync_with_stdio(false);    cin.tie(NULL);    cout.tie(NULL);    // bool s[1000001];    // sieve(s, 1000000);    // ull t;    // cin>>t;    // while(t--)    {solve();}    return 0;}
