def solve(n,a,b,seq) :    empty = 0    ans = 0    for index in range(n) :        if seq[index] == '.' :            empty += 1        else :            half = empty//2            half2 = empty-half            if a > b :                if a >= half2 :                    a -= half2                    ans += half2                else :                    ans += a                    a = 0                                if b >= half :                    b -= half                    ans += half                else :                    ans += b                    b = 0            else :                if b >= half2 :                    b -= half2                    ans += half2                else :                    ans += b                    b = 0                if a >= half :                    a -= half                    ans += half                else :                    ans += a                    a = 0            empty = 0                if a == 0 and b == 0 :            break        if (a > 0 or b > 0) and empty > 0 :         half = empty//2        half2 = empty-half        if a > b :                if a >= half2 :                    a -= half2                    ans += half2                else :                    ans += a                    a = 0                                if b >= half :                    b -= half                    ans += half                else :                    ans += b                    b = 0        else :                if b >= half2 :                    b -= half2                    ans += half2                else :                    ans += b                    b = 0                if a >= half :                    a -= half                    ans += half                else :                    ans += a                    a = 0    return ans     n,a,b = list(map(int,input().split()))seq = list(input())print (solve(n,a,b,seq))        
