   import java.awt.*;import java.io.*;import java.util.*; public class Main {    public static void main(String[] args) throws Exception {        Scanner sc=new Scanner(System.in);       // StringBuffer sb = new StringBuffer();        int n=sc.nextInt();        int[] a=new int[n];        for(int i=0;i<n;i++){            a[i]=sc.nextInt();        }        int[] b = new int[n];        for(int i=0; i<n; i++)b[i] = sc.nextInt();        Map<Integer,Integer> exit = new HashMap<>();         for(int i=0; i<n; i++){            exit.put(b[i], i);        }        int res = 0;        // exit index;        int maxIndex = -1;        for(int ai : a){            int exitIndex = exit.get(ai);            // if exitIndex >             if(exitIndex < maxIndex){                res++;            }else{                maxIndex = exitIndex;            }        }        System.out.println(res);     }}
