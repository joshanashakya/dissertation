import java.util.Scanner; public class A_Panoramix_s_Prediction {     static boolean isPrime(int n) {        int c=0;        for (int i=1; i<=n; i++) {            if (n%i==0) c++;        }        if (c==2) return true;        return false;    }     static String solve() {            int n=sc.nextInt();        int m=sc.nextInt();                if (isPrime(n) && isPrime(m)) {            for (int i=n+1; i<m; i++) {                if (isPrime(i)) return "NO";            }            return "YES";        }        return "NO";        }     static Scanner sc = new Scanner(System.in);    public static void main (String arg[]) {        System.out.println(solve());    }}
