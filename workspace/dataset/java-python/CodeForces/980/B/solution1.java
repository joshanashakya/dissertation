import java.util.*;import java.io.*; public class Ana{	static int n,k;		public static void main(String[] args) throws IOException {		Scanner sc = new Scanner(System.in);		PrintWriter out = new PrintWriter(System.out);		n=sc.nextInt();		k=sc.nextInt();		char[][] grid = new char[4][n];		for(int i=0;i<4;i++)			Arrays.fill(grid[i], '.');		if(k%2==0) {			int j = 1;			while(k>0) {				k-=2;				grid[1][j]=grid[2][j] = '#';				j++;			}		}else {			int mid = n/2;			grid[1][mid] = '#';			k--;			for(int d=1;d<n/2 && k>0;k-=2,d++)				grid[1][mid+d] = grid[1][mid-d] = '#';			for(int d=1;d<n/2 && k>0;k-=2,d++)				grid[2][mid+d] = grid[2][mid-d] = '#';		}		out.println("YES");		for(int i=0;i<4;i++) {			for(int j=0;j<n;j++)				out.print(grid[i][j]);			out.println();		}		out.close();	} 	static class Scanner {		StringTokenizer st;		BufferedReader br; 		public Scanner(InputStream s) {			br = new BufferedReader(new InputStreamReader(s));		} 		public Scanner(String s) throws FileNotFoundException {			br = new BufferedReader(new FileReader(s));		} 		public String next() throws IOException {			while (st == null || !st.hasMoreTokens())				st = new StringTokenizer(br.readLine());			return st.nextToken();		} 		public int nextInt() throws IOException {			return Integer.parseInt(next());		} 		public int[] nextIntArray(int n) throws IOException {			int[] ans = new int[n];			for (int i = 0; i < n; i++)				ans[i] = nextInt();			return ans;		} 		public long nextLong() throws IOException {			return Long.parseLong(next());		} 		public String nextLine() throws IOException {			return br.readLine();		} 		public double nextDouble() throws IOException {			return Double.parseDouble(next());		} 		public boolean ready() throws IOException {			return br.ready();		} 	}}
