import java.io.*;import java.util.*;public class Asd {static PrintWriter w=new PrintWriter(System.out);static FastScanner s=new FastScanner();	public static void main(String[] args) {				int t=1;//s.nextInt();		while(t-->0)                {		    solve();		}	       w.close();}        public static void solve()        {            int n=s.nextInt();            int k=s.nextInt();            int m=s.nextInt();             String str[]=new String[n];                        for(int i=0;i<n;i++)                  str[i]=s.next();            int cost[]=s.readArray( n);            HashMap<String,Integer> map=new HashMap<>();            for(int i=0;i<n;i++)                map.put(str[i],cost[i]);            while(k-->0)            {                int x=s.nextInt();int min=Integer.MAX_VALUE;                int arr[]=s.readArray(x);                for(int i=0;i<x;i++)                    min=Math.min(min,cost[arr[i]-1]);                for(int i=0;i<x;i++)                    map.put(str[arr[i]-1],min);            }            long sum=0;            while(m-->0)            {                String sr=s.next();                sum+=(long)map.get(sr);            }            w.println(sum);                                                           }		static class FastScanner {		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st=new StringTokenizer("");		public String next() {			while (!st.hasMoreElements())				try {					st=new StringTokenizer(br.readLine());				} catch (IOException e) {					e.printStackTrace();				}			return st.nextToken();		}		int[] readArray(int n) {			int[] a=new int[n]; 			for (int i=0; i<n; i++) a[i]=nextInt();			return a;		}				int nextInt () {			return Integer.parseInt(next());		}                long nextLong () {			return Long.parseLong(next());		}                double nextDouble () {			return Double.parseDouble(next());		}	} }
