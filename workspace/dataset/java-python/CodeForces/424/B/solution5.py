from sys import stdin, stdoutfrom math import sqrtnmbr = lambda: int(stdin.readline())lst = lambda: list(map(int,stdin.readline().split()))sq= lambda x,y: (x*x+y*y)mega=10**6def fn(rad):    sm=init    for a,b,pop in l1:        if sq(a,b)<=rad:            sm+=pop    if sm>=mega:return True    return Falsefor _ in range(1):#nmbr()):    n,init=lst()    l1=[lst() for i in range(n)]    total=sum(v[2] for v in l1)    if total+init<mega:        print(-1)        continue    l=1;r=10**12    while l<=r:        mid=(l+r)>>1        if fn(mid)==False:l=mid+1        else:r=mid-1    print(sqrt(l))
