import com.sun.org.apache.bcel.internal.generic.AALOAD;import com.sun.org.apache.bcel.internal.generic.GOTO;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair;import static jdk.nashorn.internal.runtime.regexp.joni.Syntax.Java; public class Main{        public static void main(String[] args) throws IOException    {        Scanner input = new Scanner(System.in);        int n = input.nextInt();        PriorityQueue<Integer> pqueue = new PriorityQueue<>(Collections.reverseOrder());        long total=0;        int w[]= new int[n];        int h[] = new int[n];        StringBuilder ans = new StringBuilder();        for (int i = 0; i < n; i++) {        w[i] = input.nextInt();        h[i] = input.nextInt();        pqueue.add(h[i]);        total+=w[i];                }        for (int i = 0; i < n; i++) {           long nowW = total-w[i];           long height= -1;           if(pqueue.peek()==h[i])           {               int one = pqueue.poll();               height  = pqueue.peek();               pqueue.add(one);           }           else          height = pqueue.peek();          //            System.out.println(noww+" "+height);           long result = nowW*height;           ans.append(result+" ");           pqueue.add(h[i]);                   }        System.out.println(ans);                    }        }
