import java.io.BufferedReader;import java.io.InputStreamReader;import java.io.PrintStream;import java.util.HashMap;import java.util.Map;import java.util.Scanner; public class Main { 	public static void main(String[] args) {		Scanner sc = new Scanner( new BufferedReader(new InputStreamReader(System.in))) ;		PrintStream ps = System.out;		int n = sc.nextInt();		Map<String, Integer> map =  new HashMap<String, Integer>();				map.put("Polycarp".toUpperCase(), 1);				int max = 0;		for (int i = 0; i < n; i++){			String a = sc.next().toUpperCase();			sc.next();			String b = sc.next().toUpperCase();			//ps.println(' ' + b);			int len = map.get(b) == null ? 0 : map.get(b) + 1 ;			map.put(a, len);			if (len > max) max = len;		}		ps.println(max);		sc.close();		//ps.close();	} }
