/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Solution{	public static void main (String[] args) throws java.lang.Exception	{		int n;		Scanner input = new Scanner(System.in);    	n = input.nextInt();    	String s;    	HashMap<String, Integer> m = new HashMap<String, Integer>();    	m.put("polycarp", 1);    	s = input.nextLine();    	for(int i = 0; i<n; i++) {    	    s = input.nextLine();    	   // System.out.println(s);    	    String arr[] = s.split(" ");    	    String two = arr[2].toLowerCase();    	    String zero = arr[0].toLowerCase();    	    m.put(zero, m.get(two)+1);    	}    	int max = 0;    	for (String i : m.keySet()) {    	    if(m.get(i)>max) {max= m.get(i);}    	}    	    	System.out.println(max);    }}
