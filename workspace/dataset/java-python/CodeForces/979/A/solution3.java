import java.util.*;import java.io.*; public class Main{	static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new                     InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException  e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    } 	public static void main (String[] args)	{ 		FastReader reader = new FastReader();		long dummy = reader.nextLong();		//String input = reader.next(); 		if ((dummy +1)%2 == 0 || dummy == 0)		System.out.println((dummy+1)/2);		else		System.out.println(dummy+1);		 		/*		int i, j;		Factorial[] decimal = new Factorial[10]; 		decimal[0] = new Factorial (0, 0, 0, 0);		decimal[1] = new Factorial (0, 0, 0, 0);		decimal[2] = new Factorial (1, 0, 0, 0);		decimal[3] = new Factorial (1, 1, 0, 0);		decimal[4] = new Factorial (3, 1, 0, 0);		decimal[5] = new Factorial (3, 1, 1, 0);		decimal[6] = new Factorial (4, 2, 1, 0);		decimal[7] = new Factorial (4, 2, 1, 1);		decimal[8] = new Factorial (7, 2, 1, 1);		decimal[9] = new Factorial (7, 4, 1, 1); 		Factorial inputPower = new Factorial (0, 0, 0, 0); 		for (i=0; i<input.length(); i++)		{			inputPower.addFactorial (decimal[(int)input.charAt(i) - (int)'0']);		} 		ArrayList<Integer> listString = new ArrayList<Integer>(); 		while (inputPower.sevens > 0)		{			inputPower.subFactorial (decimal[7]);			listString.add (7);		} 		while (inputPower.fives > 0)		{			inputPower.subFactorial (decimal[5]);			listString.add (5);		} 		while (inputPower.threes > 0)		{			inputPower.subFactorial (decimal[3]);			listString.add (3);		} 		while (inputPower.twos > 0)		{			inputPower.subFactorial (decimal[2]);			listString.add (2);		} 		for (i=0; i<listString.size(); i++)			System.out.print (listString.get(i)); 		System.out.println ();		*/	}	} /*class Factorial{	int twos;	int threes;	int fives;	int sevens; 	Factorial (int a, int b, int c, int d)	{		this.twos = a;		this.threes = b;		this.fives = c;		this.sevens = d; 	} 	Factorial ()	{		this.twos = 0;		this.threes = 0;		this.fives = 0;		this.sevens = 0;	} 	void addFactorial (Factorial a)	{		this.twos += a.twos;		this.threes += a.threes;		this.fives += a.fives;		this.sevens += a.sevens;	} 	void subFactorial (Factorial a)	{		this.twos -= a.twos;		this.threes -= a.threes;		this.fives -= a.fives;		this.sevens -= a.sevens;	}}*/
