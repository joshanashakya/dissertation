import java.util.*; @SuppressWarnings("unchecked")public class Main { 	public static void main(String[] args) {		Scanner in = new Scanner(System.in);		int n = in.nextInt();		int x1 = in.nextInt();		int y1 = in.nextInt();		int x2 = in.nextInt();		int y2 = in.nextInt();		if (Math.abs(x2 - x1) == n) {			int y = y1 + y2;			System.out.println(Math.min(n + y, 3*n - y));		} else if (Math.abs(y2 - y1) == n) {			int x = x1 + x2;			System.out.println(Math.min(n + x, 3*n - x));		} else {			System.out.println(Math.abs(x2 - x1) + Math.abs(y2 - y1));		}	}}
