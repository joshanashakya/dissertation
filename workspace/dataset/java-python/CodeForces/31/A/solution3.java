import java.util.ArrayList;import java.util.Collections;import java.util.Scanner; public class WormsEvolution { 	public static void main(String args[])	{		Scanner sc=new Scanner(System.in);		int n=sc.nextInt();				ArrayList<Integer> arr=new ArrayList<>();		for(int i=0;i<n;i++)		{			arr.add(sc.nextInt());		} 				for(int i=0;i<n;i++)		{			for(int j=i+1;j<n;j++)			{				int x=search(arr,(arr.get(i)+arr.get(j)));					if(x>=0)					{	System.out.println((x+1)+" "+(j+1)+" "+(i+1));					return ;					}								}		}		System.out.println(-1);	} 	private static int search(ArrayList<Integer> arr, int x) {		for(int i=0;i<arr.size();i++) {			if(x==arr.get(i))				return i;		}		return -1;	}}
