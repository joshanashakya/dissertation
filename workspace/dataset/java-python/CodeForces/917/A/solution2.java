import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; public class Main {    public static void main(String[] args) throws IOException {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        char[] arr = reader.readLine().toCharArray();        int n = arr.length;        boolean[][] forward = new boolean[n][n];        for (int i=0; i<n; i++) {            int cur = 0;            boolean mark = true;            for (int j = i; j < n; j++) {                if (arr[j] == ')') cur--;                else cur++;                if (cur < 0) mark = false;                forward[i][j] = mark;            }        }        boolean[][] backward = new boolean[n][n];        for (int i=0; i<n; i++) {            int cur = 0;            boolean mark = true;            for (int j = i; j > -1; j--) {                if (arr[j] == '(') cur--;                else cur++;                if (cur < 0) mark = false;                backward[j][i] = mark;            }        }        int ans = 0;        for (int i=0; i<n; i++) {            for (int j=i; j<n; j++) {                if (forward[i][j] && backward[i][j] && (j-i+1)%2==0) {                    ans++;                }            }        }        System.out.println(ans);    }}
