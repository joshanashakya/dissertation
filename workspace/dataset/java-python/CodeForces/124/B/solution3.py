import sysinput = sys.stdin.readline ''' ''' from itertools import permutations def transform(num, p, k):    new_num = [None] * k    for cur_i, num_i in enumerate(p):        new_num[cur_i] = num[num_i]    return int("".join(new_num))  n, k = map(int, input().split())nums = [] if n == 1:    print(0)else:    for _ in range(n):        nums.append(input().rstrip())        diff = float("inf")    for p in permutations(range(k)):        transformed = [transform(num, p, k) for num in nums]        transformed.sort()        diff = min(diff, transformed[-1] - transformed[0])        print(diff) 
