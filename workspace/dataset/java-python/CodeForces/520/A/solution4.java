import java.util.Locale;import java.util.Scanner; public class Pangram {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = Integer.parseInt(scanner.nextLine());        String line = scanner.nextLine().toLowerCase(Locale.US);         boolean[] items = new boolean[26];         for (int i = 0; i < n; i++) {            char ch = line.charAt(i);            items[ch - 'a'] = true;        }         boolean allTrue = true;        for (int i = 0; i < items.length; i++) {            if (!items[i]) {                allTrue = false;            }        }         System.out.println(allTrue ? "YES" : "NO");    }}
