import java.io.*;import java.util.*;import java.util.LinkedList;import java.util.Queue;   public class Main{    static class Pair{        int data, level;        Pair(int data, int level){            this.data = data;            this.level = level;        }    }    	public static void main(String[] args) {	    Scanner in = new Scanner(System.in);	    int n = in.nextInt();	    int m = in.nextInt();	    Set<Integer> numberSet = new HashSet<Integer>(); 	    Queue<Pair> q = new LinkedList<Pair>();	    q.add(new Pair(n, 0));	    while(!q.isEmpty()){	        Pair current = q.remove();	        if(current.data == m){	            System.out.println(current.level);	            return;	        }	        int newDouble = current.data * 2;	        int newLess = current.data - 1;	        if(!numberSet.contains(newDouble)){	            if(current.data <= m){	                q.add(new Pair(newDouble, current.level + 1));	                numberSet.add(newDouble);	            }	        }	        	        if(!numberSet.contains(newLess)){	            q.add(new Pair(newLess , current.level + 1));	            numberSet.add(newLess);	        }	        	    }	    return;	}}
