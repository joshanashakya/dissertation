import java.util.PriorityQueue;import java.util.Scanner; public class B {    public static void main(String[] args){        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();        sc.nextLine();        for(int i=0;i<t;i++){            String s = sc.nextLine();//输入字符串只可能含有1、2、3，找出包含123的最小连续子串长度            Solution(s);        }    }     public static void Solution(String s){        if(s.length()<=2){            System.out.println(0);            return;        }        PriorityQueue<Integer> L = new PriorityQueue<>();        int mid=1;        int left;        int right;         while(mid<s.length()-1){            left = mid - 1;            right = mid + 1;            while(left>=0 && s.charAt(left)==s.charAt(left+1)) left--;            while(right<=s.length()-1 && s.charAt(right)==s.charAt(right-1)) right++;            if(left>=0&&right<=s.length()-1){                if(s.charAt(left)!=s.charAt(right)) {                    L.add(right-left+1);                } else {                 }            }                        mid++;            while(mid<s.length()-1 && s.charAt(mid)==s.charAt(mid-1)){                mid++;            }           }        if(L.peek()==null){            System.out.println(0);            return;        }         System.out.println(L.peek());    }}
