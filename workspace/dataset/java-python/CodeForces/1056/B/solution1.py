import sysfrom math import sqrt input = lambda: sys.stdin.readline().rstrip("\r\n") mod = 1000000007 n, m = map(int, input().split())t = n // mck = n % mans = 0for i in range(m):    for j in range(m):        if (i ** 2 + j ** 2) % m == 0:            a = b = t            if i <= ck and i:                a += 1            if j <= ck and j:                b += 1            ans += a * bprint(ans)
