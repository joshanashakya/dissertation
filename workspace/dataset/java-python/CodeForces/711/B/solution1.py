def sum_row(arr):    ans = sum(arr[0])    for i in range(1,len(arr)):        if sum(arr[i])!=ans:            return [False]    return [True,ans]def sum_col(arr):    ans = None    for i in range(len(arr)):        temp = 0        for j in range(len(arr[0])):            temp+=arr[j][i]        if ans==None:            ans = temp        else:            if temp!=ans:                return [False]    return [True,ans]def sum_diagonal(arr):    ans = 0    ans1 = 0    for i in range(len(arr)):        ans+=arr[i][i]        ans1+=arr[i][len(arr)-i-1]    return [True,ans] if ans==ans1 else [False]def calculate_value(arr,row):    return sum(arr[row])n = int(input())a = []for i in range(n):    a.append(list(map(int,input().split())))if n==1:    print(1)    exit()flag = Falsefor i in range(n):    for j in range(n):        if a[i][j]==0:            if i!=0:                want = calculate_value(a,i-1)-sum(a[i])            else:                want = calculate_value(a,i+1)-sum(a[i])            a[i][j] = want            flag = True            break    if flag:        breakrow = sum_row(a)col = sum_col(a)dia = sum_diagonal(a)if row[0] and col[0] and dia[0]:    if want<=0:        print(-1)    elif row[1]==col[1]==dia[1]:        print(want)    else:        print(-1)else:    print(-1)
