import java.util.Scanner; public class BusToUdayland {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = Integer.parseInt(scanner.nextLine());         StringBuilder sb = new StringBuilder();        boolean didReplaceEmptySeats = false;         for (int i = 0; i < n; i++) {            String line = scanner.nextLine();            if (didReplaceEmptySeats) {             }else{                if (line.contains("OO")) {                    line = line.replaceFirst("OO", "++");                    didReplaceEmptySeats = true;                }            }            sb.append(line + System.lineSeparator());        }         if (didReplaceEmptySeats) {            System.out.println("YES");            System.out.println(sb.toString());        }else{            System.out.println("NO");        }     }}
