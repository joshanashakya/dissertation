/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Codechef{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		String str=br.readLine();		Stack <Character> st=new Stack<> ();		for(int i=0;i<str.length();i++)		{		    if(st.isEmpty()||st.peek()!=str.charAt(i))		    {		        st.push(str.charAt(i));		    }		    else if(st.peek()==str.charAt(i))		    {		        st.pop();		    }		}		StringBuilder sb=new StringBuilder("");		while(st.isEmpty()==false){		sb.append(st.pop());		}		sb.reverse();		System.out.println(sb+"");			//	String chand=st.toString();	//	for(int i=0;i<chand.length();i++)	//	{	//	 System.out.print(st.toString()+"");		 	//	}		//System.out.print(st.toString());/*		int m=st.size();		StringBuilder res=new StringBuilder("");		for(int i=0;i<m;i++)		{		    char tp=st.pop();		    res.setCharAt(i,tp);		}		for(int i=m-1;i>=0;i--)		{		    System.out.print(res.charAt(i)+"");		}	Stack <Character> res=new Stack <> ();	while(st.isEmpty()==false)	{	    char temp=st.pop();	    res.push(temp);	}	while(res.isEmpty()==false)	{	    char tmp=res.pop();	    System.out.print(tmp+"");	}*/	}}
