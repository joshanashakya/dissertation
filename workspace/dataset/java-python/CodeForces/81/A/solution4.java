import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc=new Scanner(System.in);		String str=sc.nextLine();		Stack<Character> stk=new Stack<>();		for(int i=0;i<str.length();i++){		if(stk.empty() || stk.peek()!=str.charAt(i)){			stk.push(str.charAt(i));		}				else if(stk.peek()==str.charAt(i)){			stk.pop();		}	 }	 StringBuilder sb = new StringBuilder();		while (!stk.empty())			sb.append(stk.pop()); 		System.out.println(sb.reverse());	}}
