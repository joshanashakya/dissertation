import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintStream; /** * _81B * θ(|s|) time * θ(|s|) space * * @author artyom */public class _81B implements Runnable {    private BufferedReader in;     private Object solve() throws IOException {        StringBuilder sb = new StringBuilder();        String s = nextToken();        for (int i = 0, q = 0, n = s.length(); i < n; i++) {            char x = s.charAt(i);            switch (q) {                case 0:                    if (x == '.') {                        sb.append(sb.length() == 0 ? "..." : " ...");                        q = 2;                    } else if (x == ',') {                        sb.append(',');                        q = 1;                    } else if (x != ' ') {                        sb.append(x);                        q = 4;                    }                    break;                case 1:                    if (x == '.') {                        sb.append(" ...");                        q = 2;                    } else if (x == ',') {                        sb.append(" ,");                    } else if (x != ' ') {                        sb.append(' ').append(x);                        q = 4;                    }                    break;                case 2:                    q = 3;                    break;                case 3:                    q = 0;                    break;                case 4:                    if (x == '.') {                        sb.append(" ...");                        q = 2;                    } else if (x == ',') {                        sb.append(',');                        q = 1;                    } else if (x == ' ') {                        q = 5;                    } else {                        sb.append(x);                    }                    break;                default:                    if (x == '.') {                        sb.append(" ...");                        q = 2;                    } else if (x == ',') {                        sb.append(',');                        q = 1;                    } else if (x != ' ') {                        sb.append(' ').append(x);                        q = 4;                    }            }         }        return sb;    }     //--------------------------------------------------------------    public static void main(String[] args) {        new _81B().run();    }     @Override    public void run() {        try {            in = new BufferedReader(new InputStreamReader(System.in));            PrintStream out = System.out;            out.print(solve());            in.close();        } catch (IOException e) {            System.exit(0);        }    }     private String nextToken() throws IOException {        return in.readLine();    }     private int nextInt() throws IOException {        return Integer.parseInt(nextToken());    }     private long nextLong() throws IOException {        return Long.parseLong(nextToken());    }}
