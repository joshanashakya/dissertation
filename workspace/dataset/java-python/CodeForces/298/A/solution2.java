import java.util.Scanner; public class Game{	public static void main(String[] args)	{		Scanner input = new Scanner(System.in); 	    int n;	    String s; 	    n = Integer.parseInt(input.nextLine());	    s = input.nextLine(); 	    for (int i = 0; i < n; ++i)	    {	        if (s.charAt(i) != '.')	        {	            if (s.charAt(i) == 'R')	            {	                while (s.charAt(i) == 'R')	                {	                    ++i;	                }	                System.out.println(i + " " + (i+1));	            }	            else	            {	                System.out.println((i+1) + " " + i);	            }	            break;	        }	    }	}}
