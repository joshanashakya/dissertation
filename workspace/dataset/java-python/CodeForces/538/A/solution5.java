import java.util.Scanner; public class CuttingBanner { 	public static void main(String[] args) {		System.out.println(helper());	}	public static String helper () {		Scanner in = new Scanner(System.in);		String s = in.next();		int length = s.length();		if (length < 10) return "NO";		String firstTen = s.substring(0, 10);		String lastTen = s.substring(length - 10, length);		//System.out.println(firstTen);		//System.out.println(lastTen);		if (firstTen == "CODEFORCES" || lastTen == "CODEFORCES") return "YES";		for (int i = 0; i <= 10; i ++) {			String start = s.substring(0, 10 - i);			String end = s.substring(length - i, length);			//System.out.println(start);			//System.out.println(end);			if ("CODEFORCES".equals(start.concat(end))) return "YES";		}		return "NO";	} }
