#include<bits/stdc++.h>using namespace std;const int N = 1e6 + 100;typedef long long LL;#define debug(x) cout << "debug : " << x << endl;//const LL ma = (1ll << 32) - 1;//#define int long long int g[15][15];int f[15][15];int a[15];int ans = -1;int n, m;void dfs(int u) {    if (u == n + 1) {        memset(f, 0, sizeof f);        int res = 0;        for (int i = 1; i <= n; ++i) {            for (int j = i + 1; j <= n; ++j) {                if (g[i][j] && !f[a[i]][a[j]]) {                    ++res;                    f[a[i]][a[j]] = 1;                    f[a[j]][a[i]] = 1;                }            }        }        ans = max(res, ans);        return ;    }    for (int i = 1; i <= 6; ++i) {        a[u] = i;        dfs(u + 1);    }} void solve() {    cin >> n >> m;    for (int i = 1; i <= m; ++i) {        int a, b; cin >> a >> b;        g[a][b] = 1;        g[b][a] = 1;     }    dfs(1);    cout << ans << endl; } signed main() {    ios::sync_with_stdio(0);    cin.tie(0);    cout.tie(0);#ifdef LOCAL    freopen("input.txt", "r", stdin);    freopen("output.txt", "w", stdout);#endif    int T = 1;//	cin >> T;//	scanf("%d", &T);    while (T--) {        solve();    } }   		 	 	   	   	   			  		  			
