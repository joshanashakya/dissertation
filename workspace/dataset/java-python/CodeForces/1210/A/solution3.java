import java.io.*;import java.util.*;public class Main {    public static void main(String args[])    {        FastReader input=new FastReader();        PrintWriter out=new PrintWriter(System.out);        int T=1;        while(T-->0)        {            int n=input.nextInt();            int m=input.nextInt();            int e[][]=new int[m][2];            for(int i=0;i<m;i++)            {                int u=input.nextInt();                int v=input.nextInt();                e[i][0]=u;                e[i][1]=v;            }            if(n<7)            {                out.println(m);            }            else            {                int max=0;                int a[]=new int[n+1];                for(int i=1;i<=7;i++)                {                    for(int k=1;k<=6;k++)                    {                        int x=1;                        for(int l=1;l<=7;l++)                        {                            if(l!=i)                            {                                a[l]=x;                                x++;                            }                            else                            {                                a[l]=k;                            }                        }                        HashSet<String> set=new HashSet<>();                        for(int j=0;j<m;j++)                        {                            int u=e[j][0];                            int v=e[j][1];                            String s1=a[u]+" "+a[v];                            String s2=a[v]+" "+a[u];                            if(!(set.contains(s1) || set.contains(s2)))                            {                                set.add(s1);                            }                        }                        max=Math.max(max,set.size());                    }                }                out.println(max);            }        }        out.close();    }    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }}
