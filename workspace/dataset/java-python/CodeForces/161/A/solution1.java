import java.awt.Point;import java.util.ArrayList;import java.util.List;import java.util.Scanner; public class A161 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int soldierNo = in.nextInt();        int vestNo = in.nextInt();        int X = in.nextInt();        int Y = in.nextInt();        int[] soldier = new int[soldierNo];        for (int i=0; i<soldierNo; i++) {            soldier[i] = in.nextInt();        }        int[] vest = new int[vestNo];        for (int i=0; i<vestNo; i++) {            vest[i] = in.nextInt();        }        int soldierIdx = 0;        int vestIdx = 0;        List<Point> matched = new ArrayList<>();        while (soldierIdx < soldierNo && vestIdx < vestNo) {            if (vest[vestIdx]+X < soldier[soldierIdx]) {                // vest too small                vestIdx++;            } else if (vest[vestIdx]-Y > soldier[soldierIdx]) {                // soldier too small                soldierIdx++;            } else {                // match                matched.add(new Point(soldierIdx+1, vestIdx+1));                vestIdx++;                soldierIdx++;            }        }        StringBuilder output = new StringBuilder();        output.append(matched.size()).append('\n');        for (Point p : matched) {            output.append(p.x).append(' ').append(p.y).append('\n');        }        System.out.print(output);    } }
