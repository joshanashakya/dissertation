import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer; public class OddSumArray {	public static void main (String[] args) throws IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(br.readLine());				int t = Integer.parseInt(st.nextToken());				String[] output = new String[t];				for (int i = 0; i < t; i++) {			st = new StringTokenizer(br.readLine());			int n = Integer.parseInt(st.nextToken());						st = new StringTokenizer(br.readLine());			int[] arr = new int[n];						for (int j = 0; j < n; j++) {				arr[j] = Integer.parseInt(st.nextToken());			}						if (oddSum(arr)) {				output[i] = "YES";			} else {				output[i] = "NO";			}					}				br.close();				for (String line : output) {			System.out.println(line);		}			}		static boolean oddSum(int[] arr) {				int evenCount = 0;		int oddCount = 0;				for (int i = 0; i < arr.length; i++) {			if (arr[i] % 2 == 0) {				evenCount++;			} else {				oddCount++;			}		}				if (evenCount == arr.length) {			return false;		}				if (oddCount == arr.length) {			if (arr.length % 2 == 0) {				return false;			} else {				return true; 			}		}						return true;	}}
