import java.util.Scanner; public class Practice {	public static Scanner input = new Scanner(System.in); 	public static void main(String[] args) {				int n = input.nextInt();		int[] arr = new int[n];		int m[] = new int[n];				int maxi = arr[n - 1];				for (int i = 0; i < n; i++)			arr[i] = input.nextInt();				for (int i = n - 1; i >= 0; i--) {			m[i] = Math.max(0, maxi - arr[i] + 1);			if (maxi < arr[i])				maxi = arr[i];		}				for (int i = 0; i < n; i++)			System.out.print(m[i] + " ");			} 	 }
