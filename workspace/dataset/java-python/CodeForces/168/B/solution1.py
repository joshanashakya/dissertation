import syskek = ""bol = 1for s in sys.stdin:    ok = 0    for i in range(len(s)):        if s[i] == ' ':            continue        if s[i] == '#':            ok = 1        else:            ok = 0        break    kekw = (bol == 0 and ok == 0)    for j in range(len(kek)-kekw):        if bol == 1 or kek[j] != ' ':            sys.stdout.write(kek[j])    kek = s    bol = ok for j in range(len(kek)):    if bol == 1 or kek[j] != ' ':        sys.stdout.write(kek[j])
