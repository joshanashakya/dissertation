import sysres = ''has_endl = Truefor line in sys.stdin:    if '#' in line:        i = line.index('#')        if list(line[:i]) == [' '] * i:            if not has_endl:                res += '\n'            res += line            has_endl = True            continue    for i in line.strip().split():        res += i    has_endl = Falseif not has_endl:    res += '\n'sys.stdout.write(res)
