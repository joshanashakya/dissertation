import java.util.*;import java.io.*;import java.lang.*;  public class A413 {   public static void main(String[] args) {        int n,m,min,max;    int start, end, mMin, mMax;        Scanner s = new Scanner(System.in);    n = s.nextInt();    m = s.nextInt();    min = s.nextInt();    max = s.nextInt();     int extra = n-m;    start = s.nextInt();    mMin = start;    mMax = start;    end = start;      if(end > max || end< min) {        System.out.println("Incorrect");        return;      }     for(int i=1; i<m; i++){      end = s.nextInt();      if(end > max || end< min) {        System.out.println("Incorrect");        return;      }      mMax = Math.max(end, mMax);      mMin = Math.min(end, mMin);    }    if(extra >= 2) System.out.println("Correct");    else if(mMax == max && extra == 1) System.out.println("Correct");    else if(mMin == min && extra == 1) System.out.println("Correct");    else if(mMin == min && mMax == max) System.out.println("Correct");    else System.out.println("Incorrect");  }}
