import java.io.IOException;import java.util.Scanner; /** * Created by lil9porkchop on 4/13/17. */public class A {    public static void main(String[] args) throws IOException {        Scanner scan = new Scanner(System.in);         int n = scan.nextInt();        int m = scan.nextInt();        int min = scan.nextInt();        int max = scan.nextInt(); //        int[] tests = new int[n];//        tests[0] = min;//        tests[1] = max;////        int temp;//        for (int i = 2; i < m + 2; i++) {////            temp = scan.nextInt();////            if (temp > max || temp < min) {//                System.out.println("Incorrect");//                return;//            }////            if (i >= n) {//                if (temp != max || temp != min) {//                    System.out.println("Incorrect");//                    return;//                }//            }//////            tests[temp - 1] = temp;//        }         boolean maxc = false;        boolean minc = false;         for (int i = 0; i < m; i++) {            int temp = scan.nextInt();            if (temp > max || temp < min) {                System.out.println("Incorrect");                return;            }             if (temp == max) {                maxc = true;            }             if (temp == min) {                minc = true;            }        }         int need = 2;        if (maxc && minc) {            need = 0;        }         if ((maxc && !minc) || (!maxc && minc)) {            need = 1;        }         if (m + need > n) {            System.out.println("Incorrect");            return;        }         System.out.println("Correct");    }}
