import java.util.*; public class Solution {    public static void main(String[] args) {        Scanner s = new Scanner(System.in);        int n = s.nextInt();        int cost = 0, totalCost = 0;        int[] getFrom = new int[n + 1];        int[] getTo = new int[n + 1];        Arrays.fill(getFrom, -1);        Arrays.fill(getTo, -1);         for (int i = 0; i < n; i++) {            int a = s.nextInt();            int b = s.nextInt();            int c = s.nextInt();            if (getFrom[a] != -1 || getTo[b] != -1) {                getFrom[b] = a;                getTo[a] = b;            }            else {                getFrom[a] = b;                getTo[b] = a;                cost += c;            }             totalCost += c;        }         System.out.println(Math.min(cost, totalCost - cost));         s.close();    }}
