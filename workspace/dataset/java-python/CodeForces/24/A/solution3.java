//import javafx.util.Pair; import java.io.*;import java.lang.reflect.Array;import java.util.*; public class GeOlymp{     public static void main(String[] args) throws FileNotFoundException {         //Scanner scn = new Scanner(new File("/home/bubu/Bubu/MACS/OOP/exam/src/sum.in"));         //PrintWriter out = new PrintWriter(new File("/home/bubu/Bubu/MACS/OOP/exam/src/sum.out"));         //Scanner scn = new Scanner(new File("forcycle.in"));         //PrintWriter out = new PrintWriter(new File("forcycle.out"));         /*long n = scn.nextLong();         long m = scn.nextLong();         long tots = (m*(m+1))/2 - 1;         long result = tots*(n/m) + ((n%m)*((n%m)+1))/2;*/         //out.println(result);         //out.close();          Scanner sc = new Scanner(System.in);         PrintWriter out = new PrintWriter(new BufferedOutputStream(System.out));          int n = sc.nextInt(); //         sc.nextLine();         int c1 = 0;         int c2 = 0;         int moveFrom[] = new int[n+1];         int moveTo[] = new int[n+1];         for(int i=0;i<n;i++){             int a = sc.nextInt();             int b = sc.nextInt();             int c = sc.nextInt(); // fasi             sc.nextLine();            if(moveFrom[a] == 0 && moveTo[b] == 0) {                moveFrom[a] = 1;                moveTo[b] = 1;            } else {                moveTo[a] = 1;                moveFrom[b] = 1;                c2 += c;            }            c1 += c;         }        //boolean[] visited = new boolean[n+1];        //int c1 = dfs(1, adj.get(1).get(0).x, adj.get(1).get(0).y);        //int c2 = dfs(1, adj.get(1).get(1).x, adj.get(1).get(1).y);        out.println(Math.min(c2, c1-c2));         out.close();     }     static class Triplet{        int first;        int second;        int third;        public Triplet(int a, int b, int c){            first=a;            second=b;            third = c;        }    }     static class Pair{        int x;        int y;        public Pair(int a, int b){            x=a;            y=b;        }    }}
