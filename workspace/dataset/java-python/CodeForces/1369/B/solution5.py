def solve():  n = int(input())  s = input()  first_one_idx, last_zero_idx = -1, -1  for i in range(n):    if s[i] == '1':      first_one_idx = i      break  for i in reversed(range(n)):    if s[i] == '0':      last_zero_idx = i      break  if first_one_idx < last_zero_idx and first_one_idx != -1 and last_zero_idx != -1:    ans = s[0:first_one_idx] + '0' + s[last_zero_idx + 1:]  else:    ans = s  print(ans) t = int(input())for _ in range(t):  solve()
