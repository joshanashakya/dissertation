import java.util.Scanner; public class _0694Pillars { 	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n=sc.nextInt();		int[] arr = new int[n];		int pos=-1;		for(int i=0;i<n;i++) {			arr[i]=sc.nextInt();			if(arr[i]==n)pos=i;		}		int left=pos-1;		int right=pos+1;		boolean flag=true;		int val=n-1;		while( (left>=0 || right<n )&& n>=1) {			//System.out.println(left+" "+right+" "+val);			if((left>=0 && arr[left]==val)||(right<n && arr[right]==val)) {				if(left >=0 && arr[left]==val)left--;				else if(right<n && arr[right]==val)right++;				val--;			}			else {				flag=false;				break;			}		}		if(flag)System.out.println("YES");		else System.out.println("NO");	} }
