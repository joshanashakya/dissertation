import java.util.*;public class Laptops {     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        boolean found = false;         ArrayList<LapData> list = new ArrayList<>();         for(int i = 0;i<n;i++)        {            int price = sc.nextInt();            int quality = sc.nextInt();             LapData l = new LapData(price, quality);            list.add(l);         }        sc.close();         Collections.sort(list, new Comparator<LapData>(){                        public int compare(LapData l1, LapData l2)            {                return l1.quality - l2.quality;            }        });         int cp = list.get(0).price;        for(int i = 0;i<list.size();i++)        {             if(list.get(i).price<cp)            {                found = true;                break;            }            else            {                cp= list.get(i).price;            }        }         if(found)        {            System.out.println("Happy Alex");        }        else        System.out.println("Poor Alex");             }    } class LapData{    int price;    int quality;     public LapData(int price, int quality)    {        this.price = price;        this.quality = quality;    }}
