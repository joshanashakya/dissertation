/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc = new Scanner(System.in) ;		int n = sc.nextInt() ;		int arr[][] = new int[n][2] ;		for(int i = 0; i < n ; i++){			for(int j = 0 ; j < 2 ; j++)			arr[i][j] = sc.nextInt() ;		}		if( n == 1 )		System.out.println(-1) ;		else {			int x1 = -1 , x2 = -1 , y1 = -1 , y2 = -1 ;			boolean found = false  ;			for(int i = 0 ; i < n ; i++){				for(int j = i+1 ; j < n ;j++){					if( arr[i][0] != arr[j][0] && arr[i][1] != arr[j][1])	{						x1 = arr[i][0] ;						y1 = arr[i][1] ;						x2 = arr[j][0] ;						y2 = arr[j][1] ;						found = true ;						break ;					}				}				if(found )break ;			}			if( found ){				int ans = (int)Math.abs(x2-x1) * (int)Math.abs(y2-y1) ;				System.out.println( ans );			}			else 			System.out.println(-1) ;		}			}}
