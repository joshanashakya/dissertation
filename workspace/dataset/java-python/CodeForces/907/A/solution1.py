father_size, mother_size, son_size, masha_size = list(map(int, input().split())) son_car = 0father_car = 0mother_car = 0t = max(masha_size, son_size)for car_size in range(t, 201):    if (2 * t) >= car_size and (2*son_size) >= car_size and (2*masha_size) >= car_size:        son_car = car_size        breakfor car_size in range(mother_size, 201):    if (2 * mother_size) >= car_size and (car_size > (2 * t)):        mother_car = car_size        break        for car_size in range(father_size, 201):    if (2 * father_size) >= car_size and car_size > mother_car:        father_car = car_size        break     if father_car and mother_car and son_car:    print(father_car)    print(mother_car)    print(son_car)else:    print(-1)
