import java.util.Scanner; public class B725 {     public static void main(String[] args) {        int fullRoundTime = 16;        int halfRoundTime = 7;        Scanner in = new Scanner(System.in);        String seat = in.next();        long row = Long.parseLong(seat.substring(0, seat.length()-1))-1;        char posInRow = seat.charAt(seat.length()-1);        long fullRounds = row/4;        boolean halfRound = ((row%2) == 1);        int timeInRow = "fedabc".indexOf(posInRow) + 1;        long answer = fullRoundTime*fullRounds + (halfRound ? halfRoundTime : 0) + timeInRow;        System.out.println(answer);    } }
