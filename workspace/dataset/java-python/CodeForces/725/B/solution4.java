import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;  public class CodeForce {		public static void main(String[] args) throws IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringBuilder sb = new StringBuilder();		// int t=Integer.parseInt(br.readLine());		// while(t-->0)	String sr=br.readLine();	String ss=sr.substring(0,sr.length()-1);	char ch=sr.charAt(sr.length()-1);	ArrayList<Character>list=new ArrayList<Character>();	list.add('f');	list.add('e');	list.add('d');	list.add('a');	list.add('b');	list.add('c');		Long x=Long.parseLong(ss);		long m=x%2;	long k=(x-1)/4;	long re=x-k;	long total=0l;	total=16*k;		if(m==0)	{total+=7+list.indexOf(ch)+1;			}	if(m==1)	{		total+=list.indexOf(ch)+1;	}						  System.out.println(total);   }} 
