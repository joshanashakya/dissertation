contador = 0 def dobrar_papel(a,b):    global contador    if (a <= 0 or  b <= 0):        return None        if(a == b):         contador =  contador + 1;        return None        elif (b>a):        max_fator = binario(b,a)        contador = contador + max_fator        dobrar_papel(a,b-max_fator*a)    else:        max_fator = binario(a,b)        contador+=max_fator        dobrar_papel(a-max_fator*b,b) def binario(grande ,pequeno):    aux = -1    limite_superior = grande    limite_inferior = 1    while(limite_inferior<=limite_superior):        media = (limite_superior+limite_inferior)//2        if(media == aux):            break        if(media*pequeno>=grande):            limite_superior = media-1        elif(media*pequeno < grande):            limite_inferior = media        aux = media    return media def main():    entrada = input()    entrada = entrada.split(" ")    a = int(entrada[0])    b = int(entrada[1])    dobrar_papel(a,b)    print(contador)main()
