import sysfrom array import array  # noqa: F401  def input():    return sys.stdin.buffer.readline().decode('utf-8')  n, d, m, l = map(int, input().split())last = (n - 1) * m + l if l + 1 == m:    x = last // d    print(d * (x + 1))    exit() pos = 0dp = [0] + [-1] * mflag = 1 while True:    pos += d    if pos > last or (pos % m) > l:        break     if flag:        if dp[pos % m] != -1:            delta = pos - dp[pos % m]            x = (last - pos) // delta            pos += x * delta            flag = 0            continue        else:            dp[pos % m] = pos print(pos)
