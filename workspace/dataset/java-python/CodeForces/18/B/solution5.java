import java.util.*; public class Solution {    public static void main(String[] args) {        Scanner s = new Scanner(System.in);         // long needed for input close to 10^6        long n = s.nextInt();        long d = s.nextInt();        long m = s.nextInt();        long l = s.nextInt();         // left and right of gap btwn platforms        long left = 0;        long right = 0;        long jump = 0;         for (int i = 0; i < n; i++) {            left = i * m + l;            right = (i + 1) * m;            jump = (left / d + 1) * d;             if (jump > left && jump < right) {                System.out.println(jump);                s.close();                return;            }        }         System.out.println(jump);         s.close();    }}
