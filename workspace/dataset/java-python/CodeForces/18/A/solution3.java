import java.util.Arrays;import java.util.Scanner; public class Game{	public static void main(String[] args)	{		Scanner input = new Scanner(System.in);				int[] point = new int[6];						for (int i = 0; i < 6; i++)			point[i] = input.nextInt(); 		if(right(point))			System.out.println("RIGHT");		else			if(almostRight(point))				System.out.println("ALMOST");			else				System.out.println("NEITHER");	}		public static boolean right(int[] points)	{				int[] sides = new int[3];		Arrays.fill(sides, 0);	    sides[0] = (points[4] - points[2]) * (points[4] - points[2]) + (points[5] - points[3])* (points[5] - points[3]); 	    sides[1] = (points[4] - points[0]) * (points[4] - points[0]) + (points[5] - points[1])* (points[5] - points[1]); 	    sides[2] = (points[2] - points[0]) * (points[2] - points[0]) + (points[3] - points[1])* (points[3] - points[1]); 	    	    Arrays.sort(sides);	    	    if(sides[0] > 0 && sides[2] == sides[0] + sides[1])return true;	    	    return false;	}		public static boolean almostRight(int[] points)	{		for (int i = 0; i < 6; i++) {			points[i]--;			if(right(points))return true;			points[i]++;			points[i]++;			if(right(points))return true;			points[i]--;		} 				return false;	}}
