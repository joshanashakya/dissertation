import java.util.*; public class Producing_Snow { 	static Scanner in = new Scanner(System.in);		public static void main(String[] args) {				int n = in.nextInt();				int[] arr = new int[n];				for(int i = 0; i < n; i++) {			arr[i] = in.nextInt();		} 		PriorityQueue<Long> q = new PriorityQueue<>();		long sum = 0;		for(int i = 0; i < n; i++) {			int t = in.nextInt();			q.add(arr[i]+sum);			sum+=t;			long ans = 0;			while(!q.isEmpty()&&q.peek()<=sum) {				ans += q.remove()-sum+t;			}			System.out.print(ans+(long)t*q.size() + " ");		}	}}
