p, d = (int(k) for k in input().split()) test_price = presult = pmax_num_of_nines = 0i = 1 while test_price >= p-d:    test_price_size = len(str(test_price))    num_of_nines = 0    for c in range(test_price_size):        if str(test_price)[-c-1] == '9':            num_of_nines += 1        else:            break    if num_of_nines == test_price_size and num_of_nines > max_num_of_nines:        result = test_price        break    else:        if num_of_nines > max_num_of_nines:            max_num_of_nines = num_of_nines            result = test_price    # last_number_test_price = str(test_price)[-1]    # test_price -= 10 - (9 - int(last_number_test_price))    # i = 1    # while i <= test_price_size:    last_number_test_price = str(test_price)[-1*i:]    test_price -= int('1'+'0'*i) - (int('9'*(i)) - int(last_number_test_price))    i+=1  print(result)
