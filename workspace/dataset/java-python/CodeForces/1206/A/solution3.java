import java.util.*; public class game2 {	public static void main(String[] args) 	{		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		List<Integer> nlist = new ArrayList<Integer>();				for(int i = 0;i<n;i++)		{			nlist.add(sc.nextInt());		}		HashSet<Integer> nhash = new HashSet<>(nlist);		nlist = new ArrayList<Integer>(nhash);		int m = sc.nextInt();		List<Integer> mlist = new ArrayList<Integer>();				for(int i = 0;i<m;i++)		{			mlist.add(sc.nextInt());		}		HashSet<Integer> mhash = new HashSet<>(mlist);		mlist = new ArrayList<Integer>(mhash);		Collections.sort(nlist);		Collections.sort(mlist);		int minsize;		if(nlist.size()>mlist.size())		{			minsize = mlist.size();		}		else		{			minsize = nlist.size();		}		for(int i = 0;i<minsize;i++)		{			if(nlist.contains(nlist.get(i)+mlist.get(i)) != true && mlist.contains(nlist.get(i)+mlist.get(i)) != true)			{				System.out.println(nlist.get(i) +" "+ mlist.get(i));				System.exit(0);			}			else				continue;		}		for(int i = 0;i<nlist.size();i++)		{			for(int j = 0;j<mlist.size();j++)			{				if(nlist.contains(nlist.get(i)+mlist.get(j)) != true && mlist.contains(nlist.get(i)+mlist.get(j)) != true)				{					System.out.println(nlist.get(i) +" "+ mlist.get(j));					System.exit(0);				}				else					continue;			}		}	} }
