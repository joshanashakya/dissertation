import sysfrom array import array  # noqa: F401from itertools import accumulate  def input():    return sys.stdin.buffer.readline().decode('utf-8')  n, m = map(int, input().split())a = [[0] * (m + 1)] + [[0] + list(accumulate(map(int, input().rstrip()))) for _ in range(n)]for j in range(1, m + 1):    for i in range(n):        a[i + 1][j] += a[i][j] ans = 0for si in range(n):    for ti in range(si + 1, n + 1):        for sj in range(m):            for tj in range(sj + 1, m + 1):                if a[ti][tj] - a[ti][sj] - a[si][tj] + a[si][sj] == 0:                    ans = max(ans, (ti - si + tj - sj) * 2) print(ans)
