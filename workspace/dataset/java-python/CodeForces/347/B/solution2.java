import javax.swing.*;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.*; import static java.lang.Integer.*; public class Example {     public static void main(String[] args) {          Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int sum = 0;        Map<Integer,Integer> map = new HashMap<>();        for (int i = 0; i < n; i++) {            int a =sc.nextInt();            if(a==i)                sum++;            else                map.put(i,a);         }        List<Integer> list = new ArrayList<>(map.keySet());                boolean tt=false;        for(Integer integer:list)        {                  int v1 = map.get(integer);                int v2 = map.get(v1);                if(v2==integer)                {                    tt=true;                    break;                }         }        // System.out.println(sum);if(tt){    System.out.println(sum+2);}else{    if(sum==n)    {        System.out.println(n);    }else    {        System.out.println(sum+1);    }}     }}    		   			 		  				  	  		 		 	
