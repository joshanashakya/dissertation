import java.util.List;import java.util.Scanner;import java.io.OutputStream;import java.io.IOException;import java.io.PrintWriter;import java.util.ArrayList;import java.util.Collections;import java.io.InputStream; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        TaskB solver = new TaskB();        solver.solve(1, in, out);        out.close();    }} class TaskB {    public void solve(int testNumber, Scanner in, PrintWriter out){        int m,n;        m=in.nextInt();        n=in.nextInt();        ArrayList<Long> Ai=new ArrayList<Long>(m);        Long sumA=new Long(0);        for(int i=0;i<m;++i){            Ai.add(in.nextLong());            sumA+=Ai.get(i);        }        ArrayList<Long> Bi=new ArrayList<Long>(n);        Long sumB=new Long(0);        for(int i=0;i<n;++i){            Bi.add(in.nextLong());            sumB+=Bi.get(i);        }        Collections.sort(Ai);        Collections.sort(Bi);        Long As=sumA;        Long Bs=sumB;        for(int i=0;i<Ai.size()-1;++i){            Bs+=min(Ai.get(i),sumB);        }        for(int i=0;i<Bi.size()-1;++i){            As+=min(Bi.get(i),sumA);        }        out.println(min(As,Bs));    }    private Long min(Long a,Long b){        return a>b? b:a;    }}
