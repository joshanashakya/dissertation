def binary(data,target,low,high):    if low > high:        return high    else:        mid=int((low+high)/2)        if data[mid] <= target:            return binary(data,target,mid+1,high)          else:            return binary(data,target,low,mid-1)     for _ in range(int(input())):    a,b=map(int,input().split())    total=int((a*(a-1))/2)    l=[1]    n=1    for i in range(1,a-1):       l.append(l[-1]+n)       n+=1    result=["a"]*a     k=binary(l,b,0,len(l)-1)    k+=1     kalan=b-l[k-1]    aa=a-1-k    result[aa]="b"      aaa=-1-kalan    result[aaa]="b"    print(*result,sep="")
