import java.io.BufferedReader;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*; import javafx.util.Pair; public class Main{           public static void main(String args[])    {        FastScanner input = new FastScanner();        String s = input.next();        int a[] = new int[6];        for (int i = 0; i < s.length(); i++) {            a[i] = s.charAt(i)-'0';        }        int ans = Integer.MAX_VALUE;        for (int i = 0; i <=999999; i++) {            int temp[] = new int[6];            String s1=(String.format("%06d", i));            for (int j = 0; j < 6; j++) {                temp[j] = s1.charAt(j)-'0';            }            if(temp[0]+temp[1]+temp[2]!=temp[3]+temp[4]+temp[5])            {                continue;            }            int count=0;            for (int j = 0; j < 6; j++) {                if(temp[j]!=a[j])                {                    count++;                }            }            ans = Math.min(ans, count);        }        System.out.println(ans);    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
