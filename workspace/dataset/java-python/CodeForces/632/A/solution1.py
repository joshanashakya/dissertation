n, p = map(int, input().split())a = []# ignore last value because we know the last apple count# needs to be 1 with a 'halfplus' sale to reach 0for _ in range(n-1):    a.append(input())input()# find initial apple countapples = 1for x in reversed(a):    if x == 'half':        apples *= 2    elif x == 'halfplus':        apples *= 2        apples += 1#print(apples) # add again, we removed this at the starta.append('halfplus')ans = 0for x in a:    if x == 'half':        ans += p*(apples/2)        apples /= 2    elif x == 'halfplus':        ans += p*(apples/2)        apples //= 2assert apples == 0print(int(ans))
