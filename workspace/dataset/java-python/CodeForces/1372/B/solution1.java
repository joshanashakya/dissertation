import java.util.Scanner;import java.util.ArrayList;import java.util.Collections;public class Main {    public static void main(String args[]) {        Scanner s = new Scanner(System.in);        int t = s.nextInt();        while(t-- != 0){            long n = s.nextLong();            ArrayList<Long> list = new ArrayList<>();            for(long i = 1; i <= Math.sqrt(n); i++){                if(n % i == 0){                    list.add(i);                    if(n/i != i){                        list.add(n/i);                    }                }            }            Collections.sort(list);        long a = 0,b = 0,max = Long.MAX_VALUE;        for(int i = 0; i < list.size() - 1; i++){            //System.out.println(list.get(i) + " " + (n - list.get(i)));            if(lcm(list.get(i),n - list.get(i)) < max){                max = lcm(list.get(i),n - list.get(i));                a = list.get(i);                b = n - list.get(i);            }        }        System.out.println(a + " " + b);        }          }    static long gcd(long a,long b)    {        if (a == 0)            return b;         return gcd(b % a, a);     }         static long lcm(long a,long b)    {        return (a / gcd(a, b)) * b;    }}
