import java.util.HashSet;import java.util.Scanner;import java.util.Set; public class A928 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        String S = normalize(in.next());        int N = in.nextInt();        Set<String> set = new HashSet<>();        for (int n=0; n<N; n++) {            set.add(normalize(in.next()));        }        System.out.println(set.contains(S) ? "No" : "Yes");    }     private static String normalize(String s) {        s = s.toUpperCase();        s = s.replace('O', '0');        s = s.replace('L', '1');        s = s.replace('I', '1');        return s;    } }
