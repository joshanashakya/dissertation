import java.util.*;import java.math.*;public class XeniaDivisors{	public static void main(String[] args)		{			Scanner sc = new Scanner(System.in);			int size = sc.nextInt();			ArrayList<Integer> out = new ArrayList<>();			int[] arr = new int[8];			Arrays.fill(arr,0);			boolean works = true;			for(int i = 0;i<size;i++)			{				int temp = sc.nextInt();				arr[temp]++;				if(temp==5||temp==7)					works = false;			}			while(true)			{				if(arr[1]==0)					break;				out.add(1);				arr[1]--;				if(arr[3]==0)				{					if(arr[2]==0)						break;					else					{						out.add(2);						arr[2]--;						if(arr[6]==0)						{							if(arr[4]==0)								break;							else							{								out.add(4);								arr[4]--;							}						}						else						{							arr[6]--;							out.add(6);						}					} 				}				else				{					arr[3]--;					out.add(3);					if(arr[6]==0)						break;					else					{						out.add(6);						arr[6]--;					}				}			}			if(out.size()%3!=0||out.size()==0)				works = false;			if(!works)				System.out.println("-1");			else				for(int i = 0;i<out.size();i++)				{					System.out.print(out.get(i)+" ");					if((i+1)%3==0)						System.out.println();				}		}}
