import java.util.Arrays;import java.util.Scanner; public class VasyaAndIsolatedVertices {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        long[] nm = Arrays.stream(scanner.nextLine().split(" ")).mapToLong(Long::parseLong).toArray();        if (nm[1] == 0) {            System.out.println(nm[0] + " " + nm[0]);            return;        }        long[] sums = new long[(int)nm[0] + 2];        for (int i = 2; i <= nm[0]; i++) {            sums[i] = (long) i *((long) i -1) / 2;        }        long min = nm[0] / 2 >= nm[1] ? nm[0] - 2*nm[1] : 0;        int t = Math.abs(Arrays.binarySearch(sums, nm[1]));        if (sums[t] != nm[1]) t--;        long max = nm[0] - t;        System.out.println(min + " " + max);    }}
