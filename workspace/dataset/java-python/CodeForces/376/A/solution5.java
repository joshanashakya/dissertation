/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc = new Scanner(System.in) ;		String str = sc.next() ;		long sum = 0 ;		int pivot = 0 ;		for(int i = 0 ; i < str.length() ; i++){			char c= str.charAt(i) ;			if( c == '^' ){				pivot = i ;				break ;			}		}		boolean change = false ;		for(int i = 0 ; i < str.length() ; i++){			char c = str.charAt(i) ;			if(c == '^')change = true ;			else if( !change && c != '=' ){				int num = ((int)c - (int)'0')*(pivot-i) ;			//	System.out.println(num) ;				sum+=num ;			}			else if( change && c != '='){				int num = ((int)c - (int)'0')*(pivot-i) ;			//	System.out.println(num) ;				sum+=num ;			}		}		if(sum == 0 )		System.out.println("balance") ;		else 		System.out.println( sum > 0 ? "left" : "right" ) ;	}}
