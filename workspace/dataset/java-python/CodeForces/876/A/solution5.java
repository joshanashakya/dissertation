import java.util.*; public class Absolutezero {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         int n = sc.nextInt();        int a = sc.nextInt();        int b = sc.nextInt();        int c = sc.nextInt();        int possible1 = a;        int possible2 = b;        n = n - 1;        int sum = 0;        for (int j = n; j >= 1; j--) {            int min = Math.min(possible1, possible2);            sum = sum + min;            if (min == a) {                possible1 = a;                possible2 = c;            } else if(min==b){                possible1 = b;                possible2 = c;            }else {                if(possible1==a){                    possible1 = b;                    possible2 = c;                }else {                    possible1 = a;                    possible2 = c;                }            }         }        System.out.println(sum);      }     static int countSetBits(int n) {        int count = 0;        while (n > 0) {            count += n & 1;            n >>= 1;        }        return count;    }     public List<Integer> getDivisors(int n) {        List<Integer> list = new ArrayList<>();        for (int d = 2; d * d <= n; d++) {            while (n % d == 0) {                list.add(d);                n /= d;            }        }        if (n > 1) {            list.add(n);        }        return list;    }     public int lcm(int n1, int n2) {        int gcd = 1;        for (int i = 1; i <= n1 && i <= n2; ++i) {            // Checks if i is factor of both integers            if (n1 % i == 0 && n2 % i == 0)                gcd = i;        }         int lcm = (n1 * n2) / gcd;        return lcm;    }} 		 	  	 	  		  	  	  				 				
