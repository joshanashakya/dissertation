import java.util.*; import static java.lang.System.out; public class Main {     Scanner scanner = new Scanner(System.in);     public static void main(String[] args) {        new Main().solve();    }     void solve() {         int n = scanner.nextInt();        int k = scanner.nextInt();         int ans = 0;        for (int i = 0; i < k; i++) {            int l = scanner.nextInt();            int a[] = new int[l];            for (int j = 0; j < l; j++) {                a[j] = scanner.nextInt();            }            int t = 0;            if (a[0] == 1) {                for (int j = 0; j < l - 1; j++) {                    if (a[j + 1] - a[j] == 1) {                        t++;                    } else {                        break;                    }                }            }            ans += (l - t - 1) * 2 + 1;        }        ans --;        out.println(ans);    }}
