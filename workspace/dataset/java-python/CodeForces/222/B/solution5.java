import java.io.*;import java.util.*;import java.util.stream.Collectors; public class cpp {    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }        char nextChar(){            return next().charAt(0);        }        long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }     public static void main(String[] args) throws IOException {        //Scanner in = new Scanner(System.in);        FastReader f= new FastReader();        int n=f.nextInt();        int m=f.nextInt();        int k=f.nextInt();        int arr[][] = new int[n][m];        for(int i=0;i<n;i++){            for(int j=0;j<m;j++){                arr[i][j]=f.nextInt();            }        }        int row[]=new int[n];        int col[]=new int[m];         for(int i=0;i<n;i++){            row[i]=i;        }        for(int j=0;j<m;j++) {            col[j] = j;        }        char ch;        int r;        int c;        StringBuilder sb=new StringBuilder();        while(k-->0){            String[] s=f.nextLine().split(" ");            ch=s[0].charAt(0);            r=Integer.parseInt(s[1]);            c=Integer.parseInt(s[2]);            r--;            c--;            if(ch=='g')                sb.append(arr[row[r]][col[c]]+"\n");            else if(ch=='r'){                int temp=row[r];                row[r]=row[c];                row[c]=temp;            }            else{                int temp=col[r];                col[r]=col[c];                col[c]=temp;            }        }        System.out.println(sb.toString());       } }
