import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*; public class Main{      public static void main(String[] args) throws IOException    {        FastScanner input = new FastScanner();		              Integer n = input.nextInt();      Integer m =input.nextInt();      Integer q =input.nextInt();      Integer a[][] = new Integer[n+1][m+1];        for (int i = 1; i <=n; i++) {                        for (int j = 1; j <=m; j++) {                a[i][j] =  input.nextInt();            }        }        StringBuilder ans = new StringBuilder();        Integer R[] = new Integer[n+1];        for (int i = 1; i <=n; i++) {            R[i]=i;        }        Integer C[] = new Integer[m+1];        for (int i = 1; i <=m; i++) {            C[i]=i;        }        for (int i = 0; i < q; i++) {                       char ch = input.next().charAt(0);            Integer one = input.nextInt();            Integer two = input.nextInt();            if(ch=='c')            {                 Integer temp = C[one];                C[one] = C[two];                C[two] = temp;            }else if(ch=='r')            {                Integer temp = R[one];                R[one] = R[two];                R[two] = temp;            }else            {                ans.append(a[R[one]][C[two]]+"\n");            }        }        System.out.println(ans);                    }    static class FastScanner {		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st=new StringTokenizer("");		String next() {			while (!st.hasMoreTokens())				try {					st=new StringTokenizer(br.readLine());				} catch (IOException e) {					e.printStackTrace();				}			return st.nextToken();		}				int nextInt() {			return Integer.parseInt(next());		}		int[] readArray(int n) {			int[] a=new int[n];			for (int i=0; i<n; i++) a[i]=nextInt();			return a;		}		long nextLong() {						return Long.parseLong(next());		}	} }
