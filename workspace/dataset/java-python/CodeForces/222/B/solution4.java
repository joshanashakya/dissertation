import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*; public class Main{    //    public static void SwapRow(int row1,int row2)//    {//        for (int i = row1;i<=row1; i++) {//            for (int j = 1; j <=m ; j++) {//            int temp = a[i][j];//            a[i][j] = a[row2][j];//            a[row2][j] = temp;//        }//        }//        //    }//    //    public static void SwapCol(int col1, int col2)//    {//        for (int i = 1; i <=n; i++) {//            for (int j = col1;j<=col1 ; j++) {//                int temp = a[i][j];//                a[i][j] = a[i][col2];//                a[i][col2] = temp;//            }//        }//    }     public static void main(String[] args) throws IOException    {        BufferedReader input = new BufferedReader(new InputStreamReader(System.in));		PrintWriter out = new PrintWriter(System.out);        String[] in = input.readLine().split(" ");      Integer n = Integer.parseInt(in[0]);      Integer m = Integer.parseInt(in[1]);      Integer q = Integer.parseInt(in[2]);      Integer a[][] = new Integer[n+1][m+1];        for (int i = 1; i <=n; i++) {            in  =input.readLine().split(" ");            for (int j = 1; j <=m; j++) {                a[i][j] =  Integer.parseInt(in[j-1]);            }        }        StringBuilder ans = new StringBuilder();        Integer R[] = new Integer[n+1];        for (int i = 1; i <=n; i++) {            R[i]=i;        }        Integer C[] = new Integer[m+1];        for (int i = 1; i <=m; i++) {            C[i]=i;        }        for (int i = 0; i < q; i++) {            in  = input.readLine().split(" ");            char ch = in[0].charAt(0);            Integer one = Integer.parseInt(in[1]);            Integer two = Integer.parseInt(in[2]);            if(ch=='c')            {                 Integer temp = C[one];                C[one] = C[two];                C[two] = temp;            }else if(ch=='r')            {                Integer temp = R[one];                R[one] = R[two];                R[two] = temp;            }else            {                ans.append(a[R[one]][C[two]]+"\n");            }        }        System.out.println(ans);                    } }
