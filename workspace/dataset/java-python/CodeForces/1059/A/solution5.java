import java.util.*;public class Cashier { 	public static void main(String[] args)	{		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int work = sc.nextInt();		int breakLength = sc.nextInt();		int[][] customers = new int[n][];		for (int i = 0; i < n; i++)			customers[i] = new int[] {sc.nextInt(), sc.nextInt()};		sc.close();		if (n == 0)		{			System.out.println(work / breakLength);			sc.close();			return;		}		int num = 0;		int time = 0;		for (int i = 0; i < n; i++)			if (customers[i][0] - time >= breakLength)			{				time += breakLength;				num++;				i--;			}			else time = customers[i][0] + customers[i][1];		num += (work - time) / breakLength;		System.out.println(num);	}	}
