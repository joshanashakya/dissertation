import java.io.*;import java.util.*;public class Main { 	public static final int mod=1000000007;		static long[][] dp;	public static void main(String[] args) throws IOException {				//File filein = new File("input.txt");		//BufferedReader sc=new BufferedReader(new FileReader(filein));		//PrintWriter out=new PrintWriter(new FileWriter("output.txt"));		 		//Scanner sc=new Scanner(System.in);		BufferedReader sc=new BufferedReader(new InputStreamReader(System.in));		PrintWriter out=new PrintWriter(System.out);				//int tt=Integer.parseInt(sc.readLine());		int t=1;		while(t-->0){			out.flush();			String[] ip=sc.readLine().split(" ");			int n=Integer.parseInt(ip[0]);						int[][] arr=new int[n][2];			for(int i=0;i<n;i++){				ip=sc.readLine().split(" ");				arr[i][0]=Integer.parseInt(ip[0]);				arr[i][1]=Integer.parseInt(ip[1]);			}			Arrays.sort(arr,(a,b)->Integer.compare(a[1], b[1]));						int currf=-1;			int ans=0;			for(int i=0;i<n;i++){				int start=arr[i][0];				int finish=arr[i][1];				if(start>currf){					ans++;					currf=finish;				}			}			out.println(ans); 		}		out.flush();		out.close();		sc.close();	} 	 }
