/*If you want to aim high, aim highDon't let that studying and grades consume youJust live life young******************************What do you think? What do you think?1st on Billboard, what do you think of itNext is a Grammy, what do you think of itHowever you think, I’m sorry, but shit, I have no fcking interest*******************************I'm standing on top of my Monopoly boardThat means I'm on top of my game and it don't stoptil my hip don't hop anymorehttps://www.a2oj.com/Ladder16.html*******************************Shining through the city with a little funk and soul*******************************300iq as writer = Sad!*/import java.util.*;import java.io.*;import java.math.*;    public class x825B   {      public static void main(String hi[]) throws Exception      {         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));         char[][] grid = new char[10][10];         for(int r=0; r < 10; r++)            grid[r] = infile.readLine().toCharArray();         String res = "NO";         for(int r=0; r < 10; r++)            for(int c=0; c < 10; c++)               if(works(r, c, grid))                  res = "YES";         System.out.println(res);      }      public static boolean works(int r, int c, char[][] grid)      {         ArrayList<Integer> ls = new ArrayList<Integer>();         for(int i=r; i <= r+4; i++)         {            ls.add(i);            ls.add(c);         }         if(yay(ls, grid))            return true;         ls = new ArrayList<Integer>();         for(int i=c; i <= c+4; i++)         {            ls.add(r);            ls.add(i);         }         if(yay(ls, grid))            return true;         ls = new ArrayList<Integer>();         for(int d=0; d < 5; d++)         {            ls.add(r+d);            ls.add(c+d);         }         if(yay(ls, grid))            return true;         ls = new ArrayList<Integer>();         for(int d=0; d < 5; d++)         {            ls.add(r+d);            ls.add(c-d);         }         return yay(ls, grid);      }      public static boolean yay(ArrayList<Integer> ls, char[][] grid)      {         for(int x: ls)            if(x < 0 || x >= 10)               return false;         int xc = 0;         int dc = 0;         for(int i=0; i < ls.size(); i+=2)         {            char c = grid[ls.get(i)][ls.get(i+1)];            if(c == 'X')               xc++;            else if(c == '.')               dc++;         }         return xc == 4 && dc == 1;      }   }
