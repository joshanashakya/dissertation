import java.util.Scanner; public class MaximumMinimum { 	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);				int n = sc.nextInt();		int k = sc.nextInt();		int min = Integer.MAX_VALUE;		int max = Integer.MIN_VALUE;		int ans = -1;		if(k == 1)		{			for(int x = 0; x < n; x++)			{				int num = sc.nextInt();				if(num < min)				{					min = num;				}			}			ans = min;		}		else if(k >=3)		{			for(int x = 0; x < n; x++)			{				int num = sc.nextInt();				if(num > max)				{					max = num;				}			}			ans = max;		}		else		{			int a = 0;			int b = 0;			for(int x = 0; x < n; x++)			{				int num = sc.nextInt();				if(x == 0)				{					a = num;				}				else if(x == n - 1)				{					b = num;				}			}			ans = Math.max(a, b);		}				System.out.println(ans);			} }
