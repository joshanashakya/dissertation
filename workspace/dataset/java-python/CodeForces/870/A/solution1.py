#include <bits/stdc++.h> #pragma GCC optimize "-O3" #define endl '\n' using namespace std; void setIO() {    ios_base::sync_with_stdio(false);    cin.tie(nullptr);    cout.tie(nullptr);} int main() {    setIO();        int n, m;    cin >> n >> m;        array<int, 10> cnt;    fill(cnt.begin(), cnt.end(), 0);        for (int i = 0; i < n; i++) {        int a;        cin >> a;        cnt[a]++;    }        for (int j = 0; j < m; j++) {        int a;        cin >> a;        cnt[a] += 2;    }        for (int i = 1; i < 10; i++) {        if (cnt[i] == 3) {            cout << i << endl;            return 0;        }    }        for (int i = 1; i < 10; i++) {        for (int j = 1; j < 10; j++) {            if ((cnt[i] == 1 && cnt[j] == 2) || (cnt[i] == 2 && cnt[j] == 1)) {                cout << i << j << endl;                return 0;            }        }    }        assert(false);}
