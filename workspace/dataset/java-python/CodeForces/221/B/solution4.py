from sys import stdin,stdoutnmbr = lambda: int(stdin.readline())lst = lambda: list(map(int,stdin.readline().split()))def fn(x,fact):    same=0    for ch in str(x):        for ch1 in str(fact):            if ch==ch1:return 1    return 0for _ in range(1):#nmbr()):    n=nmbr()    p=1;ans=0    while p*p<=n:        if n%p==0:            ans+=fn(n,p)            if p!=n//p:ans+=fn(n,n//p)        p+=1    print(ans)
