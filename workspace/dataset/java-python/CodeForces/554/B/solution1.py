n = int(input()) columns = []rows = [] for r in range(n):    row = input()    rows.append(row)    columnsneeded = [i for i in range(n) if list(row)[i] == '0']    columns.append(columnsneeded) res = []for mvmt in columns:    bigcnt = 0     for ro in rows:        cnt = 0         for sweep in mvmt:            if ro[sweep] == '0':                cnt += 1             elif ro[sweep] == '1':                continue         for nonsweep in [bruh for bruh in range(n) if bruh not in mvmt]:            if ro[nonsweep] == '1':                cnt += 1             elif ro[nonsweep] == '0':                continue         if cnt == n:            bigcnt += 1     res.append(bigcnt)  print(max(res))  
