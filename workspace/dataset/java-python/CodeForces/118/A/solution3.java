// import java.util.LinkedList;import java.util.*;import java.lang.*;  public class MainClass {    public static void main(String args[]) {        Scanner s = new Scanner(System.in);        String str = s.nextLine();           int n = str.length();        List <Character> vowelList = Arrays.asList('A', 'E', 'I', 'O', 'U', 'a', 'e', 'i', 'o', 'u','Y','y');         String ans = "";        for(int i=0;i<n;i++) {        	if(vowelList.contains(str.charAt(i))) {         	} else {        		ans += ("." + (Character.toLowerCase((str.charAt(i)))));        	}        }                System.out.println(ans);     }}
