import java.util.*; public class Main {     public static void main(String[] args) {        Scanner scanner=new Scanner(System.in);        String str=scanner.nextLine().toLowerCase();        char[] charArr=str.toCharArray();        List<Character> tempArr=new ArrayList<>();         for(int i=0;i<charArr.length;i++){            char current=charArr[i];            if(current!='a'&&current!='e'&&current!='i'&&current!='o'&&current!='u'&&current!='y'){                tempArr.add('.');                tempArr.add(current);            }        }         for(int i=0;i<tempArr.size();i++){            System.out.print(tempArr.get(i));        }    } }
