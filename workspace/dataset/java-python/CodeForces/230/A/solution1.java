//package codeforces; import java.util.*; public class dragons { //PROBLEM 230A DRAGONS (on codeforces) 	public static void main(String[] args) { 		Scanner s = new Scanner(System.in);				int kiritosStrength = s.nextInt();		int numDragons = s.nextInt();		dragon[] allDragons = new dragon[numDragons];		for(int i = 0; i < numDragons; i++)allDragons[i] = new dragon(s.nextInt(), s.nextInt());				Arrays.sort(allDragons);				boolean kiritoCanDefeatAllTheDragons = true;		for(int i = 0; i < numDragons; i++) {//			System.out.println("i = " + i);//			System.out.println("kiritosStrength = " + kiritosStrength); 			dragon currDragon = allDragons[i];//System.out.println("currDragon.strength = " + currDragon.strength);						if(kiritosStrength > currDragon.strength)				kiritosStrength += currDragon.bonus;						else {				kiritoCanDefeatAllTheDragons = false;				break;			}		}				String result = kiritoCanDefeatAllTheDragons == true ? "YES" : "NO";		System.out.println(result);		System.out.println();	} } class dragon implements Comparable<dragon>{		public int strength;	public int bonus;		public dragon(int s, int b){		strength = s;		bonus = b;	} 		public int compareTo(dragon other) { 		 if(this.strength > other.strength) return 1;				 // else return -1; *this would work though*		 		 else if(this.strength < other.strength) return -1;		 		 else return 0;	}	}/*2 21 99100 0 10 1100 100  */
