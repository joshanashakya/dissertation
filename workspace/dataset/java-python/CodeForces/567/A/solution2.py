def computeMinDist(x, y):    if x<0 and y<0:        return abs(x) - abs(y)    else:        return y-x  def computeMaxDist(x, y):    minVal = -1    if x<0 and y<0:        return abs(x) - abs(y)    else:        return y-x n = int(input())# print(n)cordinates = list(map(int, input().split()))# print(cordinates)for i in range(n):    minVal = -1    maxVal = -1    if i == 0:        minVal = computeMinDist(cordinates[0], cordinates[1])        maxVal = computeMaxDist(cordinates[0], cordinates[n-1])    elif i == n-1:        minVal = computeMinDist(cordinates[n-2], cordinates[n-1])        maxVal = computeMaxDist(cordinates[0], cordinates[n - 1])    else:        min1 = computeMinDist(cordinates[i-1], cordinates[i])        min2 = computeMinDist(cordinates[i], cordinates[i+1])        max1 = computeMaxDist(cordinates[0], cordinates[i])        max2 = computeMaxDist(cordinates[i], cordinates[n-1])        minVal = min1 if (min1 < min2) else min2        maxVal = max1 if (max1 > max2) else max2    print(minVal, " ", maxVal)
