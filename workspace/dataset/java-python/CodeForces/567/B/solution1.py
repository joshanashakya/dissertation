from sys import stdin ,stdoutfrom os import pathrd = lambda:stdin.readline().strip()wr = stdout.writeif(path.exists('input.txt')):    stdin = open("input.txt","r")import time ,math#------------------------------------=x = int(rd())capacity = 0 roomCount = 0amr = dict()mx = -1prev = -1 for _ in range(x):	sign , id  = map(str,rd().split())	if sign == '+': 		roomCount+=1		capacity = max(capacity,roomCount)	else:		if id in amr:			roomCount-=1		else:			capacity+=1	amr[id] = 1print(capacity) 
