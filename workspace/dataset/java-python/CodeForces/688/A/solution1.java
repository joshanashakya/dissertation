import java.util.Scanner;import java.util.StringTokenizer; public class Opponents {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String line = scanner.nextLine();         StringTokenizer tokenizer = new StringTokenizer(line);        int n = Integer.parseInt(tokenizer.nextToken());        int m = Integer.parseInt(tokenizer.nextToken());         int maxConsecutiveCount = 0;        int consecutiveCount = 0;        for (int i = 0; i < m; i++) {            String row = scanner.nextLine();            boolean aryaWins = row.contains("0");            if (aryaWins) {                consecutiveCount++;                if (consecutiveCount > maxConsecutiveCount) {                    maxConsecutiveCount = consecutiveCount;                }            }else{                consecutiveCount = 0;            }        }         System.out.println(maxConsecutiveCount);            }}
