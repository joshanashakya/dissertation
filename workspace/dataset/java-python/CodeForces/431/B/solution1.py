lista = []for i in range(5):    lista.append(input().split()) sums = []for i in range(0, 5):    for j in range(0, 5):        for k in range(0, 5):            for l in range(0, 5):                for m in range(0, 5):                    a = int(lista[i][j]) + int(lista[j][i])                    b = int(lista[k][l]) + int(lista[l][k])                    c = int(lista[j][k]) + int(lista[k][j])                    d = int(lista[l][m]) + int(lista[m][l])                    s = a + 2*b + c + 2*d                    lx = [i,j,k,l,m]                    if not any(lx.count(x) > 1 for x in lx):                        sums.append(s)print(max(sums))  								 	 		 		   	 		 			
