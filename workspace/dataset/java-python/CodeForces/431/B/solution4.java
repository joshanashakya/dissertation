import java.util.*;public class Main {	static int a[][] = new int[5][5];	static int max = 0;		public static void main(String[] args) {		Scanner input = new Scanner(System.in);				                for (int i = 0; i < 5; i++) {                    for (int j = 0; j < 5; j++) {                        a[i][j] = input.nextInt();                    }            }				int num[] = new int[]{0,1,2,3,4};				permutation(0, num);		          System.out.println(max);			} 	static void permutation(int s, int[] num) {            if(s==num.length)            {                int sum=0;                for (int i = 0; i <4; i++) {                    for (int j = i; j < 4; j+=2) {                        sum+= a[num[j]][num[j+1]]+a[num[j+1]][num[j]];                    }                }                max = Math.max(max, sum);                                                return;                            }            		          for (int i = s; i <num.length; i++) {                              swap(num, s, i);                              permutation(s+1, num);                              swap(num, s, i);            }	} 	static void swap(int[] num, int s, int i) {		int tmp = num[s];		num[s] = num[i];		num[i] = tmp;	}}
