import java.util.Scanner; public class BlackSquare {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int[]cals = new int[5];        for (int i = 0; i < 4; i++) {            cals[i + 1] = scanner.nextInt();        }         scanner.nextLine();        String s = scanner.nextLine();        int result = 0;        for (int i = 0; i < s.length(); i++) {            int val = Integer.parseInt(String.valueOf(s.charAt(i)));            result += cals[val];        }        System.out.println(result);    }}
