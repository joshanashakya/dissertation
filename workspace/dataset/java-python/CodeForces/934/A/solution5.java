import java.io.*;import java.util.*; public class ACompatiblePair { 	public static void main(String[] args) {        Scanner in = new Scanner(System.in);        PrintWriter out = new PrintWriter(System.out);		int n = in.nextInt();int m = in.nextInt();long maxm,maxn,minn,minm,rminn,rmaxn;		long arr[] = new long[n];		for(int i=0;i<n;i++) arr[i] = in.nextLong();		long arr2[] = new long[m];		for(int i=0;i<m;i++) arr2[i] = in.nextLong();		long realMax = arr[0]*arr2[0];		int realMaxi=0;		for(int i=0;i<n;i++) {			for(int j=0;j<m;j++) {				if(realMax<=arr[i]*arr2[j]) {					realMaxi = i;					realMax = arr[i]*arr2[j];				}			}		}		long secondMax = Long.MIN_VALUE;		for(int i=0;i<n;i++) {			if(i==realMaxi)				continue;			for(int j=0;j<m;j++) {				secondMax = Math.max(secondMax, arr[i]*arr2[j]);			}		}		out.println(secondMax);		out.close();	}}         
