import java.util.Scanner; public class A_OnSegmentsOwnPoints { 	public static void main(String[] args) {		Scanner scan = new Scanner(System.in);		int n = scan.nextInt();		int[] times = new int[101];		int start = scan.nextInt();		int end = scan.nextInt();		for(int i = start ; i < end ; i++) {			times[i] = 1;		}		for (int j = 0; j < n - 1; j++) {			int oStart = scan.nextInt();			int oEnd = scan.nextInt();			for(int k = oStart ; k < oEnd ; k++) {				times[k] = 0;			}		}		int totalTime = 0;		for(int l = 0 ; l < times.length ; l++) {			totalTime += times[l];		}		System.out.println(totalTime); 		scan.close();	} }
