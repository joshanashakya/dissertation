import java.util.Scanner;import java.util.Arrays;import java.util.Vector;import java.util.Collections;   public class Problem_Solving1 {            public static int binarysearch(long arr[] , long x , int m)    {       int l = 0 , r = m-1;          while (l <= r) {                 int mid = l + (r - l) / 2 ;                if (arr[mid] == x)                    return mid+1;                else if (arr[mid] > x)                    r = mid - 1;                else l = mid + 1;                    }      return -1;    }            public static void main(String[] args) {        // TODO code application logic here        Scanner input = new Scanner(System.in);           int n , m;        long k ;        n = input.nextInt();        m = input.nextInt();        k = input.nextLong();        long x = k ;                long[] lans = new long[n];        for (int i = 1; i < n+1; i++) {            lans[i - 1] = 2 * m * i ;        }                int r1 = 0 , r2 = 0  ;        for (int i = 0; i < lans.length; i++) {            if (lans[i] == k )            {                r1 = i + 1;                break;                        }            else            {              if (lans[i] > k)              {               r1 = i + 1;               break;              }              else                  continue;            }        }                           while (k > 2 * m) {                        k-= 2 * m ;        }        if (k % 2 != 0)k++;                long[] arr = new long[m];        for (int i = 0; i < m; i++) {            arr[i] = 2 * (i + 1);                    }                                r2 = (binarysearch(arr, k , m));        //System.out.println(r2);                        char r3 = 'L';        if (x % 2 == 0)            r3 = 'R';                System.out.println(r1 + " " + r2 + " " + r3);                                            }}
