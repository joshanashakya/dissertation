"""Pseudocode:Read NRead the input(), store in a list: weightssort weightsinstability;for i (0, len weights - 2, 2):    instability = difference between 2print instability"""N = input()weights = list(map(int, input().split())) weights.sort() minCount = -1 for i in range(0, len(weights) - 1):    for j in range(i + 1, len(weights)):        count = 0        tempWeights = weights.copy()        del tempWeights[i]        del tempWeights[j - 1]         for k in range(0, len(tempWeights) - 1, 2):            count += tempWeights[k + 1] - tempWeights[k]        if minCount == -1 or count < minCount:            minCount = count print(minCount)
