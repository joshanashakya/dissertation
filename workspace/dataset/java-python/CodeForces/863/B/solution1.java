import java.io.*;import java.util.*; public class kayaking {	 public static void main(String[] args) throws IOException {	       BufferedReader f = new BufferedReader(new InputStreamReader(System.in));	       PrintWriter out = new PrintWriter(System.out);	        StringTokenizer st = new StringTokenizer(f.readLine());	        int numKayaks = Integer.parseInt(st.nextToken()) * 2;	        int []diff = new int[numKayaks];	        st = new StringTokenizer(f.readLine());	        for (int i = 0; i < numKayaks; i++) {	        diff[i] = Integer.parseInt(st.nextToken());	        }	        Arrays.sort(diff);	        int min = Integer.MAX_VALUE;	        for (int i = 0; i < numKayaks; i++) {	        	for (int j = i+1; j < numKayaks; j++) {	        		int sum = 0; 	        		int index = -1;	        		for (int k = 0; k < numKayaks; k++) {	                 	if (k == i || k == j) {	         		     continue;	                	} else if (index == -1) {	                		index = k;	                	} else {	                		sum += diff[k] - diff[index];	                		index = -1;	                	}	        		}	     	       min = Math.min(min, sum);	     		     	        	}	        }   	        out.println(min);	        out.close();	 }}
