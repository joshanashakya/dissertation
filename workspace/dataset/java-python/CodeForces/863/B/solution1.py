n = int(input())l = list(map(int, input().split()))nat = Falsefor i in range(2 * n):    for c in range(i + 1, 2 * n):                ls = l[:]        ls[i] = 0        ls[c] = 0        ls = sorted(ls)        now = 0               for loc in range(1, len(ls), 2):            now += ls[loc] - ls[loc - 1]                if now < nat or str(nat) == 'False':            nat = nowprint(nat)
