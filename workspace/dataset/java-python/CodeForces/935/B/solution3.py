n = int(input())S = str(input())cur_y, cur_x = 0, 0C = [(cur_x, cur_y)]for c in S:    if c == 'U':        cur_y += 1    else:        cur_x += 1    C.append((cur_y, cur_x))ans = 0for i, (cur_x, cur_y) in enumerate(C):    if i == 0 or i == len(C)-1:        continue    if cur_x == cur_y:        pre_x, pre_y = C[i-1]        pos_x, pos_y = C[i+1]        if pre_x > pre_y and pos_x < pos_y:            ans += 1        elif  pre_x < pre_y and pos_x > pos_y:            ans += 1print(ans)
