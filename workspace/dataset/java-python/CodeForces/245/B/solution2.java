import java.util.*; public class Main{         public static void main(String[] args)    {        Scanner input = new Scanner(System.in);     StringBuilder s = new StringBuilder(input.next());       if(s.indexOf("http")==0)       {            s.insert(s.indexOf("http")+4,"://" );            }       else       {              s.insert(s.indexOf("ftp")+3,"://" );       }       int ruindex = -1;        for (int i = 1; i < s.length()-1; i++) {            if(s.charAt(i)=='r'&&s.charAt(i+1)=='u'&&s.charAt(i-1)!='/')            {                ruindex = i;                break;            }        }//        System.out.println(ruindex);        s.insert(ruindex, ".");        ruindex++;                if((ruindex+2)!=s.length())       {           s.insert(ruindex+2, "/");       }             System.out.println(s);    } }
