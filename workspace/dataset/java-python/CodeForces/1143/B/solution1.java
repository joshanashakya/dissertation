import java.io.*;import java.lang.*;import java.util.*; public class B1143 {    public static void main(String[] args) throws IOException{        StringTokenizer st;        BufferedReader f = new BufferedReader(new InputStreamReader(System.in));        st = new StringTokenizer(f.readLine());        int n = Integer.parseInt(st.nextToken());        f.close();        String nr = String.valueOf(n);        long best = n;        for(int i = n; i > -1; i--){            String num = String.valueOf(i);            long curr = 1;            for(int x = 0; x < num.length(); x++){                curr*=Integer.parseInt(num.substring(x,x+1));            }            if(curr > 0){                best = curr;                break;            }        }        //System.out.println(best);        for(int i = 1; i < nr.length(); i++){            long curr = 1;            String tbh = "";            for(int x = 0; x < i-1; x++){                tbh+=nr.substring(x,x+1);            }            tbh+=Integer.toString(Math.max(1,Integer.parseInt(nr.substring(i-1,i))-1));            for(int x = i; x < nr.length(); x++){                tbh+=9;            }            //System.out.println(tbh);            for(int x = 0; x < tbh.length(); x++){                curr*=Integer.parseInt(tbh.substring(x,x+1));            }            best = Math.max(curr, best);        }        System.out.println(best);      } }
