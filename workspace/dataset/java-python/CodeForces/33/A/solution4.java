import java.util.Arrays;import java.util.Scanner; public class A33 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int N = in.nextInt();        int M = in.nextInt();        int K = in.nextInt();        int[] minInRow = new int[M];        Arrays.fill(minInRow, Integer.MAX_VALUE);        for (int n=0; n<N; n++) {            int r = in.nextInt()-1;            int c = in.nextInt();            minInRow[r] = Math.min(minInRow[r], c);        }        int answer = 0;        for (int min : minInRow) {            answer += min;        }        System.out.println(Math.min(answer, K));    } }
