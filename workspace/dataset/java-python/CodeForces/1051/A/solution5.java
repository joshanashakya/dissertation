import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.StringTokenizer;import java.io.IOException;import java.io.BufferedReader;import java.io.FileReader;import java.io.InputStreamReader;import java.io.InputStream; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        AVasyaAndPassword solver = new AVasyaAndPassword();        solver.solve(1, in, out);        out.close();    }     static class AVasyaAndPassword {        public void solve(int testNumber, Scanner sc, PrintWriter pw) {            int q = sc.nextInt();            while (q-- > 0) {                char[] arr = sc.next().toCharArray();                int[] check = new int[3];                for (int i = 0; i < arr.length; i++) {                    if (Character.isDigit(arr[i]))                        check[0]++;                    else if (Character.isUpperCase(arr[i]))                        check[1]++;                    else if (Character.isLowerCase(arr[i]))                        check[2]++;                }                for (int i = 0; i < arr.length; i++) {                    if (Character.isDigit(arr[i]) && check[0] > 1) {                        if (check[1] == 0) {                            check[1]++;                            arr[i] = 'A';                        } else if (check[2] == 0) {                            check[2]++;                            arr[i] = 'a';                        }                        check[0]--;                    } else if (Character.isUpperCase(arr[i]) && check[1] > 1) {                        if (check[0] == 0) {                            check[0]++;                            arr[i] = '1';                        } else if (check[2] == 0) {                            check[2]++;                            arr[i] = 'a';                        }                        check[1]--;                    } else if (Character.isLowerCase(arr[i]) && check[2] > 1) {                        if (check[1] == 0) {                            check[1]++;                            arr[i] = 'A';                        } else if (check[0] == 0) {                            check[0]++;                            arr[i] = '1';                        }                        check[2]--;                    }                }                pw.println(new String(arr));            }        }     }     static class Scanner {        StringTokenizer st;        BufferedReader br;         public Scanner(FileReader r) {            br = new BufferedReader(r);        }         public Scanner(InputStream s) {            br = new BufferedReader(new InputStreamReader(s));        }         public String next() {            while (st == null || !st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    throw new RuntimeException(e);                }            }            return st.nextToken();        }         public int nextInt() {            return Integer.parseInt(next());        }     }} 
