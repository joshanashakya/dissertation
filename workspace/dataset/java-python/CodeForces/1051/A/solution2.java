import javafx.collections.transformation.SortedList;import javafx.util.Pair; import java.io.IOException;import java.net.Inet4Address;import java.util.*; public class Main {    public static void main(String[] args) throws IOException {        Scanner scanner = new Scanner(System.in);        int n=scanner.nextInt();        while (n-->0){            StringBuilder s=new StringBuilder(scanner.next());            boolean d=false,c=false,small=false;            List<Integer> list=new ArrayList<>(),list1=new ArrayList<>(),list2=new ArrayList<>();            boolean shouldNotChange=false;            for (int i = 0; i < s.length(); i++) {                if (d&&small&&c){                    shouldNotChange=true;                    break;                }                if (s.charAt(i)<='Z'&&s.charAt(i)>='A'){                    if (c){                        list.add(i);                    }else {                        c=true;                    }                }else if (s.charAt(i)<='z'&&s.charAt(i)>='a'){                    if (small){                        list1.add(i);                    }else {                        small=true;                    }                }else{                    if (d){                        list2.add(i);                    }else {                        d=true;                    }                }            }            if (!shouldNotChange){                if (c&&d&&!small){                    if (list.size()>0){                        s.setCharAt(list.get(0),'a');                    }else {                        s.setCharAt(list2.get(0),'a');                    }                }else if (d&&small&&!c){                    if (list1.size()>0){                        s.setCharAt(list1.get(0),'A');                    }else {                        s.setCharAt(list2.get(0),'A');                    }                }else if (c&&small&&!d){                    if (list.size()>0){                        s.setCharAt(list.get(0),'1');                    }else {                        s.setCharAt(list1.get(0),'1');                    }                }else if (c&&!d&&!small){                    s.setCharAt(list.get(0),'1');                    s.setCharAt(list.get(1),'a');                }else if (!c&&!d&&small){                    s.setCharAt(list1.get(0),'1');                    s.setCharAt(list1.get(1),'A');                }else if (!c&&d&&!small){                    s.setCharAt(list2.get(0),'A');                    s.setCharAt(list2.get(1),'a');                }                println(s);            }else {                println(s);            }        }    }    //printer    static void print(Object o) { System.out.print(o); }    static void println(Object o) { print(o);println(); }    static void println(){print('\n');}}
