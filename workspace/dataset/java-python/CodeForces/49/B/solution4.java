import java.util.Scanner;public class Sol {		public static int checkB(int a, int b) {		int max1=0,max2=0;		while(a>0) {			if(max1<a%10)				max1=a%10;			a/=10;		}		while(b>0) {			if(max2<b%10)				max2=b%10;			b/=10;		}		return Math.max(max1, max2)+1;	}		public static int checkT(int a, int b) {		int max=0;		while(a>0 ||b>0) {			if(max<a%10+b%10)				max=a%10+b%10;			a/=10;b/=10;		}		return max+1;	}	public static int baseNtoDec(String number, int base) {		   int power = 1;		   int num = 0;		   for(int i = number.length()-1; i>= 0; i--) {		      num += getVal(number.charAt(i))*power;		      power *=base;		   }		   return num;	} 	public static String decToBaseN(int dec, int base) {		   String res = "";		   while(dec > 0) {		      res += revVal(dec%base);		      dec /= base;		   }		   StringBuilder s = new StringBuilder(res);		   return s.reverse().toString();	}		public static int getVal(char c) {		   if(c >= '0' && c<='9')			      return (int)(c-'0');    		   return (int)(c-'A'+10);    	}		public static char revVal(int n) {		   if(n >= 0 && n <=9)			      return (char)(n+'0');    		   return (char)(n+'A'-10);    	}	public static void main(String[] args) {		Scanner in = new Scanner (System.in);		int max=0;		String a=in.next(), b=in.next(), sum;		for(int i = checkB(Integer.valueOf(a), Integer.valueOf(b)); i<500;i++) 			if(max < decToBaseN((baseNtoDec(a, i) + baseNtoDec(b, i)), i).length()) {				max= decToBaseN((baseNtoDec(a, i) + baseNtoDec(b, i)), i).length();			}					System.out.println(max);		} } 
