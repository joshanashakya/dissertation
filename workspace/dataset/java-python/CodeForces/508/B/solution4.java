import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}		public static void main(String[] args){				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				String str = io.next();		char ch[] = str.toCharArray();				int ld = ch[ch.length - 1] - '0';				int swap_idx = -1;				for(int i = 0; i < ch.length - 1; i++) {			int d = ch[i] - '0';			if(d % 2 == 0) {				if(d < ld) {					swap_idx = i;					break;				}				swap_idx = i;			}		}				if(swap_idx == -1) {			System.out.println(-1); 		}				else {			swap(ch, swap_idx, ch.length - 1);			for(char c : ch) {				System.out.print(c);			}			System.out.println();		}	}		static void swap(char ch[], int i, int j) {		char temp = ch[i];		ch[i] = ch[j];		ch[j] = temp;	}}
