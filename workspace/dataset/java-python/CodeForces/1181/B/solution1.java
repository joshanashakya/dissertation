import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger; public class Main {	public static void main(String[] args) throws IOException {		BufferedReader f = new BufferedReader(new InputStreamReader(System.in));		String sss = f.readLine();		int n = Integer.parseInt(sss);		String s = f.readLine();		BigInteger ans = new BigInteger(s);		int x = n / 2, y = n / 2 + 1;		while(x > 0 && s.charAt(x) == '0') x--;		while(y < n && s.charAt(y) == '0') y++;		if(x > 0) {			BigInteger a = new BigInteger(s.substring(0, x));			BigInteger b = new BigInteger(s.substring(x, n));			ans = ans.min(a.add(b));		}		if(y < n) {			BigInteger a = new BigInteger(s.substring(0, y));			BigInteger b = new BigInteger(s.substring(y, n));			ans = ans.min(a.add(b));		}		System.out.println(ans);	}}
