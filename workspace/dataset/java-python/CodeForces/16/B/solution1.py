'''Jana Goodman 16B Burglar   '''SPACE = ' ' def answer(n, arr):    matches, boxes = 0, n    for b, a in arr:        if a <= boxes:            matches += a * b            boxes -= a        else:            return matches + boxes * b    return matches if __name__ == '__main__':    n, m = map(int, input().split(SPACE))    containers = []    for _ in range(0, m):        a, b = map(int, input().split(SPACE))        containers.append((b, a))    containers.sort()    containers.reverse()    print(answer(n, containers))
