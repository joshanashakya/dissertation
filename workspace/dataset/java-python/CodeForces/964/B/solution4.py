n, A, B, C, T = map(int, input().split())t = [int(i) for i in input().split()]t.sort() total_sum = 0 for lower_border in range(n + 1):    # candidate = len([t[ind] for ind in range(lower_border)]) * A    candidate = n * A    for counter, ind in enumerate(range(lower_border, n)):        if counter != 0:            candidate -= ind * (t[ind] - t[ind - 1]) * B            candidate += ind * (t[ind] - t[ind - 1]) * C    candidate -= (n - lower_border) * (T - t[-1]) * B    candidate += (n - lower_border) * (T - t[-1]) * C    # print(candidate)    total_sum = candidate if candidate > total_sum else total_sum print(total_sum)
