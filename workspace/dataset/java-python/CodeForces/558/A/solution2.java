import java.io.PrintWriter;import java.util.*;  public class D { 	      public static void main(String[] args) throws Exception {		Scanner scan = new Scanner(System.in);		PrintWriter out= new PrintWriter(System.out);		Task t=new Task();		t.solve(scan,out);		out.close();	}} class Task{	class Pair {		int first;		int second;		public Pair(int first, int second) {			super();			this.first = first;			this.second = second;		}			}	@SuppressWarnings("unused")	public void solve(Scanner scan,PrintWriter out) throws Exception {		int n=scan.nextInt();		ArrayList<Pair>a1 = new ArrayList<Task.Pair>();		ArrayList<Pair>a2 = new ArrayList<Pair>();		for(int i=0;i<n;i++) {			int a=scan.nextInt(),b=scan.nextInt();			if(a<0) {				a1.add(new Pair(a, b));			}			else {				a2.add(new Pair(a, b));			}		}		Collections.sort(a1, new Comparator<Pair>(){			@Override			public int compare(Pair o1, Pair o2) {				// TODO Auto-generated method stub				return -Integer.compare(o1.first, o2.first);			}		});		Collections.sort(a2, new Comparator<Pair>(){			@Override			public int compare(Pair o1, Pair o2) {				// TODO Auto-generated method stub				return Integer.compare(o1.first, o2.first);			}		});		long res=0;		if(a1.size()==a2.size()) {			for (int i=0;i<a1.size();i++) {				res+=a1.get(i).second+a2.get(i).second;			}		}		else if(a1.size()>a2.size()){			for (int i=0;i<a2.size();i++) {				res+=a1.get(i).second+a2.get(i).second;			}			res+=(a1.get(a2.size()).second);		}		else {			for (int i=0;i<a1.size();i++) {				res+=(a1.get(i).second+a2.get(i).second);			}			res+=a2.get(a1.size()).second;		}		System.out.println(res);	}}
