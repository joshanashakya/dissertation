import java.io.*;import java.util.*; public class TestClass{	public static void main(String args[]) throws IOException{		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(br.readLine());		int n = Integer.parseInt(st.nextToken());		int m = Integer.parseInt(st.nextToken());		double kd = Double.parseDouble(st.nextToken());		int k = (int)(Math.round(kd*100));		//System.out.println(k);		HashMap<String,Integer> map = new HashMap<>();		for(int i = 0; i<n; i++){			StringTokenizer st1 = new StringTokenizer(br.readLine());			String name = st1.nextToken();			int level = Integer.parseInt(st1.nextToken());			int new_level = (level*k);			//System.out.println(new_level);			new_level /= 100;			if(new_level >= 100){				map.put(name, new_level);			}		} 		for(int i = 0; i<m; i++){			String name = br.readLine();			if(!map.containsKey(name)) {				map.put(name, 0);			}		}				ArrayList<String> names = new ArrayList<String>(map.keySet()); 		Collections.sort(names);				System.out.println(map.size());		for(String name: names) {			System.out.println(name+" "+map.get(name));		}	} 	static class Skill{		String name;		int level; 		public Skill(String s, int i){			name = s;			level = i;		} 		public Skill(String s){			name = s;			level = 0;		}	}}
