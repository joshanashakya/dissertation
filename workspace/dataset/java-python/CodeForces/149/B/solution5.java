import java.util.*; public class CF149B {	public static void main(String[] args) {		Scanner in = new Scanner(System.in); 				String input = in.next();		String hour = input.substring(0, input.indexOf(":"));		String minute = input.substring(input.indexOf(":")+1);				int minBase = 2;		for(int i = 0; i < input.length(); ++i) {			char c = input.charAt(i);			if(c == ':') continue;						minBase = Math.max(minBase, toNum(c) + 1);		}				//System.out.println(minBase);		int iHour = baseConvert(hour, minBase);		int iMinute = baseConvert(minute, minBase);		//System.out.println(iHour + ":" + iMinute);		if(iHour > 23 || iMinute > 59) {			System.out.println(0);			return;		}		if(baseConvert(hour, minBase) == baseConvert(hour, minBase+1) && baseConvert(minute, minBase) == baseConvert(minute, minBase+1)) {			System.out.println(-1);			return;		}				for(int i = minBase; i <= Integer.MAX_VALUE; ++i) {			iHour = baseConvert(hour, i);			iMinute = baseConvert(minute, i);			if(iHour < 24 && iMinute < 60) {				System.out.print(i + " ");			} else {				break;			}		}	}		public static int baseConvert(String s, int base) {		int ans = 0;		int power = 0;		for(int i = s.length()-1; i >= 0; --i) {			ans += Math.pow(base, power)*toNum(s.charAt(i));			++power;		}		return ans;	}		public static int toNum(char c) {		if(c-'0' <= 9) {			return c-'0';		} else {			return c-'A'+10;		}	}}
