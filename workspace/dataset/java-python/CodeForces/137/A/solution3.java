import java.util.*;public class Main{	 public static void main(String args[])    {        Scanner in  = new Scanner(System.in);        String s = in.next();        char ch = s.charAt(0);        int c=1;        int ans=1;        for(int i=1 ; i<s.length(); i++)        {            if(s.charAt(i)==ch)            {                if(c<5) c++;                else                {                    ans+=1;                    c=1;                }            }            else             {                ans+=1;                c=1;                ch=s.charAt(i);            }           // System.out.println(ch+" "+c+" "+ans);        }        System.out.println(ans);        }}
