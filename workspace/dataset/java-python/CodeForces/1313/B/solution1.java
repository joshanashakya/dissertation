import java.util.*;import java.io.*;public class Main {	static int[] A;	//static int[] B;	static int[][] DP;	static int n,m,a,b;	static int ans;	static void solve() throws IOException {			}	public static void main(String[] args)throws IOException {		// TODO Auto-generated method stub		BufferedReader in = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer tokenizer = new StringTokenizer(in.readLine());        m = Integer.parseInt(tokenizer.nextToken());        while(m > 0) {        	tokenizer = new StringTokenizer(in.readLine());        	int n = Integer.parseInt(tokenizer.nextToken());        	int x = Integer.parseInt(tokenizer.nextToken());        	int y = Integer.parseInt(tokenizer.nextToken());        	int s1 = Math.min(Math.max(x+y-n+1, 1), n);        	int s2 = Math.min(n, x+y-1);        	System.out.println(s1+" "+s2);        	m--;        }	}}
