import sys path = input()visited = set() def move(s, x, y):    if i == 'L':        x -= 1    elif i == 'R':        x += 1    elif i == 'U':        y += 1     else:        y -= 1    return x, y    x, y = 0, 0start = (x, y)parent = (x, y)visited.add(start)for i in path:    parent = (x, y)    x, y = move(i, x, y)    if (x, y) in visited:        print('BUG')        sys.exit()    else:        for i in "LRUD":            check = move(i, x, y)            if check != parent and check in visited:                print('BUG')                sys.exit()    visited.add((x, y))print('OK')
