s=input()x=0y=0d=dict()d[(x,y)]=-1f=0for j in range(len(s)):    if s[j]=="L":        x+=-1    elif s[j]=="R":        x+=1    elif s[j]=="U":        y+=1    else:        y+=-1    if (x,y) in d.keys():        f=1        break    elif (x-1,y) in d.keys() and (j-d[x-1,y])>1:        f=1        break    elif (x+1, y) in d.keys() and (j - d[x+1, y]) > 1:        f = 1        break    elif (x, y+1) in d.keys() and (j - d[x, y+1]) > 1:        f = 1        break    elif (x, y-1) in d.keys() and (j - d[x, y-1]) > 1:        f = 1        break     d[x,y]=jif f==1:    print('BUG')else:    print("OK")   
