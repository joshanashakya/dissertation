import java.io.BufferedReader;import java.io.BufferedWriter;import java.io.InputStreamReader;import java.io.OutputStreamWriter;import java.util.StringTokenizer; public class Main { 	public static void main(String[] args) throws Exception { 		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));		StringTokenizer st; 		st = new StringTokenizer(br.readLine());		int p = Integer.parseInt(st.nextToken());		int y = Integer.parseInt(st.nextToken()); 		boolean[] isPrime = new boolean[1000001];		for (int i = 0; i <= 1000000; i++) {			isPrime[i] = true;		}		isPrime[0] = false;		isPrime[1] = false;		for (int i = 2; i <= 1000000; i++) {			if (isPrime[i]) {				for (int j = i + i; j <= 1000000; j += i) {					isPrime[j] = false;				}			}		} 		int answer = -1;		for (int i = y; i > p; i--) {			boolean flag = true;			for (int j = 2; j <= Math.min(1000000, p); j++) {				if (isPrime[j] && i % j == 0) {					flag = false;				}			}			if (flag) {				answer = i;				break;			}		} 		bw.write(answer + ""); 		bw.flush();		bw.close();		br.close();	} }
