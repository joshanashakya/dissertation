import java.io.*;import java.util.*;public class Main {    public static void main(String args[])    {        FastReader input=new FastReader();        PrintWriter out=new PrintWriter(System.out);        int T=input.nextInt();        while(T-->0)        {            int n=input.nextInt();            int x=input.nextInt();            String s=input.next();            int pre[]=new int[n];            for(int i=0;i<n;i++)            {                int a=0;                if(s.charAt(i)=='1')                {                    a=-1;                }                else                {                    a=1;                }                if(i==0)                {                    pre[i]=a;                }                else                {                    pre[i]=pre[i-1]+a;                }            }            /*            for(int i=0;i<n;i++)            {                out.print(pre[i]+" ");            }            out.println();              */            int a=pre[n-1];            int count=0;            if(a==0)            {                for(int i=0;i<n;i++)                {                    if(pre[i]==x)                    {                        count++;                    }                }                if(count>0)                {                    out.println(-1);                }                else                {                    out.println(0);                }            }            else            {                for(int i=0;i<n;i++)                {                    int b=x-pre[i];                    if(b%a==0 && b/a>=0)                    {                        count++;                    }                }                if(x==0)                {                    count++;                }                out.println(count);            }        }        out.close();    }    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }}
