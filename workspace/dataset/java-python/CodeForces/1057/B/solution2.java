import java.io.*;import java.util.*; public class Main {     public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        InputReader inputReader = new InputReader(inputStream);        PrintWriter printWriter = new PrintWriter(outputStream);        Task task = new Task();        task.run(inputReader, printWriter);        printWriter.close();    }     static class Task {         int n;        int[] r;         private void read(InputReader in) {            n = in.nextInt();            r = new int[n];            for (int i = 0; i < n; ++i) {                r[i] = in.nextInt();            }        }         private void solve(PrintWriter out) {            int result = 0;            for (int i = 0; i < n; ++i) {                int sum = 0;                for (int j = i; j < n; ++j) {                    sum += r[j];                    if (sum > (j - i + 1) * 100 && result < j - i + 1) {                        result = j - i + 1;                    }                }            }            out.print(result);        }         void run(InputReader in, PrintWriter out) {            read(in);            solve(out);        }    }      static class InputReader {        private BufferedReader reader;        private StringTokenizer tokenizer;         InputReader(InputStream stream) {            reader = new BufferedReader(new InputStreamReader(stream), 1 << 15);            tokenizer = null;        }         String next() {            while (tokenizer == null || !tokenizer.hasMoreTokens()) {                try {                    tokenizer = new StringTokenizer(reader.readLine());                } catch (IOException e) {                    throw new RuntimeException(e);                }            }            return tokenizer.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }    }}
