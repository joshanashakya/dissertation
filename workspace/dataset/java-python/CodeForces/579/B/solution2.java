import java.io.BufferedReader;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*; public class Main{     public static void main(String[] args) throws IOException    {        FastScanner input = new FastScanner();        int n = input.nextInt()*2;        int a[][]  = new  int[n][n];        int ans [] = new int[n];        for (int i = 0; i < n; i++) {            for (int j = 0; j < n; j++) {                a[i][j] = -1;                            }        }        for (int i = 0; i < n; i++) {            for (int j = 0; j < i; j++) {                a[i][j] = input.nextInt();                            }        }        for (int it = 0; it < n/2; it++) {            int maxX = 0;            int maxY = 0;            for (int i = 0; i < n; i++) {                for (int j = 0; j < n; j++) {                    if(a[maxX][maxY]<a[i][j])                    {                        maxX = i;                        maxY=j;                    }                }            }            ans[maxX] = maxY;            ans[maxY] = maxX;            for (int i = 0; i < n; i++) {                a[i][maxX]  = a[maxX][i] = a[maxY][i] = a[i][maxY] = -1;            }                    }        for (int an : ans) {            System.out.print((an+1)+" ");        }        System.out.println("");             }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
