import java.util.Comparator;import java.util.PriorityQueue;import java.util.Scanner; public class ProblemB {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        PriorityQueue pq = new PriorityQueue<int[]>(Comparator.comparingInt(o -> ((int[])o)[0]).reversed());        for(int i = 1; i < 2 * n; i++)            for(int j = 0; j < i; j++) pq.add(new int[]{sc.nextInt(), i, j});         int[] used = new int[2*n];        while (!pq.isEmpty()) {            int[] value = (int[]) pq.poll();            if(used[value[1]] == 0 && used[value[2]] == 0) {                used[value[1]] = value[2] + 1;                used[value[2]] = value[1] + 1;            }        }         StringBuilder builder = new StringBuilder();        for(int i = 0; i < 2*n; i++) {            builder.append(used[i]);            if(i != 2*n-1) builder.append(" ");        }        System.out.println(builder);    }}
