import java.util.*;public class CF {    private static void sport(String[] a) {        int[][] dp = new int[26][26];        boolean[][] can = new boolean[26][26];        int ans=0;        for (String s : a) {            int l=s.charAt(0)-'a';            int r=s.charAt(s.length()-1)-'a';            for (int i = 0; i < 26; i++) {                if (can[i][l]) {                    can[i][r]=true;                    dp[i][r] = Math.max(dp[i][r], dp[i][l] + s.length());                }            }            can[l][r]=true;            dp[l][r]= Math.max(dp[l][r],s.length());        }        for (int i = 0; i < 26; i++) {            ans=Math.max(ans,dp[i][i]);        }        System.out.println(ans);    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n  = sc.nextInt();        String[] a = new String[n];        for (int i = 0; i < n; i++) {            a[i]=sc.next();        }        sport(a);    }}
