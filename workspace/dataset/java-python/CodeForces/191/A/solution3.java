import java.util.*;import java.io.*; public class  Dynasty_Puzzles{	public static void main(String[] args) throws IOException 	{		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		PrintWriter pw=new PrintWriter(System.out); 		int t=1; 		while(t-->0)		{			int n=Integer.parseInt(br.readLine()); 			long dp[][]=new long[26][26];			for(int i=0;i<n;i++)			{				String x=br.readLine().trim();				int a=x.charAt(0)-'a';				int b=x.charAt(x.length()-1)-'a'; 				for(int j=0;j<26;j++)				{					if(dp[j][a]==0)						continue;					dp[j][b]=Math.max(dp[j][b],dp[j][a]+x.length());				} 				dp[a][b]=Math.max(dp[a][b],x.length());			}						long ans=0;			for(int i=0;i<26;i++)			{				/*for(int j=0;j<26;j++)					pw.print(dp[i][j]+" "); 				pw.println();*/				ans=Math.max(ans,dp[i][i]);			} 			pw.println(ans);		}		pw.flush();	} 	}
