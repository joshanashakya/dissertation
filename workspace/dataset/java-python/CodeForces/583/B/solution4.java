import java.util.*;import java.util.stream.IntStream; public class Main{     public static void main(String[] args)    {        Scanner input = new Scanner(System.in);        int n = input.nextInt();        int a[] = new int[n];        for (int i = 0; i < n; i++) {            a[i] = input.nextInt();        }        int visited = 0;        int i=0;        int count=0;        int temp=0;        int extra=1;        int data=0;        while(visited<n)        {            if(i==-1)            {                    extra=1;                    i=0;                    count++;            }                        else if(i==n)            {                extra=-1;                i=n-1;                count++;            }                if(data>=a[i])                {                    a[i]=Integer.MAX_VALUE;                    data++;                    visited++;                }                                                           i+=extra;                    }        System.out.println(count);    }}
