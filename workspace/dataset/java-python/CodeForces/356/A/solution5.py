#include <iostream>#include <algorithm>#include <vector>#include <string>#include <map>#include <set>#include <cmath>#include <iomanip>#include <queue>#include <fstream>using namespace std; int main() {    /*vector<int>a(10);    for (int i = 0; i < 10; ++i) {        a[i] = 2 * i + 1;    }    int *c = &(a[5]);    cout << *c << endl;*/    set<int>k;    int n, le, ri, m, a, x;    cin >> n >> m;    for (int i = 0; i < n; ++i) {        k.insert(i + 1);    }    vector<int>win(n, 0);    for (int i = 0; i < m; ++i) {        cin >> le >> ri >> x;        a = *k.lower_bound(le);        while (a <= ri) {            k.erase(a);            if (a != x) win[a - 1] = x;            a = *k.lower_bound(a);        }        k.insert(x);    }    for (int i = 0; i < n; ++i) cout << win[i] << " ";    return 0;} 
