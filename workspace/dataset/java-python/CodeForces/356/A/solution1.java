import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.TreeSet;  public class Main {    static int n,m;    static int l,r,x;    static int[]k;    static TreeSet<Integer> K, sK, erase;    public static void main(String[]args) throws IOException{        String[]data;        BufferedReader f = new BufferedReader (new InputStreamReader(System.in));        data = f.readLine().split(" ");        n = Integer.valueOf(data[0]);        K = new TreeSet<>();        for(int i = 0 ; i < n ; ++i){            K.add(i+1);        }        k = new int[n];        m = Integer.valueOf(data[1]);        for(int i = 0 ; i < m ; ++i){            data = f.readLine().split(" ");            l = Integer.valueOf(data[0]);            r = Integer.valueOf(data[1]);            x = Integer.valueOf(data[2]);            sK = (TreeSet<Integer>) K.subSet(l,true, r,true);            erase = new TreeSet<>();            for(int ki : sK){                if(ki!=x){                    if(k[ki-1]==0){                        k[ki-1] = x;                        erase.add(ki);                        //K.remove(ki);                    }                }            }            K.removeAll(erase);        }        for(int i = 0 ; i < n ; ++i){            System.out.print(k[i] + " ");        }    }}
