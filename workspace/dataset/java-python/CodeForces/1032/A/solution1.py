#include <iostream>#include <cstdio>#include <algorithm>using namespace std;const int  N=1e2+10;int a[N];int main(){    int n,k;    scanf("%d %d",&n,&k);    for(int i=0;i<n;i++) scanf("%d",&a[i]);    sort(a,a+n);    int x,i,j,sum=0,sets=0,kind=0;    x=a[0],i=0;    while(i<n){            for(;i<n;i++){        if(a[i]==x)sum++;        else  break;    }            x=a[i];            kind++;            if(sum%k==0&&sum/k>sets) sets=sum/k;            else if(sum%k!=0&&sum/k+1>sets) sets=sum/k+1;            sum=0;        }        printf("%d",sets*kind*k-n);             return 0;}  	  						   	 		 		  	 				 	
