from math import *name = input()n = len(name) if n <= 20:    print(1, n)    print(name)    exit(0) rows = [] rows_amount = 2while n/rows_amount > 20:    rows_amount += 1  cutted = ceil(n/rows_amount)print(rows_amount, cutted) i = 0start = 0ostatok = n%rows_amount if n%rows_amount != 0 else rows_amountwhile i < rows_amount - (rows_amount - ostatok):    rows.append(name[start:(start+cutted)])    i += 1    start += cutted cutted -= 1while i < rows_amount:    rows.append(name[start:(start+cutted)])    rows[-1] += "*"    i += 1    start += cutted  for j in rows:    print(j)
