#include <cstdio>/*构造*/ char s[110]; int main(){	scanf("%s", s);	int len =0;	while((s[len] >= 'a' && s[len] <= 'z') || (s[len] >= 'A' && s[len] <= 'Z'))		++len;	int n =len/20, m =20, r =len%20;	if(r == 0){		printf("%d %d\n", n, 20);/*<<- \n*/		for(int i =0; i < n; ++i){			for(int j =0; j < m; ++j)				putchar(s[i*m+j]);			putchar('\n');		}	}	else if(n == 0){		printf("%d %d\n", 1, r);		for(int i =0; i < len; ++i)			putchar(s[i]);	}	else{		/*20-r 即空出来的字符个数*/		m -=(20-r)/(n+1);/*减少列数，一次搬 n 个字符*/		r =(20-r)%(n+1);		++n;		printf("%d %d\n", n, m);		int index =0;		for(int i =0; i < n; ++i){			for(int j =0; j < m; ++j){				if(j == m-1 && i > (n-r)-1)					putchar('*');				else{					putchar(s[index]);					++index;				}			}			putchar('\n');		}	}}
