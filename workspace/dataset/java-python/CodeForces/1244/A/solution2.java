import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer; public class Main { 	public static void main(String[] args) throws IOException {		BufferedReader bf = new BufferedReader(new InputStreamReader(System.in));				int t = Integer.parseInt(bf.readLine());		for(int tc=0; tc<t; tc++) {			StringTokenizer st = new StringTokenizer(bf.readLine());			/*			 * a = 들어아햐는 강의 : arr[0] 			 * b = 들어아햐는 실습 : arr[1]			 * c = 하나의 펜으로 쓸 수 있는 강의수 : arr[2]			 * d = 하나의 연필로 쓸 수 있는 실습수 : arr[3]			 * k = 필통에 담을 수 있는 필기구의 갯수 : arr[4]			 * x = 펜의 갯수			 * y = 연필의 갯수			 */			int[] arr = new int[5];				for(int i=0; i<arr.length; i++)				arr[i] = Integer.parseInt(st.nextToken());						int x = 0;			int y = 0;						x = arr[0] / arr[2];	// 펜의 개수 - 강의수 / 하나의 펜으로 쓸 수 있는 강의수			if(arr[0] % arr[2] != 0) x++;	// 나누어 떨어지지 않을경우 펜 하나가 더 필요함						y = arr[1] / arr[3];	// 연필의 갯수 - 실습수 / 하나의 연필로 쓸 수 있는 실습수			if(arr[1] % arr[3] != 0)	y ++;	// 나누어 떨어지지 않을경우 연필 하나 더필요			if(x+y <= arr[4])				System.out.println(x + " " + y);			else				System.out.println("-1");		}				bf.close();	} }
