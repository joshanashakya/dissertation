import java.util.ArrayList;import java.util.Arrays;import java.util.Collections;import java.util.Comparator;import java.util.Scanner; public class Polycarp {		static class Pairs{		int index;		int value;				Pairs(int index1, int value1){			index = index1;			value = value1;		}	}		public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int k = sc.nextInt();		sc.nextLine();		String array = sc.nextLine();		output(n, k, array);	} 	private static void output(int n, int k, String array) {		int[] stream = Arrays.stream(array.split(" ")).mapToInt(Integer::parseInt).toArray();		// TODO Auto-generated method stub				ArrayList<Pairs> out = new ArrayList<Pairs>();				for (int i = 0; i < stream.length; i++) {			out.add(new Pairs(i, stream[i]));		}				Collections.sort(out, Comparator.comparing(a -> -a.value));				ArrayList<Integer> indexes = new ArrayList<Integer>(k);				for (int i = 0; i < k; i++) {			indexes.add(out.get(i).index);		}				Collections.sort(indexes);				long totalProfit = 0;				for (int i = 0; i < k; i++) {			totalProfit += out.get(i).value;		}				System.out.println(totalProfit);				int previous = -1;		int numproblems = 0;				for (int i = 0; i < k-1; i++) {			numproblems = indexes.get(i) - previous;			previous = indexes.get(i);			System.out.print(numproblems + " ");		}				System.out.println(n - 1 - previous);	}}
