import java.util.*; public class Solution {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();        for (int i = 0; i < t; i++) {            int len = sc.nextInt();            int[]arr = new int[len];            for (int j = 0; j < len; j++) {                arr[j] = sc.nextInt();            }            check(arr);        }    }     private static void check(int[] arr) {        List<Integer> res = new ArrayList<>();         for (int i = 0; i < arr.length; i+=4) {            if(i + 4 <= arr.length) {                if (arr[i] + arr[i + 1] + arr[i + 2] + arr[i + 3] >= 2) {                    res.add(1);                    res.add(1);                } else {                    res.add(0);                    res.add(0);                }            }            else {                if(arr[i] + arr[i+1] == 2){                    res.add(1);                    res.add(1);                } else {                    res.add(0);                }            }        }           System.out.println(res.size());         for (int i = 0; i < res.size(); i++) {            System.out.print(res.get(i) + " ");        }        System.out.println("\n");     }}
