import java.util.*;  public class ExercisingWalk{	public static void main(String[] args)	{		Scanner input = new Scanner(System.in); 		int test = input.nextInt();  		int a = 0;		int b = 0;		int c = 0;		int d = 0;		int x = 0;		int y = 0;		int x1 = 0;		int y1 = 0;		int x2 = 0;		int y2 = 0;		int sumV = 0;		int sumH = 0;		for(int t = 0; t < test; t++)		{			a = input.nextInt();			b = input.nextInt();			c = input.nextInt();			d = input.nextInt();			x = input.nextInt();			y = input.nextInt();			x1 = input.nextInt();			y1 = input.nextInt();			x2 = input.nextInt();			y2 = input.nextInt();			String answer = "Yes"; 			sumV = d - c;			sumH = b - a; 			if(x1 == x2 && a + b > 0)			{				answer = "No";			}			if(y1 == y2 && c + d > 0)			{				answer = "No";			}			if(sumV != 0)			{				if(sumV > 0)				{					if(y + sumV > y2)					{						answer = "No";					}				}				else if(sumV < 0)				{					if(y + sumV < y1)					{						answer = "No";					}				}			}			if(sumH != 0)			{				if(sumH > 0)				{					if(x + sumH > x2)					{						answer = "No";					}				}				else if(sumH < 0)				{					if(x + sumH < x1)					{						answer = "No";					}				}			}  			System.out.println(answer);   		}	}}
