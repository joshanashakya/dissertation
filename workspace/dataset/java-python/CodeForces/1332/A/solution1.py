def q1332a():	num_cases = int(input())	for _ in range(num_cases):		a, b, c, d = tuple([int(num) for num in input().split()])		x, y, x1, y1, x2, y2 = tuple([int(num) for num in input().split()])		subproblem(a, b, c, d, x, y, x1, y1, x2, y2) def subproblem(a, b, c, d, x, y, x1, y1, x2, y2):	move_x = b - a # how much we move to the right	move_y = d - c # how much we move upwards	# check if it satisfies the first condition (destination inside of range)	if(x + move_x > x2 or x + move_x < x1 or y + move_y > y2 or y + move_y < y1):		print("NO")		return 	# check if it can either move right or left	if((a != 0 or b != 0) and x+1 > x2 and x-1 < x1):		print("NO")		return 	if((c != 0 or d != 0) and y+1 > y2 and y-1 < y1):		print("NO")		return 	print("YES")  q1332a()
