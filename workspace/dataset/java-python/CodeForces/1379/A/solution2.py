from sys import stdin,stdoutstdin.readlinedef mp(): return list(map(int, stdin.readline().strip().split()))def it():return int(stdin.readline().strip())from collections import defaultdict as dd,Counter as C,dequefrom math import ceil,gcd def check(pat, txt):	M = len(pat)	N = len(txt)	res = 0	for i in range(N - M + 1):		j = 0		while j < M:			if (txt[i + j] != pat[j]):				break			j += 1		if (j == M):			res += 1			j = 0	return res			for _ in range(it()):	n = it()	s = input()	l = 'abacaba'	k = check(l,s)	# print(k)	if k:		s = list(s)		if k !=1:			print('NO')		else:			print('YES')			for i in range(n):				if s[i]=='?':					s[i]='z'			print(''.join(s))	else:		ans=0		for i in range(n-6):			flag=0			# print(s[i])			for j in range(7):				if s[i+j] != '?' and s[i+j] != l[j]:					flag=1					# print(i)					break			if flag:					continue			v = list(s[:i] + l + s[i+7:])			k = ''.join(['z' if i == '?' else i for i in v])			if sum([1 if k[i:i+7] == l else 0 for i in range(n-6)]) == 1:				print('YES\n' + k)				# print(i)				break			flag = 1		if flag:			print("NO")
