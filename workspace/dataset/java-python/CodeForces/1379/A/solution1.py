def count(s,want):	ans = 0	for i in range(n):		if s[i:i+7]==want:			ans+=1	return ansfor _ in range(int(input())):	n = int(input())	s = input()	want = 'abacaba'	f = 0	for i in range(n-6):		flag = 0		for j in range(7):			if s[i+j]!=want[j] and s[i+j]!="?":				flag = 1		if flag==1:			continue		ss = "".join(s[:i]+want+s[i+7:])		if count(ss,want)==1:			ss = ss.replace("?",'z')			print("Yes")			print(ss)			f = 1			break	if f==0:		print("No")
