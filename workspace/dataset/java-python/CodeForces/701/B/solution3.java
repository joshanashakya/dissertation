import java.util.*;import java.lang.*;import java.io.*;import java.util.Arrays;/* Name of the class has to be "Main" only if the class is public. */public class Main{  public static void main (String[]args) throws java.lang.Exception  {     Scanner sc = new Scanner (System.in);    int n = sc.nextInt ();    long m = sc.nextLong ();    long i, rc = 0, cc = 0;    int r[] = new int[100030];      Arrays.fill (r, 0);    int c[] = new int[100030];      Arrays.fill (c, 0);    long sum = (long) n * (long) n;    for (i = 1; i <= m; i++)      {	int x, y;	  x = sc.nextInt ();	  y = sc.nextInt ();	if (r[x] == 0)	  {	    r[x] = 1;	    rc++;	  }	if (c[y] == 0)	  {	    c[y] = 1;	    cc++;	  }	System.out.print (sum - (rc * (long) n + cc * (long) n) + (rc * cc) +			  " ");      }    System.out.println ();  }}
