import java.util.Arrays;import java.util.Scanner;import java.util.Vector; public class Main {     public static void main(String[] args) {         Scanner scan=new Scanner(System.in);         int n=scan.nextInt();         int[][] dp=new int[(int) Math.pow(2,n)][n+1];         int [] a=new int[n];         String can="no";         for(int i=0;i<n;i++){             a[i]=scan.nextInt();         }         for(int i=0;i<Math.pow(2,n);i++){             int mask=i;             dp[i][0]=0;             for(int j=1;j<=n;j++){                 if(mask%2==1){                     dp[i][j]=dp[i][j-1]+a[j-1];                 }                 else{                     dp[i][j]=dp[i][j-1]-a[j-1];                 }                 mask/=2;             }             if(dp[i][n]%360==0){                 can="yes";                 break;             }         }         System.out.println(can);     } }
