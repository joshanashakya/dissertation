import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer; public class FootBallFouls {    public static void main(String[] args) throws IOException {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));         String h = reader.readLine();        String a = reader.readLine();        int n = Integer.parseInt(reader.readLine());         int[][] store = new int[2][100];         for (int i = 0; i < n; i++) {            StringTokenizer st = new StringTokenizer(reader.readLine());             int minute = Integer.parseInt(st.nextToken());             int k = 0;            if (st.nextToken().equals("a")) k = 1;             int player = Integer.parseInt(st.nextToken());             if(st.nextToken().equals("y")) store[k][player]++;            else if(store[k][player] < 2){                store[k][player] = 1000;                System.out.println((k==0?h:a) + " " + player + " " + minute);            }             if(store[k][player] == 2){                store[k][player] = 1000;                System.out.println((k==0?h:a) + " " + player + " " + minute);            }        }    }}
