import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; import java.util.*; public class Main{       public static void main(String args[])    {        FastScanner input = new FastScanner();        int n = input.nextInt();        long a[] = new long[n+1];        for (int i = 1; i <=n; i++) {            long value = input.nextLong();            a[i] = (value<0)?1L:0;                    }        for (int i = 1; i <=n; i++) {            a[i]+=a[i-1];        }//        System.out.println(Arrays.toString(a));        int needforodd=1,needforeven=0;        long ans =0;        for (int i = 1; i <=n; i++) {            if(a[i]%2==0)            {                needforodd++;                ans+=needforeven;                            }            else            {                needforeven++;                ans+=needforodd;            }        }//        System.out.println(ans);        long positive = (n*(n+1L))/2 -(ans);        System.out.println(ans+" "+positive);    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
