import java.util.Scanner; public class _0727YellowCards { 	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int firstTeam=sc.nextInt();		int secondTeam=sc.nextInt();		int maxFirstYellowCards=sc.nextInt();		int maxSecondYellowCards=sc.nextInt();		int totalCards=sc.nextInt();		int maxOfPlayers=0;		int minOfPlayers=0;		if(maxFirstYellowCards<maxSecondYellowCards){			 maxOfPlayers=Math.min((totalCards/maxFirstYellowCards),firstTeam);			 			maxOfPlayers+=Math.min(((totalCards-maxFirstYellowCards*maxOfPlayers)/maxSecondYellowCards),secondTeam);		}		else {			 maxOfPlayers=Math.min((totalCards/maxSecondYellowCards),secondTeam);					maxOfPlayers+=Math.min((totalCards-maxSecondYellowCards*maxOfPlayers)/maxFirstYellowCards,firstTeam);		}			int usedUp=firstTeam*(maxFirstYellowCards-1)+secondTeam*(maxSecondYellowCards-1);	if(usedUp<totalCards) {		minOfPlayers=totalCards-usedUp;	}		System.out.println(minOfPlayers+" "+maxOfPlayers);								 	} }
