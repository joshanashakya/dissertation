import java.util.*;import java.lang.*;import java.io.*; public class A_Yellow_Cards{	static BufferedReader br = new BufferedReader(new InputStreamReader(System.in)); 	static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));	// static Scanner scn = new Scanner(System.in);	public static void main (String[] args) throws java.lang.Exception	{		solve();	} 	public static void solve() throws IOException{		int teamOnePlayers = Integer.parseInt(br.readLine());		int teamTwoPlayers = Integer.parseInt(br.readLine());       int flagsAllowedTeamOne = Integer.parseInt(br.readLine());      int flagsAllowedTeamTwo = Integer.parseInt(br.readLine());       int totalFlags = Integer.parseInt(br.readLine());       int maxpossibletogive = (flagsAllowedTeamOne-1)*teamOnePlayers + (flagsAllowedTeamTwo-1)*teamTwoPlayers;       int minans = Math.min(totalFlags - maxpossibletogive,flagsAllowedTeamOne*teamOnePlayers + flagsAllowedTeamTwo*teamTwoPlayers);      minans = Math.max(0,minans);       int maxans=0;       if(flagsAllowedTeamOne < flagsAllowedTeamTwo){         int flagsgiven;         int cnt = Math.min(totalFlags/flagsAllowedTeamOne,teamOnePlayers);         maxans+=cnt;         flagsgiven =cnt*flagsAllowedTeamOne;         totalFlags-=flagsgiven;         cnt = totalFlags/flagsAllowedTeamTwo;         maxans+=cnt;      }else{         int flagsgiven;         int cnt = Math.min(totalFlags/flagsAllowedTeamTwo,teamTwoPlayers);         maxans+=cnt;         flagsgiven =cnt*flagsAllowedTeamTwo;         totalFlags-=flagsgiven;         cnt = totalFlags/flagsAllowedTeamOne;         maxans+=cnt;      }       maxans = Math.min(maxans,flagsAllowedTeamOne*teamOnePlayers + flagsAllowedTeamTwo*teamTwoPlayers);      // minans = Math.min(minans,flagsAllowedTeamOne*teamOnePlayers + flagsAllowedTeamTwo*teamTwoPlayers);       System.out.println(minans + " " + maxans); 	}}
