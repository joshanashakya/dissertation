#include<iostream>using namespace std;bool map[3][1000010]={0};int main(){    int n,q,x,y;    cin >> n >> q;    int wall = 0;   //不能通过的情况，初始化为0    for(int i=1;i<=q;i++){        cin >> x >> y;        map[x][y]=!map[x][y];     //地面翻转        int flag = -1;        if(map[x][y]) flag = 1; //如果该块变成了岩浆，不是减少wall        if(x==1) x=2;        else x=1;        if(map[x][y-1]) wall+=flag;        if(map[x][y]) wall+=flag;        if(map[x][y+1]) wall+=flag;        if(wall) cout << "NO" << endl;        else cout << "YES" << endl;    }    return 0;}    	 		   	  					 	 					
