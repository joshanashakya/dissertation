import sysfrom collections import defaultdict,dequeinput = lambda :sys.stdin.readline().rstrip()x,y,ax,ay,bx,by = map(int,input().split())xs,ys,t = map(int,input().split())a=[];a.append((x,y));px=x;py=y;LIMIT = 2**62 - 1while (1):    dx=ax*px + bx;    dy=ay*py + by;    if (dx>=LIMIT or dy>=LIMIT):        break;    a.append((dx,dy));    px=dx;    py=dy;px = xs;py = ys;ans = 0;a = list(a)n=len(a)for i in range(n):    for j in range(i,n):        # start to i to j        l = abs(a[j][0]-a[i][0]) + abs(a[j][1]-a[i][1])        l1 = abs(a[i][0]-xs) + abs(a[i][1]-ys) + l        # start to j to i        l2 = abs(a[j][0]-xs) + abs(a[j][1]-ys) + l        if l1<=t or l2<=t:            ans = max(ans,j-i+1)print(ans);
