import java.io.*;import java.util.*;public class Main {    public static void main(String args[])    {        FastReader input=new FastReader();        PrintWriter out=new PrintWriter(System.out);        int T=1;        while(T-->0)        {            long x0,y0,ax,ay,bx,by;            x0=input.nextLong();            y0=input.nextLong();            ax=input.nextLong();            ay=input.nextLong();            bx=input.nextLong();            by=input.nextLong();            long xs,ys,t;            xs=input.nextLong();            ys=input.nextLong();            t=input.nextLong();            long px=x0,py=y0;            ArrayList<ArrayList<Long>> list=new ArrayList<>();            ArrayList<Long> l=new ArrayList<>();            l.add(x0);            l.add(y0);            list.add(l);            while(px<=1E17 && py<=1E17)            {                long x=px*ax+bx;                long y=py*ay+by;                if((x<0 || x>=1E17) || (y<0 || y>=1E17))                {                    break;                }                ArrayList<Long> l1=new ArrayList<>();                l1.add(x);                l1.add(y);                list.add(l1);                px=x;                py=y;            }/*            for(int i=0;i<list.size();i++)            {                out.println(list.get(i));            }*/             int max=0;            for(int i=0;i<list.size();i++)            {                int count=0;                long t1=t;                long x=list.get(i).get(0);                long y=list.get(i).get(1);                long d=Math.abs(xs-x);                d+=Math.abs(ys-y);                if(d<=t1)                {                    t1=t-d;                    count++;                    for(int j=i+1;j<list.size();j++)                    {                        d=Math.abs(x-list.get(j).get(0))+Math.abs(y-list.get(j).get(1));                        if(d<=t1)                        {                            count++;                        }                    }                    d=Math.abs(xs-x);                    d+=Math.abs(ys-y);                    t1=t-d;                    max=Math.max(max,count);                    count=1;                    for(int j=i-1;j>=0;j--)                    {                        d=Math.abs(x-list.get(j).get(0))+Math.abs(y-list.get(j).get(1));                        if(d<=t1)                        {                            count++;                        }                    }                    max=Math.max(max,count);                }            }            out.println(max);        }        out.close();    }    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }}
