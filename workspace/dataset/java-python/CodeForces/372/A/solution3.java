import java.io.*;import java.util.*; public class Main {	static PrintWriter pr = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));	public static void main(String[] args) throws IOException {		int n = input.nextInt();		int []a = new int[n];		boolean []v = new boolean[n];		for (int i=0;i<n;i++){			a[i] = input.nextInt();		}		Arrays.sort(a);		int ans = 0, cnt = 0;		for (int i=n/2;i<n;i++){			if (a[i]>=2*a[cnt]){				v[cnt] = true;				cnt++;			}			ans++;		}		for (int i=0;i<n/2;i++){			if (v[i] == false){				ans++;			}		}		pr.println(ans);		pr.flush();	}		static class input{		static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		static StreamTokenizer in = new StreamTokenizer(br);		static String nextLine() throws IOException{			return br.readLine();		}		static int nextInt() throws IOException{			in.nextToken();			return (int)in.nval;		}		static long nextLong() throws IOException{			in.nextToken();			return (long)in.nval;		}		static double nextDouble() throws IOException{			in.nextToken();			return in.nval;		}	}}      			    		  			  			 		  		
