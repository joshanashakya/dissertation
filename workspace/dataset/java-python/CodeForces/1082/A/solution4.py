# I collaborated with no one in the class (other than checking discussion on discord)def distance(a, b):    return (abs(a - b) + (d - 1)) // d numTests = int(input())inputList = []           # array storing inputsc = 2e9 + 99# Read in inputsfor i in range(numTests):    inputList.append(input())for i in range(numTests):    line = inputList[i].split(" ")    n = int((line[0]))         # number of pages    x = int((line[1]))         # starting page    y = int((line[2]))         # desired page    d = int((line[3]))         # number of pages scrolled after button press    buttonPress = 0;    # print(n + ' ' + x + ' ' + y + ' ' + d)     xToY = abs(x - y)            # distance from x to y    buttonPress = c     # case: we can go to y directly from x    if (xToY % d) == 0:        buttonPress = min(buttonPress, distance(x, y))     # case: we can go to y by going to 1    xToY = y - 1    if (xToY % d) == 0:        buttonPress = min(buttonPress, distance(x, 1) + distance(1, y))    # case: we can go to y by going to n    xToY = n - y    if (xToY % d) == 0:        buttonPress = min(buttonPress, distance(x, n) + distance(n, y))    # no cases above were applicable    if buttonPress == c:        buttonPress = -1     print(buttonPress) 			 	 			 		  	 	 	 		  	  	 	
