import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}				static int gcd(int a, int b) {						if(b == 0) {				return a;			}						return gcd(b, a % b);		}			public static void main(String[] args) throws IOException {				FastIO in = new FastIO();		PrintWriter out = new PrintWriter(System.out);				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = in.nextInt();		int a[] = new int[n];				ArrayList<Integer> one = new ArrayList<>();		ArrayList<Integer> second = new ArrayList<>();		ArrayList<Integer> third = new ArrayList<>();				for(int i = 0; i < n; i++) {			a[i] = in.nextInt();			if(a[i] < 0) {				one.add(a[i]);			}			else if(a[i] > 0) {				second.add(a[i]);			}			else {				third.add(a[i]);			}		}				if(((one.size() & 1) == 1) && second.size() == 0) {			second.add(one.remove(one.size() - 1));			second.add(one.remove(one.size() - 1));		}				else if((one.size() & 1) == 0 && second.size() != 0) {			third.add(one.remove(one.size() - 1));		}		else if((one.size() & 1) == 0 && second.size() == 0){			third.add(one.remove(one.size() - 1));			second.add(one.remove(one.size() - 1));			second.add(one.remove(one.size() - 1));		}				out.print(one.size() + " ");		for(int x : one) {			out.print(x + " ");		}		out.println();				out.print(second.size() + " ");		for(int x : second) {			out.print(x + " ");		}		out.println();				out.print(third.size() + " ");		for(int x : third) {			out.print(x + " ");		}		out.println();				out.flush();		out.close();	}}
