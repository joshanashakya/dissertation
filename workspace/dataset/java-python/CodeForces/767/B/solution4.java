import java.util.*;import java.io.*;public class G { 	public static void main(String[] args) throws Exception {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(br.readLine());		long a = Long.parseLong(st.nextToken());		long b = Long.parseLong(st.nextToken());		long t = Long.parseLong(st.nextToken());		int n = Integer.parseInt(br.readLine());		if(n == 0){			System.out.println(a); return;		}		TreeMap<Long, Integer> map = new TreeMap<>();//		TreeMap<Long, Integer> mapback = new TreeMap<>();		st = new StringTokenizer(br.readLine());		for(int i = 0; i < n; i++){			long k = Long.parseLong(st.nextToken());			if(map.containsKey(k)){				map.put(k, map.get(k)+1);			}			else{				map.put(k, 1);			}			//			mapback.put(-k, map.get(k));		}		long currt = a;		long min = (long)1e12; long res = 0;		for(long k: map.keySet()){			if(k > currt && k + t <= b){				System.out.println(currt); return;			}			else{				if(currt-(k-1) < min){					min = currt-(k-1);					res = k-1;				}			}			long m = map.get(k);			if(m * t + currt >= b){				System.out.println(res);				return;			}			else{				currt += m * t;			}		}		if(currt + t <= b){			System.out.println(currt);		}		else{			System.out.println(res);		}	} }
