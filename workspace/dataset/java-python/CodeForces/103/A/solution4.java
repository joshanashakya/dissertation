import java.io.*;import java.util.*; public class Main{      public static void main(String[] args) throws IOException 	{		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer(br.readLine());        int n = Integer.parseInt(st.nextToken());        long[] sum = new long[n];                st = new StringTokenizer(br.readLine());        sum[0] = Long.parseLong(st.nextToken());        for(int i=1; i<n; i++)        {            sum[i] = Long.parseLong(st.nextToken());            sum[i] += sum[i-1] + (sum[i]-1)*i;        }        System.out.println(sum[n-1]);    }}
