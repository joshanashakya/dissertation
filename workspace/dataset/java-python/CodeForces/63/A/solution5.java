import java.io.BufferedReader;import java.io.InputStreamReader;import java.io.BufferedWriter;import java.io.OutputStreamWriter;import java.io.IOException;import java.util.ArrayList;import java.util.Collections; public class ObjectSortning_CF_63A {		public static class Tripulant implements Comparable<Tripulant>{		private String name;		private String type;				public Tripulant(String name, String type) {			this.name = name;			this.type = type;		}				public void setName(String name) {			this.name = name;		}				public void setType(String type) {			this.type = type; 		} 		public void convertToNumbers(Tripulant tripulant) {			if(tripulant.type.equalsIgnoreCase("rat")) {				tripulant.setType("1");			}else if(tripulant.type.equalsIgnoreCase("woman")) {				tripulant.setType("2");			}else if(tripulant.type.equalsIgnoreCase("child")) {				tripulant.setType("2");			}else if(tripulant.type.equalsIgnoreCase("man")) {				tripulant.setType("3");			}else if(tripulant.type.equalsIgnoreCase("captain")) {				tripulant.setType("4");			}		}				@Override		public int compareTo(Tripulant otherTripulant) {			int ret; 			int num1 = Integer.parseInt(type); 			int num2 = Integer.parseInt(otherTripulant.type); 			if(num1 < num2) {				ret = -1;			}else if(num1 > num2) {				ret = 1;			}else {				ret = 0;			}			return ret;		}					} 		public static void main(String args[]) throws IOException{		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));								//*read number of persons		String numberLine = br.readLine();		numberLine = numberLine.trim();				int number = Integer.parseInt(numberLine);		ArrayList<Tripulant> listPeople = new ArrayList<Tripulant>();				String array[] = new String[2];		String name;		String type;		//*read person and type		for(int i = 0; i < number; i++) {			String people = br.readLine();			array = people.split(" ");			name = array[0];			type = array[1];			Tripulant newTripulant = new Tripulant(name, type);			listPeople.add(newTripulant);		}				for(int i = 0; i < listPeople.size(); i++) {			listPeople.get(i).convertToNumbers(listPeople.get(i));		}				Collections.sort(listPeople);				String output = "";		for(int i = 0; i < listPeople.size();i++) {			output += listPeople.get(i).name + "\n";		}				bw.write(output);				br.close();		bw.close();	}}
