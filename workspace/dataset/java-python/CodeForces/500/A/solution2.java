import java.util.*;import java.io.*;import java.lang.*;import java.math.*; public class cp{    static BufferedReader br;    static StringTokenizer st;     public static void main(String[] args)throws IOException{         br = new BufferedReader(new InputStreamReader(System.in));         st = readLine();        int n = tokenInt();        int t = tokenInt();         st = readLine();        int[] array = readIntArray(n);         // System.out.println(Arrays.toString(array));        System.out.println(new Solver().solve(n, t, array));    }     static StringTokenizer readLine() throws IOException { return new StringTokenizer(br.readLine()); }    static int tokenInt() { return Integer.parseInt(st.nextToken()); }    static long tokenLong(){ return Long.parseLong(st.nextToken()); }    static double tokenDouble(){ return Double.parseDouble(st.nextToken()); }    static char[] tokenChar(){ return st.nextToken().toCharArray(); }    static int[] readIntArray(int n) {        int[] a=new int[n];        for (int i=1; i<n; i++) a[i]=tokenInt();        return a;    } }  class Solver{    public String solve(int n, int t, int[] array) throws IOException{         int i=1;        while(true){            i = i + array[i];            if(i >= t) break;        }        if (i == t) return "YES";        else return "NO";    }} /*18 510 9 8 7 7 7 5 5*/
