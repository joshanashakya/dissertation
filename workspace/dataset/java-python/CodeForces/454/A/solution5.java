import java.io.BufferedOutputStream;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.Arrays; public class A454A {     public static void main(String[] args) throws IOException {        A454A a = new A454A();        a.solve();    }     public void solve() throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        BufferedOutputStream bos = new BufferedOutputStream(new BufferedOutputStream(System.out));         int n = Integer.parseInt(br.readLine());         for(int i=0; i<n/2; i++){            int nd = i*2+1;            char[] cad = new char[nd];            Arrays.fill(cad, 'D');            char[] cas = new char[(n-nd)/2];            Arrays.fill(cas, '*');            bos.write(String.valueOf(cas).getBytes());            bos.write(String.valueOf(cad).getBytes());            bos.write(String.valueOf(cas).getBytes());            bos.write("\n".getBytes());        }         char[] cad1 = new char[n];        Arrays.fill(cad1,'D');        bos.write(String.valueOf(cad1).getBytes());        bos.write("\n".getBytes());         for(int i=0; i<n/2; i++){            int ns = i+1;            char[] cas = new char[ns];            Arrays.fill(cas, '*');            char[] cad = new char[n-ns*2];            Arrays.fill(cad, 'D');             bos.write(String.valueOf(cas).getBytes());            bos.write(String.valueOf(cad).getBytes());            bos.write(String.valueOf(cas).getBytes());            bos.write("\n".getBytes());        }         bos.flush();    }}
