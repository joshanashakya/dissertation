//Code by oryon#include <bits/stdc++.h>#include <ext/pb_ds/assoc_container.hpp>using namespace __gnu_pbds;using namespace std; #define pb push_back#define ppb pop_back#define int long long#define vi vector<int>#define vvi vector<vector<int>>#define mii map<int, int>#define pii pair<int, int>#define sz(a) (a).size()#define FOR(i, a, b) for(int i=a; i<b; i++)#define ROF(i, a, b) for(int i=b-1; i>=a; i--)#define all(x) (x).begin(), (x).end()#define setbits(x) __builtin_popcountll(x)#define zrobits(x) __builtin_ctzll(x)#define LB lower_bound#define UB upper_bound#define FS first#define SD second#define INF 1e18#define MAX_NUM 300001#define ps(x,y) fixed<<setprecision(y)<<xmt19937 rng(chrono::steady_clock::now().time_since_epoch().count());typedef long long ll;const long long MOD = 1e9 + 7;typedef tree<int, null_type, less<int>, rb_tree_tag, tree_order_statistics_node_update> pbds;#define deb(x) cout << #x << " " << x <<endl; ll max(ll a, ll b){    if (a > b) return a;    return b;} ll min(ll a, ll b){    if (a < b) return a;    return b;} int32_t main(){    ios::sync_with_stdio(0);    cin.tie(0); #ifndef ONLINE_JUDGE    freopen("input.txt", "r", stdin);    freopen("output.txt", "w", stdout);#endif     string s;    cin >> s;     int n = s.length();     int count[26][n];     FOR(i, 0, 26) {        count[i][0] = 0;    }     count[s[0] - 'a'][0]++;     FOR(i, 1, n) {        FOR(j, 0, 26) count[j][i] = count[j][i - 1];        count[s[i] - 'a'][i]++;    }     int testcases;     cin >> testcases;     while (testcases--) {        int l, r;        cin >> l >> r;         if (l == r || s[l - 1] != s[r - 1]) {            cout << "Yes" << endl;            continue;        }         int cnt = 0;         FOR(i, 0, 26) {            if (l == 1 && count[i][r - 1]) cnt++;            else if (l > 1 && (count[i][r - 1] - count[i][l - 2])) cnt++;        }         if (cnt > 2) cout << "Yes" << endl;        else cout << "No" << endl;     }     return 0;} 
