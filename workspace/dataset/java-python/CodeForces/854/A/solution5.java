import java.util.Scanner; public class Main { 	public static void main(String[] args) { 		Scanner input = new Scanner(System.in);		int num=0;		int deno=0;					int n = input.nextInt();			for(int i=1;i<1000;i++) {				for(int j=2;j<1000;j++) {					if(i<j && checkCoPrime(i,j)==1&& i +j ==n) {						int newNum =i;						int newDeno =j;						if(newNum>num || newDeno>deno) {							num = newNum;							deno=newDeno;						}					}				}			}			System.out.println(num+" "+deno);			}		public static int checkCoPrime(int a,int b) { 	        if (a == 0 || b == 0) 	            return 0;	        if (a == b) 	            return a;	        if (a > b) 	            return checkCoPrime(a-b, b); 	                  	        return checkCoPrime(a, b-a); 	    } 	 } 							  							 	     		 	 		
