import java.util.Arrays;import java.util.Comparator;import java.util.Scanner; public class solution {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int[] numbers = new int[n];        for (int i = 0; i < n; i++) {            numbers[i] = sc.nextInt();        }        for (int i = 0; i < n; i++) {            if (i == 0) {                if (numbers[i] % 2 != numbers[i + 1] % 2 && numbers[i] % 2 != numbers[i + 2] % 2) {                    System.out.println(i + 1);                    break;                }                continue;            } else if (i == n - 1) {                if (numbers[i] % 2 != numbers[i - 1] % 2 && numbers[i] % 2 != numbers[i - 2] % 2) {                    System.out.println(i + 1);                    break;                }                continue;            }            if (numbers[i] % 2 != numbers[i - 1] % 2 && numbers[i] % 2 != numbers[i + 1] % 2) {                System.out.print(i + 1);                break;            }        }    }}
