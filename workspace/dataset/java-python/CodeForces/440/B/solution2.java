 import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; public class A45 {    public static void main(String[] args) throws IOException {        BufferedReader inp = new BufferedReader(new InputStreamReader(System.in));        int size = Integer.parseInt(inp.readLine());        String[] s1 = inp.readLine().split(" ");        long[] given = new long[size];        long totalSum = 0;        for(int i=0;i<size;i++){            given[i] = Integer.parseInt(s1[i]);            totalSum +=given[i];        }        long each = totalSum/size;        long[] diff = new long[size];        for(int i=0;i<size;i++){            diff[i] = given[i]-each;        }        long sum = Math.abs(diff[0]);        long carry = diff[0];        for(int i=1;i<size;i++){            sum += Math.abs(diff[i]+carry);            carry = carry+diff[i];        }        System.out.println(sum);    }}
