def ceil(a,b):	return ((a+b-1)//b)def maximum_diff(arr):	ans = 0	for i in range(1,len(arr)):		ans = max(ans,abs(arr[i]-arr[i-1]))	return ansfor _ in range(int(input())):	n = int(input())	a = list(map(int,input().split()))	arr = []	for i in range(n):		if a[i]!=-1:			left = max(0,i-1)			right = min(n-1,i+1)			if a[left]==-1 or a[right]==-1:				arr.append(a[i])	if len(arr)==0:		print(0,0)	else:		k = ceil(max(arr)+min(arr),2)		for i in range(n):			if a[i]==-1:				a[i] = k		m = maximum_diff(a)		print(m,k)
