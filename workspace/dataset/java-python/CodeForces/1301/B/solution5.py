 def calc(a, mx):    l = 0    r = 10 ** 9    for i in range(1, len(a)):        if a[i] != -1 and a[i - 1] != -1:            if abs(a[i] - a[i - 1]) > mx:                return None        elif a[i] != -1:            l = max(l, a[i] - mx)            r = min(r, a[i] + mx)        elif a[i - 1] != -1:            l = max(l, a[i - 1] - mx)            r = min(r, a[i - 1] + mx)    return l if l <= r else None  def solve(a):    if calc(a, 0) is not None:        return 0, calc(a, 0)    low = 0    high = 1    while calc(a, high) is None:        high *= 2    while low + 1 < high:        mid = (low + high) // 2        if calc(a, mid) is None:            low = mid        else:            high = mid    return high, calc(a, high)  tc = int(input())for _ in range(tc):    n = int(input())    a = list(map(int, input().split()))    print(*solve(a))
