import java.util.ArrayList;import java.util.Scanner; public class P291B { 	public static void main(String[] args) {				Scanner s = new Scanner(System.in);		String command = s.nextLine() + " ";		ArrayList<String> lexemes = new ArrayList<>();		StringBuilder current = new StringBuilder("");		boolean quote = false;		for (int i = 0; i < command.length(); i++) {			if (quote) {				if (command.charAt(i) == '"') {					quote = false;					lexemes.add(current.toString());					current = new StringBuilder("");				} else {					current.append(command.charAt(i));				}			} else {				if (command.charAt(i) == '"') {					quote = true;				} else if (command.charAt(i) == ' ') {					if (current.length() != 0) {						lexemes.add(current.toString());						current = new StringBuilder("");					}				} else {					current.append(command.charAt(i));				}			}		}		for (String lexeme : lexemes) {			System.out.printf("<%s>\n", lexeme);		}			}	}
