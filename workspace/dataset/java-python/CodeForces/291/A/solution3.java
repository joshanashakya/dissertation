import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.List;import java.util.stream.Collectors;import java.util.stream.Stream; /** * CodeForces: problem/291/A */public class Polygamy {     public static void main(String[] args) throws IOException {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        reader.readLine();        List<Integer> counts = Stream.of(reader.readLine().trim().split(" "))                .map(Integer::parseInt).sorted().collect(Collectors.toList());        if (counts.size() == 1) {            System.out.println("0");            return;        }        int result = 0;        for (int i = 0; i < counts.size() - 2; i++) {            if (counts.get(i) == 0)                continue;            if (counts.get(i).equals(counts.get(i + 1))) {                if (counts.get(i).equals(counts.get(i + 2))) {                    System.out.println("-1");                    return;                }                ++i;                ++result;            }        }        if (counts.get(counts.size() - 1).equals(counts.get(counts.size() - 2)) && !counts.get(counts.size() - 1).equals(0))            ++result;        System.out.println(result);    }}
