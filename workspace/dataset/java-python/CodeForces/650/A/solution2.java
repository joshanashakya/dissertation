import java.awt.Point;        import java.io.File;        import java.io.IOException;        import java.io.PrintWriter;        import java.util.*;import java.util.Map.Entry;         public class Main {            public static void main(String[] args)          {            Scanner input = new Scanner(System.in);             int n = input.nextInt();            HashMap<Point,Integer> same = new HashMap<Point, Integer>();            TreeMap<Integer,Integer> mapx = new TreeMap<>();            TreeMap<Integer,Integer> mapy = new TreeMap<>();                                                          int count =0,extra=0;            for (int i = 0; i < n; i++) {                int x = input.nextInt();                int y = input.nextInt();                Point p = new Point(x,y);                 same.put(p, same.getOrDefault(p, 0)+1);                mapx.put(x, mapx.getOrDefault(x, 0)+1);                mapy.put(y, mapy.getOrDefault(y, 0)+1);                                                                                                                                               }            //            System.out.println(map);//            System.out.println(acces);//            System.out.println(extra);            long ans=0;            for (Entry<Integer,Integer> entry:mapx.entrySet()) {                Long k = (long)entry.getValue();                ans+=((k*(k-1))/2);            }            for (Entry<Integer,Integer> entry:mapy.entrySet()) {                Long k = (long)entry.getValue();                ans+=((k*(k-1))/2);            }            for (Entry<Point,Integer> entry:same.entrySet()) {                Long k = (long)entry.getValue();                ans-=((k*(k-1))/2);            }                                    System.out.println(ans);        }         }
