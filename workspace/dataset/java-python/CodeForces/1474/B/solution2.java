/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Main{	public static void main (String[] args) throws java.lang.Exception{	    Scanner scn = new Scanner(System.in);	    int t = scn.nextInt();	    while(t-->0){	        int d = scn.nextInt();	        long a = d+1;	        a = primeNext(a);	        long b = a+d;	        b = primeNext(b);	        long ans = a*b;	        System.out.println(ans);	    }	}	public static long primeNext(long n){	    while(true){	        if(isPrime(n)){	            break;	        }else{	            n++;	        }	    }	    return n;	}	public static boolean isPrime(long n){	    boolean flag = true;	    for(long i = 2;i*i<=n;i++){	        if(n%i==0){	            flag = false;	            break;	        }	    }	    return flag;	}}
