import java.util.*;public class Solution {     static int gcd(int a, int b)    {        if(a%b==0)            return b;        return gcd(b,a%b);    }    	public static String gameWinner(int[] ar,int n) {		// Write your code here        int x=gcd(ar[0],ar[1]);        int i;        for(i=2; i<n; i++)            x=gcd(x,ar[i]);        int max=-1;        for(i=0; i<n; i++)        {            if(ar[i]>max)                max=ar[i];        }        int res=max/x-n;        if(res%2==1)            return "Alice";        else            return "Bob";	}	public static void main(String args[])	{	    Scanner sc=new Scanner(System.in);	    int n=sc.nextInt();	    int arr[]=new int[n];	    int i;	    for(i=0; i<n; i++)	    arr[i]=sc.nextInt();	    String s=gameWinner(arr,n);	    System.out.println(s);	}}
