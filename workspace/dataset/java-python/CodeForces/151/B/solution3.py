def main():    n = int(input())    arr = []    for _ in range(n):        k, name = input().split()        arr.append([name, 0, 0, 0])        for i in range(int(k)):            num = input().replace("-", "")            if all(a > b for a, b in zip(num, num[1:])):                arr[-1][2] += 1            elif len(set(num)) == 1:                arr[-1][1] += 1            else:                arr[-1][3] += 1     t = ["If you want to call a taxi, you should call:",         "If you want to order a pizza, you should call:",         "If you want to go to a cafe with a wonderful girl, you should call:"]     for i, s in enumerate(t, start=1):        m = max(r[i] for r in arr)        print(s, ", ".join(r[0] for r in arr if r[i] == m) + ".")if __name__ == '__main__':    main()
