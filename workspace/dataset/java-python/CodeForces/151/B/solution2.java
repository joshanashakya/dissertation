import java.io.File;import java.io.FileInputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintStream;import java.io.BufferedOutputStream;import java.io.PrintWriter;import java.util.ArrayList;import java.util.InputMismatchException;import java.util.List; public class Main { 	public static void main(String[] args) {		if (args.length > 0 && args[0].toLowerCase().equals("local")) {			try {				System.setIn(new FileInputStream("input.txt"));				System.setOut(new PrintStream(new File("output.txt")));			} catch (IOException exc) {			}		}		Task task = new Task();		task.solve();	} 	public static class Task {		static final long MOD = (long) 1e9 + 7;		static final PrintWriter out = new PrintWriter(new BufferedOutputStream(System.out)); 		public void solve() {			try {				Scan sc = new Scan();				int n = (int) sc.scanLong(); 				List<String> b_taxis = new ArrayList<String>();				List<String> b_girls = new ArrayList<String>();				List<String> b_pizzas = new ArrayList<String>(); 				int best_taxis = 0;				int best_pizzas = 0;				int best_girls = 0; 				for (int i = 0; i < n; ++i) {					int k = (int) sc.scanLong();					String name = sc.scanString();					int girls = 0;					int pizzas = 0;					int taxis = 0;					for (int j = 0; j < k; ++j) {						char[] num = sc.scanString().toCharArray();						boolean allsame = true;						boolean decreasing = true;						int prev = -1;						for (char c : num) {							if (c != '-') {								int curr = c - '0';								if (prev != -1 && prev != curr) {									allsame = false;								}								if (prev != -1 && prev <= curr) {									decreasing = false;								}								prev = curr;							}						}						if (allsame) {							++taxis;						} else if (decreasing) {							++pizzas;						} else {							++girls;						}					}					if (taxis == best_taxis) {						b_taxis.add(name);					}					if (girls == best_girls) {						b_girls.add(name);					}					if (pizzas == best_pizzas) {						b_pizzas.add(name);					}					if (taxis > best_taxis) {						best_taxis = taxis;						b_taxis.clear();						b_taxis.add(name);					} 					if (girls > best_girls) {						best_girls = girls;						b_girls.clear();						b_girls.add(name);					} 					if (pizzas > best_pizzas) {						best_pizzas = pizzas;						b_pizzas.clear();						b_pizzas.add(name);					}				} 				out.println("If you want to call a taxi, you should call: " + String.join(", ", b_taxis) + ".");				out.println("If you want to order a pizza, you should call: " + String.join(", ", b_pizzas) + ".");				out.println("If you want to go to a cafe with a wonderful girl, you should call: "						+ String.join(", ", b_girls) + "."); 			} 			finally {				out.close();			}		} 		private void debug(Object... x) {			StringBuilder sb = new StringBuilder();			for (Object o : x)				sb.append(String.valueOf(o)).append(" ");			out.println(sb);		}	} 	public static class Scan {		private byte[] buf = new byte[1024];		private int index;		private InputStream in;		private int total; 		public Scan() {			in = System.in;		} 		public int scan() {			if (total < 0)				throw new InputMismatchException();			if (index >= total) {				index = 0;				try {					total = in.read(buf);				} catch (IOException e) {					throw new RuntimeException(e);				}				if (total <= 0)					return -1;			}			return buf[index++];		} 		public long scanLong() {			long integer = 0;			int n = scan();			while (isWhiteSpace(n))				n = scan();			int neg = 1;			if (n == '-') {				neg = -1;				n = scan();			}			while (!isWhiteSpace(n)) {				if (n >= '0' && n <= '9') {					integer *= 10;					integer += n - '0';					n = scan();				} else					throw new InputMismatchException();			}			return neg * integer;		} 		public double scanDouble() {			double doub = 0;			int n = scan();			while (isWhiteSpace(n))				n = scan();			int neg = 1;			if (n == '-') {				neg = -1;				n = scan();			}			while (!isWhiteSpace(n) && n != '.') {				if (n >= '0' && n <= '9') {					doub *= 10;					doub += n - '0';					n = scan();				} else					throw new InputMismatchException();			}			if (n == '.') {				n = scan();				double temp = 1;				while (!isWhiteSpace(n)) {					if (n >= '0' && n <= '9') {						temp /= 10;						doub += (n - '0') * temp;						n = scan();					} else						throw new InputMismatchException();				}			}			return doub * neg;		} 		public String scanString() {			StringBuilder sb = new StringBuilder();			int n = scan();			while (isWhiteSpace(n))				n = scan();			while (!isWhiteSpace(n)) {				sb.append((char) n);				n = scan();			}			return sb.toString();		} 		public char scanChar() {			int n = scan();			while (isWhiteSpace(n))				n = scan();			return (char) n;		} 		private boolean isWhiteSpace(int n) {			if (n == ' ' || n == '\n' || n == '\r' || n == '\t' || n == -1)				return true;			return false;		}	}}
