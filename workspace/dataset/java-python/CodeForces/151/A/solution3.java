import java.util.Scanner; public class A151 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt(), k = scanner.nextInt(), l = scanner.nextInt(), c = scanner.nextInt(),                d = scanner.nextInt(), p = scanner.nextInt(), nl = scanner.nextInt(), np = scanner.nextInt();         int millDrToast = k * l / nl, limesToast = c * d, saltToast = p / np;        System.out.println((Math.min(millDrToast, Math.min(limesToast, saltToast))) / n);    }}
