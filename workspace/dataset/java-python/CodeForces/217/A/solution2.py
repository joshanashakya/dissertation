snow_drifts = [] def check_groups(i):  snow_drifts[i][2] = True  for j in range(n):    if not snow_drifts[j][2]:      if snow_drifts[i][0] == snow_drifts[j][0] or snow_drifts[i][1] == snow_drifts[j][1]:        check_groups(j) n = int(input())for i in range(n):  x, y = map(int, input().split())  snow_drifts.append([x, y, False]) groups = -1for i in range(n):  if not snow_drifts[i][2]:    check_groups(i)    groups += 1 print(groups)		 		 		 		 	   		   	   	 	
