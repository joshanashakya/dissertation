import java.util.*; public class Main {    public static int size;     public static void main(String[] args) {        // write your code here        Scanner in = new Scanner(System.in);        int n = in.nextInt();        ArrayList<Integer> list = new ArrayList<>();        for (int i = 0; i < n; i++) {            list.add(in.nextInt());        }        ArrayList<Integer> reversed = (ArrayList<Integer>) list.clone();        ArrayList<Integer> sorted = (ArrayList<Integer>) list.clone();        Collections.sort(sorted);        Collections.sort(reversed , Collections.reverseOrder());        if (list.equals(reversed)) System.out.println(1);        else if (list.equals(sorted)) System.out.println(list.size());        else {            size(list);            System.out.println(size);        }    }     public static void size(ArrayList<Integer> list) {        List<Integer> list1 = list.subList(0 , list.size()/2);        List<Integer> list2 = list.subList(list.size()/2 , list.size());        List<Integer> sorted1 = new ArrayList<>(list1);        List<Integer> sorted2 = new ArrayList<>(list2);        Collections.sort(sorted1);        Collections.sort(sorted2);        if (list1.equals(sorted1) || list2.equals(sorted2)) {            size = Math.max(size , list1.size());        }        else {            size(new ArrayList<>(list1));            size(new ArrayList<>(list2));        }    }}
