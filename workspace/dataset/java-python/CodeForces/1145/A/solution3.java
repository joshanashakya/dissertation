import java.util.Scanner; public class p1145A {	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int a[] = new int[n];		for (int i = 0; i < n; i++)			a[i] = sc.nextInt();				boolean ok=false;		for (int len = n; len > 0; len /= 2) {			for (int c = 0; c < n; c += len) {				boolean sorted = true;								for (int i = 1; i < len; ++i)					if (a[c + i] < a[c + i - 1])						sorted = false;				if (sorted) {					System.out.println(len);					ok=true;					break;				}			}               if(ok) break;		} 	}}
