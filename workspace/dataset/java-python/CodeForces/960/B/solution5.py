from heapq import heappush, heappopn, k1, k2 = map(int, input().split())a = list(map(int, input().split()))b = list(map(int, input().split()))pq = []for i in range(n):    if abs(a[i] - b[i]) > 0:        heappush(pq, -abs(a[i] - b[i]))tot = k1 + k2while pq and tot > 0:    ce = -heappop(pq)    tot -= 1    if ce == 1:        continue    heappush(pq, -(ce - 1))ans = 0if tot > 0:    ans = tot % 2elif pq:    for elm in pq:        ans += elm ** 2print(ans)
