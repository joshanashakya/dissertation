import sys,math '''def construct(a,seg,low,high,pos):    try:        if low == high:            seg[pos] = a[low]        mid = (low+high)//2        construct(a,seg,low,mid,2*pos+1)        construct(a,seg,mid,high,2*pos+2)        seg[pos] = min(seg[2*pos+1],seg[2*pos+2])    except:        pass a = [-1,2,4,0]c = 1while c<len(a):    c = 2*cseg = [10000]*(2*c-1)print(construct(a,seg,0,3,0))def matrix(a,b):    c = [[a[0][0]*b[0][0]+a[0][1]*b[1][0],a[0][0]*b[0][1]+a[0][1]*b[1][1]],\            [a[1][0]*b[0][0]+a[1][1]*b[1][0],a[1][0]*b[1][0]+a[1][1]*b[1][1]]]    return cdef power(a,n):    if n == 0:        return [[1,1],[1,0]]    else:        if n%2 == 0:            return matrix(power(a,n//2),power(a,n//2))        else:            return matrix(matrix(a,a),power(a,n-1))a = [[1,1],[1,0]]for i in range(10):    print(power(a,i))a = [[1,1],[1,0]]c = afor i in range(10):    c = matrix(c,a)    print(c)t = int(input())for _ in range(t):    n, w = map(int,input().split())    a = list(map(int,input().split()))    a.sort()    x = 0    s = 0    while x<n and s+a[x]<=w:        s+=a[x]        x+=1    if x == 0:        print(-1)    else:        print(x)        print(a[0:x+1]) '''def cal(a,b):    seg = {0:6,1:2,2:5,3:5,4:4,5:5,6:6,7:3,8:7,9:6}    ans = 0    for i in range(a,b+1):        for c in str(i):            ans += seg[int(c)]    return ansa, b = map(int,input().split())print(cal(a,b))
