import math def is_good_string(l):    for i in l:        count_1 = 0        count_0 = 0        for j in i:                if j == "0":                    count_0 += 1                else:                    count_1 += 1        if count_1 == count_0: return False    return True    def main_function():    a = int(input())    b = int(input())    c = int(input())    d = int(input())    e = int(input())    f = int(input())    if e > f:        if a >= d:            cost = d * e            return cost        else:            cost = a * e            d -= a            if min(b, c) >= d:                cost += d * f            else: #min(b, c) < d                cost += min(b, c) * f    else:        if min(b, c) >= d:            cost = d * f            return cost        else:            cost = min(b, c) * f            d -= min(b, c)            if a >= d:                cost += d * e            else: #min(b, c) < d                cost += a * e    return cost           print(main_function())
