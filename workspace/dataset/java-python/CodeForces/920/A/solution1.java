import java.beans.DesignMode;import java.util.*; /** * @author 汪家豪 * @version 1.0 * @description * @date 2021/3/23 16:00 */public class Main {    private static int n,k,t,ans;    static Queue<Node> q = new LinkedList<>();    static boolean[] visit = new boolean[210];    static Scanner in = new Scanner(System.in);    public static void main(String[] args) {        int total = in.nextInt();        while (total-->0){            n = in.nextInt();            k = in.nextInt();            Arrays.fill(visit,false);            ans = 0;            for(int i = 0;i < k; i++){                t = in.nextInt();                Node temp = new Node();                temp.s=t;                temp.dep=1;                q.add(temp);                visit[temp.s]=true;     //将每个水壶入队            }            bfs();        }    }     public static void bfs() {        while(!q.isEmpty()){            Node x = q.poll();            ans = Math.max(ans,x.dep);            if(x.s-1>0&&!visit[x.s-1]){                Node temp = new Node();                temp.s=x.s-1;                temp.dep=x.dep+1;                visit[temp.s] = true;                q.add(temp);            }            if(x.s+1<=n&&!visit[x.s+1]){                Node temp = new Node();                temp.s=x.s+1;                temp.dep=x.dep+1;                visit[temp.s] = true;                q.add(temp);            }        }        System.out.println(ans);    }     static class Node {        int s, dep;  //s记录位置，dep记录时间    }}
