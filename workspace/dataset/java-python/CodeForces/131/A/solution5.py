def compute(string):     if string.isupper() or string[1:].isupper() or len(string)==1:        answer = ""        for character in string:            if character.isupper():                    answer += character.lower()            else:                answer += character.upper()        return answer    else:        return string if __name__ == '__main__':    string = input()    print(compute(string))
