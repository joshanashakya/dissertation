import java.util.*; public final class App {    public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int t = in.nextInt();        while(t-->0) {            int n = in.nextInt();            int[] miner = new int[n];            int[] mine = new int[n];            int j = 0, k = 0;            for(int i=0; i<2*n; i++) {                int x = Math.abs(in.nextInt());                int y = Math.abs(in.nextInt());                if(x == 0) {                    miner[j++] = y;                } else {                    mine[k++] = x;                }            }            Arrays.sort(miner);            Arrays.sort(mine);            double ans = 0;            for(int i=0; i<n; i++) {                ans += (double) Math.sqrt((long) miner[i]*miner[i] + (long) mine[i]*mine[i]);            }            System.out.println(ans);        }        in.close();    }}
