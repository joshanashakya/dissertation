import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.StringTokenizer; public class NewClass {     public static void main(String[] args) throws IOException {         BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer tok = new StringTokenizer(reader.readLine());        int n = Integer.parseInt(tok.nextToken()),                m = Integer.parseInt(tok.nextToken()),                min = Math.min(n, m),                count = 0, x, y;         for (int i = 0; i <= min; i++) {            for (int j = 0; j <= min; j++) {                x = (int) Math.pow(i, 2) + j;                y = (int) Math.pow(j, 2) + i;                if (m == y && x == n) {                    count++;                }            }        }        System.out.println(count);    }}
