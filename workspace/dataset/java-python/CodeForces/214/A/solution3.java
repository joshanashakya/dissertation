import java.util.Scanner; public class SystemofEquations {    public static void main(String[] args) {        Scanner scanner=new Scanner(System.in);        int n= scanner.nextInt();        int m= scanner.nextInt();        int a=(int)Math.max(Math.sqrt(n),Math.sqrt(m));        int b=Math.max(m,n)-(int) Math.pow(a,2);      // System.out.println(a);      // System.out.println(b);        if (m==n &&(m==1 || m==2 || m==6))            if (m==1)            System.out.println(2);            else                System.out.println(1);        else if(m==n &&  m>2)        {            System.out.println(0);        }        else if(((Math.pow(a,2)+b)==n || (Math.pow(b,2)+a)==n) &&                ((Math.pow(b,2)+a)==m || (Math.pow(a,2)+b)==m))            System.out.println(1);        else            System.out.println(0);     }}
