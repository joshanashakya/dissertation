import java.util.*;public class YoungExplorers { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int t = sc.nextInt();		while (t-- > 0) {			int n = sc.nextInt();			int[] arr = new int[n]; 			for (int i = 0; i < n; i++) {				arr[i] = sc.nextInt();			} 			Arrays.sort(arr); 			int i = 0, count = 0;			while (i < n) {				int group = 1; 				while (group < arr[i] && (i < n - 1)) {					group++;					i++;				} 				if (arr[i] <= group) {					count++;				}				i++; 			}			System.out.println(count);		}	} }
