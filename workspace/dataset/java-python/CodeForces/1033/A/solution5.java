//import java.lang.*;import java.util.*; public class Main {	public static void main(String[] args) {		Scanner scanner = new Scanner(System.in);		int n = scanner.nextInt();		int ax = scanner.nextInt();		int ay = scanner.nextInt();		int bx = scanner.nextInt();		int by = scanner.nextInt();		int cx = scanner.nextInt();		int cy = scanner.nextInt();				boolean can = false;				if (bx < ax && by < ay && cx < ax && cy < ay)			can = true;		else if (bx > ax && by < ay && cx > ax && cy < ay)			can = true;		else if (bx > ax && by > ay && cx > ax && cy > ay)			can = true;		else if (bx < ax && by > ay && cx < ax && cy > ay)			can = true;				if (can)			System.out.println("YES");		else			System.out.println("NO");		scanner.close();	} // end main}	 		  	  	           		 	   	
