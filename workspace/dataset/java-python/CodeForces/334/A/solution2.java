import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;  public class Template{    private static BufferedReader input = new BufferedReader(new InputStreamReader(System.in));    static String readln() throws IOException    {        String line = input.readLine();        return line;    }     public static void main(String[] args) throws IOException    { //        test();        solution();    }     public static void test() throws IOException {        int t = Integer.parseInt(readln());    }     public static void solution() throws IOException {        int n = Integer.parseInt(readln());        int nn = (int) Math.pow(n, 2);        for (int i = 1; i <= (nn / 2); i += (n / 2)) {            for (int j = 0; j < (n / 2); j++) {                System.out.print(i + j + " " + (nn - i - j + 1) + " ");            }            System.out.println();        }    } }
