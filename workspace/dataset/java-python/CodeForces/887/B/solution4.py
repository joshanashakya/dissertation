from itertools import permutations#C = combfrom math import * n = int(input())mat = [list(map(int, input().split())) for _ in range(n)] mp = {}for vk in mat:    for el in vk:        mp[str(el)] = Truedef app(a):    for per in permutations(a):        if per[0] != 0:            s = ''            for p in per: s += str(p)            mp[s] = True            if len(s) > 2:                mp[s[:-1]] = mp[s[1:]] = Truedef rec(a, i, n):    if i == n:        app(a)        return    a.append(0)    for j in range(len(mat[i])):        a[-1] = mat[i][j]        rec(a, i+1, n)    a.pop()rec([], 0, n) res = 0for i in range(1, 1000):    if str(i) not in mp:        res = i-1        breakprint(res)
