import java.util.*;public class asd{         static Scanner s=new Scanner(System.in);         public static void main(String args[])         {                  int t=1;                  while(t-->0)                  {                           solve();                  }                           }         public static void solve()         {                            int n=s.nextInt();                HashMap<Integer,Integer> map1=new HashMap<>();                HashMap<Integer,Integer> map2=new HashMap<>();                while(n-->0)                {                       int a=s.nextInt();                       int b=s.nextInt();                       if (map1.containsKey(a)) {                                                 map1.replace(a,  1);                     }                        else                        map1.put(a, 1);                          if (map2.containsKey(b)) {                                                         map2.replace(b,  1);                             }                                else                                map2.put(b, 1);                 }                System.out.println(Math.min(map1.size(),map2.size()));         }}
