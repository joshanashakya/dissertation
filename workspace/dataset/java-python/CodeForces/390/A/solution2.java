import java.util.Scanner; public class TestBetter{	public static void main(String[] args)	{		Scanner input = new Scanner(System.in); 		int[] freqX = new int[109], freqY = new int[109];			int n;		n = input.nextInt();			for(int i = 0; i < n; i++)		{			int x, y;			x = input.nextInt();			y = input.nextInt();				freqX[x]++;			freqY[y]++;		}			int cntX = 0;		int cntY = 0;			for(int i = 0; i <= 100; i++)			cntX += Boolean.compare((freqX[i] > 0), false);			for(int i = 0; i <= 100; i++)			cntY += Boolean.compare((freqY[i] > 0), false);			System.out.println(Math.min(cntX, cntY));	}}
