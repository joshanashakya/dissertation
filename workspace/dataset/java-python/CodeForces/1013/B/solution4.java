import java.util.*;public class a{    static int mod = (int) 1e9 + 7;    static int Infinity=Integer.MAX_VALUE;    static int negInfinity=Integer.MIN_VALUE;    public static void main(String args[])    {        Scanner d= new Scanner(System.in);        int n,x,i,z;        n=d.nextInt();        x=d.nextInt();        int a[]=new int[n];        Set <Integer> s=new HashSet<>();        Set <Integer> s1=new HashSet<>();        for(i=0;i<n;i++)        a[i]=d.nextInt();        z=-1;        for(i=0;i<n;i++)        {            if(s.isEmpty())            {s.add(a[i]);             s1.add(a[i]&x);            }            else if(s.contains(a[i]))            {                z=0;            }            else if(s.contains(a[i]&x) || s1.contains(a[i]))            {                if(z!=0)                {z=1;                s.add(a[i]);}            }            else            {                s.add(a[i]);                s1.add(a[i]&x);            }        }        if(s1.size()<s.size() && z==-1)        System.out.println(2);        else        System.out.println(z);    }}
