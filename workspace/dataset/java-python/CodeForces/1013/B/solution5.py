if __name__ == '__main__':    n, x = map(int, input().split())    a = list(map(int, input().split()))    vis = {}    for i in range(n):        vis.setdefault(a[i], 0)        vis[a[i]] += 1     if max(vis.values()) > 1:        print(0)        # exit(0)    else:        res = filter(lambda t: vis.get(t & x) and vis[t & x] > 0 and (t & x) != t, a)        tt = list(res)         if tt:            print(1)        else:            a = map(lambda t: t & x, a)            if len(set(a)) < n:                print(2)            else:                print(-1)      
