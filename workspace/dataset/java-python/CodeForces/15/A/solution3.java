import java.util.*; public class Solution {    public static void main(String[] args) {        Scanner s = new Scanner(System.in);         int n = s.nextInt();        int t = s.nextInt();         int[][] intv = new int[n][2];         for (int i = 0; i < n; i++) {            intv[i][0] = s.nextInt();            intv[i][1] = s.nextInt();        }         Arrays.sort(intv, (a,b) -> a[0] - b[0]);         int res = 2; // leftmost and rightmost pos         for (int i = 1; i < n; i++) {            // compare gap from current start pt - previous end pt            if (intv[i][0] - (double) intv[i][1] / 2 - (intv[i - 1][0] + (double) intv[i - 1][1] / 2) == t)                res++;            else if (intv[i][0] - (double) intv[i][1] / 2 - (intv[i - 1][0] + (double) intv[i - 1][1] / 2) > t)                res += 2;         }         System.out.println(res);         s.close();    }}
