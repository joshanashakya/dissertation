#include <bits/stdc++.h>using namespace std;typedef long long ll;ll n, m; ll fastpow(ll a, ll n){    ll res = 1, base = a;    while(n){        if(n & 1)            res = (res * base) % m;        base = (base * base) % m;        n >>= 1;    }    return res - 1;}  int main(){    cin >> n >> m;    ll ans = fastpow(3, n);    ans = (ans + m) % m;    cout << ans << endl;    return 0;}//2021-03-15 23:05:45.613
