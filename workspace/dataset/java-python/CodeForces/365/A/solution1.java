import java.util.HashSet;import java.util.Scanner;    public class PlayGroundHug {     /**     * @param args the command line arguments     */    private static final Scanner in = new Scanner(System.in);        public static void main(String[] args){        int n = in.nextInt();         int k = in.nextInt();        int res =0;                 for(int i=0; i<n; i++){            int c = 0;            HashSet<Integer> visited = new HashSet<>();             String str = in.next();             for(int j=0; j<str.length(); j++){                int num = Integer.parseInt(str.charAt(j)+"");                 if(num<=k && !visited.contains(num)){                     visited.add(num);                     c++;                 }            }                        if(k==0){                if(c>=1){                    res++;                }            }else{                if(c>k){                    res++;                 }            }        }                System.out.println(res);            }      }
