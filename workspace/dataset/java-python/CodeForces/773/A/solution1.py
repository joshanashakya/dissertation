#include <bits/stdc++.h>using namespace std; #define int long long int#define mod 1000000007#define vi vector<int>#define vs vector<string>#define mp make_pair#define pb push_backdouble pi = 3.141592653589793238462;string numkey="0123456789";string uni="abcdefghijklmnopqrstuvwxyz";#define pii pair<int,int>#define setf cout.setf(ios::fixed);#define setp setprecision#define mii map<int,int>#define F first#define S second#define all(x) x.begin(),x.end() int dx[4]={0,0,1,-1};int dy[4]={1,-1,0,0}; int power(int x,int y){  if(y==0)    return 1;  int res=power(x,y/2);  if(y%2==0)    return res*res;  return res*res*x; }bool check(int mid,int x,int y,int p,int q){  if((p*mid>=x) && (q-p)*mid>=(y-x))    return true;  return false; } signed main(){  #ifndef ONLINE_JUDGE  freopen("input.txt","r",stdin);  freopen("output.txt","w",stdout);  #endif   int t;  cin>>t;  while(t--)  {    int x,y,p,q;    cin>>x>>y>>p>>q;    int l=-1,r=1000000000,ans=-1;    //r*=r;    if(check(r,x,y,p,q)==false)    {      cout<<"-1\n";      continue;    }    while(r-l>1)    {      int mid=(l+r)/2;      //cout<<mid<<" ";      if(check(mid,x,y,p,q))      {         ans=mid;        r=mid;       }      else        l=mid;    }       cout<<(r*q-y)<<"\n";   }   return 0;}
