import java.util.*;public class A0773_SuccessRate {	public static void main(String[] args) { 		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();				for (int i=0; i<n; i++) {			long x = sc.nextInt();			long y = sc.nextInt();			long p = sc.nextInt();			long q = sc.nextInt();						if (!works(x, y, p, q, 1000000000)) {				System.out.println(-1);				continue;			}						long l = 0;			long r = 1000000000;			while (r > l) {				long mid = (r + l)/2;				if (works(x, y, p, q, mid))					r = mid;				else					l = mid+1;			}			System.out.println(r*q - y);		}		sc.close();	}		public static boolean works(long x, long y, long p, long q, long mid) {		if (p*mid >= x && q*mid - p*mid >= y - x)			return true;		return false;	}}
