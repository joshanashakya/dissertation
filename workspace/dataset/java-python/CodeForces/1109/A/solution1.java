 import java.util.Scanner; public class Main {  public static void main(String[] args) {    Scanner scanner = new Scanner(System.in);    int n = scanner.nextInt();    int[] arr = new int[n];    for (int i = 0; i < n; i++) {      arr[i] = scanner.nextInt();    }    solve(arr, n);    scanner.close();  }   /**   * if A⊕B=C then A=B⊕C   * @param arr   * @param n   */  private static void solve(int[] arr, int n) {    int[] prefix = new int[n];    prefix[0] = arr[0];    for (int i = 1; i < n; i++) {      prefix[i] = arr[i] ^ prefix[i - 1];    }     int[][]cnt =new int[2][(1<<20)+3];    cnt[1][0]=1;    long ans=0;    for(int i=0;i<n;i++){      ans+=cnt[i%2][prefix[i]];      cnt[i%2][prefix[i]]++;    }     System.out.println(ans);  }}
