/*If you want to aim high, aim highDon't let that studying and grades consume youJust live life young******************************If I'm the sun, you're the moonBecause when I go up, you go down********************************/import java.util.*;import java.io.*;    public class x1109B   {      public static void main(String args[]) throws Exception      {         //BufferedReader infile = new BufferedReader(new FileReader("cowdate.in"));         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));           String input = infile.readLine();         int N = input.length();         char[] arr = input.toCharArray();         if(impossible(arr, N))         {            System.out.println("Impossible");            return;         }         boolean one = false;         for(int a=1; a < N; a++)         {            String temp = input.substring(a);            temp += input.substring(0, a);            char[] arr2 = temp.toCharArray();            boolean yee = true;            for(int i=0; i < N/2; i++)               yee &= (arr2[i] == arr2[N-i-1]);            //check with original!!!            boolean yikes = true;            for(int i=0; i < N; i++)               if(arr[i] != arr2[i])                  yikes = false;            yee &= !yikes;            one |= yee;         }         if(one && N%2 == 0)            System.out.println(1);         else            System.out.println(2);      }      public static boolean impossible(char[] arr, int N)      {         int[] cnt = new int[200];         for(char c: arr)            cnt[c]++;         for(int i=0; i < 200; i++)            if(cnt[i] >= N-1)               return true;         return false;      }   }
