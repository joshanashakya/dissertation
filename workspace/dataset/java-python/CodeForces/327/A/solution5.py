# This is a sample Python script. import math#import numpy as np # Press Maj+F10 to execute it or replace it with your code. def main():    n = int(input())    arr = [int(x) for x in input().split()]    #print(arr[0:2])     cn=[]    for i in range(n):        for j in range(i,n):            cnt = 0            for k in range(n):                if i<=k and k<=j:                    cnt+= 1 - arr[k]                else:                    cnt+= arr[k]            cn.append(cnt)     print(max(cn))      return  # Press the green button in the gutter to run the script.if __name__ == '__main__':    main() # See PyCharm help at https://www.jetbrains.com/help/pycharm/
