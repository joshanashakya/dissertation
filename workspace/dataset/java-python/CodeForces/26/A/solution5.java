/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Codechef{	public static void main (String[] args) throws java.lang.Exception	{						Scanner scan  = new Scanner(System.in);				int n = scan.nextInt();				int final_count=0;				for(int i=4;i<=n;i++){		    		    int num = i;		    int count=0;		    		    int[] arr_count = new int[i+1];		    		    for(int j=2;j<=Math.sqrt(i);j++){		        		        if(num%j==0){		            		            while(num%j==0){		                arr_count[j]++;		                num = num/j;		                		            }		        }		    }		    		    if(num!=1)		     arr_count[num]++;		    		    for(int k=2;k<arr_count.length;k++)		        if(arr_count[k]>0)		          count++;		          		    if(count==2)		     final_count = final_count+1;		}				System.out.println(final_count);	}}
