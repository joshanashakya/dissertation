import java.util.*; public class Combination_B { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();				int counter = 1;		ArrayList<Integer> zero = new ArrayList<>();		ArrayList<Integer> aa = new ArrayList<>();		ArrayList<Integer> bb = new ArrayList<>();				for(int i = 0; i<n; i++) {			int a = sc.nextInt();			int b = sc.nextInt();									if(b==0) {				zero.add(a);			}			else {				aa.add(a);				bb.add(b);			}		}				Collections.sort(zero,Collections.reverseOrder());				int ans = 0;		int i = 0;		while(counter>0 && i<aa.size()) {			ans+=aa.get(i);			counter+=bb.get(i);			i++;			counter--;		}		i = 0;		while(counter>0 && i<zero.size()) {			ans+=zero.get(i);			i++;			counter--;		}				System.out.println(ans); 	} }
