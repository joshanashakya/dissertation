import java.util.*;public class Main{    public static double calcTime(double d, int vd, int vp){        return d / (vd - vp);    }    		public static void main (String[] args) throws java.lang.Exception {	    Scanner in = new Scanner(System.in);	    // 1 2 1 1 10	    // 1 2 1 1 8	    int vp = in.nextInt(); // 1	    int vd = in.nextInt(); // 2	    int t = in.nextInt();  // 1	    int f = in.nextInt();  // 1	    int c = in.nextInt();  // 8	    	    int init_d = vp * t; // 1	   // System.out.println("init : "+init_d);	    if(vp >= vd){	        System.out.println(0);	        return;	    }	    double total_time = t;	    int d = 0;	    	    double timeToReachPrincess = calcTime(init_d, vd, vp);  // 1	    	    total_time += timeToReachPrincess; 	    while(total_time * vp < c){	       // System.out.println("dragon reached in time = "+total_time * vp);	        double pd = total_time * vp;	       // System.out.println("dragon reached while princess distance = "+ pd);	        d++;	        total_time += timeToReachPrincess;	       // System.out.println("dragon took "+timeToReachPrincess+" to cave ");	       // System.out.println("total scape time "+ total_time);	        total_time += f;	       // System.out.println("dragon took "+ f +" to straighten the things");	       // System.out.println("current princess distance = "+ (total_time * vp) );	        timeToReachPrincess = calcTime(total_time * vp, vd, vp);	       // System.out.println("dargon needs "+ timeToReachPrincess +" to reach princess" );	        total_time += timeToReachPrincess;	    }	    	    System.out.println(d);	}}
