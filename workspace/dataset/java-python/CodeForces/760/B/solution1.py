def just_sum(n):    return (n * (n + 1)) // 2  def get_sum(a, b):    return just_sum(b) - just_sum(a - 1)  def check(mid,k,n,sn):        left = k - 1    right = n - k        if(left < mid):        left_sum = get_sum(mid-left,mid-1)    else:        left_sum = just_sum(mid-1)            if(right < mid):        right_sum = get_sum(mid-right,mid-1)    else:        right_sum = just_sum(mid-1)        return left_sum + right_sum + mid <= sn  def binary_search(n,k,sn):    low = 0    high = sn        while(low < high):        mid = low + (high - low + 1)//2        if(check(mid,k,n,sn)):            low = mid        else:            high = mid - 1        return low  n,m,k = map(int,input().split())sn = m - nprint(binary_search(n,k,sn)+1)
