import java.util.*;import java.io.*; public class B760{	public static void main(String[] args) throws Exception {		in.init(System.in);		long n=in.nextLong();//beds		long m=in.nextLong();//pillow		long k=in.nextLong();//bed no		if(n==m){System.out.println(1);}		else if(n==1){			System.out.println(m);		}		else{			long ans=2;			long left=k-1;			long right=n-k;			m-=n;			m--;			while(m>0)			{	ans++;				long temp=ans-2;				if(left-temp>=0){					if(right-temp>=0)					{						m=m-2*temp-1;					}					else{						m=m-temp-1-right;					}				}				else if(right-temp>=0)				{					if(left-temp>=0){						m=m-2*temp-1;					}					else{						m=m-temp-1-left;					}				}				else if(left-temp<0)				{					if(right-temp<0)					{						long hi=m/n;if(hi==0){							m-=n;						}						else{						ans--;						ans+=hi;						m=m-n*hi;						} 											}					else{						m=m-temp-1;						m=m-left;					}				}				else{					if(left-temp<0)					{						long hi=m/n;if(hi==0){							m-=n;						}						else{						ans--;						ans+=hi;						m=m-n*hi;						}					}					else{						m=m-temp-1;						m-=right;					}				}				if(m<0)					{ans--;} 			}			System.out.println(ans);		} 	} }class in {    static BufferedReader reader;    static StringTokenizer tokenizer;     static void init(InputStream input) {        reader = new BufferedReader(new InputStreamReader(input));        tokenizer = new StringTokenizer("");    }     static String next() throws IOException {        while (!tokenizer.hasMoreTokens()) {            tokenizer = new StringTokenizer(reader.readLine());        }        return tokenizer.nextToken();    }     static int nextInt() throws IOException {        return Integer.parseInt(next());    }    static long nextLong() throws IOException {        return Long.parseLong(next());    }}
