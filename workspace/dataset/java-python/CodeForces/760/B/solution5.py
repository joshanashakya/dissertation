def distribute(target,n,k,m):  right=n-k  prev=target-1    total=m-target    if right>=prev:    total-=((prev*(prev+1))//2)    total-=right-prev  else:    temp=prev-right    total-=((prev*(prev+1))//2)-((temp*(temp+1))//2)    right=k-1  prev=target-1  if right>=prev:    total-=((prev*(prev+1))//2)    total-=right-prev  else:    temp=prev-right    total-=((prev*(prev+1))//2)-((temp*(temp+1))//2)   if total<0:    return -1  else:    return 1    n,m,k=map(int,input().split())low=1high=m-n+1while low<=high:  mid=low+(high-low)//2  val=distribute(mid,n,k,m)  if val==-1:    high=mid-1  else:    low=mid+1print(high)       
