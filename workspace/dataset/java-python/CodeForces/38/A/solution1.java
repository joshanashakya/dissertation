import java.util.Scanner; public class Army {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();         int []d = new int[n];        for (int i = 1; i < n; i++) {            d[i] = scanner.nextInt();        }         int a = scanner.nextInt();        int b = scanner.nextInt();         int result = 0;        for (int i = a; i < b; i++) {            result += d[i];        }         System.out.println(result);      }}
