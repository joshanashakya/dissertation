/*If you want to aim high, aim highDon't let that studying and grades consume youJust live life young******************************If I'm the sun, you're the moonBecause when I go up, you go down*******************************I'm working for the day I will surpass youhttps://www.a2oj.com/Ladder16.html*/import java.util.*;import java.io.*;import java.math.*;    public class x466B   {      public static void main(String omkar[]) throws Exception      {         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));           StringTokenizer st = new StringTokenizer(infile.readLine());         long N = Long.parseLong(st.nextToken());         long A = Long.parseLong(st.nextToken());         long B = Long.parseLong(st.nextToken());         if(N <= (A*B)/6)            System.out.println((A*B)+"\n"+A+" "+B);         else         {            long res = Long.MAX_VALUE;            long a = 0;            long b = 0;            for(long i=Math.min(A, B); i <= (int)(Math.sqrt(6*N))+1; i++)            {               long j = 6*N/i;               if(j < Math.max(A, B))                  continue;               if((6*N)%i > 0)                  j++;               if(i*j < res)               {                  res = i*j;                  a = i;                  b = j;               }            }            if(A < B)               System.out.println(res+"\n"+a+" "+b);            else               System.out.println(res+"\n"+b+" "+a);         }      }   }
