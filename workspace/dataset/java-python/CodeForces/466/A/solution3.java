import java.util.*;public class HelloWorld{      public static void main(String []args){        //System.out.println("Hello World");        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int m = sc.nextInt();        int a = sc.nextInt();        int b = sc.nextInt();                int min = Integer.MAX_VALUE;        int q = n / m;        int r = n % m;        if (r > 0) {            min = Math.min(min, (q + 1)*b);            min = Math.min(min, n * a);            min = Math.min(min, q*b + r*a);        } else {            min = Math.min(min, n * a);            min = Math.min(min, q * b);        }        System.out.println(min);     }}
