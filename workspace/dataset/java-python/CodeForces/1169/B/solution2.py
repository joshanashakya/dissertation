 def solve(pairs):    for x in pairs[0]:        i = 1        while i < len(pairs) and x in pairs[i]:            i += 1        if i == len(pairs):            return True        for y in pairs[i]:            if all(x in p or y in p for p in pairs):                return True    return False  _, m = map(int, input().split())pairs = [tuple(map(int, input().split())) for i in range(m)]print("YES" if solve(pairs) else "NO")
