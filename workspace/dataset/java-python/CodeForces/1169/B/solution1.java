import java.io.*;import java.util.*;public class Pairs {	static int N,M, arr1[], arr2[];	public static void main(String[] args) throws IOException{		// TODO Auto-generated method stub		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(br.readLine());		N = Integer.parseInt(st.nextToken()); M = Integer.parseInt(st.nextToken());		arr1 = new int[M];		arr2 = new int[M];		for(int i = 0; i < M; ++i) {			st = new StringTokenizer(br.readLine());			int a = Integer.parseInt(st.nextToken()), b = Integer.parseInt(st.nextToken());			arr1[i] = a; arr2[i] = b;		}		//assign first to be one of them		//arr1[i] = x;		int x = arr1[0], y = -1;		boolean see = proc(x,y)||proc(arr2[0],y);		System.out.println(see?"YES":"NO");	} 	private static boolean proc(int x, int y) {		// TODO Auto-generated method stub		boolean b = false, works = true;		for(int i = 1; i < M; ++i) {			if(!b&&!(arr1[i]==x||arr2[i]==x)) {				b = true;				y=arr1[i];				continue;			}			//System.out.println(x + " " + y);			if(b&&!(arr1[i]==x||arr1[i]==y||arr2[i]==x||arr2[i]==y)) {				works = false;				break;			}		}y = -1; b= false;		//System.out.println();		if(works) return works;		works = true;		for(int i = 1; i < M; ++i) {			if(!b&&!(arr1[i]==x||arr2[i]==x)) {				b = true;				y=arr2[i];			}			//System.out.println(x + " " + y);			if(b&&!(arr1[i]==x||arr1[i]==y||arr2[i]==x||arr2[i]==y)) {				works = false;				break;			}		}		return works;	} }
