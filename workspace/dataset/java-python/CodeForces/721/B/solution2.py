# I'm stupid x3 import functools n, k = map(int, input().split())delay = 5 passwords = []for _ in range(n):    passwords.append(input()) right_password = input()  def sort_fn(kind, a, b):    if len(a) < len(b):        return -1    if len(a) > len(b):        return 1     if len(a) == len(right_password):        if a != right_password:            return 1 if kind == 'best' else -1        if b != right_password:            return -1 if kind == 'best' else 1     return 0  best = sorted(passwords, key=functools.cmp_to_key(functools.partial(sort_fn, 'best')))worst = sorted(passwords, key=functools.cmp_to_key(functools.partial(sort_fn, 'worst'))) b_n = -1w_n = -1 offset = 0for i in range(len(passwords)):    if best[i] == right_password:        b_n = i + 1 + offset    if worst[i] == right_password:        w_n = i + 1 + offset     if (i + 1) % k == 0:        offset += delay     if w_n != -1 and b_n != -1:        break print(b_n, w_n)  if __name__ == '__main__':    pass
