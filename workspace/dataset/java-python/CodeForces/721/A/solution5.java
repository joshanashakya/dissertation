import java.util.*;import static java.lang.Math.min; public class Test {    public static void main(String[] args) {         Scanner input = new Scanner(System.in);        int n = input.nextInt();        char[] s = input.next().toCharArray();        LinkedList<Integer> list = new LinkedList<>();         for (int i = 0; i < n; i++) {             int count = 0;            while (i < n && s[i] == 'B') {                count++;                i++;            }            if (count > 0)                 list.add(count);        }         System.out.println(list.size());        for (Integer a : list) {            System.out.print(a + " ");        }        System.out.println();    }}
