import java.util.ArrayList;import java.util.List;import java.util.Scanner; public class JapaneseCrossward {		public static void main(String[] args)	{		Scanner sc = new Scanner(System.in);		List<Integer> blackCellGroups = new ArrayList<>();				int n = sc.nextInt();		sc.nextLine();				String row = sc.nextLine();				char[] cells = row.toCharArray();				for(int i = 0; i < n; )		{			if(cells[i] == 'B')			{				int length = calculateSubLength(i, n, cells);				blackCellGroups.add(length);				i = i + length;			}			else			{				i++;			}		}				System.out.println(blackCellGroups.size());		for(int length : blackCellGroups)		{			System.out.print(length + " ");		}	}		private static int calculateSubLength(int startIndex, int n, char[] cells)	{		int i = startIndex;				while(i < n && cells[i] == 'B')		{			i++;		}				return (i - startIndex);	} }
