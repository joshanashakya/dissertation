import java.util.*; public class Example {    static int[] dp;     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        //  int t=sc.nextIn        int n = sc.nextInt();        String s = sc.next();        List<Integer> list = new ArrayList<>();        for (int i = 2; i * i <= n; i++) {            if (n % i == 0) {                if ((n / i) != i) {                    list.add(n / i);                    list.add(i);                } else                    list.add(i);            }        }         list.add(n);        Collections.sort(list);         //   System.out.println(list.toString());       // StringBuilder ans = new StringBuilder(s);       // s = ans.reverse().toString();      //  System.out.println(s);        for (int i = 0; i < list.size() ;i++) {            StringBuilder an = new StringBuilder(s.substring(0, list.get(i)));           // System.out.println(an);            an = an.reverse();           // System.out.println(an);            String sb = s.substring(list.get(i), n);            an.append(sb);            s = an.toString();           // System.out.println(s);          }        System.out.println(s);    }}	     		     	   	 	  		  		  	
