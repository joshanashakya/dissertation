import java.util.*;import java.io.*;public class A  {    static FastReader in=new FastReader();    static final Random random=new Random();    static long mod=1000000007L;          public static void main(String args[]) throws IOException {        int t=1;//in.nextInt();        StringBuilder res=new StringBuilder();                       loop:        while(t-->0)        {        	int n=in.nextInt();        	int a[][]=new int[n][2];        	HashSet<Integer>nums=new HashSet<>();        	int freq[]=new int[1001];        	for(int i=0;i<n;i++)        	{        		a[i][0]=in.nextInt();        		a[i][1]=in.nextInt();        		freq[a[i][0]]++;        		nums.add(a[i][0]);        	}        	HashSet<Integer>temp=new HashSet<>();        	for(int i=0;i<n;i++)        	{        		if(a[i][0]==a[i][1])        		{        			freq[a[i][0]]=1;        			if(temp.contains(a[i][0]))        			{        				nums.remove(a[i][0]);        			}        			else        			{        				temp.add(a[i][0]);        			        			}        		}        		else        		{        			if(nums.contains(a[i][1]))        			{        				nums.remove(a[i][1]);        			}        			        		}        	}        	int sum=0;        	for(int x: nums)        	{        		sum+=freq[x];        	}        	res.append(sum);        	        }        print(res);    }                      static void fun(Integer a[])    {    	a[0]=1;    }    static void ruffleSort(int[] a) {        int n=a.length;//shuffle, then sort        for (int i=0; i<n; i++) {            int oi=random.nextInt(n), temp=a[oi];            a[oi]=a[i]; a[i]=temp;        }        Arrays.sort(a);    }     static < E > void print(E res)    {        System.out.println(res);    }      static  int gcd(int a,int b)    {        if(b==0)        {            return a;        }        return gcd(b,a%b);    }        static int lcm(int a, int b)    {        return (a / gcd(a, b)) * b;    }         static int abs(int a)    {        if(a<0)            return -1*a;        return a;    }     static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new  InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException  e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }        int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }        String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }         int [] readintarray(int n) {            int res [] = new int [n];            for(int i = 0; i<n; i++)res[i] = nextInt();            return res;        }        long [] readlongarray(int n) {            long res [] = new long [n];            for(int i = 0; i<n; i++)res[i] = nextLong();            return res;        }    } }    
