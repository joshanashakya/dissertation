import java.util.ArrayList;import java.util.Scanner; public class BottleOpener {    public static void main(String[] args) {        Scanner scan = new Scanner(System.in);         int n = scan.nextInt();        int[] open = new int[1000];        ArrayList<Integer> same = new ArrayList<>();         for(int i=0;i<n;i++){            int a = scan.nextInt();            int b = scan.nextInt();             if(same.contains(a)){                open[b-1] +=1000;                open[a-1]--;            }             if(a==b){                if(!same.contains(b)){                    same.add(b);                    if(open[a-1]%1000 > 0){                        open[a-1] -= open[a-1]%1000;                    }                }            } else open[b-1] += 1000;             open[a-1]++;        }         int countC = 0;        for(int i=0;i<1000;i++){            if(open[i] > 0 && open[i] < 1000) countC += open[i];        }        System.out.println(countC);    }}
