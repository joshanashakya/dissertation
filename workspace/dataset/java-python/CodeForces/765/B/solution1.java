import java.util.*;import java.util.stream.Collectors;import java.lang.*;import java.io.*; public class Problem {		public static void main (String[] args) throws java.lang.Exception	{	    Scanner sc = new Scanner(System.in);	    String s = sc.next();	    	    String temp = "a";	    String ans="YES";	    for(int i=0;i<s.length();i++)	    { 	    	if(temp.indexOf(s.charAt(i))!=-1)continue;	    	if(s.charAt(i) == temp.charAt(temp.length()-1)+1 && i>0)	    	{	    		temp += s.charAt(i)+""; 	    	}	    	else	    	{	    		ans = "NO";	    		break;	    	}	    }	    System.out.println(ans);	}		 }
