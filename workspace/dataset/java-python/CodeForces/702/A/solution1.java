import java.util.Scanner; public class MaximumIncrease {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = Integer.parseInt(scanner.nextLine());         int[] arr = new int[n];        for (int i = 0; i < n; i++) {            arr[i] = scanner.nextInt();        }         int currentMax = 1;        int globalMax = 1;        int prev = arr[0];        for (int i = 1; i < n; i++) {            if (arr[i] > prev) {                currentMax++;            }else{                currentMax = 1;            }             prev = arr[i];             if (currentMax > globalMax) {                globalMax = currentMax;            }         }         System.out.println(globalMax);    }}
