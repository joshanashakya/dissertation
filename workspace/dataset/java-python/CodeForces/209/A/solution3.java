import java.util.*; public class Training {     private static int sport(int n) {        if (n==1){            return 1;        }        if (n==2){            return 3;        }        int mod = (int) 1e9 + 7;        long ans = 3;        long one = 1;        long two = 2;        boolean flag = false;        for (int i = n - 3; i >= 0; i--) {            if (!flag) {                one += two;                one%=mod;                ans += one % mod;            } else {                two += one;                two%=mod;                ans += two % mod;            }            ans%=mod;            flag = !flag;        }        return (int) ans;    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        System.out.println(sport(n));    }}
