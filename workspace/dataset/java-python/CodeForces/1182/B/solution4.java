import java.util.*; public class Solution {     private static int countCentralStar = 0;    private static int countMultipleDFS = 0;    private static int countMultipleFilledRows = 0;     private static void dfs(String[][] arr, int i, int j) {        if (i < 0 || i >= arr.length || j < 0 || j >= arr[0].length) {            return;        }         if (arr[i][j].equalsIgnoreCase(".")) {            return;        }          arr[i][j] = ".";         dfs(arr, i+1, j);        dfs(arr, i-1, j);        dfs(arr, i, j - 1);        dfs(arr, i, j + 1);      }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int height = sc.nextInt();        int width = sc.nextInt();         String[][] arr = new String[height][width];         for (int i = 0; i < height; i++) {            String[] line = sc.next().split("");             for (int j = 0; j < width; j++) {                arr[i][j] = line[j];            }        }           for (int i = 0; i < height; i++) {            int localCount = 0;            for (int j = 0; j < width; j++) {                 if(arr[i][j].equalsIgnoreCase("*")){                    localCount++;                }                 if (i > 0 && i < arr.length - 1 && j > 0 && j < arr[0].length - 1) {                    if (arr[i + 1][j].equalsIgnoreCase("*") && arr[i - 1][j].equalsIgnoreCase("*") && arr[i][j + 1].equalsIgnoreCase("*") && arr[i][j - 1].equalsIgnoreCase("*")) {                        countCentralStar++;                    }                }            }             if(localCount > 1){                countMultipleFilledRows++;            }        }         for (int i = 0; i < height; i++) {            for (int j = 0; j < width; j++) {                if (arr[i][j].equalsIgnoreCase("*")) {                    countMultipleDFS++;                    dfs(arr, i, j);                }            }        }         if(countCentralStar == 1 && countMultipleDFS == 1 && countMultipleFilledRows == 1){            System.out.println("YES");        } else {            System.out.println("NO");        }      }}
