import java.io.*;import java.util.*; public class Main {     public static void main(String[] args) throws IOException{        BufferedReader a = new BufferedReader(new InputStreamReader(System.in));        int t = Integer.parseInt(a.readLine());        for (int i=0; i<t ;i++) {            String s1 = a.readLine();            int n = Integer.parseInt(s1.split(" ")[0]);            int k = Integer.parseInt(s1.split(" ")[1]);            String s2 = a.readLine();            String[] s22 = s2.split(" ");            int[] arr = new int[n];            for (int j =0; j<n ;j++){                arr[j] = Integer.parseInt(s22[j]);            }             int min = Integer.MAX_VALUE;            int min_index = -1;            for (int kk=0 ; kk<n ;kk++){                if (arr[kk] < min){                    min = arr[kk];                    min_index = kk;                }            }             int sum = 0;             for (int tt = 0; tt< n; tt++){                if (tt != min_index) {                    sum += (k - arr[tt]) / min;                }            }             System.out.println(sum);         }    }}
