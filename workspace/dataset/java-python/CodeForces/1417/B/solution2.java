import java.util.Scanner; public class B1417 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int T = in.nextInt();        for (int t=0; t<T; t++) {            int N = in.nextInt();            int sum = in.nextInt();            boolean even = (sum%2 == 0);            int half = sum/2;            boolean halfToZero = true;            StringBuilder output = new StringBuilder();            for (int n=0; n<N; n++) {                int a = in.nextInt();                int color;                if (a > sum) {                    color = 0;                } else {                    if (even && a == half) {                        color = halfToZero ? 0 : 1;                        halfToZero = !halfToZero;                    } else {                        color = (a <= half) ? 0 : 1;                    }                }                output.append(color).append(' ');            }            System.out.println(output);        }    } }
