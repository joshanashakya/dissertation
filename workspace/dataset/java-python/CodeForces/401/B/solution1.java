import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*; import javafx.util.Pair; public class Main{         public static void main(String[] args)    {        FastScanner input = new FastScanner();       int x = input.nextInt();       int k = input.nextInt();       boolean xx[] = new boolean[x+1];        for (int i = 0; i < k; i++) {            int type = input.nextInt();            if(type==1)            {                int div2 = input.nextInt();                int div1= input.nextInt();                xx[div1]= xx[div2]=true;            }            else            {                int div2 = input.nextInt();                xx[div2] = true;            }        }        xx[x] = true;//        System.out.println(Arrays.toString(xx));        int max = 0,min=0;        for (int i = 1; i <=x; i++) {            if(!xx[i])            {                max++;            }        }        for (int i = 1; i <=x; i++) {            if(!xx[i])            {                min++;                i++;            }        }        System.out.println(min+" "+max);    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
