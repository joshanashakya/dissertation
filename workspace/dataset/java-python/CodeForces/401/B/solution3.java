import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;  public class CodeForce {	public static int findmin(int[] arr)	{int sum=0;		for(int i=1;i<arr.length;i++) {		int count=0;	    while(i<arr.length&&arr[i]==0) {i++;		count++;}	    	    if(count%2!=0)	    {sum=sum+count/2+1;	    		    }	    else sum=sum+count/2;	    		}		return sum;	}	public static int findmax(int[] arr)	{int sum=0;		for(int i=1;i<arr.length;i++) {		int count=0;	    while(i<arr.length&&arr[i]==0) {i++;		count++;}	    	    	    sum=sum+count;	    		}		return sum;	}			public static void main(String[] args) throws IOException {			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		// int t=Integer.parseInt(br.readLine());		// while(t-->0)		String[] sr=br.readLine().split(" ");		int x=Integer.parseInt(sr[0]);		int k=Integer.parseInt(sr[1]);		int[] count=new int[x];		for(int i=0;i<k;i++)		{sr=br.readLine().split(" ");		int a=Integer.parseInt(sr[0]);		int b=Integer.parseInt(sr[1]);		if(a==1)		{			int c=Integer.parseInt(sr[2]);			count[c]++;			count[b]++;					}		else		{			count[b]++;					}				}			System.out.print(findmin(count)+" ");		System.out.print(findmax(count));		System.out.println(""); 								}      			}  
