import sys inp = sys.stdin.readlines(); ii = 0 out = [] x, k = map(int, inp[ii].split())ii += 1 roundshappened = []for round in range(k):    data = [int(d) for d in inp[ii].split()]    ii += 1    for d in data[1:]:        roundshappened.append(d) skipped = [] for x in range(1, x):    if x not in roundshappened:        skipped.append(x) used = [] minimum = 0 for i in range(1, len(skipped)):    if skipped[i - 1] + 1 == skipped[i] and skipped[i] not in used and skipped[i - 1] not in used:        minimum += 1        used.append(skipped[i - 1])        used.append(skipped[i]) minimum += len(skipped) - len(used) maximum = len(skipped)sys.stdout.write(str(minimum) + ' ' + str(maximum))    
