import java.io.*;import java.util.*; public class A {     static long find(char[] x, int len, int curr){        if(len == 0) return 0;         if(x[curr] == '1'){            return ((pow_2[len-1]*pow_2[len-1])%mod + (2*find(x, len-1, curr+1))%mod)%mod;        }        else{            return (2*(find(x, len-1, curr+1)%mod))%mod;        }    }     static long[] pow_2;    static long mod = (int)1e9+7;     public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        char[] x = br.readLine().toCharArray();        int n = x.length;        pow_2 = new long[n+1];        pow_2[0] = 1;        for(int i = 1; i<n; i++){            pow_2[i] = (2*pow_2[i-1])%mod;        }        long ans = find(x, n, 0);        System.out.println(ans);    }}
