import java.util.Scanner; public class A_Pages { 	public static void main(String[] args) {		Scanner scan = new Scanner(System.in); 		int n = scan.nextInt();		int p = scan.nextInt();		int k = scan.nextInt();		if (p < 1 || p > n)			return;		int head = p - k < 1 ? 1 : p - k;		int tail = p + k > n ? n : p + k; 		if (p - k > 1)			System.out.printf("<<");		for (int i = head; i <= tail; i++) {			if (i == p) {				System.out.printf(" (%d) ", i);			} else {				System.out.print(" " + i + " ");			}		}		if (p + k < n)			System.out.printf(">>");		scan.close();	} }
