n, p, k = map(int, input().split())x = 0close_symbol = Truepages = ""if((p-k) > 1):    pages += "<< " if(k<=p):    for x in range (k):        if(p-k+x == 0):            continue #means that p == k and need to ommit first navigation number        pages += str(p-k+x) + " " else:    for x in range(1,p):        pages += str(x) + " "    if x == 0:        x = k-p # we want to start in 2 for second part    else:        x = k-1#x = x+p pages += "(" + str(p) + ") " while(x >=0):    if(p+k-x <= n):        pages += str(p+k-x) + " "    if(p+k-x >= n):        close_symbol = False #no >> required    x -= 1; if(close_symbol):    pages += ">>"print(pages)
