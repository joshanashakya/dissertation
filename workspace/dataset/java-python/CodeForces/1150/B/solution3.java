import java.util.*;import java.util.stream.*; public class Solution {	public static void main(String[] args) {		Scanner scan = new Scanner(System.in);		int n = scan.nextInt();		scan.nextLine();		Set<Integer> middle = new HashSet<Integer>();		Set<Integer> bottom = new HashSet<Integer>();		String result = "YES";		for(int i = 0; i < n; i++) {			StringBuilder s = new StringBuilder(scan.nextLine());			for(Integer elem : bottom) {				if(s.charAt(elem) == '.') {					s.setCharAt(elem, '#');				} else {					result = "NO";					break;				}			}			bottom.clear();			//System.out.println("after bottom " + s);			if(result.equals("NO")) break;			for(Integer elem : middle) {				if(s.charAt(elem) == '.'				&& s.charAt(elem+1) == '.'				&& s.charAt(elem+2) == '.') {					s.setCharAt(elem, '#');					s.setCharAt(elem+1, '#');					s.setCharAt(elem+2, '#');					bottom.add(elem+1);				} else {					result = "NO";					break;				}			}			middle.clear();			//System.out.println("after middle " + s);			if(result.equals("NO")) break;			for(int j = 0; j < n; j++) {				char ch = s.charAt(j);				if(ch == '.') {					if(j-1 >= 0 && j+1<n) {						middle.add(j-1);					} else {						result = "NO";						break;					}				}			}		}		if(!(middle.isEmpty() && bottom.isEmpty())) result = "No";		System.out.println(result);	}}
