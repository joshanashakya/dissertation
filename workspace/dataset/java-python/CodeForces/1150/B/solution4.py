# collaborated with no one def checkboard(board, cols, rows):    for i in range(rows):        for j in range(cols):            if board[i][j] == ".":                return "NO"    return "YES"  sizeOfBoard = int(input()) rows, cols = (sizeOfBoard, sizeOfBoard) board = [[0] * cols] * rows for x in range(sizeOfBoard):    board[x] = list(input()) for i in range(1, rows - 1):    for j in range(1, cols - 1):        if board[i][j] == "." and board[i + 1][j] == "." and board[i - 1][j] == "." and board[i][j + 1] == "." and board[i][j - 1] == ".":            board[i + 1][j] = "#"            board[i - 1][j] = "#"            board[i][j + 1] = "#"            board[i][j - 1] = "#"            board[i][j] = "#"  print(checkboard(board, cols, rows))  	      		  			 		 					 				
