 import java.util.Arrays;import java.util.Scanner; public class MainTest { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		String[] rows = new String[8];		String[] columns = new String[8];		for(int i=0 ; i<rows.length ; i++)			rows[i] = sc.nextLine();		for(int j=0 ; j<columns.length ; j++)		{			columns[j] = String.valueOf(rows[0].charAt(j));			for(int k=1 ; k<rows.length ; k++)			{				columns[j] = columns[j].concat(String.valueOf(rows[k].charAt(j)));			}		}		int counter = 0;		for(int i=0 ; i<rows.length ; i++)		{			if(rows[i].equals("BBBBBBBB"))				counter++;		}		if(counter != 8)		{			for(int i=0 ; i<columns.length ; i++)			{				if(columns[i].equals("BBBBBBBB"))					counter++;			}		}				System.out.println(counter);			} }
