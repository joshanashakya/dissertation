import java.io.*;import java.lang.*;import java.util.*; public class A1166 {    public static void main(String[] args) throws IOException{        StringBuffer ans = new StringBuffer();        StringTokenizer st;        BufferedReader f = new BufferedReader(new InputStreamReader(System.in));        st = new StringTokenizer(f.readLine());        int n = Integer.parseInt(st.nextToken());        HashMap<Integer, Integer> hashMap = new HashMap<>();        for(int i = 0; i < n; i++){            st = new StringTokenizer(f.readLine());            int r = st.nextToken().charAt(0);            if(!hashMap.containsKey(r))                hashMap.put(r,0);            hashMap.put(r, hashMap.get(r)+1);        }        f.close();        Object[] arr = hashMap.keySet().toArray();        long op = 0;        int[][] tr = new int[100][3];        tr[0][0] = 1;        for(int i = 1; i < 100; i++){            tr[i][0] = tr[i-1][0];            tr[i][1] = tr[i-1][0] + tr[i-1][1];            tr[i][2] = tr[i-1][1] + tr[i-1][2];        }        //System.out.println(Arrays.deepToString(tr));        for(int i = 0; i < arr.length; i++){            int a = hashMap.get(arr[i])/2;            int b = hashMap.get(arr[i])-a;            long choose = tr[a][2];            choose+=tr[b][2];            op+=choose;        }           System.out.println(op);    }    public static long factorial(long r){        if(r <= 1)            return 1;        return factorial(r-1)*r;    } }
