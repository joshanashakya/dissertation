vowels = ['a', 'e', 'i', 'o', 'u'] def factors(x):    lst = []    for i in range(5, x//5+1):       if x % i == 0:           lst.append([i, x//i])     return lst vowelsjoined = ''.join(vowels) n = int(input()) if n < 25 or len(factors(n)) == 0:    print(-1)    exit() cols = factors(n)[0][0]rows = factors(n)[0][1] res = "" for x in range(cols):    for y in range(rows):        res = res + vowels[(x + y) % 5] print(res) #stagger each aeiou sequence to get one in each row and column # coldict = {}## for colmod in range(rows):#     coldict[colmod] = []## for rb in range(rows):#     used = []#     ltbuilt = 0#     for x in range(cols):#         for v in vowels:#             if v not in used and v not in coldict[x]:#                 res += v#                 coldict[x].append(v)#                 used.append(v)#                 print(used, coldict[x])#                 break#         if sorted(used) == vowels:#             used = []#         if sorted(coldict[x]) == vowels:#             coldict[x] = []## print(res, len(res), rows, cols) 
