import java.util.Scanner; import static java.lang.Math.abs; public class PowerConsumptionCalculation {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         long input = scanner.nextLong();        if (input==0) {            System.out.println(0);            System.exit(0);        }        int count = 0;        int step = 1;        long sum = 0;         while(sum<abs(input) || (sum-abs(input))%2!=0){            sum+=step;            count++;            if (sum>=abs(input) && (sum-abs(input))%2==0){                System.out.println(count);            }             step++;        }     } } 	 		 		  				         	 		   	
