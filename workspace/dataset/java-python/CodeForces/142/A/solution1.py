#include<bits/stdc++.h>  using namespace std;#define FAST ios::sync_with_stdio(0); cin.tie(0); cout.tie(0); #define PB(x, v) x.push_back(v);#define M_P(a, b) make_pair(a, b)#define pll pair<ll, ll>#define ll long long int#define mll map<ll, ll>#define vl vector<ll>#define fa(x, v) for(auto x: v)#define fr(i, a, b, step) for(auto i= a;i <=b; i+=step)#define TEST  Int test; cin >> test; while(test--)#define FTEST Int TesT; cin >> TesT; for(Int test = 1; test<=TesT; test++)/**So..a**/#define B_ begin()#define E_ end()#define all(a) a.begin(), a.end()#define rall(a) a.rbegin(), a.rend()#define NL_ "\n"#define F first#define S second#define FF first.first#define FS first.second#define SF second.first#define SS second.second #define __lcm(a, b) (a/__gcd(a, b)*b)#define PI acos(-1)#define EXP 10e-6 template<typename item>item abs(item i){    if(i < 0) i= -i;    return i;}#define MMOODD 1000000009LLll mod= (1000000000+7);template<class typea>typea POW(typea b, typea p){    typea res = 1;    while(p)    {        if(p&1)        {            res*=b;            res%=mod;        }        b*=b;        b%=mod;          p/=2;    }    return res;}int  tc =0;   #define  ull unsigned long long #define pii pair<int, int> #define maxn 4*1000000+10  ll arr[1000+10];ll cnt(ll a, ll b, ll c){    return (a+1)*(b+2)*(c+2);}int main(){     FAST;    vector<ll> v;   ll n;   cin >> n;   for(ll i= 1; i*i<=n; i++)   {       if(n%i==0)       {           v.push_back(n/i);           if(n/i==i) continue;           v.push_back(i);       }   }    ll mx, mn;   mx = -1, mn = LLONG_MAX;   for(ll i= 0; i<v.size(); i++)   {       for(ll j= 0; j<v.size(); j++)       {           if((n)%(v[i]*v[j])==0)           {               ll temp = (n/(v[i]*v[j]));               vector<ll> bb;               bb.push_back(v[i]);               bb.push_back(v[j]);               bb.push_back(temp);               sort(all(bb));               do               {                   ll tem2 ;                   mx = max(mx, (tem2 = cnt(bb[0], bb[1], bb[2])));                   if(tem2- n>0) mn = min(mn, tem2);                   }while(next_permutation(all(bb)));           }       }   }    cout << mn- n<<" "<< mx- n;     return 0;}
