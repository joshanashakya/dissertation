from sys import stdin,stdoutnmbr=lambda:int(stdin.readline())lst=lambda:list(map(int, stdin.readline().split()))def fn(fin):    sm=0    for v in a:        if v>=fin:sm+=v-fin        else:return False    return sm>=reqfor _ in range(1):#nmbr()):    n,req=lst()    a=lst()    if sum(a)<req:        print(-1)        continue    l=0;r=10**12+1    while l<=r:        mid=(l+r)>>1        if fn(mid):l=mid+1        else:r=mid-1    print(r)
