import java.io.*;import java.util.*;import java.lang.*; public class Rextester{    public static void main(String[] args)throws IOException{        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st0 = new StringTokenizer(br.readLine());        StringTokenizer st = new StringTokenizer(br.readLine());        StringTokenizer st1 = new StringTokenizer(br.readLine());        br.close();        int n = new Integer(st0.nextToken());        int m = new Integer(st0.nextToken());        int[] f = new int[n+1];        int[] b = new int[m+1];        int[] finverse = new int[100005];        for(int i=0;i<m;i++){            b[i]=new Integer(st1.nextToken());        }        Arrays.fill(finverse,-2);        for(int i=0;i<n;i++){            f[i]=new Integer(st.nextToken());            if(finverse[f[i]]==-2){                finverse[f[i]]=i+1;            }            else{                finverse[f[i]]=-1;            }                  }        boolean flag = false;        StringBuffer sb = new StringBuffer();        for(int i=0;i<m;i++){            if(finverse[b[i]]==-1){                flag = true;            }            else if(finverse[b[i]]==-2){                System.out.println("Impossible");                return;            }            else{                sb.append(finverse[b[i]]).append(" ");            }        }        if(flag){            System.out.println("Ambiguity");            return;        }        else{            System.out.println("Possible\n"+sb);        }    }}        
