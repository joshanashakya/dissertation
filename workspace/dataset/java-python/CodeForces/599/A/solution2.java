import java.util.Scanner; public class A599 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int d1 = scanner.nextInt(), d2 = scanner.nextInt(), d3 = scanner.nextInt();         int a[] = {(2 * d1 + 2 * d2), (2 * d1 + 2 * d3), (2 * d2 + 2 * d3), (d1 + d2 + d3)};        int min = Integer.MAX_VALUE;        for (int i = 0; i < a.length; i++) {             if (a[i]<min) min = a[i];//            min = Math.min(min,a[i]);        }        System.out.println(min);    }}
