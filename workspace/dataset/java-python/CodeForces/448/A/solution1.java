import java.util.Scanner; public class A448 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int a1 = scanner.nextInt(), a2 = scanner.nextInt(), a3 = scanner.nextInt();        int b1 = scanner.nextInt(), b2 = scanner.nextInt(), b3 = scanner.nextInt();        int n = scanner.nextInt();        int sum1, sum2;        sum1 = a1 + a2 + a3;        sum2 = b1 + b2 + b3;        int count = 0;        if (sum1 % 5 == 0) {            count += sum1 / 5;        } else            count += sum1 / 5 + 1;                if (sum2 % 10 == 0) {            count += sum2 / 10;        } else            count += sum2 / 10 + 1;                if (count > n)            System.out.println("NO");        else            System.out.println("YES");    }}
