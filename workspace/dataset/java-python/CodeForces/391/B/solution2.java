/* * To change this license header, choose License Headers in Project Properties. * To change this template file, choose Tools | Templates * and open the template in the editor. */  import java.io.*;import java.util.*;import java.text.*;import java.math.*;import java.util.regex.*;/** * * @author fz */public class RockB {    Scanner in = new Scanner(System.in);     private RockB() throws IOException {        //in.nextLine();        String s = in.nextLine();        List<List<Integer>> dist = new ArrayList<>();        for(int i = 0; i < 26; i++)            dist.add(new ArrayList<>());        for(int i = 0; i < s.length(); i++) {            dist.get(s.charAt(i) - 'A').add(i);        }        int res = 0;        for(List<Integer> list : dist) {            int val = hp(list);            res = Math.max(res, val);//            out(val);        }        out(res);                                            }    private int hp(List<Integer> list) {        if(list.size() == 0)            return 0;        int[] dp = new int[list.size()];        dp[0] = 1;        for(int i = 1; i < dp.length; i++){            int val = 1;            for(int j = 0; j < i; j++) {                if((list.get(i) - list.get(j)) % 2 == 1) {                    val = Math.max(val, 1 + dp[j]);                }            }            dp[i] = val;        }        return dp[dp.length - 1];    }                    private int[] ns(int n) {        int[] a = new int[n];        for(int i = 0; i < n; i++) {            a[i] = in.nextInt();        }        return a;    }    private int[][] nmat(int m, int n) {//m rows and n cols        int[][] res = new int[m][n];        for(int i = 0; i < m; i++)            for(int j = 0; j < n; j++)                res[i][j] = in.nextInt();        return res;    }      private static void out(Object x) {        System.out.println(x);    }    public static void main(String[] args) throws IOException {        new RockB();    }}
