import java.io.InputStream;import java.io.InputStreamReader;import java.io.BufferedReader;import java.io.OutputStream;import java.io.PrintWriter;import java.io.IOException; /** * Built using CHelper plug-in * Actual solution is at the top * @author Tifuera */public class Main {	public static void main(String[] args) {		InputStream inputStream = System.in;		OutputStream outputStream = System.out;		InputReader in = new InputReader(inputStream);		PrintWriter out = new PrintWriter(outputStream);		TaskB solver = new TaskB();		solver.solve(1, in, out);		out.close();	}} class TaskB {    public void solve(int testNumber, InputReader in, PrintWriter out) {        String word = in.next();        int answer = 0;         for (int i = 0; i < word.length(); i++) {            answer = Math.max(answer, getAns(word, i));        }         out.println(answer);     }     private int getAns(String word, int start) {        char ch = word.charAt(start);        int res = 1;        while (start < word.length()) {            int nextIdx = start + 1;            while (nextIdx < word.length()) {                if (word.charAt(nextIdx) == ch && ((nextIdx - start) % 2 == 1)) {                    break;                }                nextIdx++;            }             if (nextIdx < word.length()) {                res++;            }            start = nextIdx;        }         return res;    } } class InputReader {    private BufferedReader reader;    private String[] currentArray;     public InputReader(InputStream inputStream) {        reader = new BufferedReader(new InputStreamReader(inputStream));    }      public String next() {        try {            currentArray = null;            return reader.readLine();        } catch (IOException e) {            throw new RuntimeException(e);        }    } } 
