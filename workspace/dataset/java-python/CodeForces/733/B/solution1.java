 import java.util.Scanner; public class Main {    public static void main(String[] args){        Scanner in=new Scanner(System.in);        int n=in.nextInt();        int[] left=new int[n];        int[] right=new int[n];        for(int i=0;i<n;i++){            left[i]=in.nextInt();            right[i]=in.nextInt();        }        int L=0,R=0;        for(int i=0;i<n;i++){            L+=left[i];            R+=right[i];        }            int min=Math.abs(L-R);            int minIndex=-1;            for(int i=0;i<n;i++){                int Lnum=L-left[i]+right[i];                int Rnum=R-right[i]+left[i];                if(Math.abs(Lnum-Rnum)>min){                    min=Math.abs(Lnum-Rnum);                    minIndex=i;                }            }            System.out.println(minIndex+1);        }}  	  					 	 	 	 				     						
