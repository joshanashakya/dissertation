import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;import javafx.util.Pair; public class Main{     public static void main(String[] args)    {         FastScanner input = new FastScanner();        int n = input.nextInt();        long L = 0, R = 0;        int left[] = new int[n];        int right[] = new int[n];        for (int i = 0; i < n; i++) {            left[i] = input.nextInt();            right[i] = input.nextInt();            L += left[i];            R += right[i];        }        long dif = Math.abs(L-R);        int ansIndex = -1;                for (int i = 0; i < n; i++) {            L -= left[i];            R -= right[i];            L += right[i];            R += left[i];                        if(dif<Math.abs(L-R))            {                dif=Math.abs(L-R);                ansIndex = i;            }                        L -= right[i];            R -= left[i];            L += left[i];            R += right[i];         }        if (dif == 0) {            System.out.println("0");        } else {            System.out.println(ansIndex+1);        }    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
