//lolimport static java.lang.Math.max;import static java.lang.Math.min;import static java.lang.Math.abs;import java.util.*;import java.io.*;import java.math.*;    public class B   {      public static void main(String hi[]) throws Exception      {         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));         StringTokenizer st = new StringTokenizer(infile.readLine());         int N = Integer.parseInt(st.nextToken());         int[] left = new int[N];         int[] right = new int[N];         for(int i=0; i < N; i++)         {            st = new StringTokenizer(infile.readLine());            left[i] = Integer.parseInt(st.nextToken());            right[i] = Integer.parseInt(st.nextToken());         }         int x = 0;         int y = 0;         for(int a: left)            x += a;         for(int a: right)            y += a;         int res = abs(x-y);         int dex = 0;         for(int i=0; i < N; i++)         {            x += right[i]-left[i];            y += left[i]-right[i];            if(res < abs(x-y))            {               res = abs(x-y);               dex = i+1;            }            x -= right[i]-left[i];            y -= left[i]-right[i];         }         System.out.println(dex);      }   }
