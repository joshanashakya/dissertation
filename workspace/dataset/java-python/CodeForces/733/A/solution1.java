import java.util.*; public class Solve {     static Scanner scan = new Scanner(System.in);     public static void solve(String s){         int lastIndex=0,count=0,val = 0;         for (int i = 0; i < s.length(); i++) {            if(s.charAt(i) == 'A' || s.charAt(i) == 'E' || s.charAt(i) == 'I' || s.charAt(i) == 'O' || s.charAt(i) == 'U' || s.charAt(i) == 'Y'){                val = (i+1) - lastIndex;                count = Math.max(count,val);                lastIndex = (i+1);            }        }        count = Math.max(count,s.length()-lastIndex+1);        System.out.println(count);    }     public static void main(String[] args) {         String s = scan.next();        solve(s);    }}
