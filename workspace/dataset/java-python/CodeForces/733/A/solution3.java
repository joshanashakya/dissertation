import java.util.*;import java.lang.*;import java.io.*; public class sample{    public static void main(String args[]){        Scanner sc = new Scanner(System.in);        String s = sc.next();        s += " ";        char[] arr = s.toCharArray();        int max = 1, prev = -1, index = -1;         for(int i = 0; i < arr.length; i++){            if(arr[i] == 'A' || arr[i] == ' ' || arr[i] == 'E' || arr[i] == 'I' || arr[i] == 'O' || arr[i] == 'U' || arr[i] == 'Y'){                index = i;                int diff = index-prev;                max = Math.max(max, diff);                prev = index;            }        }         System.out.println(max);    } }
