def jump(phrase: str) -> int:    previous, maximum, phrase = -1, -1, 'A' + phrase + 'A'    for index in range(len(phrase)):        if phrase[index] in set("AEIOUY"):            maximum = max(maximum, index - previous)            previous = index    return maximum  def read_input(filename: str = None) -> None:    from sys import stdin    stdin = open(filename) if filename else stdin    phrase = stdin.readline().strip()    print(jump(phrase))  def main():    read_input()  if __name__ == '__main__':    main()
