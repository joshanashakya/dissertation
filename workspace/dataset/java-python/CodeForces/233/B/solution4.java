import java.io.BufferedReader;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*; import javafx.util.Pair; public class Main{   public static long sumOfdigit(long n )   {       long sum = 0;       while(n!=0)       {           sum+=n%10;           n/=10;       }              return sum;          }     public static void main(String args[])    {        FastScanner input = new FastScanner();                       long n = input.nextLong();        for (long i = 1; i <=90; i++) {                      long d = (i*i)+(4*n);            long x1 = (long) Math.sqrt(d);                        if(x1*x1==d)            {                long x = (-i+x1)/2;                long sumof = sumOfdigit(x);                if(sumof==i)                {                    System.out.println(x);                    return;                }                 x = (-i-x1)/2;                sumof = sumOfdigit(x);                if(sumof==i)                {                    System.out.println(x);                    return;                }            }                                }        System.out.println("-1");     }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
