import java.io.*; public class Main {	static PrintWriter pr = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));	public static void main(String[] args) throws IOException {		int n = input.nextInt();		int []a = new int[n+1];		int []b = new int[n+1];		for (int i=1;i<=n;i++){			a[i] = input.nextInt();		}		int k = 0, flag = 0, l = 0;		for (int i=1;i<=n;i++){			if (a[i] != i){				l = i;				k = a[i]-i;				int m = a[i];				for (int j=l;j<=l+k;j++){					b[j] = m;					m -= 1;				}				flag = 1;				break;			}		}		if (flag == 0){			pr.println("0 0");			pr.flush();			return;		}else{			for (int i=l;i<=l+k;i++){				if (a[i] == b[i]){					continue;				}else{					pr.println("0 0");					pr.flush();					return;				}			}			for (int i=l+k+1;i<=n;i++){				if (a[i] == i){					continue;				}else{					pr.println("0 0");					pr.flush();					return;				}			}		}		pr.print(l + " " + a[l]);		pr.flush();	}		static class input{		static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		static StreamTokenizer in = new StreamTokenizer(br);		static String nextLine() throws IOException{			return br.readLine();		}		static int nextInt() throws IOException{			in.nextToken();			return (int)in.nval;		}		static long nextLong() throws IOException{			in.nextToken();			return (long)in.nval;		}		static double nextDouble() throws IOException{			in.nextToken();			return in.nval;		}	}}
