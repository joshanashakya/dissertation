/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc =new Scanner(System.in) ;		int n = sc.nextInt() ;		int arr[][] = new int[n][n] ;		for(int i = 0 ; i < n ; i++){			for(int j = 0 ;j < n ;j++)			arr[i][j] = sc.nextInt() ;		}		ArrayList<Integer> rows = new ArrayList<Integer>() ; 		ArrayList<Integer> cols = new ArrayList<Integer>() ; 		for(int i = 0 ; i < n ; i++){			int temp = 0 ;			for(int j = 0 ; j< n ;j++){				temp+=arr[i][j] ;			}			rows.add(temp) ;		}		for(int j = 0 ; j < n ; j++){			int temp = 0 ;			for(int i = 0 ; i< n ;i++){				temp+=arr[i][j] ;			}			cols.add(temp) ;		}		Collections.sort(rows);		Collections.sort(cols);		int count = 0 ;		for(Integer c : cols ){			for(Integer r : rows){				if(c > r)count++ ;			}		}		System.out.println(count);	}}
