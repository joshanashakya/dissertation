import java.util.Scanner; public class SummerCamp {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();         int currentLength = 0;        for (int i = 1; i < 1000; i++) {            int digitsToAdd = 0;            if (i < 10) {                digitsToAdd = 1;            } else if (i < 100)  {                digitsToAdd = 2;            }else {                digitsToAdd = 3;            }             if (currentLength + digitsToAdd < n){                currentLength += digitsToAdd;            }else {                int digitPos = n - currentLength;                String num = String.valueOf(i);                System.out.println(num.charAt(digitPos - 1));                return;             }        }    }} 
