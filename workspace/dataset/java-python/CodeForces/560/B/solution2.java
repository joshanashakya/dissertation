import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.Scanner; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main{    public static void main(String[] args)    {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        TaskB solver = new TaskB();        solver.solve(1, in, out);        out.close();    }     static class TaskB    {        public void solve(int testNumber, Scanner in, PrintWriter out)        {            int T = 1;            while (T-- > 0)            {                solveOne(in, out);            }        }         private void solveOne(Scanner in, PrintWriter out)        {            int A1 = in.nextInt(), A2 = in.nextInt();             int A = in.nextInt();            int B = in.nextInt();            int C = in.nextInt();            int D = in.nextInt();             int comb[][] = new int[8][2];             comb[0] = new int[]{Math.max(A, C), B + D};            comb[1] = new int[]{B + D, Math.max(A, C)};             comb[2] = new int[]{Math.max(A, D), C + B};            comb[3] = new int[]{C + B, Math.max(A, D)};             comb[4] = new int[]{Math.max(B, D), A + C};            comb[5] = new int[]{A + C, Math.max(B, D)};             comb[6] = new int[]{Math.max(B, C), A + D};            comb[7] = new int[]{A + D, Math.max(B, C)};             for (int i = 0; i < 8; i++)            {                if (comb[i][0] <= A1 && comb[i][1] <= A2)                {                    out.println("YES");                    return;                }            }            out.println("NO");        }     }} 
