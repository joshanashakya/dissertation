import sysa, b, w, x, c = map(int, input().split())if c <= a:    print(0)    sys.exit()d = c - aans = 0if b // x >= d:    print(d)    sys.exit()ans += (b // x + 1) d -= (b // x)b = w - (x - b % x)b0 = bg = 0det = 0while (1):    if b // x >= d:        print(ans + d)        sys.exit()    g += (b // x + 1)    det += (b // x)    ans += (b // x + 1)    d -= (b // x)    b = w - (x - b % x)    if b == b0:        break if d > det:    k = d // det    if (d % det == 0):        k -= 1    d -= det * k    ans += k * gwhile (1):    if b // x >= d:        print(ans + d)        sys.exit()    ans += (b // x + 1)    d -= (b // x)    b = w - (x - b % x)
