import java.util.Arrays;import java.util.Scanner; public class Main {    public static void main(String[] args) {             Scanner sc = new Scanner(System.in);            while(sc.hasNext()){                int n = sc.nextInt();                int s = sc.nextInt();                int[] arr = new int[n];                for (int i = 0; i < n; i++) {                    arr[i] = sc.nextInt();                }                Arrays.sort(arr);                int mid = n/2;                Long cost = 0L;                if (arr[mid]<s){                    cost += s-arr[mid];                    for (int i = mid+1; i < n; i++) {                        if (arr[i]<s){                            cost += s-arr[i];                        }                    }                }else if(arr[mid]>s){                    cost += arr[mid]-s;                    for (int i = mid-1; i >= 0; i--) {                        if (arr[i]>s){                            cost += arr[i]-s;                        }                    }                }else{                    cost = 0L;                }                 System.out.println(cost);             }        }     } 		 		 				    	 			 	  	 		  		
