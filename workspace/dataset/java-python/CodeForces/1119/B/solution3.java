import java.util.Scanner;import java.util.Arrays;import java.util.Vector;import java.util.Collections;   public class Problem_Solving1 {        public static void main(String[] args) {        // TODO code application logic here        Scanner input = new Scanner(System.in);        int n = input.nextInt();        long h = input.nextLong();        //Vector<Long> v = new Vector<>();        long[] arr = new long[n];        for (int i = 0; i < n ; i++) {            arr[i] = input.nextLong();                    }        int res = -1;        for (int k = 1; k < n+1; k++) {                        Vector<Long> v2 = new Vector<>();            for (int j = 0 ; j < k ; j ++  )            {              v2.add(arr[j]);                          }            long summ = 0 ;            Collections.sort(v2);            for (int j = v2.size() - 1; j >= 0; j-=2) {                summ += v2.get(j);            }            //System.out.println(summ);            if (summ <= h)                res = k ;            v2.clear();        }                System.out.println(res);                       }}
