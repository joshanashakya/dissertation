from sys import stdin n, k, p, x, y = map(int, stdin.buffer.readline().split())a = list(map(int, stdin.buffer.readline().split()))a.sort() Sum = 0id = kflag = Falsefor i in range(len(a)):    Sum += a[i]    if a[i] >= y and not flag:        flag = True         id = i if Sum + n - k > x or id > (n // 2):    print(-1)    exit() res = list()for i in range(min(n // 2 - id, n - k)):    res.append(1)    Sum += 1 for i in range(min(n // 2 - k + id + 1, n - k)):    res.append(y)    Sum += y if Sum > x:    print(-1)else:    print(*res)
