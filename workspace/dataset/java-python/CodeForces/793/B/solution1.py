n, m = map(int, input().split())mat = []ans = -1for i in range(n):    mat.append(input()) s, d = -1, -1for i in range(n):    for j in range(m):        if mat[i][j]=='S':            s = (i,j)        if mat[i][j]=='T':            d = (i,j) q = [(s[0], s[1], -1, -1)]vis = [0]*(n*m)pos = 0found = False tmp = [(0,1,1),(0,-1,1),(1,0,0),(-1,0,0)]for a,b,c in tmp:    x,y = d    while x<n and x>=0 and y<m and y>=0 and not mat[x][y] == '*':        vis[x*m+y]=-1        x+=a        y+=b # hori = 1 vert = 0while pos<len(q) and not found:    i,j,turn,dir = q[pos]    pos+= 1    if vis[i*m+j]==1:        continue    vis[i*m+j]=1    if turn>=1:        continue    for a,b,c in tmp:        if not dir==c:            t = 1            x,y = i+a*t, j+b*t             while x<n and x>=0 and y<m and y>=0 and not mat[x][y] == '*':                if vis[x*m+y]==-1:                    found=True                    ans=turn+1                    break                q.append((x, y, turn+1, c))                t+=1                x,y = i+a*t, j+b*t            if found:                break x,y = dprint("YES" if  ans>-1 and ans<3 else 'NO')
