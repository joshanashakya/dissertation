import java.util.Scanner; public class BuyingTorches {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int testCases = scanner.nextInt();         while (testCases-- != 0) {             long x = scanner.nextInt();            long y = scanner.nextInt();            long k = scanner.nextInt();             System.out.println(solve(x, y, k));        }    }     public static long solve(long x, long y, long k) {        x--;        long a = y * k + (k - 1) + (x - 1);        return a/x +k;    }}  
