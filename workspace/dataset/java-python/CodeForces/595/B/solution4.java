import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int n = in.nextInt();        int k = in.nextInt();        int a[] = new int[n/k];        int b[] = new int[n/k];                long result=1;                for (int i = 0; i < n/k; i++) {            a[i] = in.nextInt();        }        for (int i = 0; i < n/k; i++) {            b[i]= in.nextInt();        }                for (int i = 0; i < n/k; i++) {            long cnt = 0;                        if(b[i]==0){                cnt = (((long)Math.pow(10, k))-1)/a[i] - (((long)Math.pow(10, k-1))-1)/a[i];                            }            else{                                long temp= ( ((b[i]+1)*(long)Math.pow(10, k-1)-1)/a[i]  -  ((b[i])*(long)Math.pow(10, k-1)-1)/a[i]);                            cnt = ((long)Math.pow(10, k)-1)/a[i]-temp+1;            }            result *= cnt;            result%=1000000007;        }        System.out.println(result);    }}
