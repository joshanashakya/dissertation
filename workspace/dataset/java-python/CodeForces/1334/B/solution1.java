import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Fridge   {	public static void main(String[] args) throws IOException {		Scanner sc = new Scanner(System.in);		int T  =sc.nextInt();		while(T-->0)		{			int n  =sc.nextInt();			int x  =sc.nextInt();			int[] ar = new int[n];			List<Integer> grt = new ArrayList<Integer>();			List<Integer> sml = new ArrayList<Integer>();			long sum=0,cur=0;			for(int i=0;i<n;i++) {				ar[i]=sc.nextInt();				if(ar[i]>=x)grt.add(ar[i]);				else sml.add(ar[i]);				sum += ar[i];			}						Collections.sort(sml);			Collections.sort(grt);			int i=0;			int div=n;			while(i<n)			{				//System.out.println(sum+" "+div);				if(sum/(div) >= x)break;				else				{					if(sml.size()>0) {						sum -= sml.get(0);						div--;						sml.remove(0);					}					else if(grt.size()>0)					{						sum -= grt.get(0);						div--;						grt.remove(0);					}					else break;					i++;				}			}								System.out.println(Math.max(div, cur));		}	}}
