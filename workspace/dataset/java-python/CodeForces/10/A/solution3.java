import java.util.Scanner; public class PowerConsumptionCalculation {    static int ans=0;    public static void main(String[] args) {        int n=0;        int p1=0;        int p2=0;        int p3=0;        int t1=0;        int t2=0;        int ln=0;        int rn=0;        int lastrn=0;         Scanner scanner = new Scanner(System.in);        n = scanner.nextInt();        p1 = scanner.nextInt();        p2 = scanner.nextInt();        p3 = scanner.nextInt();        t1 = scanner.nextInt();        t2 = scanner.nextInt();          ln = scanner.nextInt();        rn = scanner.nextInt();        ans += (rn-ln)*p1;        n--;        lastrn = rn;        while(n>0){            ln = scanner.nextInt();            rn = scanner.nextInt();            ans += (rn-ln)*p1;            if ((ln-lastrn)<=t1){                ans += (ln-lastrn)*p1;            }else if ((ln-lastrn)>t1 && (ln-lastrn)<=t1+t2){                ans += t1*p1 + (ln-lastrn-t1)*p2;            }else{                ans += t1*p1 + t2*p2 + (ln-lastrn-t2-t1)*p3;            }            n--;            lastrn = rn;        }        System.out.println(ans);     }} 		 	      						 	     	 						
