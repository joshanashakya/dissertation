# cook your dish heren,p1,p2,p3,t1,t2=map(int,input().split())sum=0for i in range(n):    a=list(map(int,input().split()))    if i==0:        initial=a[0]    else:        pass    work_interval=a[1]-a[0]    sum+=work_interval*p1    interval=a[0]-initial    initial=a[1]    if interval>=t1:        sum+=(t1)*p1        interval-=t1        if interval>=t2:            sum+=(t2)*p2            interval-=t2            sum+=(interval)*p3        else:            sum+=interval*p2    else:        sum+=interval*p1print(sum)    
