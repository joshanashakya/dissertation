def solve(l):    o = l[0][1]    t = []    tt = [l[0][1] - l[0][0]]    for j in range(1, n):        t.append(l[j][0] - o)        o = l[j][1]        tt.append(l[j][1] - l[j][0])     Sum = 0    for k in range(len(t)):        if t[k] <= t1:            Sum += t[k] * p1        elif t[k] >t1 and t[k] <= t1 + t2:            Sum += t1 * p1            Sum += (t[k] - t1) * p2        else:            Sum += t1 * p1            Sum += t2 * p2            Sum += (t[k] - (t1 + t2)) *p3    Sum += sum(tt) * p1    print(Sum)  n, p1, p2, p3, t1, t2 = map(int, input().split())l = []for i in range(n):    e = list(map(int, input().split()))    l.append(e)  solve(l)
