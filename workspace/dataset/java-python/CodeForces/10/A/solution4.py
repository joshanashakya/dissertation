# p_ = power, t1 = time before activate screen saver, t2 = time before sleep, e_sum = energyn, p_wake, p_scrsaver, p_sleep, t1, t2 = map(int, input().split())e_sum = 0i = 0    start, end = map(int, input().split())e_sum += (end-start) * p_wakefor i in range(n-1):    start2, end2 = map(int, input().split())    e_sum += (end2 - start2) * p_wake    interval = start2 - end    if (interval >= t1):        if (interval >= t1 + t2):            e_sum += (interval - t2 - t1) * p_sleep + t2 * p_scrsaver + t1 * p_wake        else:            e_sum += (interval - t1) * p_scrsaver + t1 *p_wake    else:        e_sum += (interval) * p_wake    start, end = start2, end2print(e_sum)
