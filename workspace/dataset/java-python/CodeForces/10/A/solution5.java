import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);         String[] firstLine = in.nextLine().trim().split(" ");         int n = Integer.parseInt(firstLine[0]);        int normalWatt = Integer.parseInt(firstLine[1]);        int screenWatt = Integer.parseInt(firstLine[2]);        int sleepWatt = Integer.parseInt(firstLine[3]);        int screenTime = Integer.parseInt(firstLine[4]);        int sleepTime = Integer.parseInt(firstLine[5]);         int[] works = new int[n * 2];        int result = 0;        for (int i = 0; i < n; i++) {            works[i * 2] = in.nextInt();            works[i * 2 + 1] = in.nextInt();        }         for (int i = 1; i < works.length; i++) {            int gap = (works[i] - works[i - 1]);            result += gap * normalWatt;            if (i % 2 == 0) {                gap = gap - screenTime;                if (gap > 0) {                    result -= gap * (normalWatt - screenWatt);                }                                gap = gap - sleepTime;                if (gap > 0) {                    result -= gap * (screenWatt - sleepWatt);                }                            }        }         System.out.println(String.valueOf(result));                in.close();    }}
