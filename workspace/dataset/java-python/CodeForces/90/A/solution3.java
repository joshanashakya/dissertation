import java.util.Scanner; /** * Main */public class Main {     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         int r = sc.nextInt();        int g = sc.nextInt();        int b = sc.nextInt();         System.out.println(Math.max(calculateTime(r), Math.max(1 + calculateTime(g), 2 + calculateTime(b))));    }     private static int calculateTime(int x) {        if (x == 0) {            return 0;        }         return 30 + ((x - 1) / 2) * 3;    }}
