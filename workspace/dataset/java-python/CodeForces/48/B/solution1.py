def calc_trees(cont, start_row, start_col, a, b):    counter = 0    for i in range(start_row, start_row + a):        for j in range(start_col, start_col + b):            counter += cont[i][j]    return counter  n, m = list(map(int, input().split(" "))) cont = []for i in range(n):    cont.append(list(map(int, input().split(" ")))) a, b = list(map(int, input().split(" ")))ans, helper = n * m, 0for i in range(n):    for j in range(m):        if i + a <= n and j + b <= m:            helper = calc_trees(cont, i, j, a, b)            if helper < ans:                ans = helper        if i + b <= n and j + a <= m:            helper = calc_trees(cont, i, j, b, a)            if helper < ans:                ans = helperprint(ans)
