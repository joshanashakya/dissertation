study_days, sum_of_times = map(int, input(). split())minimals, maximals = [], []for _ in range(study_days):    least, most = map(int, input(). split())    minimals.append(least)    maximals.append(most)lowest_sum, highest_sum = sum(minimals), sum(maximals)if lowest_sum <= sum_of_times <= highest_sum:    print("YES")    difference_from_minimum = sum_of_times - lowest_sum    index = 0    while difference_from_minimum:        if minimals[index] < maximals[index]:            minimals[index] += 1            difference_from_minimum -= 1        else:            index += 1    print(" ".join(str(x) for x in minimals))else:    print("NO")
