import java.util.*; public class BeforeAnExam_B { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int d = sc.nextInt();		int sumTime = sc.nextInt();		int min[] = new int[d];		int max[] = new int[d];		int sumMin = 0, sumMax = 0;				for(int i = 0; i<d; i++) {			min[i] = sc.nextInt();			sumMin += min[i];						max[i] = sc.nextInt();			sumMax += max[i];		}				if(sumTime>=sumMin && sumTime<=sumMax) {			System.out.println("YES");			for(int i =0 ;i<d; i++) {				int temp =0;				for(int j = min[i]; j<=max[i]; j++) {					if(sumTime-j>=sumMin-min[i] && sumTime-j<=sumMax-max[i]) {						System.out.println(j);						sumMin = sumMin-min[i];						sumMax = sumMax-max[i];						sumTime = sumTime-j;						break;					}									}							}								}		else {			System.out.println("NO");		}		 	} }
