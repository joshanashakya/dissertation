import java.util.HashSet;import java.util.Scanner; public class Main {    public static void main(String[] args) {        int d, sum_time;        int min[] = new int[100];        int max[] = new int[100];        int total_max = 0;        int total_min = 0;        int[] ans = new int[100];        int sum = 0;        int sum_min = 0;        Scanner in = new Scanner(System.in);        d = in.nextInt();        sum_time = in.nextInt();        for (int i = 1; i <= d; i++) {            min[i] = in.nextInt();            max[i] = in.nextInt();            total_max += max[i];            total_min += min[i];        }        if(total_max < sum_time || total_min > sum_time) {            System.out.println("NO");        }        else {            System.out.println("YES");            sum_time -= total_min;            int total = 0;            for (int i = 1; i <= d; i++) {                max[i] -= min[i];                total += max[i];            }            total -= max[1];            for (int i = 1; i <= d; i++) {                //System.out.println("total: " + total + ", sum_time: " + sum_time + ", ");                if(sum_time < total) {                    System.out.print(min[i] + " ");                    total -= max[i+1];                }                else {                    System.out.print(min[i] + sum_time - total + " ");                    sum_time -= (sum_time - total);                    total -= max[i+1];                }            }         }     }}	    	  		  	 	    	    					
