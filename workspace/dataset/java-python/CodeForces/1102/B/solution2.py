from collections import defaultdictn,k=map(int,input().split())a=list(map(int, input().split()))d=defaultdict(int)for i in range(n):    d[a[i]]+=1    if d[a[i]]>k:        print('NO')        exit()ans=[-1 for i in range(n)]d=defaultdict(int)for j in range(n):    y=a[j]    d[y]+=1 t=[]for i in range(n):    t.append([d[a[i]],a[i],i])t.sort(key=lambda x:x[1],reverse=True)t.sort(key=lambda x:x[0],reverse=True)c=0for i in range(n):    c+=1    x=t[i][2]    ans[x]=c    if c==k:        c=0 print('YES')print(*ans)
