import java.util.*; public class Main {    public static void main(String [] args){        Scanner in = new Scanner(System.in);        int n;        int x1,y1,x2,y2;        while(in.hasNext())        {            int maxx = -1,minx = 40000,maxy = -1,miny = 40000;            n = in.nextInt();            long ans1,ans2 = 0;            for(int i = 0; i < n; ++i)            {                x1 = in.nextInt();                y1 = in.nextInt();                x2 = in.nextInt();                y2 = in.nextInt();                maxx = Math.max(x2,maxx);                minx = Math.min(x1,minx);                maxy = Math.max(y2,maxy);                miny = Math.min(y1,miny);                ans2 += Math.abs(x2 - x1) * Math.abs(y2 - y1);            }            ans1 = Math.abs(maxx - minx) * Math.abs(maxy - miny);            if(maxx - minx != maxy - miny || ans1 != ans2)                System.out.println("NO");            else                System.out.println("YES");        }        in.close();    }}
