def get(x):	low = 1	high = x	ans = -1	while low <= high:		mid = (low + high) // 2		if mid * mid <= x:			low = mid + 1			ans = mid		else:			high = mid - 1	return ans n = int(input())f = 0for i in range(0, 64):	a = 1	b = (2 ** (i + 1)) - 3	c = - 2 * n	d = b * b - 4 * a * c	sq = get(d)	if sq ** 2 == d:		x = - b + sq		if ((x & 1) == 0 and ((x // 2) & 1) and (x // 2) > 0):			f = 1;			ans = (x >> 1) * (1 << i);			print(ans)if f == 0:	print(-1)
