import java.util.*;import java.lang.*;import java.io.*;public class Codechef{    /* Name of the class has to be "Main" only if the class is public. */ 		// your code goes here			public static void main (String[] args) throws java.lang.Exception	{ BufferedReader br=new BufferedReader(new InputStreamReader(System.in)); int n=Integer.parseInt(br.readLine().trim()); StringBuilder sb=new StringBuilder();            //while(t-->0)            {                   int c=0,max=0;                   String[] s=br.readLine().trim().split("\\s+");                   for(int i=0;i<n;i++){                       if(i==0){                           c=1;                           max=1;                       }                       else {                           if(Integer.parseInt(s[i-1])<=Integer.parseInt(s[i]))                           {                               c++;                               max=Math.max(max,c);                           }                           else {                               max=Math.max(max,c);                               c=1;                           }                       }                   }                   sb.append(max);            }            System.out.println(sb);    	}}
