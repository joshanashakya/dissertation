import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; public class RestoringIPv6 {	public static void main(String[] args) throws NumberFormatException, IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		int n = Integer.parseInt(br.readLine());		for(int i = 0 ; i < n ; i++){			String[] s = {"0000", "0000", "0000", "0000", "0000", "0000", "0000", "0000"};			String curr = br.readLine();			if(curr.contains("::")){				String[] split = curr.split("::");				if(split.length == 0) for(int j = 0 ; j < s.length ; j++) System.out.print(s[j] + (j == s.length-1?"":":"));				else{					if(curr.substring(0, 2).equals("::")){						String[] whatsleft = split[1].split(":");						for(int j = s.length-1 , k = whatsleft.length-1; j >= s.length-whatsleft.length ; j--, k--){							s[j] = s[j].substring(0, 4-whatsleft[k].length()) + whatsleft[k];						}					}					else if(curr.substring(curr.length()-2, curr.length()).equals("::")){						String[] whatsleft = split[0].split(":");						for(int j = 0 ; j < whatsleft.length ; j++){							s[j] = s[j].substring(0, 4-whatsleft[j].length()) + whatsleft[j];						}					}					else{						String[] left = split[0].split(":"), right = split[1].split(":");						for(int j = 0 ; j < left.length ; j++){							s[j] = s[j].substring(0, 4-left[j].length()) + left[j];						}						for(int j = s.length-1, k = right.length-1 ; j >= s.length-right.length ; j--, k--){							s[j] = s[j].substring(0, 4-right[k].length())+right[k];						}					}					for(int j = 0 ; j < s.length ; j++) System.out.print(s[j] + (j == s.length-1?"":":"));				}			}			else{				String[] address = curr.split(":");				for(int j = 0 ; j < address.length ; j++){					s[j] = s[j].substring(0, 4-address[j].length()) + address[j];				}				for(int j = 0 ; j < s.length ; j++) System.out.print(s[j] + (j == s.length-1?"":":"));			}			System.out.println();		}	}}
