import java.util.Scanner; public class StickersAndToys {   public static void main(String[] args) {    Scanner xScanner = new Scanner(System.in);    int numQueries = xScanner.nextInt();     for(int i = 0; i < numQueries; i++) {      int numEggs = xScanner.nextInt();      int numStickers = xScanner.nextInt();      int numToys = xScanner.nextInt();      int numFewerObjects = Math.min(numStickers, numToys);      //-- Wir ziehen jedes Mal das andere Objekt      int numDrawTimesWorstCase = numEggs - numFewerObjects;      //-- Beim nächstem Ziehen müssen wir auch diese Objekt ziehen      System.out.println(numDrawTimesWorstCase + 1);    }  }}
