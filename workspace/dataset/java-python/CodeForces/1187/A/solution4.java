import java.util.*;import java.math.BigInteger;import java.io.*;public class A {	public static void main(String args[]) {		FastScanner in=new FastScanner();		int t=in.nextInt();		while(--t>=0) {			int n=in.nextInt();			int st=in.nextInt();			int to=in.nextInt();			int common=Math.max(st-(n-to), 0);			int ost=st-common;			int oto=to-common;			int ans=Math.max(n-(st+to), 0)+Math.max(ost, oto)+1;			System.out.println(ans);		}	} 	///////////////////////////	static void ruffleSort(int[] a) {		ArrayList<Integer> l=new ArrayList<>();		for (int i:a) l.add(i);		Collections.sort(l);		for (int i=0; i<a.length; i++) a[i]=l.get(i);	}		static class FastScanner {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(""); 		String next() {			while (!st.hasMoreTokens())				try {					st = new StringTokenizer(br.readLine());				} catch (IOException e) {					e.printStackTrace();				}			return st.nextToken();		}		int nextInt() {			return Integer.parseInt(next());		} 		int[] nextArray(int n) {			int[] a = new int[n];			for (int i = 0; i < n; i++)				a[i] = nextInt();			return a;		} 		long nextLong() {			return Long.parseLong(next());		}	}}
