import java.util.*;import java.lang.*;import java.io.*; public class Expedition {		public static void main (String[] args) throws java.lang.Exception	{	    Scanner sc = new Scanner(System.in);	    int n = sc.nextInt();	    List<Integer> pos = new ArrayList<Integer>();	    List<Integer> neg = new ArrayList<Integer>();	    int psum=0,nsum=0;	    for(int i=0;i<n;i++) {	    	int p = sc.nextInt();	    	if(p>=0) {	    		pos.add(p);	    		psum += p;	    	}	    	else {	    		neg.add(p);	    		nsum += p;	    	}	    }	    if(psum%2!=0)	    {	    	System.out.println(psum);	    }	    else if(psum%2==0 && psum>0)	    {	    	int posoddfound=Integer.MAX_VALUE,negoddfound=Integer.MIN_VALUE;	    	for(int k : pos)	    	{	    		if(k%2!=0)	    		{	    			posoddfound = Math.min(posoddfound, k); 	    		}	    	}	    	for(int k : neg)	    	{	    		if(k%2!=0)	    		{	    			negoddfound = Math.max(negoddfound, k); 	    		}	    	}	    	//System.out.println(posoddfound+" "+negoddfound+" "+negoddfound*-1);	    		    	if(negoddfound == Integer.MIN_VALUE || posoddfound < negoddfound*-1)	    	{	    		psum-=posoddfound;	    	}	    	else psum+=negoddfound;	    	System.out.println(psum);	    }	    else	    {	    	int negoddfound=Integer.MIN_VALUE;	    	for(int k : neg)	    	{	    		if(k%2!=0)	    		{	    			negoddfound = Math.max(negoddfound, k); 	    		}	    	}	    	//System.out.println(nsum +" "+negoddfound+" "+negoddfound*-1);	    	if(nsum!=negoddfound)nsum = negoddfound;	    	System.out.println(nsum);	    }	    	}}
