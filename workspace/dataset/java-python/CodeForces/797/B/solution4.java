/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Codechef{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		int temp = 0;		int post_odd = Integer.MAX_VALUE;		int negt_odd = Integer.MIN_VALUE;		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		int length = Integer.parseInt(br.readLine());		StringTokenizer st = new StringTokenizer(br.readLine());		int a[] = new int [length];		int count = 0;		while(st.hasMoreTokens()) {		    a[count] = Integer.parseInt(st.nextToken());		    count++;		}		for(int i = 0; i < a.length; i++) {		    if(a[i] >= 0)		        temp += a[i];		    if(a[i] % 2 == 1)		        post_odd = Math.min(a[i],post_odd);		    if(a[i] % 2 == -1)		        negt_odd = Math.max(a[i],negt_odd);		}		if(temp % 2 != 0)		    System.out.println(temp);		else		    System.out.println(Math.max(temp - post_odd,temp + negt_odd));	}}
