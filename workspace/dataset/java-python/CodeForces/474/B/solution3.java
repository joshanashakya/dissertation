import java.lang.*;import java.util.*; public class Abhishek {     public static void main(String[] args) throws Exception {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int[] a = new int[n];        for (int i = 0; i < n; i++) {            a[i] = sc.nextInt();        }        int[] psum = new int[n];        psum[0] = a[0];        for (int i = 1; i < n; i++) {            psum[i] = psum[i - 1] + a[i];        }        int m = sc.nextInt();//        int[] q =  new int[m];        for (int i = 0; i < m; i++) {            int q = sc.nextInt();            int l=0,r=n-1;            while (l<=r){                int mid = l+(r-l)/2;                if(q<=psum[mid]){                    if(mid!=0 ){                        if(q<=psum[mid-1]){                            r = mid-1;                        }else{                            System.out.println(mid+1);                            break;                        }                    }else{                        System.out.println(mid+1);                        break;                    }                }else{                    l = mid+1;                }            }         } //       System.out.println(Arrays.toString(psum));      } }
