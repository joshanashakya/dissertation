import java.util.*;import java.io.*; public class _contest { 	public static void main(String args[])	{ 		FastReader in=new FastReader();		PrintWriter so = new PrintWriter(new BufferedWriter(new  OutputStreamWriter(System.out)));		int n = in.nextInt();		int x[] = new int[n];		int y[] = new int[n];		int ue=0 , uo=0 , le=0 , lo=0 , su=0 , sl=0,odd=0,co=0;		for(int i=0 ; i<n ; i++)		{		    x[i]=in.nextInt();		    y[i]=in.nextInt();		    if(x[i]%2!=0&&y[i]%2==0) uo++;		    if(y[i]%2!=0&&x[i]%2==0)lo++;		    if(x[i]%2!=0&&y[i]%2!=0)		     {		    	co++;		    	odd+=2;		     }		    su+=x[i];		    sl+=y[i];		}			    if(su%2==0&&sl%2==0) so.println("0");		else if((lo+uo+odd)%2==0)			{			if(co%2==0) so.println("1");			else if(co%2!=0&&(uo>0||lo>0)) so.println("1");			else so.println("-1");			}		else so.println("-1");		so.flush();	} 	static class FastReader	{ 		BufferedReader br; 		StringTokenizer st;  		public FastReader() 		{ 			br = new BufferedReader(new InputStreamReader(System.in)); 		}  		String next() 		{ 			while (st == null || !st.hasMoreElements()) 			{ 				try				{ 					st = new StringTokenizer(br.readLine()); 				} 				catch (IOException  e) 				{ 					e.printStackTrace(); 				} 			} 			return st.nextToken(); 		}  		int nextInt() 		{ 			return Integer.parseInt(next()); 		}  		long nextLong() 		{ 			return Long.parseLong(next()); 		}  		double nextDouble() 		{ 			return Double.parseDouble(next()); 		}  		int[] readIntArray(int n)		{			int a[]=new int[n];			for(int i=0;i<n;i++)a[i]=nextInt();			return a;		} 		long[] readLongArray(int n)		{			long a[]=new long[n];			for(int i=0;i<n;i++)a[i]=nextLong();			return a;		} 		String nextLine() 		{ 			String str = ""; 			try			{ 				str = br.readLine(); 			} 			catch (IOException e) 			{ 				e.printStackTrace(); 			} 			return str; 		} 	} }
