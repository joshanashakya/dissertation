import java.io.*;import java.util.*; public class Spotlights{     public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        // int test = Integer.parseInt(br.readLine().trim());         // while(test-->0){            String ss[] = br.readLine().trim().split(" ");            int n = Integer.parseInt(ss[0]);            int m = Integer.parseInt(ss[1]);             int a[][] = new int[n][m];            int r[] = new int[n];            int c[] = new int[m];            int res=0;             for(int i=0;i<n;i++) {                int or=0;                String s[] = br.readLine().trim().split(" ");                for(int j=0;j<m;j++){                    a[i][j] = Integer.parseInt(s[j]);                    if(a[i][j]==1){                        c[j]++;                        or++;                    }                }                r[i] = or;            }             for(int i=0;i<n;i++){                int o=0;                for(int j=0;j<m;j++){                    if(a[i][j]==0){                        if(o>0){                            res++;                        }                        if(r[i]-o>0){                            res++;                        }                    }else{                        o++;                    }                }            }             for(int i=0;i<m;i++){                int o=0;                for(int j=0;j<n;j++){                    if(a[j][i]==0){                        if(o>0){                            res++;                        }                        if(c[i]-o > 0){                            res++;                        }                    }else{                        o++;                    }                }            }             System.out.println(res);        // }    }}
