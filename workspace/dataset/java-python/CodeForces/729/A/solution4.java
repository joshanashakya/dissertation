import java.util.*; public class P729A_Interview_with_Oleg {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int size = sc.nextInt();        sc.nextLine();        String input = sc.nextLine();        System.out.println(solve(input));    }     public static String solve(String input) {        boolean flag = true;        while(flag == true) {            int index = input.indexOf("ogo");            if(index != -1) {                input = manipulate(input, index);            }            else{                flag = false;            }        }        return input;    }     public static String manipulate(String input, int index) {        int copy = index;        boolean flag = true;        boolean goOn = true;        int last = -1;        for(int i = index + 3; i < input.length() && goOn; i++) {            if(flag) {                //check for g                char ch = input.charAt(i);                if(ch == 'g') {                    if(i+1 != input.length()){                        ch = input.charAt(i+1);                        if(ch == 'o') {                            i++;                            continue;                        }                    }                    goOn = false;                    last = i;                    continue;                }                else{                  goOn = false;                  last = i;                  continue;                }            }        }        if(last == -1) {            last = input.length();        }        return substr(input, copy, last);    }     public static String substr(String input, int start, int end) {        String beginning = input.substring(0, start);        String ending = "";        if(end != input.length())        ending = input.substring(end, input.length());        String result = beginning + "***" + ending;        return result;    }}
