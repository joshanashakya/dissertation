import java.io.*;import java.util.*; public class Main {    static int N = 2 * (int) 1e5;    static int[] temp;    static int[] prefixSum;     public static void main(String[] args) throws IOException {        Scanner in = new Scanner(System.in);        PrintWriter pw = new PrintWriter(System.out);        temp = new int[N + 5];        int n = in.nextInt(), k = in.nextInt(), m = in.nextInt();        for (int i = 0; i < n; i++) {            int a = in.nextInt(), b = in.nextInt();            temp[a]++;            temp[b + 1]--;        }        for (int i = 1; i <= N; i++) {            temp[i] += temp[i - 1];        }        prefixSum = new int[N + 5];        for (int i = 0; i <= N; i++) {            if (temp[i] >= k) prefixSum[i] = 1;        }        for (int i = 1; i <= N; i++) {            prefixSum[i] += prefixSum[i - 1];        }     //   debug(prefixSum);        for (int i = 0; i < m; i++) {            int l = in.nextInt() - 1, r = in.nextInt();            pw.println(prefixSum[r] - prefixSum[l]);        }        pw.close();    }     static void debug(Object... obj) {        System.err.println(Arrays.deepToString(obj));    }}
