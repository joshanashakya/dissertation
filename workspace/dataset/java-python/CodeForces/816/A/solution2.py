from datetime import timedelta as time x , y = map(int,input().split(':'))base = time(hours=x , minutes=y) start = basemm = time(minutes=1)# base += mm if x==23 and y==59 :    print(1) else:    x = False    while 1:         if base == time(hours=23 , minutes=59):            x = True            break           t = str(base)        t = t[:-3]        t = t.rjust(5 , '0')        if t == t[::-1]: break        base += mm      diff = list(map(int , str(base -start).split(':')))    diff = (diff[0] * 60 + diff[1])     if x: diff+=1    print(diff)
