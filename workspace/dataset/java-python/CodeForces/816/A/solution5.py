HOUR = 24MIN = 60  def pal_time(hour: int, minute: int) -> bool:    if not (0 <= hour < HOUR and 0 <= minute < MIN):        raise ValueError(f"the time {hrs}:{mins} is out of bounds")    clock = str(hour).zfill(len(str(HOUR))) + str(minute).zfill(len(str(MIN)))    return clock == clock[::-1]  hrs, mins = [int(i) for i in input().split(':')]if not (0 <= hrs < HOUR and 0 <= mins < MIN):    raise ValueError(f"the time {hrs}:{mins} is out of bounds") time = 0while not pal_time(hrs, mins):    mins += 1    time += 1    if mins == MIN:        mins = 0        hrs += 1        if hrs == HOUR:            hrs = 0 print(time)
