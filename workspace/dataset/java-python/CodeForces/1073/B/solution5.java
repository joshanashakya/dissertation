import java.util.HashMap;import java.util.Scanner; public class VasyaAndBooks2 {    public static void main(String[] args) {         Scanner entrada = new Scanner(System.in);        String n; //declarando        n = entrada.nextLine(); //primeiro input        StringBuilder ans = new StringBuilder(); // ultimo output        String[] pilha, ordem; //declarando        pilha = entrada.nextLine().split(" ");//entradalista        ordem = entrada.nextLine().split(" ");//entradalista        Integer[] indic = new Integer[pilha.length];//listadeindices        Integer limite = 0;//indice limite        HashMap<String,Integer> posicoes = new HashMap<>();         for(int indx = 0; indx<pilha.length; indx++) {            posicoes.put(pilha[indx],indx);         }        for(int indx = 0; indx< pilha.length; indx++){            indic[indx] = posicoes.get(ordem[indx]) + 1;        }         Boolean has = false;//para impressao certa sem espaço vazio no inicio do output ans        for(int indx = 0; indx< pilha.length; indx++){            if(indic[indx] > limite){                if(has == false){                    has = true;                    ans.append(indic[indx]-limite);                }                else{                    ans.append(" ").append(indic[indx] - limite);                }                limite = indic[indx];            }            else{                ans.append(" 0");            }        }        System.out.println(ans.toString());    }}  	  	 		   		 				   				 		
