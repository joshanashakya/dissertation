import java.io.IOException;import java.util.*;import java.util.Arrays;import java.util.Scanner; public class Bit_year{		public static void main(String[] args) throws IOException {				Scanner sc = new Scanner(System.in);		long n = sc.nextLong();		long m = sc.nextLong();				String s1 = Long.toBinaryString(n);		String s2 = Long.toBinaryString(m);		int diff = s2.length()-s1.length(); 		int ans=0;		while(diff>=0)		{			char[] a1 = new char[s1.length()+diff];			Arrays.fill(a1, '1');			for(int i=a1.length-1;i>0;i--)			{				a1[i]='0';				if(a1.length>16)				{					long k = Long.parseLong(new String(a1),2);					//System.out.println(k+" "+new String(a1));					if(k>=n && k<=m)ans++;				}				else				{					int  k = Integer.parseInt(new String(a1),2);					if(k>=n && k<=m) {//System.out.println(k+" "+new String(a1));						ans++;					}				}				a1[i]='1';			}			diff--;		}		System.out.println(ans);				}}
