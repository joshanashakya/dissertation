import java.util.Collections;import java.util.*;import java.lang.*;import java.math.BigInteger;import java.io.*;import java.io.*;import java.util.*; import java.util.Arrays;public class CodeChef { 	public static void main(String args[]) throws IOException { 		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringBuilder sb=new StringBuilder();                 				String[] sr=br.readLine().split("");		String[] pr=br.readLine().split("");		Integer[] arr=new Integer[sr.length];		Integer[] arr1=new Integer[pr.length];				for(int i=0;i<sr.length;i++)		arr[i]=Integer.parseInt(sr[i]);		for(int i=0;i<pr.length;i++)		arr1[i]=Integer.parseInt(pr[i]);				 Arrays.sort(arr1,Collections.reverseOrder()); 		int l=0,m=0;				for(;l<sr.length;l++)		{			for(;m<pr.length;m++)			{				if(arr1[m]>arr[l])					{arr[l]=arr1[m];m++;break;}				else {					break;}			}		}		for(int i=0;i<sr.length;i++)			System.out.print(arr[i]);						                                	}                                     	}
