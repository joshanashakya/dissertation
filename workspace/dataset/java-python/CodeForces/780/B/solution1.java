n = int(input())k = list(map(int, input().split()))s = list(map(int, input().split()))  def t_s(j, u):    while (u - j) > 1e-6:        f = (u - j) / 3        x2 = t_p(j + f)        x3 = t_p(u - f)        if x2 < x3:            u = j + 2 * f        else:            j = j + f    return t_p((j + u) / 2)  def t_p(p):    return max(abs(p - k[i]) / s[i] for i in range(n))  r = 0t = 1e10for p in k:    r = max(p, r)    t = min(p, t)print(t_s(t, r))   		  			   	 	 			  	    	
