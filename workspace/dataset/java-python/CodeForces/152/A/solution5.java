import java.util.Scanner; public class Experiment {	static Scanner sc = new Scanner(System.in); 	public static void main(String args[]) {		int n = sc.nextInt();		int m = sc.nextInt();		String[] arr = new String[n];		int maxi[] = new int[m];		for (int i = 0; i < n; i++) {			arr[i] = sc.next();			for (int j = 0; j < m; j++) {				maxi[j] = Math.max(maxi[j], arr[i].charAt(j));			}		}		int res = 0;		for (int i = 0; i < n; i++) {			for (int j = 0; j < m; j++) {				if (maxi[j] == arr[i].charAt(j)) {					res++;					break;				}			}		}		System.out.println(res); 	}}
