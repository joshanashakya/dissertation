import java.io.*;import java.util.*; public class MAIN{     private static BufferedReader reader = null;    private static BufferedWriter writer = null;     public static void main(String[] args) throws Exception{         // initialize input handlers         // file        // reader = new BufferedReader(new InputStreamReader(new FileInputStream("input.txt")));        // writer = new BufferedWriter(new OutputStreamWriter(new FileOutputStream("output.txt")));        // stdin/stdout        reader = new BufferedReader(new InputStreamReader(System.in));         // main logic        String[] s = reader.readLine().split(" ");        int n = Integer.parseInt(s[0]);         int[] a = new int[n];        s = reader.readLine().split(" ");        for(int i=0;i<n;++i){            a[i] = Integer.parseInt(s[i]);        }         int[] towers = new int[1001];        for(int i=0;i<towers.length;++i){            towers[i] = 0;        }         for(int i=0;i<a.length;++i){            ++towers[a[i]];        }         int max = Integer.MIN_VALUE;        int count = 0;        for(int i=0;i<towers.length;++i){            if(towers[i] > 0){                ++count;                max = (towers[i] >= max) ? towers[i] : max;            }        }         //output        System.out.println(max + " "+ count);        //writer.write("");        //writer.flush();     }     private static class Pair{        int first;        int second;        public Pair(int first, int second) {            this.first = first;            this.second = second;        }        public Pair getInversePair(){            return new Pair(                    this.second,                    this.first            );        }        public boolean equals(Pair p){            return p.first == this.first && p.second == this.second;        }    }     private static List<List<Integer>> getCombinations(int[] a, int count){        List<List<Integer>> ret = new ArrayList<>(0);        if(a == null || a.length > 64 || count <= 0 || count > a.length){            return null;        } else {            long lim = (1L<<a.length);            for(long i=0;i<lim;++i){                List<Integer> poss = getBitPositionList(i);                if(poss.size() == count){                    List<Integer> list = new ArrayList<>(0);                    for(int j=0;j< poss.size();++j){                        list.add(a[poss.get(j)]);                    }                    ret.add(list);                }            }        }        return ret;     }     private static int getBitCount(long n){        int onCount = 0;        for(long i=0L;i<64L;++i){            if((n&(1L<<i)) != 0){                ++onCount;            }        }        return onCount;    }    private static List<Integer> getBitPositionList(long n){        List<Integer> idxs = new ArrayList<>(0);        for(long i=0L;i<64L;++i){            if((n&(1L<<i)) != 0){                idxs.add(((int) i));            }        }        return idxs;    }    private static Pair getIrreducibleFraction(int x, int y){        int min = (x <= y) ? x : y;        for(int i=2; i<=min; ++i){            while (x%i==0 && y%i==0){                x = x/i;                y = y/i;            }        }        return new Pair(x,y);    }     private static int getDigitSumInBase(int n, int b){        int sum = 0;        while (true){            sum += n%b;            n = n/b;            if(n==0){                break;            }        }        return sum;    }     private static List<Integer> getSegmentedPrimes(int start, int end){         List<Integer> ret = new ArrayList<>(0);        List<Integer> primes = getPrimes(((int) Math.ceil(Math.sqrt(end))));         boolean[] flags = new boolean[(end-start+1)+1];        for(int i=0; i< flags.length; ++i){            flags[i] = true;        }        for(int i=0;i< primes.size();++i){            int lLim = (start / primes.get(i)) * primes.get(i);            while (lLim< start) {                lLim += primes.get(i);            }            for (int j = lLim; j <= end; j += primes.get(i)){                flags[j- start] = false;            }        }        for(int i = start; i<= end; ++i){            if(flags[i- start]){                ret.add(i);            }        }         return ret;     }    private static List<Integer> getPrimes(int n){         List<Integer> primes = new ArrayList<>(0);         boolean[] flags = new boolean[n+1];        for(int i=0; i<flags.length; ++i){            flags[i] = true;        }        flags[0] = false;        flags[1] = false;         for(int i=2;i*i<=n;++i){            if(flags[i]){                for(int j = i*i; j <= n; j += i){                    flags[j] = false;                }            }        }         for(int i=0, len = n+1; i < len; ++i){            if(flags[i]){                primes.add(i);            }        }         return primes;    }    private static boolean isPrime(int n){        if(n<=1){            return false;        } else if(n==2){            return true;        } else {            for(int i=2;i<n;++i){                if(n%i == 0){                    return false;                }            }        }        return true;    }     private static void displayArray(int[] A, String header){        System.out.println(header);        for (int i=0;i<A.length; ++i){            System.out.println(A[i]);        }    }    private static void displayList(List<Integer> L, String header){        System.out.println(header);        for (int i=0;i<L.size(); ++i){            System.out.println(L.get(i));        }    }     private static void swap(boolean[] arr, int i1, int i2){        boolean tmp = arr[i1];        arr[i1] = arr[i2];        arr[i2] = tmp;    } }
