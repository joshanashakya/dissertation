import java.util.Scanner; public class MaximumInTable {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();         int[][] arr = new int[n][n];          for (int i = 0; i < n; i++) {            arr[0][i] = 1;        }         for (int i = 0; i < n; i++) {            arr[i][0] = 1;        }         int res = calculateValue(n - 1, n - 1, arr);        System.out.println(res);     }     private static int calculateValue(int i, int j, int[][] arr) {        if (arr[i][j] != 0) {            return arr[i][j];        }         int result =  calculateValue(i - 1, j, arr) + calculateValue(i, j - 1, arr);         arr[i][j] = result;        return result;    }}
