import sysinput = sys.stdin.readline ''' ''' num_usb, num_ps, num_both = map(int, input().split())m = int(input()) usb = []ps = [] for _ in range(m):    c, con = input().rstrip().split()    c = int(c)    if con == "USB":        usb.append(c)    else:        ps.append(c) usb.sort(reverse=True)ps.sort(reverse=True) cost = 0count = 0 while num_usb and usb:    num_usb -= 1    cost += usb.pop()    count += 1 while num_ps and ps:    num_ps -= 1    cost += ps.pop()    count += 1 usb.extend(ps)both = usbboth.sort(reverse=True) while num_both and usb:    num_both -= 1    cost += both.pop()    count += 1 print(count, cost) 
