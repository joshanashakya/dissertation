import java.util.*;public class USBvsPS1400 { 	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int a = sc.nextInt();		int b = sc.nextInt();		int c = sc.nextInt();		int m = sc.nextInt();		PriorityQueue<Long> mUSB = new PriorityQueue<Long>();		PriorityQueue<Long> mPS = new PriorityQueue<Long>();		for (int i = 0;i<m;i++) {			long mouse = sc.nextInt();			String t = sc.next();			if (t.contentEquals("USB")) {				mUSB.add(mouse);			}else {				mPS.add(mouse);			}		}		long price = 0;		int i = 0;		for (;i<a&&!mUSB.isEmpty();i++) {			price += mUSB.poll();		}		int j = 0;		for (;j<b&&!mPS.isEmpty();j++) {			price += mPS.poll();		}		int k = 0;		for (;k<c;k++) {			if (!mUSB.isEmpty()&&!mPS.isEmpty()) {				if (mUSB.peek()<=mPS.peek()) {					price += mUSB.poll();				}else {					price += mPS.poll();				}			}else if (!mUSB.isEmpty()) {				price += mUSB.poll();			}else if (!mPS.isEmpty()) {				price += mPS.poll();			}else {				break;			}		}		System.out.println((i+j+k)+" "+price); 	} }
