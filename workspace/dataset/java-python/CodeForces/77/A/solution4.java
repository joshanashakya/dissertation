#include <stdio.h> #include <string.h> #include <math.h> #include <stdlib.h> /*int c(const void *a, const void *b) {	return *(int *)a - *(int *)b;}*/#include <stdio.h>  #include <string.h>  #include <math.h>  #include <stdlib.h>  /*int c(const void *a, const void *b) {         return *(int *)a - *(int *)b; }*/  int min(int a, int b) {         return (a < b) ? a : b; }  int max(int a, int b) {         return (b < a) ? a : b; }  int n, a, b, c, k[100][3], kCnt = 0, r = -1, arr[10][10], maxHappy = 0; int gr[3][10], s[3];  void rec(int warrior, int grouping) {         for (int i = 0; i < 3; i++) {                 if (s[i] == k[grouping][i])                         continue;                 gr[i][s[i]++] = warrior;                 if (warrior == 7) {                         int happy = 0;                         for (int g = 0; g < 3; g++)                                 for (int x = 0; x < s[g]; x++)                                         for (int y = 0; y < s[g]; y++)                                                  happy += arr[gr[g][x]][gr[g][y]];                         if (happy > maxHappy)                                 maxHappy = happy;                 } else {                         rec(warrior + 1, grouping);                 }                 s[i]--;         } }  int main() {          //freopen("input.txt", "r", stdin);          //freopen("output.txt", "w", stdout);          scanf ("%d", &n);         for (int i = 0; i < n; i++) {                 char s1[20], s2[20], s3[20];                 scanf("%s%s%s", s1, s2, s3);                 int i1, i2;                 if (!strcmp(s1, "Anka"))                         i1 = 1;                 if (!strcmp(s1, "Chapay"))                         i1 = 2;                 if (!strcmp(s1, "Cleo"))                         i1 = 3;                 if (!strcmp(s1, "Troll"))                         i1 = 4;                 if (!strcmp(s1, "Dracul"))                         i1 = 5;                 if (!strcmp(s1, "Snowy"))                         i1 = 6;                 if (!strcmp(s1, "Hexadecimal"))                         i1 = 7;                  if (!strcmp(s3, "Anka"))                         i2 = 1;                 if (!strcmp(s3, "Chapay"))                         i2 = 2;                 if (!strcmp(s3, "Cleo"))                         i2 = 3;                 if (!strcmp(s3, "Troll"))                         i2 = 4;                 if (!strcmp(s3, "Dracul"))                         i2 = 5;                 if (!strcmp(s3, "Snowy"))                         i2 = 6;                 if (!strcmp(s3, "Hexadecimal"))                         i2 = 7;                  arr[i1][i2] = 1;          }         scanf("%d%d%d", &a, &b, &c);         for (int i = 1; i <= 5; i++) {                 for (int j = 1; 7 - i - j >= 1; j++) {                         int pa = a / i, pb = b / j, pc = c / (7 - i - j);                         int _min = min(pa, min(pb, pc)), _max = max(pa, max(pb, pc));                         int rr = _max - _min;                         if (rr < r || r < 0) {                                 r = rr; 								kCnt = 0;                                k[kCnt][0] = i;                                 k[kCnt][1] = j;                                 k[kCnt][2] = 7 - i - j;                         } else if (rr == r) {							kCnt++;							k[kCnt][0] = i;                             k[kCnt][1] = j;                             k[kCnt][2] = 7 - i - j; 						}                }         } 		for (int i = 0; i <= kCnt; i++)			rec(1, i);         printf("%d %d", r, maxHappy);         return 0;  }
