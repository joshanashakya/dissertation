#include<iostream>#include<cstdio>#include<cmath> using namespace std; typedef long long ll; const int maxn=300005; //p-4q>=0 int main(){	int t;	scanf("%d",&t);	double a,b;	while(t--)	{		scanf("%lf%lf",&a,&b);		if(b==0)		{			puts("1.000000000");			continue;		}		double tmp;		if(b*4>=a) tmp=(a/(b*8));		else tmp=(a-b*2)/a;		tmp*=0.5;		tmp+=0.5;		printf("%.10lf\n",tmp);			}	return 0;}  	 	  		    	   	 	     					 	
