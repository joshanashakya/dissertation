import java.io.IOException;import java.util.*;public class Punctuation { 	public static void main(String[] args) throws IOException{		// TODO Auto-generated method stub		Scanner sc  = new Scanner (System.in);				String str = sc.nextLine();		String ans = "";		Stack<Character> s = new Stack<Character>();				for (int i = str.length() - 1 ; i  >= 0 ; i--) {			if (str.charAt(i) == ','||str.charAt(i) == '.'||str.charAt(i) == '?'||str.charAt(i) == '!') {				if (s.peek() == ' ')					s.push(str.charAt(i));				else {					s.push(' ');					s.push(str.charAt(i));				}				continue;			}			if (str.charAt(i) == ' ') {				if (s.peek() == ',' || s.peek() =='.' || s.peek() == '?' || s.peek() == '!'||s.peek() == ' ')					continue;			}			s.push(str.charAt(i));		}				while (!s.isEmpty())			ans += s.pop();				System.out.println(ans);//		System.out.println("galileo galilei was an italian physicist, mathematician, astronomer");			} }
