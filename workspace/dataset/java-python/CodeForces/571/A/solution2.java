import java.io.*;import java.util.StringTokenizer; public class Main {    public void solve() {        int a[] = new int[3];        for (int i = 0; i < 3; i++) {            a[i] = ni();        }        long l = ni();         long total = (l + 1) * (l + 2) * (l + 3) / 6;//        System.out.println("total: " + total);        for (int i = 0; i < 3; i++) {            int x = a[i % 3];            int y = a[(i + 1) % 3];            int z = a[(i + 2) % 3];             for (int j = 0; j <= l; j++) {                int cur = x + j;                if (y + z > cur) {                    continue;                }                long s = Math.min(l - j, cur - y - z);//                System.out.println("sub: " + (s + 1) * (s + 2) / 2);                total -= (s + 1) * (s + 2) / 2;            }        }        write(total + "\n");    }         public static void main(String[] args){        Main m = new Main();        m.solve();        try {            m.writer.close();        } catch (IOException e) {};    }    BufferedReader reader;    BufferedWriter writer;    StringTokenizer tokenizer;    public Main() {        reader = new BufferedReader(new InputStreamReader(System.in));        writer = new BufferedWriter(new OutputStreamWriter(System.out));    }    int ni() {        return Integer.parseInt(n());    }    String n() {        if (tokenizer == null || !tokenizer.hasMoreTokens()) {            try {                tokenizer = new StringTokenizer(reader.readLine());            } catch (IOException e) {}        }        return tokenizer.nextToken();    }    void write(String s) {        try {            writer.write(s);        } catch(IOException e) {};    }}
