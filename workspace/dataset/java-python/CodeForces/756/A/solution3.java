import java.util.Scanner; public class Main {    static Scanner scanner = new Scanner(System.in);    static boolean mark[];    static int a[];    static int n;    public static void main(String[] args) {        n = scanner.nextInt();        a=new int[n];        int b[]=new int[n];        mark=new boolean[n];        for (int i=0;i<n;i++){            a[i]=scanner.nextInt()-1;        }        int sum=0;        int ans=0;        for (int i=0;i<n;i++){            b[i]=scanner.nextInt();            sum+=b[i];        }        if (sum%2==0){            ans=1;        }        int low=sDFS();        if (low==1){            System.out.println(ans);            return;        }        System.out.println(low+ans);    }    private static int sDFS(){        int sum=0;        for (int i=0;i<n;i++){            if (!mark[i]){                sum++;                DFS(i);            }        }        return sum;    }    private static void DFS(int x){        mark[x]=true;        if (!mark[a[x]]){            DFS(a[x]);        }    }}
