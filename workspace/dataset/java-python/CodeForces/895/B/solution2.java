import java.util.Arrays;import java.util.Scanner; public class XK_Segment {		public static int lowerBound(long arr[], long key)	{		int left=0;		int right=arr.length;		int mid;				while(left<right)		{			mid=(left+right)/2;			if(key<=arr[mid])			{				right=mid;			}			else			{				left=mid+1;			}		}		return left;	}		public static int upperBound(long arr[], long key)	{		int left=0;		int right=arr.length;		int mid;				while(left<right)		{			mid=(left+right)/2;			if(key>=arr[mid])			{				left=mid+1;			}			else			{				right=mid;			}		}		return left;	} 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner in=new Scanner(System.in);		int n=in.nextInt();		long x=in.nextLong();		long k=in.nextLong();		long[] arr=new long[n];				for(int i=0;i<n;i++)		{			arr[i]=in.nextLong();		}				Arrays.sort(arr);		long temp;		long res=0;		long left;		long right;				for(int i=0;i<n;i++)		{			temp=(arr[i]-1)/x+k;			left=temp*x;			left=Math.max(arr[i], left);			right=temp*x+x-1;			res=res+upperBound(arr,right)-lowerBound(arr,left);		}				System.out.println(res); 	} }
