import java.util.Scanner; public class Main {     static int n, sol, auxs;    static int pizza[];     public static void main(String args[]) {        Scanner l = new Scanner(System.in);        n = l.nextInt();        pizza = new int[n];        for (int i = 0; i < n; i++) {            pizza[i] = l.nextInt();        }        sol = 360;        for (int i = 0; i < n; i++) {            auxs = 0;            for (int k = 0; i + k <= n; k++) {                sol = Math.min(sol, Math.abs(360 - auxs * 2));                if (i + k < n) {                    auxs += pizza[i + k];                }            }        }        System.out.println(sol);    }}
