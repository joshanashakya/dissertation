import java.io.*;import java.util.*; public class pizzaseparate {	public static void main (String [] args) throws IOException{		BufferedReader f = new BufferedReader(new InputStreamReader(System.in));		PrintWriter out = new PrintWriter(System.out);		StringTokenizer st = new StringTokenizer(f.readLine());		int n = Integer.parseInt((st.nextToken()));		int ans = 360;		int [] slices = new int[360];		st = new StringTokenizer(f.readLine());		int total = 360;		for (int i = 0; i < n; i++) {			slices[i] = Integer.parseInt((st.nextToken()));		}		for (int i = 0; i < n; i++) {			int currentDiff = 0;			for (int j = i; j < n; j++) {				currentDiff += slices[j];				ans = Math.min(ans, Math.abs((total - currentDiff) - currentDiff));			}		}		out.println(ans);		out.close();	}}
