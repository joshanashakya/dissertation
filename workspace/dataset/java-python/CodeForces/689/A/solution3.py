import sys n = int(sys.stdin.readline())digits = [int(x) for x in sys.stdin.readline().strip()] # print digits def can_go_left(digits):    return all(x not in digits for x in [0, 1, 4, 7]) def can_go_right(digits):    return all(x not in digits for x in [0, 3, 6, 9]) def can_go_up(digits):    return all(x not in digits for x in [1, 2, 3]) def can_go_down(digits):    return all(x not in digits for x in [0, 7, 9])  if can_go_left(digits) or can_go_right(digits) or can_go_up(digits) or can_go_down(digits):    print("NO")else:    print("YES")
