from collections import deque intersections = int(input())shortcuts = [0] + list(map(int, input().split()))costs = [float('inf')] * (intersections + 1)costs[1] = 0queue = deque([1]) while queue:    t = queue.popleft()     if t + 1 <= intersections and costs[t] + 1 < costs[t + 1]:        costs[t + 1] = costs[t] + 1        queue.append(t + 1)     if t - 1 >= 1 and costs[t] + 1 < costs[t - 1]:        costs[t - 1] = costs[t] + 1        queue.append(t - 1)     if costs[t] + 1 < costs[shortcuts[t]]:        costs[shortcuts[t]] = costs[t] + 1        queue.append(shortcuts[t]) print(' '.join(str(i) for i in costs[1:]))
