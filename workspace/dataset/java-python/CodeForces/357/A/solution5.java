import java.util.Arrays;import java.util.Scanner; /* * To change this license header, choose License Headers in Project Properties. * To change this template file, choose Tools | Templates * and open the template in the editor. *//** * * @author Dell */public class NewMain49 {     /**     * @param args the command line arguments     */    public static void main(String[] args) {        // TODO code application logic here         Scanner s = new Scanner(System.in);        int n = s.nextInt();         int sum = 0;        int[] ar = new int[n + 1];        for (int i = 0; i < n; i++) {            int x = s.nextInt();            sum += x;            ar[i + 1] = sum;        }         int x = s.nextInt();        int y = s.nextInt();        //System.out.println(Arrays.toString(ar));        for (int i = 1; i < n + 1; i++) {            int a = ar[i], b = (ar[n] - ar[i]);            //System.out.println(a + " " + b);            if ((a >= x && a <= y) && (b >= x && b <= y)) {               // System.out.println("ans " + (i + 1) + " " + a + " " + b);                System.out.println((i + 1));                return;            }        }        System.out.println(0);    } }
