import java.util.*;public class Main{	public static void main(String[] args) {		//System.out.println("Hello World");		Scanner s=new Scanner(System.in);		int n=s.nextInt();		StringBuffer str1=new StringBuffer(s.next());		StringBuffer str2=new StringBuffer(s.next());		ArrayList<Integer> list = new ArrayList<>();	    int arr1[]=new int[26];int arr2[]=new int[26];	    for(int i=0;i<str1.length();i++)	    {	        arr1[(int)str1.charAt(i)-97]++;	        arr2[(int)str2.charAt(i)-97]++;	    }	    for(int i=0;i<26;i++)	    {	        if(arr1[i]!=arr2[i])	        {	            System.out.println(-1);	            return;	        }	    }		for(int i=0;i<str1.length();i++)		{		    char ch1=str1.charAt(i);		    char ch2=str2.charAt(i);		    if(ch1!=ch2)		    {		        int index=-1;		        for(int j=i+1;j<str1.length();j++)		        {		            char ch3=str1.charAt(j);		            if(ch3==ch2)		            {		                index=j;break;		            }		        }		        for(int j=index-1;j>=i;j--)		        {		            list.add(j);		        }		        str1.delete(index,index+1);		        str1.insert(i,new StringBuffer(String.valueOf(ch2)));		    }		}		System.out.println(list.size());		for(int i:list)		{		    System.out.print(i+1+" ");		}	}}
