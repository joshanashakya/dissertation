import java.util.ArrayList;import java.util.Arrays;import java.util.Scanner; public class Cp{    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n=sc.nextInt();        char a[]=sc.next().toCharArray();        char b[]=sc.next().toCharArray();        ArrayList<Integer> list = new ArrayList<>();        for (int i=0;i<n;i++){            if (a[i]==b[i])continue;            int pos=-1;            for (int j=i+1;j<n;j++){                if (a[j]==b[i]){                    pos=j;                    break;                }            }            if (pos==-1){                System.out.println(-1);                return;            }            for (int j=pos-1;j>=i;j--){                char temp=a[j];                a[j]=a[j+1];                a[j+1]=temp;                list.add(j);            }        }        System.out.println(list.size());        for (int i=0;i<list.size();i++){            System.out.print((list.get(i))+1+" ");        }     }}
