import java.util.*; public class TestCodeforces {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        solve(scanner);        scanner.close();    }     public static void solve(Scanner scanner) {         String  s = scanner.nextLine(), s2 = scanner.nextLine(), s3 = scanner.nextLine();         String res = "";        int len = s3.length();        for (int i = 0; i < len; i++) {            char c = s3.charAt(i);            if (c >= '0' && c <= '9') res += c;            else if (c >= 'A' && c <= 'Z') res += Character.toUpperCase(s2.charAt(s.indexOf(Character.toLowerCase(c))));            else res += s2.charAt(s.indexOf(c));        }        System.out.println(res);      }}
