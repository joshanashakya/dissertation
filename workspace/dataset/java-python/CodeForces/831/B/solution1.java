import java.util.*;import java.lang.*; public class trial {	public static void main(String[] args)    {        Scanner sc=new Scanner(System.in);        HashMap<Character,Character> sts = new HashMap<>();                String s1 = sc.next();        String s2 = sc.next();                for(int i = 0;i<s1.length();i++)        {        	sts.put(s1.charAt(i), s2.charAt(i));        }                 String test = sc.next();        for(int i = 0;i<test.length();i++)        {        	if(Character.isAlphabetic(test.charAt(i)) && test.charAt(i)>90)        			{        				System.out.print((Character.toLowerCase(sts.get(test.charAt(i)))));        			}        	else if(Character.isAlphabetic(test.charAt(i)) && test.charAt(i)<=90)        	{	        		System.out.print(Character.toUpperCase(sts.get(Character.toLowerCase(test.charAt(i)))));        	}        	else	        		System.out.print(test.charAt(i));        }    }  }
