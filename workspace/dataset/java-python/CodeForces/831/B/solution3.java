import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;import static java.util.Arrays.sort;import javafx.util.Pair; public class Main{     public static void main(String[] args)    {         FastScanner input = new FastScanner();        String FKeyboard= input.next();        String SKeyboard= input.next();        String typed= input.next();       StringBuilder ans  = new StringBuilder();        for (int i = 0; i < typed.length(); i++) {            if(typed.charAt(i)>='A'&&typed.charAt(i)<='Z')            {                String c = (typed.charAt(i)+"").toLowerCase();                System.out.print((SKeyboard.charAt(FKeyboard.indexOf(c))+"").toUpperCase());            }            else if(typed.charAt(i)>='a'&&typed.charAt(i)<='z')            {                String c = typed.charAt(i)+"";              System.out.print(SKeyboard.charAt(FKeyboard.indexOf(c)));            }            else                System.out.print(typed.charAt(i));        }        System.out.println("");    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
