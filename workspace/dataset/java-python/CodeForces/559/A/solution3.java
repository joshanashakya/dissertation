import java.io.File;import java.util.Scanner;import java.util.StringTokenizer; public class p006 {	public static void main(String args[]) throws Exception {	    StringTokenizer stok = new StringTokenizer(new Scanner(System.in).useDelimiter("\\A").next());		StringBuilder sb = new StringBuilder();		int a1 = Integer.parseInt(stok.nextToken());		int a2 = Integer.parseInt(stok.nextToken());		int a3 = Integer.parseInt(stok.nextToken());		int a4 = Integer.parseInt(stok.nextToken());		int a5 = Integer.parseInt(stok.nextToken());		int a6 = Integer.parseInt(stok.nextToken());		int v = a1;		long tot = 0;		for(int i=0;i<Math.min(a2, a6);i++,v++) tot += v*2+1;		for(int i=Math.min(a2, a6);i<Math.max(a2, a6);i++) tot +=2*v;		for(int i=Math.max(a2, a6);i<a2+a3;i++,v--) tot+=2*v-1;		System.out.println(tot);	}}
