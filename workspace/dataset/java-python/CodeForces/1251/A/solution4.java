import java.util.ArrayList;import java.util.Collections;import java.util.Scanner; public class BrokenKeyboard {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int testCases = scanner.nextInt();        scanner.nextLine();        for (int j = 0; j < testCases; j++) {            String str = scanner.nextLine();            ArrayList<Character> letters = new ArrayList<>();            char ch = str.charAt(0);            int counter = 1;            for (int i = 1; i < str.length(); i++) {                if (ch == str.charAt(i)) {                    counter++;                } else {                    if (counter % 2 != 0) {                        letters.add(ch);                    }                    counter = 1;                    ch = str.charAt(i);                }            }            if (counter % 2 != 0) {                letters.add(ch);            }            if (letters.size() != 0) {                Collections.sort(letters);                System.out.print(letters.get(0));                for (int i = 1; i < letters.size(); i++) {                    if (letters.get(i) != letters.get(i - 1)) {                        System.out.print(letters.get(i));                    }                }            }            System.out.println("");        }    }}
