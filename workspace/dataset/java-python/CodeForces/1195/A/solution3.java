/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Codechef{	public static void main (String[] args) throws java.lang.Exception	{		BufferedReader bf = new BufferedReader(new InputStreamReader(System.in));	String []s1=bf.readLine().split(" ");	int available=0;	int count=0;	int n=Integer.parseInt(s1[0]);	if(n%2==0)	available=n/2;	else	available=1+n/2;	int k=Integer.parseInt(s1[1]);	int a[]=new int[n];	int b[]=new int[k+1];	for(int i=0;i<n;i++)	{	    a[i]=Integer.parseInt(bf.readLine());	    b[a[i]]++;	}//	System.out.println(Arrays.toString(b));	int sum=0;	for(int i=1;i<k+1;i++)	{    while(b[i]>=2 && available>0)    {        count++;        available--;        b[i]=b[i]-2;    }	sum=sum+b[i];	}//	System.out.println("count"+count);	int sangam=Math.min(available,sum);	System.out.println((2*count)+sangam);	}}
