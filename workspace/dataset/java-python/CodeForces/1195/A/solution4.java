import java.util.*;import java.io.*; public class A1195 {	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int k = sc.nextInt();		//int[] ar = new int[k];				Map<Integer, Integer> map = new HashMap<>();		for (int i = 0; i < n; i++) {			int in = sc.nextInt();			if (map.containsKey(in)) map.put(in, map.get(in) + 1);			else map.put(in, 1);		}				int good = 0, bad = 0;		for (Map.Entry<Integer, Integer> kv : map.entrySet()) {			if (kv.getValue() % 2 == 0) good += kv.getValue() / 2;			else {				good += kv.getValue() / 2;				bad += 1;			}		}				//System.out.printf("good: %d, bad: %d\n", good, bad);		System.out.println(good * 2 + (int)Math.max(bad / 2, (bad + 1) / 2));						//for (int i = 0; i < n; i++) ar[sc.nextInt() - 1]++;		//Arrays.sort(ar);		//int max = n % 2 == 0 ? n / 2 : (n + 1) / 2;		//System.out.println(Arrays.toString(ar));		//System.out.println(max);		//int cnt = 0, curr;		//for (int i = k - 1; i >= 0; i--) {			//curr = ar[i];			//if (curr % 2 == 0) {				//if (curr / 2 <= max) {					//cnt += curr;					//max -= curr / 2;				//} else {					//cnt += max * 2;				//}			//} else {				//if ((curr + 1) / 2 <= max) {					//cnt += curr;					//max -= (curr + 1) / 2;				//} else {					//cnt += max * 2;				//}			//}		//}				//System.out.println(cnt);	}}
