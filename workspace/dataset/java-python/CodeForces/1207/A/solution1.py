def solve():    b, p, f = map(int, input().split())    h, c = map(int, input().split())     # compute first case    minimal = min(b // 2, p)    first = (minimal * h) + (min((b - minimal * 2) // 2, f) * c)     # compute second case    minimal = min(b // 2, f)    second  = (minimal * c) + (min((b - minimal * 2) // 2, p) * h)      print(max(first, second)) t = int(input())while t:    solve()    t -= 1
