import java.util.*; public class Array_493 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String first = scanner.next();        String second = scanner.next();         String ans = solution(first, second);         System.out.println(ans);    }     private static String solution(String first, String second) {        char[] fArr = first.toCharArray();        char[] sArr = second.toCharArray();         int i = fArr.length - 1;        while (i >= 0) {            if (fArr[i] == 'z') {                fArr[i] = 'a';            } else {                fArr[i]++;                break;            }            i--;        }         return test(String.valueOf(fArr), String.valueOf(sArr)) ? String.valueOf(fArr) : "No such string";    }     public static boolean test(String first, String second) {        return first.compareTo(second) < 0;    }}
