n=int(input())N=[]d={}c=0M={}for i in range(n):    s=input()    p=set(s)    if len(p)>2:        continue    else:        N.append(len(s))        d.update({c:p})        c+=1        if len(p)==1:            if s[0] not in M.keys():                M.update({s[0]: len(s)})            else:                M[s[0]]+=len(s)best=0done=[]for i in d:    if d[i] not in done:        p=d[i]        c_sum=0        for j in d:            if d[j].issubset(p)==True:                c_sum+=N[j]        done.append(d[i])        best=max(c_sum,best)best_single=0for i in M:    for j in M:        if i!=j:            best_single=max(best_single,M[i]+M[j])print(max(best,best_single))      
