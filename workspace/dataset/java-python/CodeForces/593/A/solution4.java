import java.util.*;import static java.lang.Math.*;import java.io.*; public class SolutionB {             public static void main(String args[])throws IOException{              Scanner sc = new Scanner(System.in);              int n = sc.nextInt();              List<String> words = new ArrayList<String>();              for(int i = 0; i < n; i++){                     String s = sc.next();                     Set<Character> set = new HashSet<Character>();                     boolean valid = true;                     for(int j = 0; j < s.length(); j++){                            set.add(s.charAt(j));                            if(set.size() > 2){                                   valid = false;                                   break;                            }                     }                     if(valid)words.add(s);              }              int max = 0;              for(int i = 0; i < 26; i++){                     for(int j = i+1; j < 26; j++){                            char c1 = (char)(i + 'a');                            char c2 = (char)(j + 'a');                            int curr = 0;                            for(String s : words){                                   boolean valid = true;                                   for(int k = 0; k < s.length(); k++){                                          char c = s.charAt(k);                                          if(c != c1 && c != c2){                                                 valid = false;                                                 break;                                          }                                   }                                   if(valid)curr += s.length();                            }                            max = Math.max(curr, max);                     }              }              System.out.println(max);      }}
