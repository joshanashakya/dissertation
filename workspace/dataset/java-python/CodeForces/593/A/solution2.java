import java.util.ArrayList;import java.util.Arrays;import java.util.Collections;import java.util.HashMap;import java.util.Scanner;import java.util.TreeMap;import java.util.TreeSet;  public class Main {         public static void main(String[] args) {       Scanner input = new Scanner(System.in);       int n = input.nextInt();       String s[] = new String[n];       int max =0;        for (int i = 0; i < n; i++) {            s[i] = input.next();        }        int len =0;        for (char i = 'a'; i <='z'; i++) {                        for (char j = 'a'; j <='z'; j++) {                len=0;                for (int k = 0; k < n; k++) {                    String test = s[k];                    String x = String.valueOf(i);                    String y = String.valueOf(j);                    test = test.replaceAll(x,"");                    test = test.replaceAll(y,"");                    if(test.length()==0)                    {                        len+=s[k].length();                                            }                                  }                                max = Math.max(len, max);        }        }        System.out.println(max);                                                  }    }
