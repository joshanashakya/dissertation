import java.io.BufferedReader; import java.io.*;import java.io.InputStreamReader; import java.util.Scanner; import java.util.StringTokenizer; import java.util.*;import java.math.*;public class Main {     static class FastReader     {         BufferedReader br;         StringTokenizer st;           public FastReader()        {             br = new BufferedReader(new                     InputStreamReader(System.in));         }           String next()        {             while (st == null || !st.hasMoreElements())             {                 try                {                     st = new StringTokenizer(br.readLine());                 }                 catch (IOException  e)                 {                     e.printStackTrace();                 }             }             return st.nextToken();         }           int nextInt()         {             return Integer.parseInt(next());         }           long nextLong()         {             return Long.parseLong(next());         }           double nextDouble()         {             return Double.parseDouble(next());         }           String nextLine()         {             String str = "";             try            {                 str = br.readLine();            }             catch (IOException e)             {                 e.printStackTrace();             }             return str;         }     }    public static void main(String args[]) throws Exception    {        FastReader sc=new FastReader();        PrintWriter pw=new PrintWriter(System.out);        int t=1;//sc.nextInt();        while(t-->0)        {           //int n=sc.nextInt(),           int a=0,b=0,c=0,p=0,e=0;           String s=sc.next();           for(int i=0;i<s.length();i++)           {             if(s.charAt(i)=='|' && p==0 && e==0)             a++;             if(s.charAt(i)=='|' && p==1 && e==0)             b++;             if(s.charAt(i)=='|' && p==1 && e==1)             c++;             if(s.charAt(i)=='+') p=1;             if(s.charAt(i)=='=') e=1;           }             if(a+b==c) pw.print(s);           else if(a+b>c && a+b-c==2)           {               c++;               if(a>1) a--;               else b--;               if(a>0 &&b>0 && c>0)               {                   for(int i=0;i<a;i++) pw.print("|");                   pw.print("+");                   for(int i=0;i<b;i++) pw.print("|");                   pw.print("=");                   for(int i=0;i<c;i++) pw.print("|");               }               else pw.print("Impossible");           }           else if(c>a+b && c-a-b==2)           {               c--;               if(a==0) a++;               else b++;               if(a>0 && b>0 && c>0)               {                   for(int i=0;i<a;i++) pw.print("|");                   pw.print("+");                   for(int i=0;i<b;i++) pw.print("|");                   pw.print("=");                   for(int i=0;i<c;i++) pw.print("|");               }            else pw.print("Impossible");           }           else pw.println("Impossible");        }           pw.close();    }}  
