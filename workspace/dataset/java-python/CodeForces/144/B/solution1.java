import java.awt.Point;import java.util.ArrayList;import java.util.List;import java.util.Scanner; public class B144 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int xFrom = in.nextInt();        int yFrom = in.nextInt();        int xTo = in.nextInt();        int yTo = in.nextInt();        if (xFrom > xTo) {            int temp = xFrom;            xFrom = xTo;            xTo = temp;        }        if (yFrom > yTo) {            int temp = yFrom;            yFrom = yTo;            yTo = temp;        }        int N = in.nextInt();        int[] X = new int[N];        int[] Y = new int[N];        int[] R = new int[N];        for (int n=0; n<N; n++) {            X[n] = in.nextInt();            Y[n] = in.nextInt();            R[n] = in.nextInt();        }        List<Point> generals = new ArrayList<>();        for (int x=xFrom; x<xTo; x++) {            generals.add(new Point(x, yFrom));        }        for (int y=yFrom; y<yTo; y++) {            generals.add(new Point(xTo, y));        }        for (int x=xTo; x>xFrom; x--) {            generals.add(new Point(x, yTo));        }        for (int y=yTo; y>yFrom; y--) {            generals.add(new Point(xFrom, y));        }        int answer = 0;        for (Point p : generals) {            boolean heated = false;            for (int n=0; n<N; n++) {                int dx = p.x-X[n];                int dy = p.y-Y[n];                if (dx*dx+dy*dy <= R[n]*R[n]) {                    heated = true;                    break;                }            }            if (!heated) {                answer++;            }        }        System.out.println(answer);    } }
