def solve(n,heights):    max_val = heights[0]    max_val_index = 0    min_val = heights[n-1]    min_val_index = n-1     for i in range(0, n):        if heights[i]>max_val:            max_val = heights[i]            max_val_index = i        if heights[i]<=min_val:            min_val= heights[i]            min_val_index=i    swaps = max_val_index    swaps += (n-1) - min_val_index     if max_val_index>min_val_index:        swaps-=1     return swapsif __name__ == "__main__":    n = int(input())    heights = list(map(int, input().split(' ')))    print(solve(n, heights))
