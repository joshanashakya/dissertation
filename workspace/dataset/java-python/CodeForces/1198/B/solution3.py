from sys import stdin,stdoutfor _ in range(1):#int(stdin.readline())):    n=int(stdin.readline());last=[0]*n    a=list(map(int,stdin.readline().split()))    update=[];qq=int(stdin.readline())    for q in range(qq):        pair=list(map(int,stdin.readline().split()))        if pair[0]==2:            update+=[pair[1]]        else:            last[pair[1]-1]=q            a[pair[1]-1]=pair[2]            update+=[0]    for i in range(qq-2,-1,-1):        update[i]=max(update[i+1],update[i])    # print(update)    for i in range(n):        a[i]=max(a[i],update[last[i]])    print(*a) 			  		  	  	  			  	 		  	 	 	
