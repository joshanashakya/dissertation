import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class pblm3 {	static int sum;		public static void main(String[] args) throws IOException {					Scanner sc = new Scanner(System.in);			int n = sc.nextInt();			int[] ar = new int[n];			for(int i=0;i<n;i++)ar[i]=sc.nextInt();			Arrays.sort(ar);						List<Integer> ll = new ArrayList<Integer>();			int i=0,val=1;			while(val<=3000)			{				while(i<n && val<ar[i])					ll.add(val++);				val++;				i++;			}			int sum=0;			//System.out.println(Arrays.toString(ar));			for(i=1;i<n;i++)			{				if(ar[i]==ar[i-1] && !ll.contains(ar[i]))				{					sum++;					ar[i]++;				}				else if(ar[i] < ar[i-1])				{					sum += (ar[i-1]-ar[i]+1);					ar[i]+=(ar[i-1]-ar[i]+1);				}							}			//System.out.println(Arrays.toString(ar));			System.out.println(sum);							}}				  
