import java.io.*;import java.util.*; public class codeforces { 	BufferedReader br;	StringTokenizer st;		public codeforces(){ // constructor				br = new BufferedReader(new InputStreamReader(System.in));	}		String next(){				while (st == null || !st.hasMoreElements()){			try{				st = new StringTokenizer(br.readLine());			}						catch (IOException e){				e.printStackTrace();			}		}		return st.nextToken();	}		int nextInt(){				return Integer.parseInt(next());	}		long nextLong(){				return Long.parseLong(next());	}		double nextDouble(){		return Double.parseDouble(next());	} 	String nextLine(){				String str = "";		try{						str = br.readLine();		}		catch (IOException e){						e.printStackTrace();		}		return str;	} 	public static void main(String[] args) throws Exception{			codeforces io = new codeforces();				int n = io.nextInt();		int arr[] = new int[n];				Set<Integer> set = new HashSet<>();				for(int i = 0; i < n; i++) {			arr[i] = io.nextInt();		}				int min_coins = 0;				for(int ele : arr) {			if(set.contains(ele)) {				int new_coolness = ele;				while(set.contains(new_coolness)) {					min_coins++;					new_coolness++;				}				set.add(new_coolness);			}			set.add(ele);		}		System.out.println(min_coins);	}}
