import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }     public static void main(String[] args) throws IOException    {        Scanner input = new Scanner(System.in);             int n = input.nextInt();        int m = input.nextInt();        String s =  input.next();        char ans[] = s.toCharArray();        char letter[] = new char[26];        for (int i = 0; i < 26; i++) {            letter[i] = (char)('a'+i);        }                for (int i = 0; i < m; i++) {            char start = input.next().charAt(0);            char end = input.next().charAt(0);            for (int j = 0; j < 26; j++) {                if(letter[j]==start)                {                    letter[j]=end;                }                else if(letter[j]==end)                {                    letter[j]= start;                }            }//            System.out.println(Arrays.toString(letter));        }        StringBuilder result = new StringBuilder();        for (int i = 0; i < n; i++) {            result.append(letter[ans[i]-'a']);        }        System.out.println(result);             } }
