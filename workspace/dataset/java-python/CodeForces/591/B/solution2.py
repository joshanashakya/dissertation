from collections import *d=defaultdict(list)n,m=map(int,input().split())s=input()flag=0flag2=0temp,temp1=0,0for i in range(m):    x,y=input().strip().split(" ")    if x in d.keys():        flag=1        for i in d.keys():            if d[i]==x:                temp=i                break    else:        flag=0        d[x]=y    if y in d.keys():        flag2=1        for i in d.keys():            if d[i]==y:                temp1=i                break    else:        flag2=0        d[y]=x        if flag==1:        d[temp]=y    if flag2==1:        d[temp1]=x                            for i in s:    if d[i]!=[]:        print(d[i],end="")    else:        print(i,end="")print() 
