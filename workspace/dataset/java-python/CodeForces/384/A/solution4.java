/*import java.util.Arrays;import java.util.Collections;import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int n = in.nextInt();        int k = in.nextInt();        int l = in.nextInt();        int num = n * k;        Integer wood[] = new Integer[num];        for (int i = 0; i < num; i++) {            wood[i] = in.nextInt();        }        Arrays.sort(wood, Collections.reverseOrder());        int num2 = num / k;        int v[] = new int[num2];        int index = 0;        for (int i = 0; i < num; i += k, index++) {            int min = Integer.MAX_VALUE;            for (int j = i; j < i + k; j++) {                v[index] = wood[j] < min ? wood[j] : min;            }        }        if (v[0] - v[num2 - 1] <= l) {            long ans = 0;            for (int i = 0; i < num2; i++) {                ans += v[i];            }            System.out.println(ans);        } else {            int need = v[num2 - 1] + l;            Integer canGive[] = new Integer[num + 1];            canGive[0] = 0;            for (int i = num2-1; i >= 0; i--)                if (v[i] <= need) {                    for (int j = i * k; j < k * (i + 1) - 1; j++) {                        canGive[++canGive[0]] = wood[j];                    }                }            int needNum = 0;            for (int i = 0; i < num2; i++) {                if (v[i] > need)                    needNum++;                else                    break;            }            if (needNum > canGive[0]) {                System.out.println(0);            } else {                Arrays.sort(canGive, 1, canGive[0] + 1, Collections.reverseOrder());                long ans = 0;                int j = 1;                for (int i = 0; i < num2; i++) {                    if (v[i] > need)                        ans += canGive[j++];                    else                        ans += v[i];                }                System.out.println(ans);            }        }    }}*/    import java.util.Scanner; public class Main {    public static void main(String args[]) {        Scanner in = new Scanner(System.in);        while(in.hasNext()){            int n = in.nextInt();            int code = (n*n+1)/2;            System.out.println(code);            for (int i = 0; i < n; i++) {                StringBuffer stringBuffer = new StringBuffer();                for (int j = 0; j < n; j++) {                    if ((i+j+2)%2==0)                        stringBuffer.append('C');                    else                        stringBuffer.append('.');                }                System.out.println(stringBuffer);            }        }    } }               	  	   	 	 	  		     	 			    	
