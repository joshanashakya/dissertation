import java.util.*;import static java.lang.Math.abs;import static java.lang.Math.min; public class Test {    public static void main(String[] args) {                Scanner input = new Scanner(System.in);        int t = input.nextInt();         while (t-->0) {             int n = input.nextInt();            int m = input.nextInt();            int[][] a = new int[n][m];             for (int i = 0; i < n; i++) {                for (int j = 0; j < m; j++) {                    a[i][j] = input.nextInt();                }            }             int sum = 0, neg = 0, min = abs(a[0][0]);            for (int i = 0; i < n; i++) {                for (int j = 0; j < m; j++) {                     if (a[i][j] < 0) {                        neg++;                    }                    min = min(abs(a[i][j]), min);                    sum += abs(a[i][j]);                }               }            System.out.println((neg % 2 == 0) ? sum : sum - 2*min);        }       }}
