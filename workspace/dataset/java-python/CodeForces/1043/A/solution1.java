import java.io.BufferedReader;import java.io.InputStreamReader;import java.util.Arrays;  public class Solution {      public static void main(String[] args) throws Exception {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));         String input = br.readLine();        int n = Integer.parseInt(input.replaceAll(",", ""));        input= br.readLine();        int[] a = Arrays.stream(input.split(" ")).mapToInt(Integer::parseInt).toArray();        int k= a[0];        for (int i = 0; i < n; i++) {            if(a[i]>k)                k=a[i];        }        int A=0;        int B=0;                for (int i = 0; i < n; i++) {            B+=a[i];            A+= k-a[i];         }        int diff= B-A;        if(diff>=0)        {               System.out.println((diff/n)+k+1);         }        else        {            System.out.println(k);        }         }      }      	 	 			 	   	 	 	 	 		 				 	
