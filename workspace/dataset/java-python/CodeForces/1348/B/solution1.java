import java.util.*; public class Test {     static Scanner sc;         public static void main(String[] args) {        sc = new Scanner(System.in);               int t = Integer.parseInt(sc.next());        while(t-->0) solve();    }     static void solve(){                    int n = sc.nextInt(); int k = sc.nextInt();         boolean[] arr = new boolean[n+1];         for(int i = 0; i < n; i++){            arr[sc.nextInt()] = true;        }         List<Integer> al = new ArrayList<>();        for(int i = 1; i <= n; i++)            if(arr[i]) al.add(i);         if(al.size() > k){            System.out.println(-1); return;        }                while (al.size() < k) {            al.add(1);        }         System.out.println(n*k);               for(int i = 1; i <= n; i++)            for(Integer j : al)                System.out.print(j+" ");         System.out.println();     }    }
