import java.io.BufferedReader;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.Arrays;import java.util.StringTokenizer; public class Problem500A {	public static void main(String[] args) throws Exception {		BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));		PrintWriter writer = new PrintWriter(System.out); 		int n = Integer.parseInt(reader.readLine());		StringTokenizer tokenizer = new StringTokenizer(reader.readLine()); 		Long[] coords = new Long[2 * n];		for (int i = 0; i < 2 * n; i++) {		    coords[i] = (long) Integer.parseInt(tokenizer.nextToken());        }         Arrays.sort(coords); 		long minCoord = coords[0];		long maxCoord = coords[coords.length - 1]; 		long minArea = (coords[n - 1] - coords[0]) * (coords[2 * n - 1] - coords[n]);		for (int i = 1; i < n; i++) {		    minArea = Math.min(minArea, (maxCoord - minCoord) * (coords[i + n - 1] - coords[i]));        } 		writer.println(minArea);  		reader.close();		writer.close();	}}
