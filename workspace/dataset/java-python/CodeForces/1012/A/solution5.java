import java.util.*;import java.io.*;import java.net.URI;public class b{  static class FastReader     {         BufferedReader br;         StringTokenizer st;           public FastReader()         {             br = new BufferedReader(new                     InputStreamReader(System.in));         }           String next()         {             while (st == null || !st.hasMoreElements())             {                 try                {                     st = new StringTokenizer(br.readLine());                 }                 catch (IOException  e)                 {                     e.printStackTrace();                 }             }             return st.nextToken();         }           int nextInt()         {             return Integer.parseInt(next());         }           long nextLong()         {             return Long.parseLong(next());         }           double nextDouble()         {             return Double.parseDouble(next());         }           String nextLine()         {             String str = "";             try            {                 str = br.readLine();             }             catch (IOException e)             {                 e.printStackTrace();             }             return str;         }     }      public static void main(final String [] args)throws IOException{        FastReader sc=new FastReader();        int n=sc.nextInt();                n=2*n;         long [] x=new long[n];         for(int i=0;i<n;i++)x[i]=sc.nextLong();         Arrays.sort(x);         long area=Math.abs(x[0]-x[n/2-1])*Math.abs(x[n/2]-x[n-1]);         long z=Integer.MAX_VALUE;         for(int i=1;i<n/2;i++)            z=Math.min(z,Math.abs(x[i]-x[i+(n/2)-1]));         long sarea=z*Math.abs(x[0]-x[n-1]);         System.out.println(Math.min(area,sarea));      }       }
