import java.util.*;public class CollectingBeatsIsFun {    public static void main(String[] args) {         Scanner sc = new Scanner(System.in);        int k = sc.nextInt();        Map<Integer,Integer> map = new HashMap<>();        boolean flag = true;         for(int i = 0;i<4;i++)        {            String temp = sc.next();            for(int j = 0;j<temp.length();j++)            {                if(temp.charAt(j)!='.')                {                    int num = temp.charAt(j)-'0';                    if(map.containsKey(num))                    {                        map.put(num,map.get(num)+1);                    }                    else                    map.put(num,1);                }            }        }        sc.close();        for(Map.Entry<Integer,Integer> entry:map.entrySet())        {            if(entry.getValue()>2*k)            {                flag = false;                break;            }        }        if(flag)        System.out.println("YES");        else        System.out.println("NO");            } }
