#include<bits/stdc++.h>#define pb push_back#define int long longusing namespace std;const int siz = 1e6+5;int t,n,T,m,x,pos,a[siz],mx,cnt[siz],dp[siz];int32_t main(){    ios_base::sync_with_stdio(0); cin.tie(0);     cin>>n>>T;    for(int i=1;i<=n;i++)	{		cin>>a[i];		cnt[a[i]]++;		mx=max(mx,cnt[a[i]]);	}    int ans = 0;    int l = n;    while(--T&&l<10000){		for(int i=1;i<=n;i++) a[++l]=a[i];	}    for(int i = 1;i<=l;i++)    {        dp[i]++;        for(int j = 1;j<i;j++) if(a[i]>=a[j]) dp[i] = max(dp[i],1+dp[j]);        ans = max(ans,dp[i]);    }    cout<<ans+mx*T<<endl;}
