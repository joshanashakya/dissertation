import java.util.*;import java.lang.*; public class Solution{        int[] xs;    int[] ys;    int lx;    int tx;    int ly;    int ty;    private static int cx;    private static int cy;     public Solution(int[][] a){                xs = new int[4];        ys = new int[4];                for(int i=0;i<4;i++){            xs[i]=a[i][0];            ys[i]=a[i][1];        }         lx = getMin(xs);        tx = getMax(xs);        ly = getMin(ys);        ty = getMax(ys);         cx = (getMax(xs)-getMin(xs))/2+getMin(xs);        cy = (getMax(ys)-getMin(ys))/2+getMin(ys);    }     public boolean contain(int[] p){        if(p[0]>=lx&&p[0]<=tx&&p[1]>=ly&&p[1]<=ty){return true;}        return false;    }             private int getMax(int[] inputArray){                int max = inputArray[0];        for(int i=0;i<inputArray.length;i++){            if(max<inputArray[i]){max=inputArray[i];}        }        return max;    }        private int getMin(int[] inputArray){                int min = inputArray[0];        for(int i=0;i<inputArray.length;i++){            if(min>inputArray[i]){min=inputArray[i];}        }        return min;    }     public static double mdis(int[] p){        return Math.abs(p[0]-cx)+Math.abs(p[1]-cy);    }        public static double dis(int[] p){        return Math.sqrt((p[1] - cy) * (p[1] - cy) + (p[0] - cx) * (p[0] - cx));    }     private static void check(Solution A, int[][] b){        int k =0;        for(int i=-100;i<=100;i++){            for(int j=-100;j<=100;j++){                int[] p = new int[]{i,j};                if(A.contain(p)&&mdis(p)<=mdis(b[0])){System.out.println("YES");return;}            }        }        System.out.println("NO");        return;    }     public static void main(String args[]) {                Scanner scan = new Scanner(System.in);                int[][] a = new int[4][2];        int[][] b = new int[4][2];                for(int i=0;i<4;i++){            for(int j=0;j<2;j++){                a[i][j]=scan.nextInt();            }        }         for(int i=0;i<4;i++){            for(int j=0;j<2;j++){                b[i][j]=scan.nextInt();            }        }                Solution A = new Solution(a);        Solution B = new Solution(b);                check(A, b);    }}
