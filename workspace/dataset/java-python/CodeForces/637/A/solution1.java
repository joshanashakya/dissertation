import java.util.*; public class M_Round_5 { 	public static void main(String[] args) {			Scanner sc = new Scanner(System.in);		// E. Voting for Photos		int n = sc.nextInt();		int a[] = new int[n];		HashMap<Integer, Integer> h = new HashMap<>();		int like=1, id=0;		for(int i=0; i<n; i++) {			a[i] = sc.nextInt();			if( !h.containsKey(a[i]) ) {				h.put(a[i], 1);			}			else {				int v = h.get(a[i]) + 1;				h.put(a[i], v);				if(v>like) {					like=v; id = a[i];				}			}		}		if(id==0)			System.out.println(a[0]);		else 			System.out.println(id);	}}
