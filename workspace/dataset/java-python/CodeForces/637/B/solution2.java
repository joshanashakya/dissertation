import java.util.*; public class Main{    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        ArrayList<String> li = new ArrayList<>();        Set<String> s = new HashSet<>();        int t = sc.nextInt();        String[] str = new String[t];        for (int i = 0; i < t; i++) {            str[i] = sc.next();        }        for (int i = str.length - 1; i >= 0; i--) {            if (!(s.contains(str[i]))) {                s.add(str[i]);                li.add(str[i]);            }        }        String[] ans = new String[li.size()];        int iterator = 0;        for (String i : li) {            ans[iterator++] = i;        }        for (String i : ans) {            System.out.println(i);        }    }}  	    		   		     	 	   		  	
