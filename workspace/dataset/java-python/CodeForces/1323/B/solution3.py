def gen_tbl(a):	n = len(a)	ans = [0] * (n+1)	so_far = 0	for i in range(n):		if a[i] == 0: 			for j in range(1, so_far+1):				ans[j] += (so_far - j + 1)					so_far = 0		else:			so_far += 1	for j in range(1, so_far+1):		ans[j] += (so_far - j + 1)			so_far = 0	return ans n, m, k = map(int,input().split())a = list(map(int,input().split()))b = list(map(int,input().split())) ans = 0tbl_a = gen_tbl(a)tbl_b = gen_tbl(b)for i in range(1, n+1):	j = k // i	if k != i * j: continue	if j <= m:		ans += tbl_a[i] * tbl_b[j]print(ans)
