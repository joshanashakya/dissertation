 import java.util.*;  public class Main {  static  Scanner input = new Scanner(System.in);    public static void main(String[] args) {                                       solve();                     }     private static void solve() {        long n = input.nextInt();        long minx = Integer.MAX_VALUE;        long maxx = Integer.MIN_VALUE;        long miny = Integer.MAX_VALUE;        long maxy = Integer.MIN_VALUE;        for (long i = 0; i < n; i++) {            long x = input.nextInt();            long y = input.nextInt();            minx = Math.min(minx, x);            maxx = Math.max(maxx, x);            miny = Math.min(miny, y);            maxy = Math.max(maxy,y);        }        long len = Math.max(maxx-minx,maxy - miny);        System.out.println(len*len);    } }
