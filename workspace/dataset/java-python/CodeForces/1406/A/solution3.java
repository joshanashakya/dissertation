import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;public class M {	static Scanner scanner=new Scanner(System.in);	public static void main(String[] args) {		int q=scanner.nextInt();		while(q-->0) {			int n=scanner.nextInt();			int a[]=new int [n];			for(int i=0;i<n;i++)a[i]=scanner.nextInt();			Arrays.sort(a);			Set<Integer>set=new HashSet<Integer>();			Set<Integer>set2=new HashSet<Integer>();			for(int i=0;i<n;i++) {				if(!set.contains(a[i]))set.add(a[i]);				else set2.add(a[i]);			}			List<Integer>list=new ArrayList<Integer>();			list.addAll(set);			Collections.sort(list);			List<Integer>list2=new ArrayList<Integer>();			list2.addAll(set2);			Collections.sort(list2);			int x=-1,y=-1;			for(int i:list) {				if(i==x+1)x=i;				else break;			}			for(int i:list2) {				if(i==y+1)y=i;				else break;			}//			System.out.println(set+" "+x);//			System.out.println(set2+" "+y);			System.out.println(x+y+2);		}	}}
