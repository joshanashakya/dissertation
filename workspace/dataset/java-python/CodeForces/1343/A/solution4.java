import java.util.*;import java.io.*; public class Main{	public static void main(String args[]) throws IOException	{		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		int T=Integer.parseInt(br.readLine());		StringBuilder ans=new StringBuilder();		while(T-->0)		{    		int n=Integer.parseInt(br.readLine());    		int k=2;    		double up=0;    		while(true)    		{    		    double d=Math.pow(2.0,k)-1;    		    double e=n/d;    		    if(Math.ceil(e/1.0)==Math.floor(e/1.0))    		    {    		        up=e;    		        break;    		    }    		    k++;    		}    		ans.append((int)up+"\n");		}		System.out.print(ans);	}}
