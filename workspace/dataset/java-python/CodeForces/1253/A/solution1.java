import java.util.*;import java.io.*; public class Main {    class FastScanner{        BufferedReader br=new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st=new StringTokenizer("");        String next() {        while(!st.hasMoreTokens()) {        try {        st=new StringTokenizer(br.readLine());        }        catch(IOException e) {        e.printStackTrace();        }        }        return st.nextToken();        }        int nextInt() {        return Integer.parseInt(next());        }        long nextLong() {        return Long.parseLong(next());        }        String nextLine() {        String str = "";        try {        str = br.readLine();        } catch (IOException e) {        e.printStackTrace();        }        return str;        }}        /**         * Main         */     public static void main(String[] args) {        FastScanner scan=new Main().new FastScanner();        int t=scan.nextInt();       while(t-->0){           int n=scan.nextInt();             int a[]=new int[n];            for(int i=0;i<n;++i){             a[i]=scan.nextInt();                       }           int b[]=new int[n];           for(int i=0;i<n;++i){               b[i]=scan.nextInt();           }           int diff=0;           int temp=0;           int get=0;           int check=0;                      HashSet<Integer>H=new HashSet<>();           for(int i=0;i<n;++i){             diff=a[i]-b[i];             if(diff>0){                 check=1;                 break;             }             else              if(diff<0){                 H.add(a[i]-b[i]);                 if(H.size()>=2||temp==1){                     check=1;                     break;                 }             }             else{                 if(H.size()!=0){                temp=1;                 }             }                   }            if(check==1){                System.out.println("NO");            }            else{                System.out.println("YES");            }        }                 }}
