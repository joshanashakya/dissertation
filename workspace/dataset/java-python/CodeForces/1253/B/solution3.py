def modulo(x):    if (x>=0):        return x    else:        return -xdef verificar_dicionario(dicionario):    for valor in dicionario.values():        if(valor == 0):            return False#algum dos funcionarios nao saiu    return True def regras(lista):     """    para cada i :        verifica se i pertence ao dicionario            se sim, a flag incerto, torna-se nao funciona (saio da funçao retornando false)            se nao, verifca se i é positivo                se sim, cria uma chave com i.                se nao, verifica se mod(i) está no dicionario                    se nao, a flag incerto torna-se nao funciona, pois um numero negativo antes de um positivo(retorno false)                    se sim, entao i vira o valor de mod(i) (ADICIONO i ao dicionario)                    (só agora eu posso verificar se todas as condiçoes sao cumpridas pois essa era a condição final)                        contador_dias+=1                        adiciono o tamanho desse dicionario a lista         se o numero é positivo ele vai ser uma chave do dicionario    retorno true    para cada i:        verifica se i é positivo:            se sim, verifica se i pertence ao dicionario:                se sim, a flag incerto, torna-se nao funciona (saio da funçao retornando false)                se nao, adiciona i como sendo uma nova chave            se nao,verifica se mod(i) pertence ao dicionario                se nao,a flag incerto torna-se nao funciona, pois um numero negativo antes de um positivo(retorno false)                se sim, entao i se torna o valor associado a chave mod(i) (ADICIONO i ao dicionario)                    (só agora eu posso verificar se todas as condiçoes sao cumpridas pois essa era a condição final)                    contador_dias+=1                    adiciono o tamanho do dicionario a lista    """    lista_dias = []    dicionario = {}    flag = 0    contador_dias = 0    for i in lista:        if (i>0):            if(i in dicionario):#como i > 0 entao ele deveria estar como sendo uma das chaves                return -1,-1            else:                dicionario[i] = 0                flag = 0        else:            if(modulo(i) not in dicionario):                return -1,-1            else:                dicionario[modulo(i)] = i                resposta = verificar_dicionario(dicionario)                 if (resposta):                    flag = 1                    contador_dias+=1                    lista_dias.append(len(dicionario)*2)                    dicionario = {}                else:                    flag = 0    if (flag == 1):                   return contador_dias,lista_dias    else:        return -1,-1#problema quando for negativo, a saida ta errada da pra ajeitar colocando uma lista como parametro def main():    tamanho = int(input())    lista = input()    lista = lista.split()    lista = list(map(lambda x: int(x),lista))    contador_dias,lista_dias = regras(lista)    print(contador_dias)    if(contador_dias == -1):        return None    for i in range(len(lista_dias)):        if (i<len(lista_dias)-1):            print(lista_dias[i],end=" ")        else:            print(lista_dias[i])main()	 	 	  	 	     					 	 			 		
