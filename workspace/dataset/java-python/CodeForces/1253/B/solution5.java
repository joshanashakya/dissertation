import java.util.ArrayList;import java.util.HashSet;import java.util.List;import java.util.Scanner;import java.util.Set;import java.util.stream.Collectors; public class Main {	public static void main(String[] args) {		Scanner sc = new Scanner(System.in); 		int n = sc.nextInt();		int[] a = new int[n];		for (int i = 0; i < a.length; i++) {			a[i] = sc.nextInt();		}					List<Integer> eventNums = new ArrayList<>();		Set<Integer> left = new HashSet<>();		Set<Integer> entered = new HashSet<>();		int eventNum = 0;		for (int ai : a) {			if (ai > 0) {				if (left.contains(ai) || entered.contains(ai)) {				    System.out.println("-1");					return;				} 				entered.add(ai);			} else {				if (!entered.contains(-ai)) {					System.out.println("-1");					return;				} 				entered.remove(-ai);				left.add(-ai);			} 			eventNum++; 			if (entered.isEmpty()) {				eventNums.add(eventNum);				left.clear();				entered.clear();				eventNum = 0;			}		} 		if (eventNum != 0) {			System.out.println("-1");					return;		} 		System.out.println(eventNums.size());		for (int i = 0; i < eventNums.size(); i++)             System.out.print(eventNums.get(i) + " "); 	}}
