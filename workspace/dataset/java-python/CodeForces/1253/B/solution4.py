def resolve(arr):    res = []    entrou = set()    dentro = set()    count = 0    for c in arr:        if c > 0:            #entrada            if c in entrou:                return []            entrou.add(c)            dentro.add(c)        else:            #saída            if -1 * c not in dentro:                return []            dentro.remove(-1 * c)            count += 2            if not dentro:                res.append(count)                entrou.clear()                count = 0    if dentro:        return []    else:        return res  n = int(input())arr = [int(x) for x in input().split()]res = resolve(arr)if res == []:    print(-1)else:    print(len(res))    print(" ".join(map(str, res)))							 	 		 		 	 	    						
