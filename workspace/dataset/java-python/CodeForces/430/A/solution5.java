import java.awt.*;import java.io.*;import java.util.*; public class Abc {    public static void main(String[] args) {        FastReader sc = new FastReader();        int n=sc.nextInt();int m=sc.nextInt();        int arr[]=new int[n];        int orig[]=new int[n];        for (int i=0;i<n;i++){            arr[i]=sc.nextInt();            orig[i]=arr[i];        }        Arrays.sort(arr);        StringBuilder sb=new StringBuilder();        int ans[]=new int[101];        for (int i=0;i<n;i++){            ans[arr[i]]=i%2;        }        for (int i=0;i<n;i++){            sb.append(ans[orig[i]]+" ");        }        System.out.println(sb);    }    static int bit[];    static void init(int arr[]){        int n=arr.length;        bit=new int[n+1];        for (int i=0;i<n;i++){         }    }    static long sum(int i){        long res=0;        for (i++;i>0;i-=(i&(-i))){            res+=bit[i];        }        return res;    }    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }}
