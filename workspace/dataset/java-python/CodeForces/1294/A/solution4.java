import java.util.Scanner; public class A1294 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        for (int i = 0; i < t; i++) {            int a = scanner.nextInt(), max = 0, diff = 0, b = scanner.nextInt(),                    c = scanner.nextInt(), n = scanner.nextInt();            max = Math.max(max, a);            max = Math.max(max, b);            max = Math.max(max, c);            diff = 3 * max - a - b - c;            if (n >= diff && (n - diff) % 3 == 0){                System.out.println("YES");            }else {                System.out.println("NO");            }        }    }}
