import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.Arrays; public class CF1294A {    public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        int t = Integer.parseInt(br.readLine());        while (t-- > 0) {            int[] a = Arrays.stream(br.readLine().split("\\s+")).mapToInt(Integer::parseInt).toArray();            int maxValue = Math.max(a[0], a[1]);            maxValue = Math.max(maxValue, a[2]);            int desiredValue = a[0] + a[1] + a[2] + a[3];            System.out.println((desiredValue % 3 == 0 && (desiredValue / 3) >= maxValue) ? "YES" : "NO");        }    }}
