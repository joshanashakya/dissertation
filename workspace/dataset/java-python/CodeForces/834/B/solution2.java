import java.util.*;public class Solution{    public static void main(String[] args){        Scanner s=new Scanner(System.in);        int n=s.nextInt();        int k=s.nextInt();        s.nextLine();        String s1=s.nextLine();        int[] last=new int[26];        boolean c[]=new boolean[26];        //Arryas.fill(c,false);        int found=0;        for(int i=0;i<n;i++){            last[s1.charAt(i)-(int)('A')]=i;                    }        int size=0;        for(int j=0;j<n;j++){            if(!c[s1.charAt(j)-'A']){                c[s1.charAt(j)-'A']=true;                size++;            }            if(size>k){                found=1;                break;            }            if(j==last[s1.charAt(j)-'A'])                size--;        }        if(found==1)            System.out.println("YES");        else            System.out.println("NO");            }}
