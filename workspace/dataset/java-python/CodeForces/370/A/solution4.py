r1, c1, r2, c2 = list(map(int,input().split())) # print(r1, c1, r2, c2) def torre(r1, c1, r2, c2):    if (r1 == r2) or (c1 == c2):        return 1    else:        return 2 def arfil(r1, c1, r2, c2):    p1 = r1 + c1    p2 = r2 + c2    if (p1 % 2) != (p2 % 2):        return 0        elif abs(r1 - r2) == abs(c1 - c2):        return 1    else:        return 2        def rey(r1, c1, r2, c2):    return max(abs(r1 - r2), abs(c1 - c2)) def minimos(r1, c1, r2, c2):    t = torre(r1, c1, r2, c2)    a = arfil(r1, c1, r2, c2)    r = rey(r1, c1, r2, c2)    return t, a , r torre, arfil, rey = minimos(r1, c1, r2, c2)print(torre, arfil, rey)
