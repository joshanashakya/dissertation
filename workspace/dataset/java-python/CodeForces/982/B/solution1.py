n=int(input())a = [int(a) for a in input().split()] code = [] for i in range(1,n+1):    d = dict(x=a[i-1],y=i)    code.append(d)code=sorted(code, key = lambda i:i["x"],reverse=True)intro=[]extro=[] for i in range(1,n+1):    intro.append(code[i-1]['y']) inp=input() for i in inp:    if i=='0':        intro_seat=intro.pop()        print(intro_seat,end=' ')        extro.append(intro_seat)     else:        print(extro.pop(),end=' ') print()
