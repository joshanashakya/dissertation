import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;import javafx.util.Pair; public  class CodeForce {static	BufferedReader br = new BufferedReader(new InputStreamReader(System.in));static StringBuilder sb = new StringBuilder(); 	static boolean flag = false;	 static int gcd(int a, int b) 	    { 	      if (b == 0) 	        return a; 	      return gcd(b, a % b);  	    } 	static int next(String [] sr,int ind)	{		return Integer.parseInt(sr[ind]);	} 	public static void main(String[] args) throws IOException {			//	int t=Integer.parseInt(br.readLine());	//	while(t-->0)	//{	String[] sr=br.readLine().split(" ");	int n=next(sr,0);				seat[] arr=new seat[n];		sr=br.readLine().split(" ");	String ss=br.readLine();	for(int i=0;i<n;i++)	{	int y=next(sr,i);	arr[i]=new seat(i,y);	}	Arrays.sort(arr); 	Stack<Integer> q=new Stack<>();		int[] res=new int[ss.length()];	int j=0;	for(int i=0;i<ss.length();i++)	{		if(ss.charAt(i)=='0')		{			res[i]=arr[j].ind+1;			q.push(j);					j++;					}		else		{			res[i]=arr[q.pop()].ind+1;					}			}	for(int i=0;i<ss.length();i++)	{		System.out.print(res[i]+" ");			}	 																															}						}class seat implements Comparable<seat>{		int ind;	int width;	seat(int a,int b)	{		ind=a;		width=b;	}		public int compareTo(seat arg0) {		return this.width-arg0.width;	}}   
