import java.util.*;import java.util.stream.*; public class Solution {	public static void main(String[] args) {		Scanner scan = new Scanner(System.in);		int n = scan.nextInt();		int k = scan.nextInt();		scan.nextLine();		String s = scan.nextLine();		Set<Character> sch = new HashSet<Character>();		for(int i = 0; i < s.length(); i++) {			sch.add(s.charAt(i));		}		List<Character> li = new ArrayList<Character>(sch);		Collections.sort(li);		//System.out.println(Arrays.toString(li.toArray()));		int index = 0;		int rIndex = 0;		int res = 0;		while(index < li.size() && rIndex < k) {			char nextCh = li.get(index).charValue();			//System.out.println(nextCh + " " + (nextCh - 'a' + 1));			res += nextCh - 'a' + 1;			rIndex++;			if(index < li.size()-1 && li.get(index+1).charValue() - nextCh == 1) {				index++;			}			index++;		}		if(rIndex < k) res = -1;		System.out.println(res);	}}
