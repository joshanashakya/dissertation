[num_broken, length, max_pieces] = [int(el) for el in input().split()] positions = input().split() distances = [] for i in range(num_broken-1):  diff = int(positions[i+1])-int(positions[i])-1  distances.append(diff)  distances.sort() uncut_size = int(positions[-1])-int(positions[0]) + 1 if max_pieces == 1:  print(uncut_size)else:  print(uncut_size - sum(distances[(-max_pieces + 1):])) 	  	 	  	 		       		  		 			
