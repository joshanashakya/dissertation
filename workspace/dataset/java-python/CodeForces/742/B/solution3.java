import java.util.*;import java.io.*;public final class Example{    public static void main(String[] args){        Map<Integer,List<Integer>> map=new HashMap<>();        try{            BufferedReader buffer=new BufferedReader(                    new InputStreamReader(System.in)                );            args=buffer.readLine().split(" ");            int n=Integer.parseInt(args[0]);            int x=Integer.parseInt(args[1]);            args=buffer.readLine().split(" ");            int[] arr=new int[n+1];            for(int i=1;i<=n;i++){                arr[i]=Integer.parseInt(args[i-1]);            }            for(int i=1;i<=n;i++){                int y=arr[i];                if(!map.containsKey(y)){                    map.put(y,new ArrayList<Integer>());                }                map.get(y).add(arr[i]);                if(x==0){                    continue;                }                y=y^x;                if(!map.containsKey(y)){                    map.put(y,new ArrayList<Integer>());                }                map.get(y).add(arr[i]);            }            long counter=0;            if(x==0){                for(int key:map.keySet()){                    long m=map.get(key).size();                    counter+=(m*(m-1))/2;                }            }            else{                for(int key:map.keySet()){                    //System.out.println(key);                    List<Integer> list=map.get(key);                    int m=list.size();                    //System.out.println(m);                    int one=0;                    int two=0;                    for(int i=m-1;i>=0;i--){                        //System.out.println(list.get(i));                        if(list.get(i)==key){                            counter+=two;                            one++;                        }                        else{                            counter+=one;                            two++;                        }                    }                }                counter=counter/2;            }            System.out.println(counter);        }        catch(IOException e){            System.out.println(-1);        }    }}
