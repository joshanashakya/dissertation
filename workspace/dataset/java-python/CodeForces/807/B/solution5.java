import java.util.*;import java.io.*;import java.text.*; public class B807 {	static HashSet <Integer> hs ;	public static void f(int s) {		hs = new HashSet<Integer>();		int i = (s / 50) % 475;		for (int j = 0; j < 25; j++)		{			i = (i * 96 + 42) % 475;			hs.add(26 + i);		} 	} 	public static void main(String[] args) throws IOException{		Scanner sc = new Scanner(System.in);		PrintWriter pw = new PrintWriter(System.out);		int p =sc.nextInt();		int x =sc.nextInt();		int y =sc.nextInt();		int c = x;		int t = 0;		while(c>=y)		{			f(c);			if(hs.contains(p))			{				pw.println(t);				pw.flush();				pw.close();				return;			}			c-=50;			}		c=x+50;		boolean b =true;		while(true) {			if(b)t++;			b=!b;			f(c);			if(hs.contains(p))			{				pw.println(t);				pw.flush();				pw.close();				return;			}			c+=50;		} 	} 	static class Scanner {		StringTokenizer st;		BufferedReader br; 		public Scanner(InputStream s) {			br = new BufferedReader(new InputStreamReader(s));		} 		public String next() throws IOException {			while (st == null || !st.hasMoreTokens())				st = new StringTokenizer(br.readLine());			return st.nextToken();		} 		public int nextInt() throws IOException {			return Integer.parseInt(next());		} 		public long nextLong() throws IOException {			return Long.parseLong(next());		} 		public String nextLine() throws IOException {			return br.readLine();		} 		public double nextDouble() throws IOException {			String x = next();			StringBuilder sb = new StringBuilder("0");			double res = 0, f = 1;			boolean dec = false, neg = false;			int start = 0;			if (x.charAt(0) == '-') {				neg = true;				start++;			}			for (int i = start; i < x.length(); i++)				if (x.charAt(i) == '.') {					res = Long.parseLong(sb.toString());					sb = new StringBuilder("0");					dec = true;				} else {					sb.append(x.charAt(i));					if (dec)						f *= 10;				}			res += Long.parseLong(sb.toString()) / f;			return res * (neg ? -1 : 1);		} 		public boolean ready() throws IOException {			return br.ready();		} 	} }
