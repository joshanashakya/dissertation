import java.io.*;import java.io.BufferedReader; import java.io.IOException; import java.util.*;  public class A254{        public static void main (String[] args)throws IOException	{        FileReader fin = new FileReader("input.txt");        BufferedReader bin= new BufferedReader(fin);                      FileWriter fout=new FileWriter("output.txt");        BufferedWriter bout=new BufferedWriter(fout);        PrintWriter pout=new PrintWriter(bout);                        String num=bin.readLine();        int n=Integer.valueOf(num);        String txt=bin.readLine();        String[] val = txt.split("\\s+");        //Integer arr[]=new Integer[2*n];        List<Integer>[] l =new ArrayList[5001];        for(int i=0;i<l.length;i++)        {            l[i]=new ArrayList<>();        }         for(int i=0;i<2*n;i++)        {               int temp=Integer.parseInt(val[i]);            l[temp].add(i+1);         }        int flag=1;        for(List<Integer> templ : l)        {            if(templ.size()%2==1)            {                pout.println(-1);                flag=0;                break;            }         }        if(flag==1)        {            for(List<Integer> templ : l)            {                for(int i=0;i<templ.size()-1;i+=2)                {                pout.println(templ.get(i)+" "+templ.get(i+1));                }            }        }                              pout.close();        bout.close();        fout.close();        bin.close();           }}
