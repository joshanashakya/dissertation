def func(a,b,prev):	if a==0:		if b==0:			if prev==0:				return 0			else:				return -1		else:			return -1 	if a==1:		if b==0:			if prev==0 or prev==1:				return 1-prev			else:				return -1		elif b==1:			if prev==1:				return 1			else:				return -1		else:			return -1 	if a==2:		if b==0:			if prev==0 or prev==2:				return 2-prev			else:				return -1		elif b==2:			if prev==2:				return 2			else:				return -1		else:			return -1 	if a==3:		if b==0:			return 3-prev		elif b==1:			if prev==1 or prev==3:				return 4-prev			else: 				return -1		elif b==2:			if prev==2 or prev==3:				return 5-prev			else:				return -1		else:			if prev==3:				return 3			else:				return -1  n = input()arr = input()brr = input()arr = list(arr.split(" "))brr = list(brr.split(" ")) n = int(n)for i in range(n-1):	arr[i] = int(arr[i])	brr[i] = int(brr[i]) for i in range(4):	a = arr[0]	b = brr[0]	t = list()	prev = i	t.append(prev)	pos = 1 	for j in range(1,n):	 prev = func(a,b,prev)	 t.append(prev)	 if prev==-1:	 	pos=0	 	break 	 if j==n-1:	 	break	 a = arr[j]	 b = brr[j] 	if pos==1:		print("YES")		for x in t:			print(x,end=" ")		exit() print("NO")
