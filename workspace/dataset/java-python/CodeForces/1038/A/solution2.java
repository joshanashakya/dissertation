import java.util.*;public class contest14 {	static Scanner scn = new Scanner(System.in);	public static void main(String[] args) {		// TODO Auto-generated method stub		int n = scn.nextInt();		int k = scn.nextInt();		String str = scn.next();				HashMap<Character,Integer> map = new HashMap<>();				for(int i=0;i<k;i++) {			char ch = (char)('A'+i);			map.put(ch, 0);		}				//System.out.println(map);				for( int i=0;i<str.length();i++) {			map.put(str.charAt(i), map.getOrDefault(str.charAt(i), 0)+1);		}				int min = Integer.MAX_VALUE;		for(char key:map.keySet()) {			if(map.get(key)<min)				min=map.get(key);		}		System.out.println(k*min);			} }   			 		 		 	     	     	   		
