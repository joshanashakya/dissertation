import java.io.*;import java.lang.*;import java.util.*;import java.io.*;import java.util.*;public class Main{	static Scanner scanner=new Scanner(System.in);	static Set<Long>set=new HashSet<Long>();	static int a[];	static long b[];	public static void main(String[] args) {		int n=scanner.nextInt();		int m=scanner.nextInt();		long a1=scanner.nextLong();		long s=0;		for(int i=1;i<n;i++) {			s=gcd(s, Math.abs(scanner.nextLong()-a1));		}		StringBuilder sb=new StringBuilder();		for(int i=0;i<m;i++) {			sb.append(gcd(s, a1+scanner.nextLong())+" ");		}		System.out.println(sb);	}	static long gcd(long gcd,long l) {		if(l==0)return gcd;		else return gcd(l, gcd%l);	}}            
