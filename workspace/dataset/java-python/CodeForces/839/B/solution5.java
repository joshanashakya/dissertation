import java.io.*;import java.util.*;         public class Solution {            public static void main(String[] args) throws Exception    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        PrintWriter out = new PrintWriter(System.out);                StringTokenizer st = new StringTokenizer(br.readLine());                int n=  Integer.parseInt(st.nextToken());        int k=Integer.parseInt(st.nextToken());                int[] a = new int[k];                st = new StringTokenizer(br.readLine());        for(int i=0;i<k;i++) a[i] =  Integer.parseInt(st.nextToken());                int fourSize = n, twoSize = 2*n;                Arrays.sort(a);        int one = 0, two = 0, three= 0;                for(int i= a.length-1;i>=0;i--){             if(a[i] >= 4){                  int countOfFours = a[i]/4;                  int size = Math.min(countOfFours,fourSize);                  countOfFours-=size;                  fourSize-=size;                                    if(countOfFours > 0){                       size = Math.min(2*countOfFours,twoSize);                       twoSize-=size;                  }                                    int rem = a[i]%4;                  if(rem == 1) one++;                  else if(rem == 2){                       if(twoSize > 0)twoSize--;                       else two++;                  }                  else if(rem == 3) three++;             }             else if(a[i] == 2){                  if(twoSize > 0)twoSize--;                     else two++;             }             else if(a[i] == 3) three++;             else one++;        }     //    System.out.println("one = "+one+" two = "+two+" three = "+three);         int remSpace = 0;                     if(three > 0){                 int size = Math.min(twoSize,three);                     three-=size;                     one+=size;                     twoSize-=size;                                      if(three > 0){                      size = Math.min(fourSize,three);                      fourSize-=size;                 }            }                        if(two > 0){                 int size = Math.min(twoSize,three);                 two-=size;                 twoSize-=size;                                  if(two > 0){                     size = Math.min(fourSize,two);                      fourSize -= size;                     two-=size;                     remSpace+=size;                 }            }                        int moreSpace = one+2*two;            remSpace+= twoSize+2*fourSize;           boolean possible = moreSpace<=remSpace;           if(possible) out.println("YES");           else out.println("NO");           out.close();    } }
