// practice with rainboyimport java.io.*;import java.util.*; public class CF123B extends PrintWriter {	CF123B() { super(System.out, true); }	Scanner sc = new Scanner(System.in);	public static void main(String[] $) {		CF123B o = new CF123B(); o.main(); o.flush();	} 	int count(int x1, int x2, int b) {		if (x1 > x2)			return count(x2, x1, b);		if (x1 > 0)			return x2 / b - x1 / b;		if (x2 < 0)			return -x1 / b - -x2 / b;		return -x1 / b + x2 / b + 1;	}	void main() {		int a = sc.nextInt();		int b = sc.nextInt();		int x1 = sc.nextInt();		int y1 = sc.nextInt();		int x2 = sc.nextInt();		int y2 = sc.nextInt();		a *= 2;		b *= 2;		int x, y;		x = x1 - y1; y = x1 + y1; x1 = x; y1 = y;		x = x2 - y2; y = x2 + y2; x2 = x; y2 = y;		println(Math.max(count(x1, x2, b), count(y1, y2, a)));	}}
