n, k = map(int, input().split())  # n is the number of people, k the max elevator capacityf = list(map(int, input().split()))    # The floors everyone wants to go tol = map f.sort(reverse=True)    # We want to start with the highest floors to make sure we don't waste any trips. time = 0while len(f) > k:    time += 2 * (max(f[:k]) - 1)    f = f[k:]time += 2 * (max(f) - 1) print(time)   
