import mathimport collections def solve(h1, a1, c1, h2, a2):    cnt = 0    moves = []    while(h2 > 0):        if h2 - a1 <= 0:            moves.append("STRIKE")            h2 -= a1        else:            if h1 - a2 <= 0:                moves.append("HEAL")                h1 += c1            else:                moves.append("STRIKE")                h2 -= a1        h1 -= a2        cnt += 1    return str(cnt) + "\n" + "\n".join(moves)   h1, a1, c1 = [int(s) for s in input().split()]h2, a2= [int(s) for s in input().split()]result = solve(h1, a1, c1, h2, a2)print(result)
