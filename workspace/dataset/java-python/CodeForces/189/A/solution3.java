import java.util.*;public class Main{    public static void main(String []args){        Scanner sc=new Scanner(System.in);        int n=sc.nextInt();        int a=sc.nextInt();        int b=sc.nextInt();        int c=sc.nextInt();        int ans=1;        for(int i=0;i<=n;i++){            int g=n-i*a;            // if(g%3==0)            if(g>=0){                for(int j=0;j<=n;j++){                    int k=g-j*b;                    if(k%c==0&&k>=0)                    {ans=Math.max(ans,i+j+k/c);}                    // else                    // ans=Math.max(ans,i+j);                }            }                    }        System.out.println(ans);    }}
