import java.util.*;import java.io.*;import java.math.*;public class Solution{    public static void main(String[] args)throws IOException{        BufferedReader br =  new BufferedReader(new InputStreamReader(System.in));        PrintWriter out = new PrintWriter(System.out);        StringTokenizer st = new StringTokenizer(br.readLine());        long k1 = Long.parseLong(st.nextToken());        long k2 = Long.parseLong(st.nextToken());        long k3 = Long.parseLong(st.nextToken());        st = new StringTokenizer(br.readLine());        long t1 = Long.parseLong(st.nextToken());        long t2 = Long.parseLong(st.nextToken());        long t3 = Long.parseLong(st.nextToken());        st = new StringTokenizer(br.readLine());        int n = Integer.parseInt(st.nextToken());        long[] a = new long[n];        long[] b = new long[n];        st = new StringTokenizer(br.readLine());        for(int i=0;i<n;i++){            a[i] = Long.parseLong(st.nextToken());            b[i] = a[i];        }        //k1 t1        for(int i=0;i<Math.min((int)k1,n);i++){            b[i] += t1;        }        for(int i=(int)k1;i<n;i++){            b[i] = Math.max(b[i],b[i-(int)k1])+t1;        }        //k2 t2        for(int i=0;i<Math.min((int)k2,n);i++){            b[i] += t2;        }        for(int i=(int)k2;i<n;i++){            b[i] = Math.max(b[i],b[i-(int)k2])+t2;        }        //k3 t3        for(int i=0;i<Math.min((int)k3,n);i++){            b[i] += t3;        }        for(int i=(int)k3;i<n;i++){            b[i] = Math.max(b[i],b[i-(int)k3])+t3;        }        long max = 0;        for(int i=0;i<n;i++) max = Math.max(max,b[i]-a[i]);        out.println(max);        out.flush();   }}
