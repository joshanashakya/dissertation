import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.StringTokenizer; public class BusGame {    public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        PrintWriter pw = new PrintWriter(System.out);        StringTokenizer st = new StringTokenizer(br.readLine());        int x = Integer.parseInt(st.nextToken());        int y = Integer.parseInt(st.nextToken());         int turn = 0;         int xToTake[] = {0, 1, 2};        int yToTake[] = {22, 12, 2};        turn: while (true) {            if (turn == 0) {                for (int i = 2; i >= 0; i--) {                    if (x >= xToTake[i] && y >= yToTake[i]){                        x -= xToTake[i];                        y -= yToTake[i];                        turn = 1;                        continue turn;                    }                }                break;            } else {                for (int i = 0; i <= 2; i++) {                    if (x >= xToTake[i] && y >= yToTake[i]){                        x -= xToTake[i];                        y -= yToTake[i];                        turn = 0;                        continue turn;                    }                }                break;            }        }         if (turn == 0)            pw.println("Hanako");        else            pw.println("Ciel");         pw.flush();        pw.close();    }}
