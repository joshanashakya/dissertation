x, y = map(int,
           input().strip().split())  # -2, -2# -0, -22# total, -2, -24a = min(x//2, y//24)x -= a*2y -= a*24 while x and y:    tot = 220    while x > 0 and tot >= 100:        x -= 1        tot -= 100    while y > 0 and tot >= 10:        y -= 1        tot -= 10    if tot:        print("Hanako")        exit(0)    tot = 220    while y > 0 and tot >= 210:        y -= 1        tot -= 10    while y >= 10 and tot:        y -= 10        tot -= 100    while x > 0 and tot >= 100:        x -= 1        tot -= 100    if tot:        print("Ciel")        exit(0)if x:    print("Hanako")    exit(0)if not (y//22)%2:    print("Hanako")else:    print("Ciel")
