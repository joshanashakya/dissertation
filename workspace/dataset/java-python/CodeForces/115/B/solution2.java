import java.io.*;import java.util.*; public class Lawnmower {	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int m = sc.nextInt();		int n = sc.nextInt();		char[][] grid = new char[m][n];		for (int i = 0; i < m; i++) {			String line = sc.next();			for (int j = 0; j < n; j++) {				grid[i][j] = line.charAt(j);			}		}				int[][] nums = new int[m][2];		int max = 0;		for (int i = 0; i < m; i++) {			int left = 0;			int right = n - 1;			while (left < n && grid[i][left] != 'W') left++;			while (right >= 0 && grid[i][right] != 'W') right--;			nums[i][0] = left;			nums[i][1] = right;			if (left != n && right != -1) max = i;		}		int sum = 0;		int pos = 0;		for (int i = 0; i <= max - 1; i++) {			if (i % 2 == 0) {				sum += Math.max(Math.max(nums[i][1], nums[i + 1][1]), pos) - pos;				pos = Math.max(Math.max(nums[i][1], nums[i + 1][1]), pos);			} else {				sum += pos - Math.min(Math.min(nums[i][0], nums[i + 1][0]), pos);				pos = Math.min(Math.min(nums[i][0], nums[i + 1][0]), pos);			}			sum++;		}		sum += ((max) % 2 == 0) ? Math.max(nums[max][1], pos) - pos :			pos - Math.min(pos, nums[max][0]);		System.out.println(sum);	}}
