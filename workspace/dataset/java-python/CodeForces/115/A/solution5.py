def dfslen(i,prev):    global visited,d     visited[i]=1        if len(d[i])==0:        return prev     ans=prev    for item in d[i]:        if visited[item]==0:            ans=max(ans,dfslen(item,prev+1))        return ans  n=int(input())d=[[]for _ in range(n+1)]root=[]for i in range(1,n+1):    x=int(input())    if x==-1:        root.append(i)        continue    d[x].append(i)    # d[i].append(x)# print(root,d)visited=[0]*(n+1)ans=1# print(d)for  item in root:    ans=max(ans,dfslen(item,1))print(ans)
