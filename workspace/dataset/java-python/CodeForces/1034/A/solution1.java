import java.util.*; public class Solution{        public static int gcd(int a, int b)    {        if (a == 0)            return b;                 return gcd(b%a, a);    }           public static void main(String []args)       {              Scanner sc = new Scanner(System.in);              int n = 15000001;             int prime[] = new int[n + 1];        for (int i = 0; i <= n; i++)            prime[i] = i;         for (int p = 2; p * p <= n; p++)        {            // If prime[p] is not changed, then it is a            // prime            if (prime[p] == p)            {                // Update all multiples of p                for (int i = p * p; i <= n; i += p)                    prime[i] = p;            }        }     int len = sc.nextInt();    int arr[] = new int[15000001];    int a[] = new int[n];    int g = sc.nextInt();    a[0] = g;    for(int i = 1 ; i < len;  i++)    {     a[i] = sc.nextInt();     g = gcd(g,a[i]);    }    for(int i =0 ; i < len ; i++)    {           int x = a[i];                     while(x > 1)           {                   int cnt1 = 0 , cnt2 = 0;                  int y = prime[x];                  while(x%y == 0)                  {                         x = x/y;                         cnt1++;                  }                  int temp = g;                  while(temp%y == 0)                  {                         temp = temp/y;                         cnt2++;                  }                  if(cnt1 > cnt2)                  {                  arr[y]++;                 // System.out.println(cnt1 + " " + cnt2 + " " + y);                  }           }    }        int ans = len;   // System.out.println(arr[2]);    for(int i = 1 ; i <= 15000000 ; i++)    {           if(arr[i] != 0)           {                //  System.out.println(i + " " + arr[i]);           ans = Math.min(ans,len-arr[i]);           }    }    if(ans == len)    System.out.println("-1");        else    System.out.println(ans);       }}
