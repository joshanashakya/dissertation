import java.util.*;import java.io.*;import java.lang.*;import java.math.*;public class B {    public static void main(String[] args) throws Exception {        BufferedReader bf = new BufferedReader(new InputStreamReader(System.in));        // Scanner scan = new Scanner(System.in);        PrintWriter out = new PrintWriter(new OutputStreamWriter(System.out));        // int n = Integer.parseInt(bf.readLine());        StringTokenizer st = new StringTokenizer(bf.readLine());        long p = Long.parseLong(st.nextToken());        int k = Integer.parseInt(st.nextToken());        long cur_p = p;        int counter = 0;        ArrayList<Integer> coeff = new ArrayList<Integer>();        while(cur_p != 0) {          int remainder = 0;          if(cur_p < 0) remainder = (int)((k-((-1L*cur_p) % k))%k);          else remainder = (int)(cur_p % (1L*k));          coeff.add(remainder);          cur_p -= remainder;          cur_p /= (-1*k);          counter++;        }        out.println(coeff.size());        StringBuilder sb = new StringBuilder();        for(int i=0; i<coeff.size(); i++) sb.append(coeff.get(i)+" ");        out.println(sb.toString());        // int[] a = new int[n];        // for(int i=0; i<n; i++) a[i] =  Integer.parseInt(st.nextToken());         out.close(); System.exit(0);    }  }
