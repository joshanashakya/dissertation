import java.util.Scanner; public class LoveTriangle { 	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);						int planesNumber = sc.nextInt();     sc.nextLine();						//if[0] = 5 means plane 1 likes plane 5		String[] likedPlanes = sc.nextLine().split(" ");				boolean loveTriangle = false;				for (int i = 0; i < planesNumber && !loveTriangle; i++) {			String a = likedPlanes[i];			String b = likedPlanes[Integer.parseInt(a)-1];			String c = likedPlanes[Integer.parseInt(b)-1];			String d = likedPlanes[Integer.parseInt(c)-1];						loveTriangle = d.equals(a);					}				if(loveTriangle) {			System.out.println("YES");		}		else {			System.out.println("NO");		}								sc.close(); 	} }
