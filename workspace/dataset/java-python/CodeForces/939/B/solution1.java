import javax.print.DocFlavor;import javax.swing.*;import java.awt.image.BandedSampleModel;import java.util.*; public class Example {    static long[][] dp;    //  static    int[]  ar;    static int mod = 1000000000 + 7;     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         long n =sc.nextLong();        int k =sc.nextInt();        long[] arr =new long[k];        for(int i=0;i<k;i++){            arr[i]=sc.nextLong();        }        int ind=0;        long min=Long.MAX_VALUE;        for(int i=0;i<k;i++){            if(n%arr[i]<min){                min=(n%arr[i]);                ind=i;            }        }        System.out.println((ind+1)+" "+n/arr[ind]);    }    public  static int Naser(int[] coins ,int amont,int  i){           if(amont==0)            return 0;        if(amont<0)            return 0;        if(i<0)            return 0;        return Math.min(1+Naser(coins,amont-coins[i],i),Naser(coins,amont,i-1));      }   }       		   	 	  		 	  					   		 	  	
