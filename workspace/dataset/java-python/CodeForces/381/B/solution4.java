import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*; public class CF381B {    public static void main(String[] args) {        FastReader input = new FastReader();        PrintWriter pw = new PrintWriter(System.out);         int n = input.nextInt();         int max = Integer.MIN_VALUE;         int[] freq = new int[5001];         int[] arr = new int[n];         Set<Integer> set = new TreeSet<Integer>();         for(int i = 0;i < n;i++){            arr[i] = input.nextInt();            max = Math.max(max,arr[i]);            freq[arr[i]]++;            set.add(arr[i]);        }         ArrayList<Integer> list = new ArrayList<Integer>();        for(int i : set){            if(i != max){                if(freq[i] > 1){                    list.add(i);                }            }        }             Collections.sort(list);            Collections.reverse(list);            pw.println(set.size() + list.size());             for(int i : set){                pw.print(i + " ");            }            for(int i : list){                pw.print(i + " ");            }            pw.println();           pw.flush();        pw.close();    }    static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    }}
