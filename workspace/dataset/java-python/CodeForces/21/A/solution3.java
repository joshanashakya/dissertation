import java.io.*; public class Main {    static String str;    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));     static boolean judge(char x) {        return x >= 'A' && x <= 'Z' || x >= 'a' && x <= 'z' || x >= '0' && x <= '9' || x == '_';    }     public static void main(String[] args) throws IOException {        //        br = new BufferedReader(new InputStreamReader(new FileInputStream("/Users/huangweixuan/testdata.txt")));        str = br.readLine();        boolean flag = true;        int i = 0, len = 0;        while (i < str.length() && judge(str.charAt(i))) {            ++i;            ++len;        }        if (str.charAt(i) == '@' && len > 0 && len < 17) {            len = 0;            int tmp = 0;            ++i;            if (i < str.length() && str.charAt(i) == '.') {                flag = false;            }            while (flag && i < str.length() && (judge(str.charAt(i)) || str.charAt(i) == '.')) {                ++tmp;                if (str.charAt(i) == '.') {                    len += tmp--;                    if (!(tmp > 0 && tmp < 17) || !(len > 0 && len < 32)) {                        flag = false;                        break;                    }                    tmp = 0;                }                ++i;            }            len += tmp;            if (str.charAt(i - 1) == '.') {                flag = false;            }            if (!flag || i == str.length()) {                if (!(len > 0 && len < 32)) {                    flag = false;                }                bw.write(flag ? "YES" : "NO");                bw.close();                return;            }            if (flag && i < str.length() && str.charAt(i) == '/') {                len = 0;                ++i;                while (i < str.length() && judge(str.charAt(i))) {                    ++i;                    ++len;                }                if (i == str.length() && len > 0 && len < 17) {                    flag = true;                } else {                    flag = false;                }            } else {                flag = false;            }        } else {            flag = false;        }        bw.write(flag ? "YES" : "NO");        bw.close();    }}
