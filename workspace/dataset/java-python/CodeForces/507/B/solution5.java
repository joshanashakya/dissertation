import java.util.Scanner; public class CentreMover {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int r = scanner.nextInt();        int x = scanner.nextInt();        int y = scanner.nextInt();        x -= scanner.nextInt();        y -= scanner.nextInt();         double X = (double)x/(2*r);        double Y = (double)y/(2*r);         double distance = Math.sqrt(X*X + Y*Y);         System.out.println((int)Math.ceil(distance));    }}
