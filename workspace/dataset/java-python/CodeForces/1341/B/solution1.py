import sys def answer(n, k, a):    peaks = [False] * n #n-2 would be fine too.    for i in range(1, n-1):        if a[i] > a[i-1] and a[i] > a[i+1]:            peaks[i] = True    #have peaks.    count = peaks[1:(k-1)].count(True)    best_count = count    addr = 0    for i in range(1, (n-k)+1, 1):        if peaks[i] == True:            count -= 1        if peaks[i+(k-2)] == True:            count += 1        if count > best_count:            addr = i            best_count = count     return [best_count+1, addr+1] #list of 2 ints[p, l] #l is 1-based. def main():    t = int(input())    while t:        n,k = [int(i) for i in sys.stdin.readline().split()]        a = [int(i) for i in sys.stdin.readline().split()]        print(' '.join(map(str, answer(n, k, a))))        t -= 1    returnmain()
