import java.util.*;import java.io.*;import static java.lang.Math.*; public class PracticeProblem{    /*     * This FastReader code is taken from GeeksForGeeks.com     * https://www.geeksforgeeks.org/fast-io-in-java-in-competitive-programming/     *     * The article was written by Rishabh Mahrsee     */    public static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException  e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    }     public static FastReader in = new FastReader();    public static PrintWriter out = new PrintWriter(System.out);     public static void main(String[] args)    {        solve();        out.close();    }     private static void solve()    {        char[] s1 = in.nextLine().toCharArray();        char[] s2 = in.nextLine().toCharArray();         int[][] charAfter = new int[s1.length][26];         for (int i = 0; i < s1.length; i++)            Arrays.fill(charAfter[i], -1);         for (int i = s1.length - 2; i >= 0; i--)        {            // Copy previous array            System.arraycopy(charAfter[i + 1], 0, charAfter[i], 0, 26);            charAfter[i][s1[i + 1]-'a'] = i + 1;        }        int[] indexOf = new int[26];        System.arraycopy(charAfter[0], 0, indexOf, 0, 26);        indexOf[s1[0] - 'a'] = 0;         int answer = 0;         for (int i = 0; i < s2.length; i++)        {            if (indexOf[s2[i] - 'a'] == -1)            {                out.println(-1);                return;            }             int cur = indexOf[s2[i] - 'a'];            i++;            for (; i < s2.length; i++)            {                cur = charAfter[cur][s2[i] - 'a'];                if (cur == -1)                    break;            }             i--;            answer++;        }         out.println(answer);    } }
