import java.util.Scanner; public class B1401 {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int T = in.nextInt();        for (int t=0; t<T; t++) {            int[] A = new int[3];            int[] B = new int[3];            for (int i=0; i<3; i++) {                A[i] = in.nextInt();            }            for (int i=0; i<3; i++) {                B[i] = in.nextInt();            }            int d = Math.min(B[2], A[0]);            B[2] -= d;            d = Math.min(B[2], A[2]);            B[2] -= d;            A[2] -= d;            int answer = 2*(Math.min(A[2],B[1])-B[2]);            System.out.println(answer);        }    } }
