import java.util.Arrays;import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int n,a,temp=2000000222;        n=in.nextInt();        a=in.nextInt();        if(n==1)        {            System.out.println(0);            return;        }        int arr[]=new int[n];        for(int i=0;i<n;i++)            arr[i]=in.nextInt();        Arrays.sort(arr);        int temp2;        temp=arr[n-2]-arr[0]+Math.min(Math.abs(arr[0]-a),Math.abs(arr[n-2]-a));        temp2=arr[n-1]-arr[1]+Math.min(Math.abs(arr[1]-a),Math.abs(arr[n-1]-a));        System.out.println(Math.min(temp,temp2));    }} 
