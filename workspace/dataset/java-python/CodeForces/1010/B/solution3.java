import java.io.*;import java.util.*; public class E { 	public static void main(String[] args) {        FastScanner in = new FastScanner();        PrintWriter out = new PrintWriter(System.out);        int m = in.nextInt(), n = in.nextInt();        int low = 1, high = m;        int mid = 0;        int flse[] = new int[n];                                       boolean done = false;        int i = 0;        while(i<n){                           	out.println(m); out.flush();        	int x = in.nextInt();        	if(x==0){        		done = true; break;        	}        	if(x==1) flse[i] = -1;        	else flse[i] = 1;        	i++;        }        if(done){        	out.close(); return;        }                         i = 0;                while(low<=high){        	mid = (low+high)/2;        	out.println(mid); out.flush();        	int x = in.nextInt();        	if(x==0||x==-2) break;        	if(flse[i]==1){        		if(x==-1) high = mid-1;        		else low = mid+1;         	}        	else{        		if(x==-1) low = mid+1;        		else high = mid-1;        	}        	i = (i+1)%n;                 }        out.close();	}		static class FastScanner {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer("");				String next() {			while(!st.hasMoreTokens())				try { st = new StringTokenizer(br.readLine()); }				catch(IOException e) {}			return st.nextToken();		}				String nextLine(){			try{ return br.readLine(); } 			catch(IOException e) { } return "";		}				int nextInt() {			return Integer.parseInt(next());		}				long nextLong() {			return Long.parseLong(next());		}				int[] readArray(int n) {			int a[] = new int[n];			for(int i=0;i<n;i++) a[i] = nextInt();			return a;		}	} 	static final Random random = new Random(); 	static void ruffleSort(int[] a){		int n = a.length;		for(int i=0;i<n;i++){			int j = random.nextInt(n), temp = a[j];			a[j] = a[i]; a[i] = temp;		}		Arrays.sort(a); 		}}
