/*If you want to aim high, aim highDon't let that studying and grades consume youJust live life young******************************If I'm the sun, you're the moonBecause when I go up, you go down*******************************I'm working for the day I will surpass youhttps://www.a2oj.com/Ladder16.html*/import java.util.*;import java.io.*;import java.math.*;    public class x1010B   {      public static void main(String omkar[]) throws Exception      {         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));           StringTokenizer st = new StringTokenizer(infile.readLine());         int M = Integer.parseInt(st.nextToken());         int N = Integer.parseInt(st.nextToken());         StringBuilder sb = new StringBuilder();         int[] arr = new int[N];         Arrays.fill(arr, 1);         for(int i=0; i < N; i++)         {            ask(1);            int val = Integer.parseInt(infile.readLine());            if(val == 0)               return;            if(val == -1)               arr[i] = -1;          }         int low = 1;         int high = M;         int dex = 0;         while(low != high)         {            int mid = (low+high)/2;            ask(mid);            int val = arr[dex%N]*Integer.parseInt(infile.readLine());            if(val == 1)               low = mid+1;            else if(val == -1)               high = mid-1;            else               return;            dex++;         }         ask(low);      }      public static void ask(int x)      {         System.out.println(x);      }   }
