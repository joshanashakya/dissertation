import java.util.Arrays;import java.util.Scanner;  public class SerejaAndCoatRack { 	/**	 * @param args	 */	public static void main(String[] args) {		// TODO Auto-generated method stub 		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int d = sc.nextInt();		int ans = 0;		int arr[] = new int[n];		for(int i=0;i<n;i++){			arr[i] = sc.nextInt();			ans += arr[i];		}		int m = sc.nextInt();		ans -= (m-n)*d;		if(m<n){			ans = 0;			Arrays.sort(arr);			for(int i=0;i<m;i++)				ans+=arr[i];		}		System.out.println(ans);	} }
