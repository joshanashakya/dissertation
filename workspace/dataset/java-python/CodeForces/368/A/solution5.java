import java.util.Arrays;import java.util.Scanner; /** * * @author marwa */public class Main {     public static void main(String[] args) throws java.lang.Exception {        /*------------------------------------A. Sereja and Coat Rack----------------------------------------------------**/        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int d = sc.nextInt();        int[] visitors = new int[n];        int total = 0;         for (int i = 0; i < visitors.length; i++) {            visitors[i] = sc.nextInt();         }        Arrays.sort(visitors);        int m = sc.nextInt();         int ppl, penalty;        if (m > n) {            for (int i = 0; i < n; i++) {                total += visitors[i];            }            ppl = m - n;            penalty = ppl * d;        } else {            for (int i = 0; i < m; i++) {                total += visitors[i];            }            penalty = 0;        }        int cost = total - penalty;         System.out.println(cost);     } }
