import java.util.Scanner; public class n368B {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();        int m = scanner.nextInt();        int[] numbers = new int[100010];        int[] result = new int[n];        int a;        int uniq = 0;        for (int i = 0; i < n; i++) {            a = scanner.nextInt();            result[i] = a;            if (numbers[a] == 0)                uniq++;            numbers[a]++;        }         int[] dp = new int[n];        dp[0] = uniq;        for (int i = 1; i < n; i++) {            numbers[result[i - 1]]--;            if (numbers[result[i - 1]] == 0)                uniq--;            dp[i] = uniq;        }        for (int i = 0; i < m; i++) {            a = scanner.nextInt();            System.out.println(dp[a - 1]);        }    }}
