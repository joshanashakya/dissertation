import java.util.*;import java.io.*; //1100 public class ShirtsFromSponsor {     static int[] amounts;    static int K;    static String[] shirts;     public static void main(String[] args) throws IOException    {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer(br.readLine());        amounts = new int[5];        for (int i = 0; i < 5; i++)            amounts[i] = Integer.parseInt(st.nextToken());        st = new StringTokenizer(br.readLine());        K = Integer.parseInt(st.nextToken());        shirts = new String[K];        for (int i = 0; i < K; i++)        {            st = new StringTokenizer(br.readLine());            shirts[i] = st.nextToken();        }        for (String size : shirts)        {            int index = convert(size);            if (amounts[index] > 0)            {                System.out.println(size);                amounts[index]--;            }            else            {                boolean done = false;                int diff = 1;                while (!done)                {                    //System.out.print((index + diff) + " " + (index - diff));                    if (index + diff < 5)                    {                        if (amounts[index + diff] > 0)                        {                            System.out.println(convert(index + diff));                            amounts[index+diff]--;                            done = true;                        }                    }                    if (index - diff >= 0 && !done)                    {                        if (amounts[index - diff] > 0)                        {                            System.out.println(convert(index - diff));                            amounts[index - diff]--;                            done = true;                        }                    }                    diff++;                }            }        }    }     public static int convert(String s)    {        if (s.equals("S"))            return 0;        else if (s.equals("M"))            return 1;        else if (s.equals("L"))            return 2;        else if (s.equals("XL"))            return 3;        return 4;    }     public static String convert(int i)    {        if (i == 0)            return "S";        else if (i == 1)            return "M";        else if (i == 2)            return "L";        else if (i == 3)            return "XL";        return "XXL";    }}
