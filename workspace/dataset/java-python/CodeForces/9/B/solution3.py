import sysimport logginglogging.root.setLevel(level=logging.INFO) stop_num,v_bus,v_student = map(int,sys.stdin.readline().strip().split())stops = list(map(int,sys.stdin.readline().strip().split()))destination = tuple(map(int,sys.stdin.readline().strip().split()))logging.info(f"v_bus={v_bus},v_student={v_student}, stop_num={stop_num}")logging.info(f"stops={stops}")logging.info(f"destination={destination}") timecost = [] def walk_time(begin,end=destination):    d = ((begin[0]-end[0])**2 + (begin[1]-end[1])**2)**(1/2)    return (d/v_student,d)    # return d*v_bus pre_loc = 0bus_time = 0for index,loc in enumerate(stops[1:]):    logging.info(f"index={index},loc={loc}")    bus_time += (loc-pre_loc)/v_bus    # bus_time += (loc-pre_loc)*v_student    w_t, d_s = walk_time((loc,0))    total_time = bus_time + w_t    timecost.append((total_time,d_s,index+2))    pre_loc = loc ans = min(timecost)print(ans[2])
