import java.util.*;import java.lang.Math.*;public class Main{	public static void main(String[] args) {		Scanner sc=new Scanner(System.in);		int n=sc.nextInt();		double vb=sc.nextDouble();		double vs= sc.nextDouble();		int []arr=new int[n];		for(int i=0;i<n;i++)		{		    arr[i]=sc.nextInt();		  		}		double x=sc.nextDouble();		double y=sc.nextDouble();		double temp=(arr[1]/vb)+(Math.sqrt(Math.pow((arr[1]-x),2)+Math.pow(y,2))/vs);		double dist=Math.sqrt(Math.pow((arr[1]-x),2)+Math.pow(y,2));		//System.out.println(dist);		int r=2;		double distn=0.0,distr=dist;		for(int i=2;i<n;i++)		{		   double b=(arr[i]/vb)+(Math.sqrt(Math.pow((arr[i]-x),2)+Math.pow(y,2))/vs);		    distn=Math.sqrt(Math.pow((arr[i]-x),2)+Math.pow(y,2));		    //System.out.println(distn);		   if(b<temp)		   {		       temp=b;		       r=i+1;		       distr=Math.sqrt(Math.pow((arr[i]-x),2)+Math.pow(y,2));		   }		   else if(b==temp)		   {		       if(distr>distn)		       {		           r=i+1;		       }		   }		}	    	    		System.out.println(r);	}}
