import java.io.*;import java.math.*;import java.security.*;import java.text.*;import java.util.*;  public class CodeForce {			public static void main(String[] args) throws IOException {			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		// int t=Integer.parseInt(br.readLine());		// while(t-->0)		String[] sr=br.readLine().split(" ");		int n=Integer.parseInt(sr[0]);		Double u=Double.parseDouble(sr[1]);			Double v=Double.parseDouble(sr[2]);		 sr=br.readLine().split(" ");		double[] arr=new double[n];	for(int i=0;i<n;i++)	{		arr[i]=Double.parseDouble(sr[i]);			}	 sr=br.readLine().split(" ");	 		double x=Double.parseDouble(sr[0]);		double y=Double.parseDouble(sr[1]);				double min=Integer.MAX_VALUE;		int ind=1;		for(int i=1;i<n;i++)		{			double l=arr[i];			double dis1=Math.sqrt( Math.pow((x-l),2)+Math.pow(y,2)  );			double dis2=l/u;			double time=dis1/v+dis2;			if(time<=min)			{ min=time;		ind =i+1;}			}		System.out.println(ind);						}							}      			
