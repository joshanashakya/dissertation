import java.util.*;import java.lang.*;import java.io.*; public class Solution {    	public static void main (String[] args) throws java.lang.Exception	{		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		PrintWriter out = new PrintWriter(System.out);		StringTokenizer st = new StringTokenizer(br.readLine());				int a = Integer.parseInt(st.nextToken());		int b = Integer.parseInt(st.nextToken());		int c = Integer.parseInt(st.nextToken());		int d = Integer.parseInt(st.nextToken());				a = find(a);		d = find(d); 		if(a == -1 || d == -1) out.println("Impossible");		else if( (b+c) != a*d){		    if( b==0 && c==0){		        if(a == 1 && d == 1) out.println("0");		        else if(a >1 && d == 1) out.println(print(a,0));		        else if(d>1 && a == 1) out.println(print(d,1));		        else out.println("Impossible");  		    }		  else out.println("Impossible");  		} 		else{		    StringBuilder sb = new StringBuilder();		    while(a>0){		        sb.append(0);		        a--;		    }		    a = sb.length();		    while(b>a){		        sb.append(1);		        b-=a;		    }		    if(b > 0){		       c -= (a-b);		       sb.insert(b,1);		    }		    StringBuilder result = new StringBuilder();		    while(c>0){		        result.append(1);		        c-=a;		    }		    result.append(sb);		    out.println(result.toString());		}	  out.close();	}		public static int find(double x){ 	    x = Math.sqrt(1+8*x);	    int ans = (int)x;	    	    if(x-(double)ans > 0) return -1;	     return (1+ans)/2;	}		public static String print(int x,int v){	    StringBuilder sb= new StringBuilder();	    while(x > 0){	        sb.append(v);	        x--;	    }	    return sb.toString();	}}
