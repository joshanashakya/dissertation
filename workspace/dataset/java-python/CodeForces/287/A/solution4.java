import java.util.*;import java.io.*; public class _contest { 	public static void main(String args[])	{ 		FastReader in=new FastReader();		PrintWriter so = new PrintWriter(new BufferedWriter(new  OutputStreamWriter(System.out)));		char a[][] = new char[4][4];		for(int i=0 ; i<4 ; i++)		{		    String s = in.next();		    for(int j=0 ; j<4 ; j++)		    {		        a[i][j]=s.charAt(j);		    }		}		int flag=0;		 outer:   for(int i=0 ; i<3 ; i++)		    {		        int c1=0 , c2=0 ;		        for(int j=0 ; j<3 ; j++)		        {		            if(a[i][j]=='#')c1++;else c2++;		            if(a[i][j+1]=='#')c1++;else c2++;		            if(a[i+1][j]=='#')c1++;else c2++;		            if(a[i+1][j+1]=='#')c1++;else c2++; 			        if(c1>=3 || c2>=3 )			        {			            flag=1;                        break outer;			        }			        c1=c2=0;		        }		       		    }		    if(flag==0) so.println("NO");		    else so.println("YES");		so.flush();	} 	static class FastReader	{ 		BufferedReader br; 		StringTokenizer st;  		public FastReader() 		{ 			br = new BufferedReader(new InputStreamReader(System.in)); 		}  		String next() 		{ 			while (st == null || !st.hasMoreElements()) 			{ 				try				{ 					st = new StringTokenizer(br.readLine()); 				} 				catch (IOException  e) 				{ 					e.printStackTrace(); 				} 			} 			return st.nextToken(); 		}  		int nextInt() 		{ 			return Integer.parseInt(next()); 		}  		long nextLong() 		{ 			return Long.parseLong(next()); 		}  		double nextDouble() 		{ 			return Double.parseDouble(next()); 		}  		int[] readIntArray(int n)		{			int a[]=new int[n];			for(int i=0;i<n;i++)a[i]=nextInt();			return a;		} 		long[] readLongArray(int n)		{			long a[]=new long[n];			for(int i=0;i<n;i++)a[i]=nextLong();			return a;		} 		String nextLine() 		{ 			String str = ""; 			try			{ 				str = br.readLine(); 			} 			catch (IOException e) 			{ 				e.printStackTrace(); 			} 			return str; 		} 	} }
