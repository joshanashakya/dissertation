import java.util.HashSet;import java.util.Scanner; public class TolikAndHisUncle { 	public static void main(String[] args) 	{		boolean debug = false;		HashSet<String> set = new HashSet<>();		Scanner sc = new Scanner(System.in);				int w = sc.nextInt();		int h = sc.nextInt();				int x = 1;		int y = 1;		int lastx = 1;		int lasty = 1;				int tot = w*h;		int don = 0;				int dx, dy;		StringBuilder sb = new StringBuilder();				for(int xdis = 0; xdis <= w/2; xdis++)		{			x = xdis + 1;			int x2 = w - x + 1;			for(int ydis = 0; ydis < h; ydis++)			{				if(don >= w*h)break;				y = ydis+1;				int y2 = h - y + 1;//				if(h%2 == 0 && xdis == w/2 && ydis >= h/2)break;				//				dx = x-lastx; dy = y-lasty; lastx = x; lasty = y;//				if(set.contains(dx + " " + dy)) {System.out.println("ERROR " + dx + " " + dy);}else if(debug){System.out.println("GO " + dx + " " + dy);} set.add(dx + " " + dy);								sb.append(x + " " + y + "\n");				don++;				if(don >= w*h)break;//				if(xdis == w/2 && ydis >= h/2)break;				//				dx = x2-lastx; dy = y2-lasty; lastx = x2; lasty = y2;//				if(set.contains(dx + " " + dy)) {System.out.println("ERROR " + dx + " " + dy);}else if(debug){System.out.println("GO " + dx + " " + dy);} set.add(dx + " " + dy);								sb.append(x2 + " " + y2 + "\n");				if(don >= w*h)break;				don++;			}//			System.out.println(x + " " + x2); 		}				System.out.println(sb);	} }
