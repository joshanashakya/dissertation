def isLeapYear(year):    return ((year % 400) == 0) or (  ((year % 4) == 0) and not ((year % 100) == 0)  )   year = int(input()) newYear = yearisLeap = isLeapYear(year) offset = 0while True:    #add year    offset = (offset + 365 + (1 * isLeapYear(newYear))) % 7    newYear += 1     if offset == 0 and isLeap == isLeapYear(newYear):        print(newYear)        break     if newYear > (year + 2000):        print("couldn't find year")        break 			 				  			 	 			 	  			  	
