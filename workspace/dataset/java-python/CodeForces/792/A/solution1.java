import java.util.Arrays;import java.util.Scanner;public class Main {	public static void main(String args[])	{		Scanner in=new Scanner(System.in);		int n=in.nextInt();		Long a[]=new Long[n];		for(int i=0;i<n;i++)			a[i]=in.nextLong();		Arrays.sort(a);		int count=0;		long min=Integer.MAX_VALUE;		for(int i=0;i<n-1;i++)		{			if(Math.abs(a[i]-a[i+1])==min)				count++;			if(Math.abs(a[i]-a[i+1])<min)			{				min=Math.abs(a[i]-a[i+1]);				count=1;			} 		}  		System.out.println(min+" "+count);	}}
