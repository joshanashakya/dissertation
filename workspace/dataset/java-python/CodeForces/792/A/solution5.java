import java.util.Arrays;import java.util.Scanner; public class BusRoute {    public static void main(String[] args) {        Scanner in= new Scanner(System.in);        int n=in.nextInt();        Integer [] stations= new Integer[n];        for (int i=0; i<n; i++){            stations[i]= in.nextInt();        }        Arrays.sort(stations);        int sd= stations[1]-stations[0];        for (int i=1; i<n-1; i++){            if(stations[i+1]-stations[i]<sd){                sd=stations[i+1]-stations[i];            }        }        int count=0;        for (int i=n-1; i>0; i--){            for (int j=i-1; j>=0; j--){                if ((stations[i]-stations[j])>sd){                    break;                }                if ((stations[i]-stations[j])==sd){                    count++;                    if (j!=0){                        if (stations[j]!=stations[j-1]){                            break;                        }                    }                }            }        }        System.out.print(sd+" "+count);    }}
