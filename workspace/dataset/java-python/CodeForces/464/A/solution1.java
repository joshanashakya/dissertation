#include<bits/stdc++.h>using namespace std;#define all(a)      a.begin(),a.end()#define bitcount    __builtin_popcount#define mp          make_pair#define pie         3.14159265359#define int         long long #define double      long double#define mod         1000000007#define pb          push_back#define ff          first#define ss          second#define endl        "\n"#define inf         INT_MAX;#define infi        LLONG_MAX;#define pii         pair<int,int>#define ppii        pair<int, pair<int,int>>  int fact[10000001] = {0}; int gcd(int a, int b) {     if (b == 0)        return a;    return gcd(b, a % b);} void Find_Factorial(int max) {     fact[0] = 1;    for(int i=1 ; i <= max ; i++)        fact[i]=(fact[i-1]*i)%mod;} int binpow(int a, int b) {     int res = 1;    while(b > 0) {        if(b & 1)            res = (res * a)%mod;        b = b>>1;        a = (a*a)%mod;    }     return res;} int nCr(int n, int r) {     if(r < 0 || r > n)        return 0;    int val = fact[n];    val *= (binpow(fact[r], mod - 2)%mod);    val %= mod;    val *= (binpow(fact[n-r], mod - 2)%mod);    val %= mod;    return val;} string s;int n, p; bool check(char now, int ind){    if(ind < 0 || ind >= n)        return true;        if(now != s[ind])        return true;    return false;} void test_case() {     cin >> n >> p;    cin >> s;     for(int i=n-1 ; i>=0 ; i--)    {        for(char now=s[i]+1 ; now<'a'+p ; now++)        {            if(check(now, i-1) && check(now, i-2))            {                s[i] = now;                for(int j=i+1 ; j<n ; j++)                {                    for(char val='a' ; val<'a'+p ; val++)                    {                        if(check(val, j-1) && check(val, j-2))                        {                            s[j] = val;                            break;                        }                    }                }                 cout << s << endl;                return;            }        }    }     cout << "NO" << endl;     return; }signed main() {     ios_base::sync_with_stdio(false);    cin.tie(NULL); cout.tie(NULL);    // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);    int t;    t = 1;    //cin >> t;     while (t--) test_case();     return 0;}    
