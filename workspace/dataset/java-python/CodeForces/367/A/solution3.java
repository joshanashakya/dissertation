/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc=new Scanner(System.in);		String s=sc.next();		int m=sc.nextInt();		int n=s.length();		int[] cx=new int[n+1];		int[] cy=new int[n+1];		int[] cz=new int[n+1];				for(int i=1;i<=s.length();i++)		{			cx[i]+=cx[i-1];			cy[i]+=cy[i-1];			cz[i]+=cz[i-1];						if(s.charAt(i-1)=='x')			cx[i]++;			else if(s.charAt(i-1)=='y')			cy[i]++;			else			cz[i]++;		}				for(int i=0;i<m;i++)		{			int l=sc.nextInt();			int r=sc.nextInt();			int k=r-l+1;			if(k<3)			System.out.println("YES");			else			{				int x=cx[r]-cx[l-1];				int y=cy[r]-cy[l-1];				int z=cz[r]-cz[l-1];				if(Math.abs(x-y)<=1 && Math.abs(y-z)<=1 && Math.abs(x-z)<=1)				System.out.println("YES");				else				System.out.println("NO");			}		}					}}
