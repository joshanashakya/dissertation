import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.Scanner; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        BBrowser solver = new BBrowser();        solver.solve(1, in, out);        out.close();    }     static class BBrowser {        public void solve(int testNumber, Scanner in, PrintWriter out) {            int n = in.nextInt();            int p = in.nextInt();            int l = in.nextInt();            int r = in.nextInt();            int res = 0;             int distToL = Math.abs(p - l);            int distToR = Math.abs(r - p);            if (l > 1 && r < n) {                res += Math.min(distToL, distToR);                res++;                res += r - l;                res++;            } else {                if (l > 1) {                    res += distToL;                    res++;                } else if (r < n) {                    res += distToR;                    res++;                }            }            out.println(res);        }     }} 
