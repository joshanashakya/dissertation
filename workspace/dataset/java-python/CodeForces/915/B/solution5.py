'''Рассмотрим такой случай:6  3  2 5n pos l r 1 2 3 4 5 6    ^  ^ ^ ^ ^ Вначале закрываем вкладки слева, т.к. pos - l < r - pos.Для того, чтобы закрыть все вкладки слева необходимо перейти к позиции l [и затратить при этом abs(pos - l) секунд].Далее следует перейти к позиции r [и затратить при этом abs(r - pos) секунд].'''(n, pos, l, r) = map(int, input().split())res = 0if pos - l < r - pos:    if l != 1: # проверяем, а нужно ли вообще закрывать вкладки слева        res += abs(pos - l) + 1        pos = l    if r != n: # проверяем, а нужно ли вообще закрывать вкладки справа        res += abs(r - pos) + 1else:    if r != n: # проверяем, а нужно ли вообще закрывать вкладки справа        res += abs(r - pos) + 1        pos = r    if l != 1: # проверяем, а нужно ли вообще закрывать вкладки слева        res += abs(pos - l) + 1print(res)
