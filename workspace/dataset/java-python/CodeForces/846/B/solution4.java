/*If you want to aim high, aim highDon't let that studying and grades consume youJust live life young******************************If I'm the sun, you're the moonBecause when I go up, you go down*******************************I'm working for the day I will surpass youhttps://www.a2oj.com/Ladder16.html*/import java.util.*;import java.io.*;import java.math.*;    public class x846B   {      public static void main(String omkar[]) throws Exception      {         BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));           StringTokenizer st = new StringTokenizer(infile.readLine());         int N = Integer.parseInt(st.nextToken());         int K = Integer.parseInt(st.nextToken());         int M = Integer.parseInt(st.nextToken());         int[] arr = new int[K];         st = new StringTokenizer(infile.readLine());         for(int i=0; i < K; i++)            arr[i] = Integer.parseInt(st.nextToken());         Arrays.sort(arr);         int res = 0;         for(int complete=0; complete <= N; complete++)         {            long sum = 0L;            for(int x: arr)               sum += x;            sum *= complete;            if(sum > M)               break;            int temp = complete*(K+1);            //lol            ArrayList<Integer> ls = new ArrayList<Integer>();            for(int i=complete; i < N; i++)               for(int b=0; b < K-1; b++)                  ls.add(arr[b]);            Collections.sort(ls);            for(int x: ls)            {               if(sum+x > M)                  break;               sum += x;               temp++;            }            res = Math.max(res, temp);         }         System.out.println(res);      }   }
