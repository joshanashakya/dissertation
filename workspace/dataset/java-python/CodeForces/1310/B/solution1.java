/* ID: tommatt1 LANG: JAVA TASK: */import java.util.*;import java.io.*;public class cf1310b{ 	public static void main(String[] args)throws IOException {		PrintWriter out = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));		BufferedReader bf=new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st=new StringTokenizer(bf.readLine());		int n=Integer.parseInt(st.nextToken());		int k=Integer.parseInt(st.nextToken());		if(k==0) {			out.println(0);			out.close();			System.exit(0);		}		int bb=(1<<(n-1));		st=new StringTokenizer(bf.readLine());		int[][] dp=new int[1<<n][4];		for(int i=0;i<(1<<n);i++) {			Arrays.fill(dp[i], -(int)Math.pow(10, 9));			dp[i][0]=0;		}		for(int i=0;i<k;i++) {			int a1=Integer.parseInt(st.nextToken())-1;			a1/=2;			a1+=bb;			dp[a1][3]=dp[a1][1];			dp[a1][1]=dp[a1][2]=1;		}		for(int b=bb-1;b>0;b--) {			for(int i=0;i<4;i++) {				for(int j=0;j<4;j++) {					dp[b][i|j]=Math.max(dp[b][i|j], dp[2*b][i]+dp[2*b+1][j]+(i|j));				}			}		}		out.println(Math.max(Math.max(dp[1][0], dp[1][1]),Math.max(dp[1][2],dp[1][3]))+1);		out.close();	} }
