import java.util.*; public class Solution{       public static void main(String []args)       {             Scanner sc = new Scanner(System.in);             int n = sc.nextInt();             Stack<Integer> st = new Stack<Integer>();             int arr[] = new int[n];             for(int i = 0 ; i < n ; i++)             {                    arr[i] = sc.nextInt();             }             int max = 0;             for(int i = 0 ; i < n ; i++)             {              if(i>0)              max = Math.max(max,arr[i]^arr[i-1]);                            while(st.size() > 0 && st.peek() < arr[i])              {                     int x = st.pop();                     max = Math.max(max,x^arr[i]);                                   }              if(st.size() > 0)              max = Math.max(max,arr[i]^st.peek());                            st.push(arr[i]);             }             System.out.println(max);       }}
