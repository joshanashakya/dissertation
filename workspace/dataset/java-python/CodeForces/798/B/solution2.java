import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Cat_Furrier {	public static boolean samechars(String[] st)	{		int[] ar = new int[26];		for(int i=0;i<st[0].length();i++)ar[st[0].charAt(i)-'a']++;				for(int j=1;j<st.length;j++)		{			int[] tar = new int[26];			for(int i=0;i<st[j].length();i++)tar[st[j].charAt(i)-'a']++;			if(!Arrays.equals(ar, tar))return false;		}		return true;	}		public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int n =sc.nextInt();		String[] st = new String[n];		for(int i=0;i<n;i++)st[i]=sc.next();				boolean ispossible = samechars(st);		if(ispossible)		{			int min=Integer.MAX_VALUE;			for(int i=0;i<n;i++)			{				int sum=0;				for(int j=0;j<n;j++)				{					if(j==i)continue;					String temp = st[j]+st[j];					if(temp.indexOf(st[i])==-1) {sum = Integer.MAX_VALUE;break;}					sum += temp.indexOf(st[i]);				}				min = Math.min(min, sum);			}			System.out.println((min<Integer.MAX_VALUE)?min:-1);		}		else		{			System.out.println(-1);		}						}		}
