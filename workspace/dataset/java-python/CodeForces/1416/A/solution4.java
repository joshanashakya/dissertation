import java.util.*;public class KamazingNumbers { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int t = sc.nextInt();		while (t-- > 0) {			int n = sc.nextInt();			int[] arr = new int[n];			for (int i = 0; i < n; i++) {				arr[i] = sc.nextInt();			}			int[] last = new int[n + 1];			int[] max_diff = new int[n + 1];			Arrays.fill(last, -1);			Arrays.fill(max_diff, -1);			for (int i = 0; i < n; i++) {				max_diff[arr[i]] = Math.max(max_diff[arr[i]], i - last[arr[i]]);				last[arr[i]] = i;			}			for (int k = 1; k <= n; k++) {				max_diff[k] = Math.max(max_diff[k], n - last[k]);			} 			int[] nums = new int[n + 2];			Arrays.fill(nums, Integer.MAX_VALUE); 			for (int k = 1; k <= n; k++) {				nums[max_diff[k]] = Math.min(nums[max_diff[k]], k);			}			int min = Integer.MAX_VALUE;			StringJoiner sj = new StringJoiner(" ");			for (int k = 1; k <= n; k++) {				min = Math.min(min, nums[k]);				if (min == Integer.MAX_VALUE)					sj.add("-1");				else					sj.add("" + min);			} 			System.out.println(sj.toString());		}	} }
