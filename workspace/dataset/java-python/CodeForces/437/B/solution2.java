import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}			public static void main(String[] args) throws IOException {				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int sum = io.nextInt();		int limit = io.nextInt();				int s = 0;		int c = 0;				StringBuilder sb = new StringBuilder();				for(int i = limit; i > 0; i--) {						int num = lowbit(i);									if(s + num <= sum) {				s += num;				c++;			sb.append(i + " ");			}		}				if(s == sum) {			System.out.println(c);			System.out.println(sb);		}		else {			System.out.println(-1);		}					}		static int lowbit(int num) {						for(int i = 0; i < 32; i++) {						int mask = 1 << i;						if((num & mask) != 0) {				return (int)Math.pow(2, i);			}					}				return 0;	}}
