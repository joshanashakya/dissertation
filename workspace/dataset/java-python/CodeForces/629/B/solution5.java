import java.io.BufferedReader;import java.io.InputStream;import java.io.InputStreamReader;import java.util.ArrayList;import java.util.Arrays;import java.util.Scanner;import java.util.StringTokenizer;  public class relative { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int s[] = new int[n];		int e[] = new int[n];		char gender[] = new char[n];		for(int i = 0;i < n;i++) {			gender[i] = sc.next().charAt(0);			s[i] = sc.nextInt();			e[i] = sc.nextInt();		}		int ans = 0;		int boys[] = new int[367];		int girls[] = new int[367];		for(int i = 0;i < n;i++) {			for(int j = s[i];j <= e[i];j++) {				if(gender[i]=='M')					boys[j]++;				else					girls[j]++;			}		}		for (int i = 0; i < 367; i++) {			ans = Math.max(ans, Math.min(boys[i], girls[i])*2);		}		System.out.println(ans);			} 	static boolean check(int s1,int e1,int s2,int e2) {		return e1 < s2 || e2 < s1;	}	//	private static class Scanner {//		public BufferedReader reader;//		public StringTokenizer st;////		public Scanner(InputStream stream) {//			reader = new BufferedReader(new InputStreamReader(stream));//			st = null;//		}////		public String next() {//			while (st == null || !st.hasMoreTokens()) {//				try {//					String line = reader.readLine();//					if (line == null)//						return null;//					st = new StringTokenizer(line);//				} catch (Exception e) {//					throw (new RuntimeException());//				}//			}//			return st.nextToken();//		}////		public int nextInt() {//			return Integer.parseInt(next());//		}////		public long nextLong() {//			return Long.parseLong(next());//		}////		public double nextDouble() {//			return Double.parseDouble(next());//		}//	} }
