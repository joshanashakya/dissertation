import java.util.HashMap;import java.util.Map;import java.util.Scanner;import java.util.Set; public class UniqueBidAuction {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int cases = sc.nextInt();        while (cases>0){            // here n is the number of participants            int n = sc.nextInt();            int [] allParticipants = new int[n];            Map<Integer,Integer> allRecords = new HashMap<>();            for(int i=0; i<n; i++){                allParticipants[i]=sc.nextInt();                  if(allRecords.get(allParticipants[i])==null){                    allRecords.put(allParticipants[i],1);                }else {                    int rep = allRecords.get(allParticipants[i]);                    allRecords.replace(allParticipants[i],++rep);                }            }            Set<Integer> keys = allRecords.keySet();            int min = Integer.MAX_VALUE;            for (Integer i:keys){                int val = allRecords.get(i);                if(val == 1){                    if(i<min)                        min = i;                }            }            for(int i=0; i<n; i++){                if(allParticipants[i]==min){                    System.out.println((i+1));                    break;                }            }            if(min == Integer.MAX_VALUE)                System.out.println(-1);            cases--;        }    }}
