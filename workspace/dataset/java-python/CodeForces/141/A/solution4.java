/* package whatever; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static void main (String[] args) throws java.lang.Exception	{	Scanner scan=new Scanner(System.in);	String s1=scan.next();	String s2=scan.next();	String s=scan.next();	String p=s1+s2;	char[] arrayOfP=p.toCharArray();	Arrays.sort(arrayOfP);	String x=new String(arrayOfP);	char[] arrayOfS=s.toCharArray();	Arrays.sort(arrayOfS);	String y=new String(arrayOfS);	if(x.equals(y))	{		System.out.print("YES");	}	else	{		System.out.print("NO");	}	}}
