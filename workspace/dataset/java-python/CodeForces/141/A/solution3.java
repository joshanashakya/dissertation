import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Ideone{	public static String sort(String inputString)    {        // convert input string to char array        char tempArray[] = inputString.toCharArray();                  // sort tempArray        Arrays.sort(tempArray);                  // return new sorted string        return new String(tempArray);    }	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc=new Scanner(System.in);				String guest=sc.nextLine();		String host=sc.nextLine();		String pile=sc.nextLine(); 		if(guest.length()+host.length()!=pile.length())			System.out.println("NO");		else{						String pile_new=new String();			pile_new=sort(host+guest);			pile=sort(pile);									if(!pile_new.equals(pile))				System.out.println("NO");			else				System.out.println("YES");		}	}}
