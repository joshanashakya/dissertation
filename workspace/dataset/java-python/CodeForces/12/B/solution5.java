import java.io.*;import java.util.*; public class Main {    static final int N = 15;    static String str1, str2;    static Character[] arr = new Character[N];    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));     public static void main(String[] args) throws IOException {        //        br = new BufferedReader(new InputStreamReader(new FileInputStream("/Users/huangweixuan/testdata.txt")));        str1 = br.readLine();        str2 = br.readLine();        if (str1.length() != str2.length()) {            bw.write("WRONG_ANSWER");            bw.close();            return;        }        for (int i = 0; i < str1.length(); ++i) {            arr[i] = str1.charAt(i);        }        Arrays.sort(arr, 0, str1.length());        //        Arrays.sort(arr, 0, str1.length(), Comparator.reverseOrder());        if (arr[0] == '0' && str1.length() > 1) {            int i = 1;            while (i < str1.length() && arr[i] == '0') {                ++i;            }            Character tmp = arr[i];            arr[i] = arr[0];            arr[0] = tmp;        }        for (int i = 0; i < str2.length(); ++i) {            if (arr[i] != str2.charAt(i)) {                bw.write("WRONG_ANSWER");                bw.close();                return;            }        }        bw.write("OK");        bw.close();    }}
