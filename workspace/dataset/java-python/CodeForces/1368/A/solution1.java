import java.util.*; public class Runner {	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);				int tests = sc.nextInt();				for(int i = 0; i < tests; i++) {			int a = sc.nextInt();			int b = sc.nextInt();			int n = sc.nextInt();						int count = 0;			while(a <= n && b <= n) {				int smaller = getSmaller(a, b);				if(smaller == a) {					a += b;				} else {					b += a;				}								count++;			}						System.out.println(count); 		}					}		private static int getSmaller(int a, int b) {		if(a < b) {			return a;		}		return b;	}		private static int[] stringToIntArray(String str) {		String[] strs = str.trim().split(" ");				int[] intArr = new int[strs.length];						for(int i = 0; i < strs.length; i++) {			intArr[i] = Integer.parseInt(strs[i]);		}				return intArr;	} } 	     	   		 	    			  	  	  	
