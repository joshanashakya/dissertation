import java.util.Scanner; public class A1368 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        for (int i = 0; i < t; i++) {            int a = scanner.nextInt(), c, k=0, b = scanner.nextInt(), n = scanner.nextInt();            if (a < b) {                c = a;                a = b;                b = c;            }            do {                if (k % 2 == 0) {                    b += a;                    c = b;                } else {                    a += b;                    c = a;                }                k++;            } while (c <= n);            System.out.println(k);        }    }}
