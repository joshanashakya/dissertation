    import java.io.*;    import java.util.*;    import java.text.*;    import java.math.*;    import java.util.regex.*; public class Solution {               public static long mod = 1000000007;     public static void main(String[] args) throws IOException{   //         BufferedReader bf = new BufferedReader(new InputStreamReader(System.in));         PrintWriter  pw = new PrintWriter(System.out);         Scanner in = new Scanner(System.in);                          String s = in.nextLine();         int max = -1;            int m = 1000000;         int n = s.length();      //   pw.print(s.substring(1, 2));         for(int i=0; i<n; i++)         {             for(int j=i+1; j<n; j++)             {                    String  s1 = s.substring(0, i);                    String s2 = s.substring(i, j);                    String s3 = s.substring(j, n);                    //pw.println(s1+" "+s2+" "+s3);                                     if(s1.length()>0 && s2.length()>0 && s3.length()>0                    && s1.length()<=7 && s2.length()<=7 && s3.length()<=7)                 {                     int d1 = Integer.parseInt(s1);                                          int d2 = Integer.parseInt(s2);                     int d3 = Integer.parseInt(s3);                     boolean flag = false;                     if(s1.charAt(0)=='0' && s1.length()>1)                         flag = true;                     if(s2.charAt(0)=='0' && s2.length()>1)                           flag = true;                     if(s3.charAt(0)=='0' && s3.length()>1)                           flag = true;                     if(d1>m || d2>m || d3>m)                         flag = true;                     if(!flag)                     {                     // pw.println(s1+" "+s2+" "+s3+");                     //pw.println(d1+" "+d2+" "+d3);                     max = Math.max(max, d1+d2+d3);                     }                 }                                 }         }         pw.print(max);                                         pw.flush();         pw.close();        }    }
