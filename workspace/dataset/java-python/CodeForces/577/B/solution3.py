import sys n, m = map(int,input().split())arr = list(map(int, input().split())) if n >= m or 0 in arr:    print("YES")    sys.exit(0) dp = [False] * m for val in arr:    val = val % m    dp1 = dp.copy()    dp1[val] = True    for i in range(m):        if dp[i]:            dp1[(i + val) % m] = True    dp, dp1 = dp1, dp    if dp[0]:        print("YES")        sys.exit(0) print("NO") #  print(dp,arr)
