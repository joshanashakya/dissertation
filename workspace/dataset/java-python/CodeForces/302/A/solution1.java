import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}				static int gcd(int a, int b) {						if(b == 0) {				return a;			}						return gcd(b, a % b);		}			public static void main(String[] args) throws IOException {				FastIO io = new FastIO();		PrintWriter out = new PrintWriter(System.out);				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = io.nextInt();		int m = io.nextInt();				int arr[] = new int[n];		int neg = 0;				for(int i = 0; i < n; i++) {			arr[i] = io.nextInt();			if(arr[i] < 0) {				neg++;			}		}				for(int i = 1; i <= m; i++) {			int l = io.nextInt();			int r = io.nextInt();						if(l == r) {				out.println(0);			}			else if(((r - l + 1) & 1) == 1) {				out.println(0);			}			else {				int len = r - l + 1;				if((neg >= len / 2) && ((n - neg) >= len / 2)) {					out.println(1);				}				else {					out.println(0);				}			}		}		out.flush();		out.close();	}}
