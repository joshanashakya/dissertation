import java.util.*; public class Main{     public static void main(String[] args)    {        Scanner input = new Scanner(System.in);        int n = input.nextInt();        int m = input.nextInt();        long time = 0;        ArrayList<Long> a = new ArrayList<>();        for (int i = 0; i < n; i++) {            int c = input.nextInt();            int t = input.nextInt();            time += c * t;            a.add(time);         }//        System.out.println(a);        StringBuilder ans = new StringBuilder();        for (int i = 0; i < m; i++) {            long x = input.nextLong();            long prev = 0;            int index = Collections.binarySearch(a, x);            if (index < 0) {                ans.append(-index+"\n");            } else {               ans.append((index + 1)+"\n");            }//            System.out.println(index);        }        System.out.println(ans);     } }
