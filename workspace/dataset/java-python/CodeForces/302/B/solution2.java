import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Cat_Furrier {			public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int n  =sc.nextInt();		int m  =sc.nextInt();		int[] ar = new int[n+1];		for(int i=1;i<=n;i++)		{			int time = sc.nextInt()*sc.nextInt();			ar[i] = ar[i-1]+time;		}				while(m-- > 0)		{			int st=1,end=n,song=-1;			int pos = sc.nextInt();			while(st<=end)			{				int mid = st + (end-st)/2;				if(pos > ar[mid])st=mid+1;				else				{					song=mid;					end=mid-1;				}			}						System.out.print(song+" ");				}				}		}
