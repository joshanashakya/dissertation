import java.util.Scanner; public class Playlist {    public static void main(String[] args) {        Scanner scan = new Scanner(System.in);         int n = scan.nextInt();        int m = scan.nextInt();        Integer[] duration = new Integer[n+1];        duration[0] = 0;         for (int i = 1; i < n+1; i++) {            duration[i] = duration[i-1] + scan.nextInt()*scan.nextInt();        }         int current = 1;        StringBuilder ans = new StringBuilder();         for (int i = 0; i < m; i++) {            int v = scan.nextInt();            for (int j = current; j < n+1; j++) {                if(duration[j] >= v){                    ans.append(j + "\n");                    current = j;                    break;                }            }        }        System.out.println(ans);    }}
