import java.io.*; //https://codeforces.com/problemset/problem/379/Apublic class NewYearCandles {     private static BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));    private static BufferedWriter writer = new BufferedWriter(new OutputStreamWriter(System.out));     public static void main(String[] args) {         try {            String[] strings = reader.readLine().split(" ");            int a = Integer.parseInt(strings[0]);            int b = Integer.parseInt(strings[1]);             int count = 0;            int rem = 0;            while (a > 0) {                 count = count + a;                rem = rem + a % b;                a = a / b;                 if (rem / b >= 0) {                    a = a + rem / b;                    rem = rem % b;                }             }             writer.write(String.valueOf(count));             reader.close();            writer.close();        } catch (IOException e) {            e.printStackTrace();        }     }}
