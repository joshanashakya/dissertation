n = int(input()) import sysdef get_ints(): return list(map(int, sys.stdin.readline().strip().split()) ) arr = get_ints() arr = sorted(arr)arr = arr[::-1]maxs = arr[0]times = 0 for index, elem in enumerate(arr):    if index == n-1:        break     times += maxs - elem  lastelem = arr[-1]# print(times)# print(lastelem)while times < lastelem:    times += n     times -= 1     maxs += 1 print(maxs)     
