def binSearch():	l, r = max_, min_ + max_	while l < r:		games = l + (r-l)//2		if games*n-total > games:			r = games		elif games*n-total < games:			l = games+1		else:			return games	return l  n = int(input())rounds = list(map(int, input().split()))max_, min_, total = 0, float('inf'), 0for i in range(n):	max_ = max(max_, rounds[i])	min_ = min(min_, rounds[i])	total += rounds[i] print(binSearch())
