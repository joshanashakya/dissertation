import java.util.LinkedHashSet;import java.util.Scanner;import java.util.Set; public class i_Wanna_Be_The_Guy {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         int n = sc.nextInt();        int p = sc.nextInt();        Set<Integer> elements = new LinkedHashSet<>();         while (p-- >= 1) {            elements.add(sc.nextInt());        }        int q = sc.nextInt();        while (q-- >= 1) {            elements.add(sc.nextInt());        }        if (elements.size() == n) {            System.out.println("I become the guy.");        } else            System.out.println("Oh, my keyboard!");    }}
