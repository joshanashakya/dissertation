// ctrl+shift+o all unrecognised classes are automatically imported in code//ctrl+I to indent the code properly// ctrl+D to delete the whole current line// ctrl+space gives the autocomplete recomendation// sysout and ctrl+space gives println() statement// alt+shift+r TO rename the file// ctrl+f11 to run the code import java.util.*;public final class test{    public static void main(String[] args) {    	Scanner s=new Scanner(System.in);    	int n=s.nextInt();    	int m1=s.nextInt();    	Set<Integer>set=new HashSet<>();    	for(int i=0;i<m1;i++)    		set.add(s.nextInt());    	int m2=s.nextInt();    	for(int i=0;i<m2;i++)    		set.add(s.nextInt());    	if(n==set.size())    		System.out.print("I become the guy.");    	else    		System.out.print("Oh, my keyboard!");    	s.close();    	    	    } }
