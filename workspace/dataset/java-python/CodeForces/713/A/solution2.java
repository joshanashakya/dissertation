/****************************************************************************** *******************************************************************************/import java.util.*;public class Main{    static HashMap<Long,Integer> h = new HashMap<>();    public static void query(char c, long num)    {       if(c=='?')       {             if(h.containsKey(num))           System.out.println(h.get(num));           else           System.out.println("0");       }else if(c=='+' || c=='-')       {           String s ="";           while(num!=0)           {            long i = num%10;           if(i%2 ==0)            s += "0";            else            s +="1";            num = num/10;           }            StringBuilder sb = new StringBuilder();            sb.append(s);            sb = sb.reverse();            String temp = new String(sb);             num = Long.parseLong(temp,10);            if(c=='+')            {                if(h.containsKey(num))                h.put(num,h.get(num)+1);                else                h.put(num,1);            }else            {                h.put(num,h.get(num)-1);            }       }     }	public static void main(String[] args)	{		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		for(int i=0;i<n;i++)		{		    char ch = sc.next().charAt(0);		    long num = sc.nextLong();		    query(ch,num);		}			}} 
