from sys import stdinfrom sys import stdoutfrom collections import defaultdict r = set('02468') def mask(n):    result = ''    for x in n:        if x in r:            result += '0'        else:            result += '1'    return int(result)    n = int(stdin.readline())a = defaultdict(int)for _ in range(n):    command, x = stdin.readline().strip().split(' ')    m = mask(x)    if command == '+':        a[m] += 1    if command == '-':        a[m] -= 1    if command == '?':        stdout.write(str(a[m]) + '\n')        stdout.flush()
