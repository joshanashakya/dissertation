class LN:    data = None    left = None    right = None     def __init__(self, data, left, right):        self.data = data        self.left = left        self.right = right inst = input() root = LN(1, None, None)ptr = rooti = 2 for j in range(len(inst)-1):    if inst[j] == 'l':        ptr.left = LN(i, ptr.left, ptr)        if ptr.left.left:            ptr.left.left.right = ptr.left        ptr = ptr.left        if root.left:            root = root.left    else:        ptr.right = LN(i, ptr, ptr.right)        if ptr.right.right:            ptr.right.right.left = ptr.right        ptr = ptr.right     i += 1 trav = rootprint(root.data) while trav.right:    trav = trav.right    print(trav.data)
