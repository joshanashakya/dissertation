import java.lang.*;import java.util.*; public class Abhishek {    public static void main(String[] args) throws Exception {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        sc.nextLine();        StringBuilder s = new StringBuilder(sc.nextLine());        char max = s.charAt(0);        boolean fl = false;        for (int i = 1; i < n; i++) {            max = (char)Math.max(s.charAt(i),s.charAt(i-1));            if(s.charAt(i-1)>s.charAt(i)) {                s.deleteCharAt(i - 1);                fl = true;                break;            }         }        if(!fl){            s.deleteCharAt(s.indexOf(String.valueOf(max)));        }         System.out.println(s);    }}
