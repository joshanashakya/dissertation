import math def is_prime(n):    if n == 1:        return False     for i in range(2, int(math.sqrt(n))+1):        if n%i == 0:            return False     return True def sieve(n):    ass = []    is_prime = [True]*(n+1)    is_prime[0] = False    is_prime[1] = False     for i in range(2, int(math.sqrt(n))+1):        if not is_prime[i]:            continue        for j in range(i*2, n+1, i):            is_prime[j] = False    for i in range(n+1):        if is_prime[i]:            ass.append(i)    return(ass) S = sieve(10**5+50) n = int(input())if n%2 == 0:    print(n//2)else:    if is_prime(n):        print(1)        exit()    for p in S:        if n%p == 0:            break    print(1+(n-p)//2)
