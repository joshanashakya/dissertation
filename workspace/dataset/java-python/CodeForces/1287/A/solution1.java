import java.util.*; public class Practice { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int t = sc.nextInt();		int n;		for(int j = 0;j<t;j++)		{			n = sc.nextInt();						char[] ch = new char[n];		String str = sc.next();		ch = str.toCharArray();		//		for(int i = 0;i<ch.length;i++)//		{//			System.out.println(ch[i]);//		}						HashSet<Integer> hash = new HashSet<>();		int max = 0;		for(int i = 0;i<ch.length;i++)		{			if(ch[i]=='A')			{				max = 0;				int temp = i;				i = i+1;				while(i<ch.length)				{					if(ch[i] !='A')					{					max = max+1; 					i++;					}					else						break;				}				//i = temp;				i = i-1;				hash.add(max);			}			else			{				hash.add(max);				continue;			}						}		System.out.println(Collections.max(hash));		}	}}
