from sys import stdinn, k = map(int, input().split())cards = [input() for i in range(n)]chk = set(cards)ans = 0for i in range(n - 1):    for y in range(i + 1, n):        build = ""        for l in range(k):            a, b = cards[i][l], cards[y][l]            if a == b:                build += a            else:                if "S" not in a + b:                    build += "S"                elif "T" not in a + b:                    build += "T"                else:                    build += "E"        if build in chk:            ans += 1print(ans//3)
