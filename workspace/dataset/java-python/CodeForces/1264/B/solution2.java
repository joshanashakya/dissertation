//created by Whiplash99import java.io.*;import java.util.*;public class Main{    private static void NO()    {        System.out.println("NO");        System.exit(0);    }    public static void main(String[] args) throws Exception    {        BufferedReader br=new BufferedReader(new InputStreamReader(System.in));         int i,N;         String[] s=br.readLine().trim().split(" ");        int zero=Integer.parseInt(s[0]);        int one=Integer.parseInt(s[1]);        int two=Integer.parseInt(s[2]);        int three=Integer.parseInt(s[3]);         ArrayDeque<Integer> first=new ArrayDeque<>();        ArrayDeque<Integer> second=new ArrayDeque<>();         int x=0;        while (zero>0)        {            if(x==0)            {                first.add(0);                zero--;            }            else            {                if(one==0) NO();                first.add(1);                one--;            }            x^=1;        }         x=0;        while (three>0)        {            if(x==0)            {                second.add(3);                three--;            }            else            {                if(two==0) NO();                second.add(2);                two--;            }            x^=1;        }         if(first.isEmpty()&&second.isEmpty())        {            int min=Math.min(one,two);            x=0;            for(i=0;i<min*2;i++)            {                if(x==0) first.addLast(1);                else first.addLast(2);                 x^=1;            }            one-=min; two-=min;             if(one>0)            {                first.addLast(1);                one--;            }            if(two>0)            {                first.addFirst(2);                two--;            }        }        else if(first.isEmpty())        {            if(one>two) NO();             int min=Math.min(one,two);            x=0;            for(i=0;i<min*2;i++)            {                if(x==0) second.addFirst(2);                else second.addFirst(1);                 x^=1;            }            one-=min; two-=min;             if(two>2) NO();            else            {                if(two>0)                {                    second.addFirst(2);                    two--;                }                if(two>0)                {                    second.addLast(2);                    two--;                }            }        }        else if(second.isEmpty())        {            if(two>one) NO();             int min=Math.min(one,two);            x=0;            for(i=0;i<min*2;i++)            {                if(x==0) first.addLast(1);                else first.addLast(2);                 x^=1;            }            one-=min; two-=min;             if(one>2) NO();            else            {                if(one>0)                {                    first.addFirst(1);                    one--;                }                if(one>0)                {                    first.addLast(1);                    one--;                }            }        }        else        {            if(one==0||two==0) NO();            first.addLast(1); second.addFirst(2);            one--; two--;             int min=Math.min(one,two);            for(i=0;i<min*2;i++)            {                if(i%2==0) first.addLast(2);                else first.addLast(1);            }            one-=min; two-=min;             if(one>two)            {                if(one>1) NO();                first.addFirst(1);                one--;            }            else if(two>one)            {                if(two>1) NO();                second.addLast(2);                two--;            }        }         if(zero>0||one>0||two>0||three>0) NO();        StringBuilder sb=new StringBuilder();        for(int a:first) sb.append(a).append(" ");        for(int a:second) sb.append(a).append(" ");         System.out.println("YES");        System.out.println(sb);    }}
