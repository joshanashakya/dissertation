import sys,io,os# input = io.BytesIO(os.read(0, os.fstat(0).st_size)).readlineinput = sys.stdin.readlinestack = [0]cnt = 0val = 0k = 2**32 - 1flag = 0for _ in range (int(input())):    curr = input().strip().split()    if len(curr) == 2:        stack.append("for")        stack.append(int(curr[1]))        stack.append(0)    elif curr == ["end"]:        cnt = stack.pop()        times = stack.pop()        stack.pop()        curr = cnt * times        stack[-1]+=curr        if curr>k:            flag = 1            break    else:        stack[-1]+=1    if stack[-1]>k:        flag = 1        breakif flag:    print("OVERFLOW!!!")else:    print(stack[0])
