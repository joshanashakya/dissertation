import java.util.Scanner; public class Test {	public static void main(String[] args) { 		Scanner input = new Scanner(System.in);		int n = input.nextInt();		int[] arr = new int[n];		long sum = 0;		int min = 0; 		for (int i = 0; i < n; i++) {			arr[i] = input.nextInt();		} 		for (int g = 0; g < n; g++) {			sum += arr[g];		} 		if (sum % 2 == 0) {			System.out.println(sum);		} 		else {			for (int y = 0; y < n; y++) {				if (min == 0 && arr[y] % 2 == 1) {					min = arr[y];				} 				if (min != 0 && arr[y] % 2 == 1 && min > arr[y]) {					min = arr[y];				}			} 			if (min == 0) {				System.out.println(min);			} 			if (min != 0) {				System.out.println(sum - min);			}		}	}}
