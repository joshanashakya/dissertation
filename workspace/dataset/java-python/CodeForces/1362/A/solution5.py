import sys input = sys.stdin.buffer.readline t = int(input())for _ in range(t):    a, b = map(int, input().split())    a, b = max(a, b), min(a, b)     div, mod = divmod(a, b)    if mod != 0:        print(-1)        continue     count = 0     for factor in [8, 4, 2]:        while div % factor == 0:            count += 1            div //= factor     print(count if div == 1 else -1)
