import java.util.Scanner; public class Main{    public static void main(String[] args){        int x,y,a,b,min,max,sum=0,fir,lir;        Scanner scanner = new Scanner(System.in);        x= scanner.nextInt();        y= scanner.nextInt();        a= scanner.nextInt();        b= scanner.nextInt();        min = Math.min(x,y);        max = Math.max(x,y);        int limit = b;        int common = max;        int temp= common/min;        while (temp*min!=common&&common<=b)        {            common+=max;            temp=common/min;        }        temp = ((int)(a/common)*common);        fir = (temp >=a)? temp : temp +common;        temp= ((int)(b/common)*common);        while(temp<=b)        {            temp+=common;        }        lir= temp-common;        System.out.print(((lir-fir)/common)+1);     }}
