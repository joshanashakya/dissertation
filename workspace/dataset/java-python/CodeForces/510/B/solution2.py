import syssys.setrecursionlimit(10000) def is_cycle(x, y, X, Y):    set.add((x, y))    for dX, dY in [(-1, 0), (0, 1), (1, 0), (0, -1)]:        if 0 <= x + dX < n and 0 <= y + dY < m and (x + dX, y + dY) != (X, Y) and dots[x + dX][y + dY] == dots[x][y]:            if (x + dX, y + dY) in set or is_cycle(x + dX, y + dY, x, y):                return True    return False  n, m = map(int, input().split())dots = [input() for i in range(n)]set = set()for i in range(n):    for j in range(m):        if not (i, j) in set and is_cycle(i, j, -1, -1):            print('Yes')            exit() print('No')	   					 	  		  	  		  	  		 	
