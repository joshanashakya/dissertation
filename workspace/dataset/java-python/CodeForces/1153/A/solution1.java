import java.util.*;import java.io.*; public class Main{     public static void main(String[] args) {         Scanner sc = new Scanner();         int n = sc.nextInt();         int t = sc.nextInt();         int min = Integer.MAX_VALUE;        int ans = -1;         for(int i=0;i<n;i++)        {            int a = sc.nextInt();            int b = sc.nextInt();             if(a>=t && a<=min)            {                min=a;                ans = i;            }             if(a<t)            {                int tmp = t-a;                 int fac = (int)(Math.ceil((double)tmp/b));                 int tme = fac*b+a;                 if(tme<min)                {                    min = tme;                    ans=i;                }            }        }         out.println(ans+1);         out.close();    }     static  PrintWriter out=new PrintWriter(System.out);    static class Scanner {        BufferedReader br=new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st=new StringTokenizer("");        String nextLine() {            while (!st.hasMoreTokens())                try {                    st=new StringTokenizer(br.readLine());                } catch (IOException e) {}            return st.nextToken();        }        int nextInt() {            return Integer.parseInt(nextLine());        }     }}
