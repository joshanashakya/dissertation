import java.io.IOException;import java.io.InputStreamReader;import java.util.TreeSet;import java.util.Scanner;import java.util.Collections;import java.util.HashSet;import java.util.Arrays;import java.lang.Math;import java.io.BufferedReader; public class lottery {    public static void main(String[] args) throws IOException {        Scanner scan = new Scanner(System.in);        int len = scan.nextInt();        int width = scan.nextInt();        int height = scan.nextInt();        int[] front = new int[width];        int[] left = new int[len];        int[][] top = new int[len][width];        for (int i = 0; i < width; i++) {            front[i] = scan.nextInt();        }        for (int i = 0; i < len; i++) {            left[i] = scan.nextInt();        }        for (int i = 0; i < len; i++) {            for (int j = 0; j < width; j++) {                top[i][j] = scan.nextInt();            }        }        int[][] ans = new int[len][width];        for (int i = 0; i < len; i++) {            for (int j = 0; j < width; j++) {                if(top[i][j] == 1){                    ans[i][j]= Math.min(front[j],left[i]);                }            }        }        for (int i = 0; i < len; i++) {            for (int j = 0; j < width; j++) {                System.out.print(ans[i][j]+" ");            }            System.out.println();        }    }}
