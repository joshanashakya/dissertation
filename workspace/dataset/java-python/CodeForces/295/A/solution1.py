import sys n, m, k = [int(x) for x in sys.stdin.readline().strip().split(" ")]arr = [int(x) for x in sys.stdin.readline().strip().split(" ")] delta = []delta.append(arr[0])for i in range(1, n):    delta.append(arr[i] - arr[i - 1]) ops = [] for i in range(m):    l, r, d = [int(x) for x in sys.stdin.readline().strip().split(" ")]    op = (l, r, d)    ops.append(op) num_calls = [0] * mfor i in range(k):    x, y = [int(x) for x in sys.stdin.readline().strip().split(" ")]    num_calls[x - 1] += 1    if y < len(num_calls):        num_calls[y] -= 1  calls = 0for o in range(m):    calls += num_calls[o]    op = ops[o]    l = op[0]    r = op[1]    d = op[2]    c = calls * d    delta[l - 1] += c    if r < len(delta):        delta[r] -= c    o += 1 arr[0] = delta[0]for i in range(1, n):    arr[i] = delta[i] + arr[i - 1]print(' '.join(str(x) for x in arr))
