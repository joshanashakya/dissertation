import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }      public static void main(String[] args) throws IOException    {         Scanner input = new Scanner(System.in);      int n = input.nextInt();      int m = input.nextInt();      int k  = input.nextInt();      long a[] = new long[n+1];         for (int i = 1; i <=n; i++) {            a[i] = input.nextLong();                    }         int L[] = new int[m+1];         int R[] = new int[m+1];         long D[] = new long[m+1];          long query[] = new long[m+2];         for(int i = 1; i <=m; i++) {            L[i] = input.nextInt();            R[i] = input.nextInt();                        D[i] = input.nextLong();                   }                          for (int i = 1; i <=k; i++) {            int x = input.nextInt();            int y = input.nextInt();            query[x]++;            query[y+1]--;                    }         for (int i = 1; i <=m; i++) {            query[i]+=query[i-1];                    }         long update[] = new long[n+2];         for (int i = 1; i <=m; i++) {            update[L[i]]+=(D[i]*query[i]);            update[R[i]+1]-=((D[i]*query[i]));        }         for (int i = 1; i <=n; i++) {            update[i]+=update[i-1];        }         for (int i = 1; i <=n; i++) {             System.out.print((update[i]+a[i])+" ");        }                 System.out.println("");                }     }
