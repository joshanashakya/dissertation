import java.util.*; public class Task1174A {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        t *= 2;        List<Integer> list = new ArrayList<>();        while (t-- > 0) {            list.add(scanner.nextInt());        }         Collections.sort(list);        if (!list.get(0).equals(list.get(list.size() - 1)))            list.forEach(System.out::println);        else {            System.out.println(-1);        }    }}
