import java.util.Arrays;import java.util.Scanner; public class EhabFailstoBeThanos {    public static void main(String[] args) {         Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();        int[] numbers = new int[n + n];         for (int i = 0; i < n + n; i++) {            numbers[i] = scanner.nextInt();        }        System.out.println(solve(n, numbers));     }     public static String solve(int n, int[] nums) {        String[] res = new String[nums.length];        String result = "";        int count = 0;        for (int i = 0; i < nums.length; i++) {            for (int j = 1; j < nums.length; j++) {                if (nums[i] == nums[j]) {                    count++;                }            }        }        int temp = nums.length;        int temp1 = nums.length - 1;        int temp2 = temp * temp1;        if (count == temp2) {            result = "-1";        } else {            Arrays.sort(nums);            for (int i = 0; i < nums.length; i++) {                result += nums[i];                if (nums.length - 1 > i) {                    result += " ";                }            }        }        return result;     }}
