//package graphs;import java.util.*;public class PW { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner s=new Scanner(System.in);		int n=s.nextInt();		int a[]=new int[n];		List<Integer> odd=new ArrayList<>();		List<Integer> even=new ArrayList<>();		for(int i=0;i<n;i++){			a[i]=s.nextInt();			if(a[i]%2==0)				even.add(a[i]);			else				odd.add(a[i]);		}				if(even.size()>0&&odd.size()>0){		int os=0,es=0;		Collections.sort(odd);		Collections.sort(even);		while(os<odd.size()&&es<even.size())		{			if(odd.get(os)<even.get(es))			{				System.out.print(odd.get(os)+" ");				os++;			}			else			{				System.out.print(even.get(es)+" ");				es++;			}		}				while(os<odd.size())		{			System.out.print(odd.get(os)+" ");			os++;		}	    while(es<even.size())	    {	    	System.out.print(even.get(es)+" ");			es++;	    }		}		else		{			for(int i:a)				System.out.print(i+" ");		}	   // System.out.println();														}	public static boolean prime(long n)	{		if(n<=2)			return true;				for(int i=2;i<=Math.sqrt(n);i++)		{			if(n%i==0)				return false;		}				return true;			}}    	   			    	      		 	  		
