import java.util.*;import java.io.*; public class KeyRaces {    public static void main(String[] args) {        FastReader reader = new FastReader();         int s = reader.nextInt();        int v1 = reader.nextInt();        int v2 = reader.nextInt();        int t1 = reader.nextInt();        int t2 = reader.nextInt();        int total1 = 0;        int total2 = 0;        total1 = t1 + (v1 * s) + t1;        total2 = t2 + (v2 * s) + t2;         if (total1 < total2)            System.out.println("First");        else if (total2 < total1)            System.out.println("Second");        else            System.out.println("Friendship");    }     static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }} 
