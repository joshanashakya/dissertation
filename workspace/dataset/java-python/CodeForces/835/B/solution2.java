import java.util.*;import java.io.*; public class Solution { static int rock = 0; static BufferedReader br  = new BufferedReader(new InputStreamReader(System.in));	public static void main(String[] args) throws Exception {				//int test = Integer.parseInt(br.readLine());		StringBuilder sb = new StringBuilder("");		//while(test-->0){			int k = Integer.parseInt(br.readLine());			char c[] = br.readLine().toCharArray();			int ans = 0;			int sum = 0;			for(int i =0;i<c.length;i++)sum+= (c[i]-'0');			if(sum>=k){				ans = 0;			}else{				sort(c);				for(int i =0;i<c.length;i++){					int curr = c[i] - '0';					sum-=curr;					boolean ok = false;					for(int j=1;j<=9;j++){						if(sum+j>=k){							ans++;							ok = true;							break;						}					}					if(ok)break;					else{						ans++;						sum+=9;											}				}			}			sb.append(ans);			sb.append("\n");		//}					System.out.println(sb);	}	public static void sort(char c[]){		ArrayList<Character>list = new ArrayList<>();		for(char it:c)list.add(it);		Collections.sort(list);		for(int i =0;i<c.length;i++)c[i] = list.get(i);			}	public static long[] readArray(int n) throws Exception{		long result[] = new long[n];		String l[] = br.readLine().split(" ");		for(int i =0;i<n;i++)result[i] = Long.parseLong(l[i]);		return result;					}	}class Pair{	long a,b;	public Pair(long a,long b){		this.a =a;		this.b  = b;	}}		   
