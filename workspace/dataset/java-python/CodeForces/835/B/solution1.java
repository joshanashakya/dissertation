import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;import static java.util.Arrays.sort;import javafx.util.Pair; public class Main{    public static int SumOfDigits(char s[])    {        int sum=0;        for (int i = 0; i < s.length; i++) {            sum+=(s[i]-'0');        }        return sum;    }     public static void main(String[] args)    {         FastScanner input = new FastScanner();        int k = input.nextInt();        char s[] = input.next().toCharArray();        int sum =SumOfDigits(s);        if(sum>=k)        {            System.out.println("0");            return;        }        Arrays.sort(s);        int count=0;        for (int i = 0; i < s.length; i++) {            int ch = s[i]-'0';            if(ch!=9)            {                sum-=ch;                sum+=9;                count++;                if(sum>=k)                {                    System.out.println(count);                    return;                }            }        }    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
