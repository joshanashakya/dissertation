 def isPal(s):    return s == s[::-1]       strng = input()k = int(input())leng = len(strng)rem = leng % kcheck = 1if(rem >= 1 or k > leng):    print("NO")    check = 0#n = leng // k  # splitting according to length#chunks = [strng[i:i+n] for i in range(0, len(strng), n)]# Now chunks contains the splitted string which now has to test for palindrome checkingif check == 1:    n = leng // k    chunks = [strng[i:i+n] for i in range(0, len(strng), n)]    count = 1    for flag in chunks:        ans = isPal(flag)        if(ans):            count = count + 1        else:            count = 0            break     if count == 0:        print("NO")     elif count >= 1:        print("YES")   
