import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.TreeMap;import java.util.Map;import java.util.Scanner;import java.util.SortedMap; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main{    public static void main(String[] args)    {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Scanner in = new Scanner(inputStream);        PrintWriter out = new PrintWriter(outputStream);        TaskB solver = new TaskB();        solver.solve(1, in, out);        out.close();    }     static class TaskB    {        public void solve(int testNumber, Scanner in, PrintWriter out)        {            int T = 1;            while (T-- > 0)            {                solveOne(in, out);            }        }         private void solveOne(Scanner in, PrintWriter out)        {            int rows = in.nextInt(), cols = in.nextInt(), Q = in.nextInt();            int[][] M = CPUtils.readIntMatrix(rows, cols, in);            SortedMap<Integer, Integer> map = new TreeMap<>();            int[] row_sum = new int[rows];            for (int row = 0; row < rows; row++)            {                int cnt = maxWindow(M[row]);                map.merge(cnt, 1, Integer::sum);                row_sum[row] = cnt;            }            while (Q-- > 0)            {                int r = in.nextInt() - 1, c = in.nextInt() - 1;                int max = row_sum[r];                remove(max, map);                M[r][c] = invert(M[r][c]);                max = maxWindow(M[r]);                row_sum[r] = max;                map.merge(max, 1, Integer::sum);                 out.println(map.lastKey());            }        }         private int invert(int i)        {            if (i == 0) return 1;            else return 0;        }         private int maxWindow(int arr[])        {            int max = 0;            for (int i = 0; i < arr.length; i++)            {                int cnt = 0;                while (i < arr.length && arr[i] == 1)                {                    cnt++;                    i++;                }                max = Math.max(max, cnt);            }            return max;        }         private void remove(int i, SortedMap<Integer, Integer> map)        {            int occ = map.get(i);            occ--;            if (occ == 0) map.remove(i);            else map.put(i, occ);        }     }     static class CPUtils    {        public static int[] readIntArray(int size, Scanner in)        {            int[] array = new int[size];            for (int i = 0; i < size; i++)            {                array[i] = in.nextInt();            }            return array;        }         public static int[][] readIntMatrix(int rows, int columns, Scanner in)        {            int[][] matrix = new int[rows][columns];            for (int i = 0; i < rows; i++)            {                matrix[i] = CPUtils.readIntArray(columns, in);            }            return matrix;        }     }} 
