r, c, g = map(int, input().split()) board = []scores = [] for i in range(r):    board.append([int(x) for x in input().split()])     curr = 0    m = 0    for num in board[-1]:        if num == 1:            curr += 1        else:            m = max(m, curr)            curr = 0    m = max(m, curr)     scores.append(m) for i in range(g):    x, y = map(int, input().split())    x-=1    y-=1    if board[x][y]:        board[x][y] = 0    else:        board[x][y] = 1     curr = 0    m = 0    for num in board[x]:        if num == 1:            curr += 1        else:            m = max(m, curr)            curr = 0    m = max(m, curr)     scores[x] = m     print(max(scores)) 			       		 	 						 	  					
