# map(int, input().split(" "))n, m, q = map(int, input().split(" "))a = []  def cons_ones(l, m):    ans = 0    c = 0    for i in range(m):        if l[i] == 1:            c += 1        else:            ans = max(ans, c)            c = 0    ans = max(ans, c)    return ans  anss = []for i in range(n):    s = list(map(int, input().split(" ")))    anss.append(cons_ones(s, m))    a.append(s) for i in range(q):    x, y = map(int, input().split(" "))    x -= 1    y -= 1    if a[x][y]:        a[x][y] = 0    else:        a[x][y] = 1    anss[x] = cons_ones(a[x], m)    print(max(anss))
