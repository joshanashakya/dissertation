import java.util.*;import java.lang.*;import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);	    int t = sc.nextInt();	    for (int p=0; p<t; p++)	    {	        int n = sc.nextInt();    	    String ans = "";    	    int prev = -1;    	    boolean c = true;    	    boolean d = true;    	    for (int i=0; i<n; i++)    	    {    	        int a = sc.nextInt();    	        if (prev != -1)    	        {    	            if (c && a%n != (prev+1)%n)    	            {    	                c = false;     	            }    	            if (d && a%n != (n+prev-1)%n)    	            {    	                d = false;    	            }    	        }    	        prev = a;    	    }    	    if (c || d)    	    ans = "YES";    	    else    	    ans = "NO";    	    System.out.println(ans);	    }	    }}
