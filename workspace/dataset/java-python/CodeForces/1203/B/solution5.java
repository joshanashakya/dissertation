import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.*;import java.math.*;public class code {	static int dp[][];	static boolean visited[];	static int ans = 0;	public static void main(String[] args)throws IOException {		FastReader sc = new FastReader();		PrintWriter pw = new PrintWriter(System.out);		int t = sc.nextInt();		while (t-- > 0) {			int n = sc.nextInt();			int arr[] = new int[4 * n];			for (int i = 0; i < 4 * n; i++) {				arr[i] = sc.nextInt();			}			Arrays.sort(arr);			ArrayList<Integer> list = new ArrayList<>();			boolean flg = true;			n = 4 * n;			for (int i = 0; i < n - 1; i += 2) {				if (arr[i] == arr[i + 1])					list.add(arr[i]);				else {					flg = false;					break;				}			}			//System.out.println(list);			if (!flg)				System.out.println("NO");			else {				int area = list.get(0) * list.get(list.size() - 1);				//System.out.println(area);				int p = list.size() - 1;				for (int i = 1; i < ((list.size() - 1) / 2) + 1; i++) {					//System.out.println(list.get(i) * list.get(p - i));					if (list.get(i) * list.get(p - i) == area)						continue;					else {						flg = false;						break;					}				}				if (flg)					System.out.println("YES");				else					System.out.println("NO");			}  		} 	}  }      class FastReader {	BufferedReader br;	StringTokenizer st; 	public FastReader() {		br = new BufferedReader(new		                        InputStreamReader(System.in));	} 	String next() {		while (st == null || !st.hasMoreElements()) {			try {				st = new StringTokenizer(br.readLine());			} catch (IOException  e) {				e.printStackTrace();			}		}		return st.nextToken();	} 	int nextInt() {		return Integer.parseInt(next());	} 	long nextLong() {		return Long.parseLong(next());	} 	double nextDouble() {		return Double.parseDouble(next());	} 	String nextLine() {		String str = "";		try {			str = br.readLine();		} catch (IOException e) {			e.printStackTrace();		}		return str;	}}
