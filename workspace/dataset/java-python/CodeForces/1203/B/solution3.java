import java.util.Arrays;import java.util.Scanner; public class EqualRectangles {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int q = scanner.nextInt();         while (q-- != 0) {            int n = scanner.nextInt();            int[] sides = new int[(4 * n)];             for (int i = 0; i < sides.length; i++) {                sides[i] = scanner.nextInt();            }            System.out.println(solve(sides, n));        }    }     public static String solve(int[] sides, int n) {        Arrays.sort(sides);         boolean ans = true;        int y = sides[0] * sides[sides.length - 1];        int j = sides.length - 1;        for (int i = 0; i < sides.length / 2; i += 2, j -= 2) {            if (sides[i] == sides[i + 1] && (sides[j] == sides[(j - 1)])                    && sides[i] * sides[j] == y) {            } else {                ans = false;                break;            }        }         if (ans) {            return "YES";        } else {            return "NO";        }    }}
