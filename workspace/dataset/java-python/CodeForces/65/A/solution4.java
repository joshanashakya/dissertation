        import java.lang.reflect.Array;        import java.util.*;        import java.util.function.BiConsumer;        import java.util.function.Consumer;        import java.util.function.Function;        import java.util.stream.*; public class Main{    public static void main(String arg[]){        // 入力を読み込む        Scanner sc = new Scanner(System.in);        //int n = Integer.parseInt(sc.next());        //int m = Integer.parseInt(sc.next());        //int b = Integer.parseInt(sc.next());        //String str1 = sc.next();        //String getLine = sc.nextLine();        //int n = Integer.parseInt(getLine.split(" ")[0]);        //int m = Integer.parseInt(getLine.split(" ")[1]);        //int n = Integer.parseInt(sc.next());        //int x1 = Integer.parseInt(sc.next());        //int y1 = Integer.parseInt(sc.next());        //int x2 = Integer.parseInt(sc.next());        //int y2 = Integer.parseInt(sc.next());        //List<Integer> x = IntStream.range(1, 101).boxed().collect(Collectors.toList());        //String str2 = sc.next();        //String str3 = sc.next();        /*List<Integer> arr = new ArrayList<>();        for(int i = 0; i < n; ++i){            arr.add(Integer.parseInt(sc.next()));        }*/        /*List<String> arr = new ArrayList<>();        for(int i = 0; i < n; ++i){            arr.add(sc.nextLine());        }*/        int a = Integer.parseInt(sc.next());        int b = Integer.parseInt(sc.next());        int c = Integer.parseInt(sc.next());        int d = Integer.parseInt(sc.next());        int e = Integer.parseInt(sc.next());        int f = Integer.parseInt(sc.next());        // 判定・表示        if(d == 0){            // 変換時に消滅してしまう＝ループできない＝儲からない            System.out.println("Hermione");        }else if(c == 0){            // 最初の砂を無限の金に変換できる＝無限に儲かる            System.out.println("Ron");        }else if(b == 0){            // 変換時に消滅してしまう＝ループできない＝儲からない            System.out.println("Hermione");        }else if(a == 0){            // 最初の砂を無限の金に変換できる＝無限に儲かる            System.out.println("Ron");        }else if(f == 0){            // 金を砂に変換できない＝稼いだ金の一部を砂に戻せない＝無限には儲からない            System.out.println("Hermione");        }else if(e == 0){            // 砂を無から生み出せる＝無限の砂を材料にできる＝無限に儲かる            System.out.println("Ron");        }else{            // 変換効率を計算する            double per = 1.0 * b / a * d / c * f / e;            if(per > 1.0){                System.out.println("Ron");            }else{                System.out.println("Hermione");            }        }    }}
