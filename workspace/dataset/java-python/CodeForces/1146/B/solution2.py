def check(arr, s):    if len(arr) != len(s):        return False    n = len(arr)    for i in range(n):        if arr[i] != s[i]:            return False    return Trues = input()n = len(s)cta = s.count('a')ans = ":("new = 0if cta == n:    ans = selif cta == 0:    if n % 2 == 0 and s[:n//2] == s[n//2:]:        ans = s[:n // 2]else:    ind = 0    rem = []    for i in range(n):        if s[i] == 'a':            ind = i    for i in range(ind):        if s[i] != 'a':            rem.append(s[i])            new += 1    for i in range(ind + 1, n):        rem_len = n - i        if rem_len == new:            if check(rem, s[i:]):                ans = s[:i]            break        else:            rem.append(s[i])            new += 1print(ans)
