import java.util.*;import java.io.*; public class Main {    public static void main(String[] args) throws IOException {        FastScanner in = new FastScanner(System.in);        PrintWriter out = new PrintWriter(System.out);        new Main().run(in, out);        out.close();    }     void run(FastScanner in, PrintWriter out) {        int a = in.nextInt();        int b = in.nextInt();        int mod = in.nextInt();        if (mod <= b) {            // 2 can form anything            out.println(2);            return;        }         // 1        // b <= m        // p2 can form everything from (0 to m-1) % m        long pow = (long)Math.pow(10, 9);         for (int i = 1; i < mod && i <= a; i++) {            long l = i * pow;             long m = l%mod;            long invmod = (mod-m)%mod;            if (invmod > b) {                // System.out.println(l + " " + m + " " + invmod);                out.println(1 + " " + String.format("%09d", i));                return;            }        }         out.println(2);    }     static class FastScanner {        BufferedReader br;        StringTokenizer st;         public FastScanner(InputStream in) {            br = new BufferedReader(new InputStreamReader(in));            st = null;        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }    }}
