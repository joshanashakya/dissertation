import copyn,k = map(int,input().split()) tabs = list(map(int,input().split())) tabs.insert(0,0)max_value = 0 for each_b in range(1,n+1):    temp = copy.deepcopy(tabs)     i = each_b        while i<=n:        #print("value of i",i,each_b)        temp[i] = 0         i+=k          i =  each_b    while i>0:        temp[i] = 0        i =i - k           value = abs(temp.count(1) - temp.count(-1))      if(value >max_value):        max_value = value         print(max_value)
