#include <bits/stdc++.h>#define FIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)#define fi first#define se second#define pr(x, y) make_pair(x, y)#define th this-> #define pii pair<int, int>typedef long long ll;#define pll pair<ll, ll>typedef unsigned long long ull;using namespace std;const int INF = 1e9;const ll LINF = 1e18;const double PHI = 1.618034;ll mod = 1e9+7;// ZZXXmap<pll, bool> fr;map<int, bool> vis;vector<vector<ll>> vec;bool check(ll node){	vector<int> con;	con.push_back(node);	queue<int> que;	que.push(node);	vis[node] = 1;	while(!que.empty()){		int z = que.front();		que.pop();		for(auto x : vec[z]){			if(!vis[x]){				vis[x] = 1;				con.push_back(x);				que.push(x);			}		}	}	for(auto x : con) 		if(vec[x].size() != con.size()-1) return 0;	return 1;} int main(){ FIO; int n, m;	cin >> n >> m;	vec = vector<vector<ll>> (n+3);	for (int i=1; i<=m; i++){ ll a, b;		cin >> a >> b;		vec[a].push_back(b);		vec[b].push_back(a);	}	bool y = 1;	for (int i=1; i<=n; i++){		if(!vis[i]) y &= check(i);	}	cout << (y?"YES":"NO") << endl;	return 0;}	 	 	  				 	 	      	  	  	
