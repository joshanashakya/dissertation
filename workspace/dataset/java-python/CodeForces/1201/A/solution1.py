import collectionscases, n = map(int, input().split())marks = []while cases:    cases -= 1    s = list(input())    marks.append(s)arr = list(map(int, input().split()))traverse = [[marks[i][j] for i in range(len(marks))] for j in range(len(marks[0]))] ans = 0for n, m in zip(arr, traverse):    count = collections.Counter(m)    mx = max(count.values())    ans += mx *n print(ans)        
