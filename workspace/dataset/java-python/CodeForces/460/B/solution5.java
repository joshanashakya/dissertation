import java.io.*;import java.util.*;public class test1{    public static void main(String[] args){        Scanner sc = new Scanner(System.in);        int a = sc.nextInt();        int b = sc.nextInt();        int c = sc.nextInt();        long x = 1, counter = 0;        List<Long> result = new ArrayList<>();        for(int digitSum=1; digitSum<=81; digitSum++){            x = b* ((long)Math.pow(digitSum,a)) + c;                       // System.out.println("digi x sumofd :"+digitSum+" "+x+" "+sumOfDigits(x));            if(digitSum == sumOfDigits(x) && x > 0 && x < 1000000000){                counter++;                result.add(x);                // System.out.println("b "+b);                // System.out.println("woc "+ b * ((int)Math.pow(digitSum,a)));                // System.out.println("ds "+digitSum+" x "+x+" sumofd "+sumOfDigits(x));            }        }                System.out.println(counter);        for(long num : result)            System.out.print(num+" ");                    }     static int sumOfDigits(long number){        int sum = 0;        while(number > 0){            sum += number%10;            number /= 10;        }        return sum;    }}
