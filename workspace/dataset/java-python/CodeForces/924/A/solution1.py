typedef long long ll;#define INF 1000000000#include <iostream> using namespace std; bool scomp(string s1, string s2) {    int count = 0, prob = 0;    for(int j = 0; j < s1.length(); j++) {        if((s1[j] == s2[j]) && (s1[j] == '#')) {            if(prob) {return 1; }            count++;        } else if(((s1[j] == '#') || (s2[j] == '#')) && count) {            return 1;        }        prob += (s2[j] != s1[j]);    }    return 0;} int main() {  int n, j, m, l;  scanf("%d %d", &n, &m);  string s[n];  for(j = 0; j < n; j++) {      cin>> s[j];  }  for(j = 0; j < n - 1; j++) {      for(l = j + 1; l < n; l++) {          if(scomp(s[j], s[l])) {              printf("No");              return 0;          }      }  }  printf("Yes");    return 0;}
