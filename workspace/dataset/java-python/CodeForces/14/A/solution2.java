//package codeforces;import java.util.*; public class Letter { 	public static void main(String[] args) {		Scanner scan = new Scanner(System.in); 		int N = scan.nextInt();		int M = scan.nextInt();				//System.out.println(N);		//System.out.println(M);				scan.nextLine();				char[][] A = new char[N][M];		for(int i = 0; i < N; i++) {			String S = scan.nextLine();			//System.out.println(S);			for(int j = 0; j < M; j++) {				A[i][j] = S.charAt(j); 			}		}		//for(char[] a : A) {			//System.out.println(Arrays.toString(a));		//}		int x_min = N - 1; 		int y_min  = M - 1; 		int x_max = 0,  y_max = 0;		for(int x = 0; x < N; x++ ) {			for(int y = 0; y < M; y++) {				if(A[x][y] == '*') {					//if(x < x_min) {					//	x_min = x; 					//}					x_min = Math.min(x_min, x);					if(y < y_min) {						y_min = y; 					}					//if(x > x_max) {					//	x_max = x; 					//}					x_max = Math.max(x_max, x);					y_max = Math.max(y_max, y);					}			}		}		//System.out.println(x_min);		//System.out.println(x_max);		//System.out.println(y_min);		//System.out.println(y_max);		for(int i = x_min; i <= x_max; i++) {			for(int j = y_min; j <= y_max; j++) {				//System.out.println("i:" + i + " j:" + j);				System.out.print(A[i][j]); 			}			System.out.println();		} 	} }
