import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.Arrays;import java.util.StringTokenizer; public class Main {	static double inc;	static double dec;		public static void main(String[] args) throws IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		StringTokenizer st = new StringTokenizer(br.readLine());		int n = Integer.parseInt(st.nextToken());		int k = Integer.parseInt(st.nextToken());		double per = ((100-k)/100.0);		int max=0;		int[] arr = new int[n];		int min= 10001;		st = new StringTokenizer(br.readLine());		for(int i=0; i<n; i++)		{			arr[i]=Integer.parseInt(st.nextToken());			max = Math.max(max, arr[i]);			min = Math.min(min, arr[i]);		}		double lo=min;		double hi=max;		double ans=lo;		while(hi-lo>(double)1e-6)		{			double mid= lo + (hi-lo)/2.0;			check(arr,mid);//			System.out.println(hi + " " + lo + " " + mid);//			System.out.println(dec*per + " " + inc);			if(dec*per>inc)			{				lo=mid;				ans=mid;			}			else				hi=mid;					}		System.out.println(ans);			}	public static void check(int[]arr, double mid)	{		inc=0;		dec=0;		for(int i=0; i<arr.length; i++)		{			if(arr[i]<mid)				inc+=mid-arr[i];			else				dec+=arr[i]-mid;		}				}  } 		  	 					   	  		 	  	 		 	
