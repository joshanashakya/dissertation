import java.io.*;import java.util.*;public class Ishu{    static Scanner scan = new Scanner(System.in);    static void tc()    {    int n = scan.nextInt();    int k = scan.nextInt();    int[] min = new int[n];    int[] max = new int[n];    int i;    for(i=0;i<k;++i)        {        int pos = scan.nextInt();        if(min[pos-1] == 0)            min[pos-1] = i + 1;        max[pos-1] = i + 1;        }    int ans = 0;    for(i=0;i<n;++i)        {        if(min[i] == 0)            {            if(i - 1 >= 0)                ans++;            ans++;            if(i + 1 < n)                ans++;            }        else            {            if(i - 1 >= 0)                {                if(max[i-1] < min[i])                    ans++;                }            if(i + 1 < n)                {                if(max[i+1] < min[i])                    ans++;                }            }        }    System.out.println(ans);    }    public static void main(String[] args)    {    int t = 1;   // t = scan.nextInt();    while(t-- > 0)        tc();    }}
