def make_divisors(n):    divisors = []    for i in range(1, int(n**0.5)+1):        if n % i == 0:            divisors.append(i)            if i != n // i:                divisors.append(n//i)     #divisors.sort(reverse=True)    return divisors import mathl, r, x, y = map(int, input().split())if y%x != 0:    print(0)    exit()z = y//xd = make_divisors(z)ans = 0for i in d:    j = z//i    if math.gcd(i, j) == 1:        if l <= i*x <= r and l <= j*x <= r:            ans += 1print(ans)
