import java.io.*;import java.util.*; public class A_299{    static PrintWriter pw = new PrintWriter(System.out);     public static void main(String arg[]) throws IOException {        InputStreamReader isr = new InputStreamReader(System.in);        BufferedReader br = new BufferedReader(isr);        StringTokenizer token = new StringTokenizer(" ");         int n=0;        int arr[];        int i=0;        int j=0;        int min=Integer.MAX_VALUE;         n=Integer.parseInt(br.readLine());        arr=new int[n];         token=new StringTokenizer(br.readLine());             for(j=0;j<=n-1;j++) {                arr[j] = Integer.parseInt(token.nextToken());                if(arr[j]<min)                    min=arr[j];            }         pw.println(getResult(arr,n,min));        pw.close();     }     private static long getResult(int arr[],int n,int min) {        int i=0;         for(i=0;i<=n-1;i++)            if(arr[i]%min!=0)                return -1;         return min;     }}
