// ctrl+shift+o all unrecognised classes are automatically imported in code//ctrl+I to indent the code properly// ctrl+D to delete the whole current line// ctrl+space gives the autocomplete recomendation// sysout and ctrl+space gives println() statement// alt+shift+r TO rename the file// ctrl+f11 to run the code import java.util.*;public final class test{    public static void main(String[] args) {    	Scanner s=new Scanner(System.in);    	int n=s.nextInt();    	if(n%4==0 || n%7==0 || n%47==0 || n%74==0 || n%447==0 || n%474==0 || n%744==0)    		System.out.print("YES");    	else    	{    		String str=""+n;    		boolean b=true;    		brk:for(int i=0;i<str.length();i++)    		{    			if(str.charAt(i)!='4' && str.charAt(i)!='7')    			{    				b=false;    				break brk;    			}    		}    		if(b==true)    			System.out.print("YES");    		else    			System.out.print("NO");    			    		    	}    	s.close();    	    	    } }
