import java.util.Scanner;import java.util.Vector; public class p902B {    static Vector<Integer>[] list;    static boolean visited[];    static int ans,color[];    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int n=sc.nextInt();        createGraph(n,sc);        for(int i=1;i<=n;i++) color[i]=sc.nextInt();        dfs(1,0);        System.out.println(ans);    }    static void createGraph(int n,Scanner sc) {        visited=new boolean[n+1];        list=new Vector[n+1];        color=new int[n+1];        for(int i=0;i<=n;i++) list[i]=new Vector<>();        for(int i=2;i<=n;i++) addEdges(i,sc.nextInt());    }    static void addEdges(int i,int x) {        list[i].add(x);        list[x].add(i);    }    static void dfs(int i,int c) {        visited[i]=true;        if(color[i]!=c) ans++;        for(int x:list[i])            if(!visited[x])                dfs(x,color[i]);    }}
