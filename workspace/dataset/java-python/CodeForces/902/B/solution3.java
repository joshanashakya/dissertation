import java.util.ArrayList;import java.util.Scanner; public class Main {		static int n;	static ArrayList<Integer>[] adj;	static int[] c;	static int sum = 1;		static void dfs(int prev,int u)	{		if(c[u]!=c[prev]) sum++;		for(int v:adj[u])			if(v!=prev)				dfs(u,v);	} 	public static void main(String[] args)	{		Scanner sc = new Scanner(System.in);				n = sc.nextInt();		adj = new ArrayList[n];		for(int i=0;i<n;i++) adj[i] = new ArrayList<Integer>();				for(int u=1;u<n;u++)		{			int v = sc.nextInt()-1;			adj[u].add(v);			adj[v].add(u);		}				c = new int[n];		for(int i=0;i<n;i++) c[i] = sc.nextInt();				for(int u:adj[0])			dfs(0,u);				System.out.println(sum);			}	}  	 			   		    	   	  	   	  	
