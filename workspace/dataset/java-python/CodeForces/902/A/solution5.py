import sys, collections n, m = map(int, sys.stdin.readline().split())arr = []for _ in range(n):    a, b = map(int, sys.stdin.readline().split())    arr.append((a,b))q = collections.deque()check = [False] * 101check[0] = Trueq.append(0)while q:    x = q.popleft()    for i in range(n):        if arr[i][0] == x:            for j in range(arr[i][0], arr[i][1] + 1):                if not check[j] and j <= 100:                    check[j] = True                    q.append(j) print('YES' if check[m] else 'NO')   
