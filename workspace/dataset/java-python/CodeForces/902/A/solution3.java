//package test;import java.util.*;	import java.io.*;public class ex9 {	public static void main (String []args) throws IOException { 		Scanner sc = new Scanner (System.in);		int n = sc.nextInt(), m = sc.nextInt();		int prevlim = 0, bmax = 0;		Boolean [] b = new Boolean [m];  		int st = sc.nextInt(), lim = sc.nextInt();		if (st == 0) bmax = lim; 		else {			System.out.print("NO");			return;		}		for (int i = 1; i < n; i++){			st = sc.nextInt(); lim = sc.nextInt();			if (st <= bmax && lim > bmax) bmax = lim;		}		 		if (bmax >= m)		System.out.print("YES");		else System.out.print("NO");	} }
