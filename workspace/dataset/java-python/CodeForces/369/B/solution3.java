import java.util.Scanner; public class KidsMark {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        int k = scanner.nextInt();        scanner.nextInt();        scanner.nextInt();        int sall = scanner.nextInt();        int sk = scanner.nextInt();         int c = 0;        int d = 0;         int a = sall - sk;        int b = n - k;        if(b != 0) {            c = a / b;            d = a % b;        }        int e = sk/k;        int f = sk%k;         StringBuilder ans = new StringBuilder();         for (int i = 0; i < b; i++) {            int val = c;            if(d > 0){                val++;                d--;            }             ans.append(val).append(" ");        }         for (int i = b; i < n; i++) {            int val = e;            if(f > 0) {                val++;                f--;            }             ans.append(val).append(" ");        }         System.out.println(ans);    }}
