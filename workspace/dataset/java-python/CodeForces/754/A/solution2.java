import java.io.*;import java.util.*; import static java.lang.Math.*; public class Task {    public static void main(String[] args) throws IOException {        Reader reader = new Reader();        PrintWriter out = new PrintWriter(System.out);         int n = reader.nextInt();        int[] arr = new int[n];        for (int i = 0; i < n; ++i) {            arr[i] = reader.nextInt();        }         int sum = 0;        for (int i = 0; i < n; ++i) {            sum += arr[i];        }         if (sum == 0) {            int sum1 = 0;            int i = 0;            while (i < n) {                sum1 += arr[i];                ++i;                if (sum1 != 0) {                    break;                }            }             if (i == n) {                out.println("NO");            } else {                out.println("YES");                out.println(2);                out.println(String.format("%d %d", 1, i));                out.println(String.format("%d %d", i + 1, n));            }        } else {            out.println("YES");            out.println(1);            out.println(String.format("%d %d", 1, n));        }         reader.close();        out.close();    }} class Reader {    private BufferedReader bufferedReader;    private StringTokenizer tokenizer;     public Reader() {        bufferedReader = new BufferedReader(new InputStreamReader(System.in), 32786);    }     public void close() throws IOException {        bufferedReader.close();    }     public String next() throws IOException {        while (tokenizer == null || !tokenizer.hasMoreTokens()) {            tokenizer = new StringTokenizer(bufferedReader.readLine());        }         return tokenizer.nextToken();    }     public int nextInt() throws IOException {        return Integer.parseInt(next());    }     public long nextLong() throws IOException {        return Long.parseLong(next());    }}
