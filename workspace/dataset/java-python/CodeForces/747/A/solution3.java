import java.util.*; public class Main {    public static void main(String[] args) {        Scanner input = new Scanner(System.in);        int a = input.nextInt();        int ans1 = 0, ans2 = 0, minn = Integer.MAX_VALUE;        for (int i = 1; i <= a; i++) {            if (a % i == 0) {                int temp1 = i, temp2 = a / i;                if (Math.abs(temp2-temp1)< minn) {                    minn = Math.abs(temp2-temp1);                    ans1 = Math.min(temp1,temp2);                    ans2 = Math.max(temp1,temp2);                }            }        }        System.out.println(ans1 + " " + ans2);    }     public static int minCost(int[] array) {        int len = array.length;        if (len == 0 || len == 1) {            return 0;        }        int sum = 0;        LinkedList<Integer> queue = new LinkedList<>();        Arrays.sort(array);        queue.offer(array[0]);        for (int i = 1; i < len; i++) {            int temp = queue.getLast();            if (array[i] <= temp) {                sum += temp - array[i] + 1;                queue.addLast(temp + 1);            } else {                queue.addLast(temp);            }        }        return sum;    }     public static int mixx(int a, int b, int c) {        return Math.min(Math.min(a, b), c);    }}  	 	 				 					 	     	 		 	 	 	
