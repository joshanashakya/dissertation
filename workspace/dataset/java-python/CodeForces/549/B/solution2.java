import java.util.Scanner; public class Code549B { 	public static void main(String[] args) {		Scanner scanner = new Scanner(System.in);				int n = Integer.parseInt(scanner.nextLine());				char[][] s = new char[n][n];		int[] a = new int[n];		int[] dp = new int[n];				for(int i = 0; i < n; i++)			s[i] = scanner.nextLine().toCharArray();				for(int i = 0; i < n; i++)			a[i] = scanner.nextInt();				StringBuilder sb = new StringBuilder();		int count = 0;				while(true) {			int zero = -1;						for(int i = 0; i < n; i++) { 				if(a[i] == dp[i]) {					zero = i;					break;				}			}						if(zero < 0)				break;						count++;			sb.append(zero + 1).append(" ");					for(int j = 0; j < n; j++) {				if(s[zero][j] == '1')					dp[j]++;			}		}				sb.append("\n");				System.out.println(count);		System.out.println(sb);				scanner.close();	} }
