import java.util.Scanner; public class A1257 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        for (int i = 0; i < t; i++) {            int n = scanner.nextInt(), x = scanner.nextInt(),                    a = scanner.nextInt(), b = scanner.nextInt();            int diff = Math.abs(a - b);            int maxLength = diff + x;            if (maxLength >= n) System.out.println(n - 1);            else System.out.println(maxLength);        }    }}
