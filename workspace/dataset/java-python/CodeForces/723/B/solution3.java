import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.*; public class Main{        public static void main(String[] args)     {              FastScanner input = new FastScanner();       int n = input.nextInt();       String s = input.next();       int max = Integer.MIN_VALUE;       int parenMax = 0;       int count=0;        int parencount=0;        for (int i = 0; i < n; i++) {            if(s.charAt(i)=='(')            {                max = Math.max(max, count);               count=0;               parencount=0;                for (i = i+1; ; i++) {                    if(s.charAt(i)==')')                    {                         if(parencount>0)                             parenMax++;                        parencount=0;                        break;                    }                    else if(s.charAt(i)=='_')                    {                       if(parencount>0)                             parenMax++;                        parencount=0;                    }                    else                        parencount++;                }            }            else if(s.charAt(i)=='_')            {               max = Math.max(max, count);               count=0;            }            else            {                count++;            }        }                 max = Math.max(max, count);        System.out.println(max+" "+parenMax);    }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
