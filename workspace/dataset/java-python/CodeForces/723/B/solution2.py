n = int(input())s = str(input())outside, inside = '', ''p = 0while p < n:    if s[p] == '(':        pd = p + 1        while pd < n:            if s[pd] == ')':                pd += 1                break            inside += s[pd]            pd += 1        p = pd        inside += '_'    else:        outside += s[p]        p += 1        if p < n:            if s[p] == '(' or s[p] == ')':                outside += '_'outside = outside.split('_')inside = inside.split('_')val = 0for j in range(len(outside)):    val = max(val, len(outside[j]))length = 0for j in range(len(inside)):    if len(inside[j]) > 0:        length += 1print(val, length)
