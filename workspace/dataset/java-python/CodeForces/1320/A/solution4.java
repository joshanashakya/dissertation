import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Expedition {		public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int[] ar = new int[n+1];		Map<Integer,Long> map = new HashMap<Integer,Long>();		for(int i=1;i<=n;i++) {			ar[i]=sc.nextInt();			int key = i-ar[i];			if(map.containsKey(key))			{				map.replace(key , map.get(key)+ar[i]);			}			else				map.put(key, (long)ar[i]);		}		long max=0;		for(Map.Entry<Integer, Long> entry : map.entrySet()) {			max = Math.max(max, entry.getValue());		}				System.out.println((long)max);						}}
