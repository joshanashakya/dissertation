/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Codechef{	public static void main (String[] args) throws java.lang.Exception	{		Scanner sc = new Scanner(System.in);		PrintWriter out = new PrintWriter(System.out);	    long n = sc.nextLong();	    int m = sc.nextInt();	    long k = sc.nextLong();	    long p[] = new long[m];	    for(int i = 0; i<m; i++)p[i] = sc.nextLong();	    int cnt = 0;	    int special = 0;	    for(int i = 0; i<m; i++){	        //long prev = (p[i] - special) / k - ((p[i] - special) % k == 0 ? 1 : 0);	        long top = p[i] - special + k - ((p[i] - special) % k == 0 ? k : (p[i] - special) % k);	        int incnt = special + 1;	        while(i < m - 1 && p[i + 1] - special <= top){	            i++; incnt++;	        }	        special = incnt;	        //out.println(i + " " + 34);	        cnt++;	    }	    out.println(cnt);	    out.flush();	}}
