import java.util.Scanner;import java.util.Arrays;import java.util.Vector;import java.util.Collections;   public class Problem_Solving1 {           public static void main(String[] args) {        // TODO code application logic here        Scanner input = new Scanner(System.in);        String s = input.next();        int cr = 0 , cb = 0 , cy = 0 , cg = 0 ;        char[] arr = new char[s.length()];        for (int i = 0; i < arr.length; i++) {            arr[i] = s.charAt(i);        }//        for (int i = 0; i < arr.length; i++) {//            System.out.println(arr[i]);//        }              int[] arr2 = new int[4];       arr2[0]=arr2[1]=arr2[2]=arr2[3] = 0 ;        for (int i = 0; i < arr.length; i++) {            if (arr2[0]!=0&&arr2[1]!=0&&arr2[2]!=0&&arr2[3]!=0)break;            else            {            int x = (i + 1)%4;            if (x == 0)x = 4;            if (arr[i] == 'R') arr2[0] = x;            else if (arr[i] == 'B')arr2[1]=x;            else if (arr[i] == 'Y')arr2[2] = x;            else if (arr[i] == 'G')arr2[3]=x;            else continue;                        }                            }               for (int i = 0; i < arr.length; i++) {            if (arr[i] == '!')            {              int y = (i + 1) % 4;              if (y == 0) y = 4;              if (y == arr2[0]) cr++;              else if (y == arr2[1])cb++;              else if (y == arr2[2])cy++;              else cg++;                                      }                    }        System.out.println(cr + " " + cb + " " + cy + " " + cg);    }}
