import java.util.*;import java.io.*;public class Main {	static int[] A;	static int[] B;	static int[] C;	static int n;	static int ans;	public static void main(String[] args)throws IOException {		// TODO Auto-generated method stub		BufferedReader in = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer tokenizer = new StringTokenizer(in.readLine());        n = Integer.parseInt(tokenizer.nextToken());        C = new int[n];        int o=0,e=0;        StringTokenizer tokenizer1 = new StringTokenizer(in.readLine());        for(int i=0;i<n;i++) {        	C[i] = Integer.parseInt(tokenizer1.nextToken());        	if(C[i]%2==0) {        		e++;        	}        	else {        		o++;        	}        }        A = new int[e];        B = new int[o];        int cnt1=0,cnt2=0;        for(int i=0;i<n;i++) {        	if(C[i]%2==0) {        		A[cnt1]=C[i];        		cnt1++;        	}        	else {        		B[cnt2]=C[i];        		cnt2++;        	}        }        if(Math.abs(e-o)<=1) {        	System.out.print(0);        	return;        }        Arrays.sort(A);        Arrays.sort(B);        int se = e-1;        int so = o-1;        if(se>so) {        	while(so>=0) {        		B[so]=0;        		so--;        		A[se]=0;        		se--;        	}        	A[se]=0;        	for(int i=0;i<e;i++) {        		ans += A[i];        	}        }        else {        	while(se>=0) {        		B[so]=0;        		so--;        		A[se]=0;        		se--;        	}        	B[so]=0;         	for(int i=0;i<o;i++) {        		ans += B[i];        	}        }        System.out.print(ans);			}	class MyComparator implements Comparator<Integer>{	    @Override	    public int compare(Integer o1, Integer o2) {	        if(o1 < o2) { 	            return 1;	        }else if(o1 > o2) {	            return -1;	        }else {	            return 0;	        }	    }	}	}
