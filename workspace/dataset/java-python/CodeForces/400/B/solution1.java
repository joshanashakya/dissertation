import java.util.*; public class Main {     public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int n = in.nextInt();        int m = in.nextInt();        Set<Integer> set = new HashSet<>();        for (int i = 0; i < n; i++) {            String str = in.next();            int sIndex = 0;            int gIndex = 0;            for (int j = 0; j < m; j++) {                if (str.charAt(j) == 'G') gIndex = j;                else if (str.charAt(j) == 'S') sIndex = j;            }            if (sIndex - gIndex > 0) set.add(sIndex - gIndex);            else{                System.out.println(-1);                System.exit(0);            }        }         System.out.println(set.size());        in.close();     }}       
