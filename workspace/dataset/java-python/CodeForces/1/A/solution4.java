import java.util.Scanner; public class Main {     public static void main(String[] args) {        long m, n, a;        Scanner scanner = new Scanner(System.in);         m = scanner.nextLong();        n = scanner.nextLong();        a = scanner.nextInt();         long result = 0;         if (m >= a) {            if (m % a == 0)                result = m / a;            else                result = m / a + 1;        } else             result = 1;         if (n >= a) {            if(n % a == 0)                result *= n / a;            else                result *= n / a + 1;        }         System.out.println(result);     }}
