number_of_queries = int(input())for _ in range(number_of_queries):    stringed_input = input()    try:        x, y = map(int, stringed_input[1:].split("C"))        if not x or not y:            raise ValueError        column_string = ""        while y:            y, remainder = divmod(y, 26)            if not remainder:                remainder = 26                y -= 1            column_string = chr(64 + remainder) + column_string        print(f"{column_string}{x}")    except ValueError:        column_string = ""        while ord(stringed_input[0]) > 64:            column_string += stringed_input[0]            stringed_input = stringed_input[1:]        column_number = 0        for index, letter in enumerate(column_string[::-1]):            column_number += pow(26, index) * (ord(letter) - 64)        print(f"R{stringed_input}C{column_number}")
