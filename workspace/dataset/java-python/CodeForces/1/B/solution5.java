import java.util.Scanner; public class Main {    public static void main(String args[]) {    	Scanner sc = new Scanner(System.in);    	int tc = sc.nextInt();    	while(tc-->0) {    		String str = sc.next();    		int cIndex = str.indexOf("C");    		if(str.startsWith("R")&&Character.isDigit(str.charAt(1))&&cIndex!=-1) {    			int rol = Integer.parseInt(str.substring(1,cIndex));    			int col = Integer.parseInt(str.substring(cIndex+1));    			StringBuffer sb = new StringBuffer();    			while(col>0) {    				sb.insert(0, (char)((col-1)%26+'A'));    				col = (col-1)/26;    			}    			System.out.println(sb.toString()+rol);    		}else {    			int nIndex = 0;    			while(!Character.isDigit(str.charAt(nIndex)))    				nIndex++;    			int rol = Integer.parseInt(str.substring(nIndex));    			String colstr = str.substring(0,nIndex);    			int col = 0;    			for(int i=0;i<colstr.length();i++) {    				col *= 26;    				col += colstr.charAt(i)-'A'+1;    			}    			System.out.println("R"+rol+"C"+col);    		}    	}    	sc.close();    }}
