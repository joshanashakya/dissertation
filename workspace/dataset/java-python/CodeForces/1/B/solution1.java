import org.omg.Messaging.SYNC_WITH_TRANSPORT; import java.util.Scanner; public class n1B {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();        String s;        for (int i = 0; i < n; i++) {            s = scanner.next();            StringBuilder c1 = new StringBuilder(), n1 = new StringBuilder();            int flag = 0;            for (int j = 0; j < s.length(); j++) {                char t = s.charAt(j);                if (Character.isDigit(t))                    n1.append(t);                if (Character.isUpperCase(t)) {                    if (n1.length() == 0 && flag == 0)                        c1.append(t);                    else {                        flag = 1;                        c1.delete(0, c1.length());                        c1.append(n1.toString());                        n1.delete(0, n1.length());                    }                }            }            if (flag == 0) {                int c = 0;                for (int j = 0; j < c1.length(); j++) {                    c = c * 26 + c1.charAt(j) - 'A' + 1;                }                System.out.println("R" + n1.toString() + "C" + c);            } else {                int num = Integer.parseInt(n1.toString());                StringBuilder res = new StringBuilder();                while (num > 26) {                    int t = num % 26;                    if (t == 0) {                        res.append((char)(t + 25 + 'A'));                        num = (num - 26) / 26;                    }                    else {                        res.append((char)(t - 1 + 'A'));                        num = (num - t) / 26;                    }                }                res.append((char)(num - 1 + 'A'));                System.out.println(res.reverse().toString() + c1);            }        }    }}
