import java.util.*; public class Q3 { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner scanner = new Scanner(System.in);		int n = scanner.nextInt();		int t = scanner.nextInt();		scanner.nextLine();		String str = scanner.nextLine();		while(t>0) {			for(int i=1;i<str.length();i++) {				if(str.charAt(i-1)=='B'&&str.charAt(i)=='G') {					StringBuilder sb = new StringBuilder(str);					sb.setCharAt(i-1, 'G');					sb.setCharAt(i, 'B');					str = sb.toString();					++i;				}			}			--t;		}		System.out.println(str);	}}
