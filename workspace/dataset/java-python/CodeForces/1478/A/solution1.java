import java.util.*; public class Runner {	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);						int tests = sc.nextInt();				for(int t = 0; t < tests; t++) {			int numOfBalls = sc.nextInt();						sc.nextLine();			int[] balls = stringToIntArray(sc.nextLine());						int lowestIndex = 0;			int total = 0;			for(int i = 0; i < numOfBalls; i++) {				int ithBall = balls[i];								if(ithBall == -1) continue;								int cur = ithBall;				for(int j = i+1; j < numOfBalls; j++) {					if(balls[i] == -1) continue;										if(balls[j] > cur) {						cur = balls[j];						balls[j] = -1;					}									}								total++;								balls[i] = -1;			}						System.out.println(total);		}	} 	private static int[] stringToIntArray(String str) {		String[] strs = str.trim().split(" ");				int[] intArr = new int[strs.length];						for(int i = 0; i < strs.length; i++) {			intArr[i] = Integer.parseInt(strs[i]);		}				return intArr;	} }		 	  	 				 					  	  		 		  	
