import java.util.*; public final class Test {     public static void main(String args[]) {        int t;        Scanner sc = new Scanner(System.in);        t = sc.nextInt();        while (t-- > 0) {            int n = sc.nextInt();            int a[] = new int[n];            for (int i = 0; i < n; i++) {                a[i] = sc.nextInt();            }            System.out.println(getMinColor(a));        }        sc.close();    }     static int getMinColor(int[] numbers) {            int maxDuplicateCount = 0;            int curDuplicate = 0;            for(int i=0;i<numbers.length-1;i++) {                curDuplicate = 0;                while(i < numbers.length-1 && numbers[i] == numbers[i+1]) {                    curDuplicate++;                    i++;                }                if(curDuplicate > maxDuplicateCount ) {                    maxDuplicateCount = curDuplicate;                }            }                        return maxDuplicateCount +1;        }}
