import java.io.*;import java.util.*; public class B {    public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer(br.readLine());        int x = Integer.parseInt(st.nextToken());        int ops=0;        StringBuilder ans = new StringBuilder();        if ((x & (x + 1)) == 0)           System.out.println(0);        else{          while((x & (x + 1)) != 0){          	if (x%2==0) {          		ops+=2;          		x++;          		ans.append(0+" ");          		continue;          	}          	ops=ops+2;             int pos = getleftmostunsetbit(x);            int t = (int)Math.pow(2,pos+1)-1;            x=x^t;            x+=1;            ans.append(Integer.toString(pos+1).toString()+" ");          }          System.out.println(ops);          System.out.println(ans);         }     }     public static int getleftmostunsetbit(int n)     {          if ((n & (n + 1)) == 0)             return n;               // Find position of leftmost unset bit.         int pos = 0;         for (int temp = n, count = 0; temp > 0;                            temp >>= 1, count++)             if ((temp & 1) == 0)                  pos = count;           return pos;    }       }
