def count(l):    count_even = 0    count_odd = 0    for i in l:        if not i % 2:            count_even += 1        else:            count_odd += 1    return count_even, count_odd  def main_function():    n, m = [int(i) for i in input().split(" ")]    a = [int(i) for i in input().split(" ")]    b = [int(i) for i in input().split(" ")]    a_even, a_odd = count(a)    b_even, b_odd = count(b)    count_1 = min(a_even, b_odd) + min(b_even, a_odd)    count_2 = min(b_even, a_odd) + min(b_odd, a_even)    return max(count_2, count_1)  print(main_function())
