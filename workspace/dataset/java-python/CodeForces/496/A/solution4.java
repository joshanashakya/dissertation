import java.lang.*;import java.util.*; import java.io.*; public class _temp2 {	public static void main(String[] args)  throws NumberFormatException, IOException {		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		int n = Integer.parseInt(br.readLine());		String[] st = br.readLine().split(" ");		int[] a = new int[n];		for(int i=0;i<n;i++){			a[i]=Integer.parseInt(st[i]);		}		int max=Integer.MIN_VALUE;		for(int i=0;i<n-1;i++){			max=Math.max(max,a[i+1]-a[i]);		}		int min=Integer.MAX_VALUE;		for(int i=0;i<n-2;i++){			min=Math.min(min,a[i+2]-a[i]);		}			System.out.println(Math.max(min,max));	}}
