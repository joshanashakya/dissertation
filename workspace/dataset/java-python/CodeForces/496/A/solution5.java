import java.util.Arrays;import java.util.Scanner; public class InQueue {	static Scanner sc = new Scanner(System.in); 	public static void main(String[] args) { 		int n = sc.nextInt();		int arr[] = new int[n];		for (int i = 0; i < n; i++) {			arr[i] = sc.nextInt();		}		int ans = Integer.MAX_VALUE; 		for (int i = 1; i < n - 1; i++) {			int res = Integer.MIN_VALUE;			for (int j = 0; j < n - 1; j++) {				if (j == i) {					continue;				} else if (j + 1 == i && j + 2 < n) {					res = Math.max(res, arr[j + 2] - arr[j]); 				} else {					res = Math.max(res, arr[j + 1] - arr[j]);				}			}//			System.out.println("The value of res in " + i + " case is " + res);			ans = Math.min(ans, res);		}		System.out.println(ans); 	} }
