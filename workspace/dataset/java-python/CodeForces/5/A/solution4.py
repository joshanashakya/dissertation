'''Jana Goodman 5A chat server'''import sys ADD = '+'REMOVE = '-'SEND = ':'if __name__ == '__main__':    chatters, answer = 0, 0    for command in sys.stdin:        if command[0] == ADD:            chatters += 1        elif command[0] == REMOVE:            chatters -= 1        else:            name, message = command.split(SEND)            answer += chatters * len(message.strip())    print(answer)
