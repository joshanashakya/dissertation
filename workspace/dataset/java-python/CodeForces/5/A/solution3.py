import sys users, ans, n = [], 0, 0 for line in sys.stdin:    line = line.rstrip()    if (line[0:1] == '+') :users.append(line[1:])    elif (line[0:1] == '-') :users.remove(line[1:])    else: n = line.index(':')    if (n != 0) :            ans = ans + len(line[n + 1:]) * len(users)            n = 0    #print(f'line = {line}  len = {len(line)}')print(ans) '''in order to quit press:  ctrl + d                +Mike                 users = ['Mike', 'Tea', 'Dito']Mike:hello            answer = 5 + 4 + 9+TeaTea:hi+DitoDito:hey'''
