import java.io.*;import java.util.*; public class SecretMessage { 	public static void main(String[] args){		       Scanner sc=new Scanner(System.in);		       TreeSet<String> in=new TreeSet();		       int z=0;		       while(sc.hasNext()) {		    	   String s=sc.nextLine();		    	   if(s.charAt(0)=='+') {		    		   in.add(s.substring(1));		    	   }		    	   else if(s.charAt(0)=='-') {		    		   in.remove(s.substring(1));		    	   }else {		    	   String s1[]=s.split(":");		    	   if(s1.length>1) {		    	   z+=in.size()*s1[1].length();		    	   }}		       }		      		      System.out.println(z);		     	} }
