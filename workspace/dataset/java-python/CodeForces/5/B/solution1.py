import syss=[item.strip() for item in sys.stdin] l=0for item in s:    l=max(l,len(item))arr=[]one=0two=1arr.append(['*' for _ in range(l+2)])for item in s:    temp=['*']    z=len(item)    temp+=[' 'for _ in range((l-z+one)//2)]    temp+=[c for c in item]    temp+=[' 'for _ in range((l-z+two)//2)]    temp.append('*')    if (l-z)%2==1:        one=1-one        two=1-two    arr.append(temp)arr.append(['*' for _ in range(l+2)])for item in arr:    print(''.join(item))
