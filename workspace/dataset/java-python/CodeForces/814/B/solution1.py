import sysinput = sys.stdin.buffer.readline n = int(input())a = list(map(int,input().split()))b = list(map(int,input().split())) '''diffs =[]miss = set(list(range(1,n+1)))for i in range(n):    if a[i] != b[i]:        diffs.append(i)    miss.discard(a[i]) if len(diffs) == 1:    # no other option    a[diffs[0]] = list(miss)[0]else:    # must be 2    if {a[diffs[0]],b[diffs[1]]} == miss:        a[diffs[1]] = b[diffs[1]]    else:        a[diffs[0]] = b[diffs[0]]''' two = -1missing = -1 counts = [[] for i in range(n+1)]for i in range(n):    counts[a[i]].append(i)    if len(counts[a[i]]) == 2:        two = a[i] for i in range(1,n+1):    if len(counts[i]) == 0:        missing = i        break def diff(a,b):    diffs = 0    for i in range(n):        diffs += a[i]!=b[i]    return diffs for i in range(2):    perm = a.copy()    perm[counts[two][i]] = missing     if diff(perm,b) == 1:        print(*perm)        break
