import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.math.BigInteger;import java.util.*;import static java.util.Arrays.sort;import javafx.util.Pair; public class Main{     public static void main(String[] args)    {         FastScanner input = new FastScanner();         int n = input.nextInt();        int a[] = new int[n];        int b[] = new int[n];        int store[] = new int[n + 1];        for (int i = 0; i < n; i++) {            a[i] = input.nextInt();         }        for (int i = 0; i < n; i++) {            b[i] = input.nextInt();        }        for (int i = 0; i < n; i++) {            if (a[i] == b[i]) {                store[a[i]]++;            }        }                ArrayList<Integer> ans = new ArrayList<>();        for (int i = 0; i < n; i++) {            if (a[i] != b[i]) {                if (store[b[i]] == 0) {                    store[b[i]]++;                                       ans.add(b[i]);                                    } else if (store[a[i]] == 0) {                    store[a[i]]++;                    ans.add(a[i]);                                     } else {                    for (int j = 1; j <= n; j++) {                        if (store[j] == 0) {                                                        ans.add(j);                                                      store[j]++;                            break;                        }                    }                }            }            else            {                ans.add(a[i]);                        }        }        int acount=0,bcount=0;        int ca[] = new int[2];        int cb[] = new int[2];        int j =0,k=0;        for (int i = 0; i < n; i++) {            if(a[i]!=ans.get(i))            {                ca[j]=i;                j++;                acount++;            }            if(b[i]!=ans.get(i))            {                cb[k]=i;                k++;                bcount++;            }        }//        System.out.println(acount+" "+bcount);        if(acount>1)        {            int temp  = ans.get(ca[0]);            ans.set(ca[0], ans.get(ca[1]));            ans.set(ca[1], temp);                    }        else if(bcount>1)        {            int temp  = ans.get(cb[0]);            ans.set(cb[0], ans.get(cb[1]));            ans.set(cb[1], temp);        }        for (Integer an : ans) {            System.out.print(an+" ");        }         System.out.println("");                     }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
