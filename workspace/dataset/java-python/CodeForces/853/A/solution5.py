import sysinput = sys.stdin.readlinefrom heapq import * n, k = map(int, input().split())c = list(map(int, input().split()))ic = [(-c[i], i+1) for i in range(n)]pq = []idx = 0ans_v = 0ans_arr = [-1]*n for t in range(k+1, k+n+1):    while idx<n:        if ic[idx][1]<=t:            heappush(pq, ic[idx])            idx += 1        else:            break     ci, i = heappop(pq)    ci *= -1    ans_v += (t-i)*ci    ans_arr[i-1] = t    print(ans_v)print(*ans_arr)
