N,M = map(int,input().split())mat1 = [list(map(int, input().split())) for i in range(N)]mat2 = [list(map(int, input().split())) for i in range(N)] ans = 'Possible'for r in range(N):    for c in range(M):        bigger_v = max(mat1[r][c], mat2[r][c])        smaller_v = min(mat1[r][c], mat2[r][c])                if r > 0:            bigger_prev = max(mat1[r-1][c], mat2[r-1][c])            smaller_prev = min(mat1[r-1][c], mat2[r-1][c])            if not (bigger_v > bigger_prev and smaller_v > smaller_prev):                ans = 'Impossible'                break        if c > 0:            bigger_prev = max(mat1[r][c-1], mat2[r][c-1])            smaller_prev = min(mat1[r][c-1], mat2[r][c-1])            if not (bigger_v > bigger_prev and smaller_v > smaller_prev):                # print(r,c)                # print('bigger_v', bigger_v)                # print(bigger_prev)                # print('smaller_v', smaller_v)                # print('smaller_prev', smaller_prev)                # print(r,c)                ans = 'Impossible'                break    if ans == 'Impossible':        breakprint(ans)
