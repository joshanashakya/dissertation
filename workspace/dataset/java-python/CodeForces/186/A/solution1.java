import java.util.Arrays;import java.util.Scanner; public class A186 {    public static void main(String[] args) {        Scanner s = new Scanner(System.in);        char[] first = s.nextLine().toCharArray();        char[] second = s.nextLine().toCharArray();         boolean match = true;        if (first.length != second.length) {            match = false;        } else {            int diff = 0;            for (int i = 0; i < first.length; i++) {                if (first[i] != second[i]) {                    diff++;                }            }            if (diff != 2) {                match = false;            } else {                Arrays.sort(first);                Arrays.sort(second);                match = Arrays.equals(first, second);            }        }        System.out.println(match == true ? "YES" : "NO");      }}
