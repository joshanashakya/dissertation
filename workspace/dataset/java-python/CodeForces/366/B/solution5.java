import java.util.*;public class B_366B { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner s = new Scanner(System.in);		int n = s.nextInt();		int k = s.nextInt();		int[] arr = new int[n];		for(int i = 0 ; i < n ; i++)		{			arr[i] = s.nextInt();		}				solve(arr, n, k);	}		static void solve(int[] arr,int n,int k)	{ 		int[] sum = new int[k];		int ans = 0;		for(int i = 0; i < arr.length ; i++)		{			sum[i%k] += arr[i];		}				for(int i = 0 ; i < k ; i++)		{			if(sum[i] < sum[ans])			{				ans = i;			}		}				System.out.println(ans+1);	} }
