import java.io.OutputStream;import java.io.IOException;import java.io.InputStream;import java.io.PrintWriter;import java.util.StringTokenizer;import java.io.IOException;import java.io.BufferedReader;import java.io.FileReader;import java.io.InputStreamReader;import java.io.FileNotFoundException;import java.io.InputStream; /** * Built using CHelper plug-in * Actual solution is at the top */public class Main {    public static void main(String[] args) {        InputStream inputStream = System.in;        OutputStream outputStream = System.out;        Reader in = new Reader(inputStream);        PrintWriter out = new PrintWriter(outputStream);        TaskA solver = new TaskA();        solver.solve(1, in, out);        out.close();    }     static class TaskA {        public void solve(int testNumber, Reader in, PrintWriter out) {             int n = in.nextInt();             for (int i = 0; i < 4; i++) {                int a = in.nextInt();                int b = in.nextInt();                int c = in.nextInt();                int d = in.nextInt();                 if (a + c <= n) {                    out.println((i + 1) + " " + a + " " + (n - a));                    out.flush();                    return;                }                if (a + d <= n) {                    out.println((i + 1) + " " + a + " " + (n - a));                    out.flush();                    return;                }                if (b + c <= n) {                    out.println((i + 1) + " " + b + " " + (n - b));                    out.flush();                    return;                }                if (b + d <= n) {                    out.println((i + 1) + " " + b + " " + (n - b));                    out.flush();                    return;                }            }             out.println(-1);            out.close();        }     }     static class Reader {        BufferedReader r;        StringTokenizer str;         Reader() {            r = new BufferedReader(new InputStreamReader(System.in));        }         public Reader(InputStream in) {            r = new BufferedReader(new InputStreamReader(in));        }         Reader(String fileName) throws FileNotFoundException {            r = new BufferedReader(new FileReader(fileName));        }         public String getLine() {            try {                return r.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return null;        }         public String next() {            if (str == null || !str.hasMoreTokens()) {                str = new StringTokenizer(getLine());            }            return str.nextToken();        }         public int nextInt() {            return Integer.parseInt(next());        }     }} 
