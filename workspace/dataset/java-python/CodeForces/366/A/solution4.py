n = int(input()) guards = []pos = 0for i in range(4):    guardpost = list(map(int, input().split(' ')))    if i == 0:        guards = guardpost    else:        if sum(guards) > sum(guardpost) and guardpost.count(n) <= 0:            guards = guardpost            pos = i guard1 = min(guards[0], guards[1])guard2 = min(guards[2], guards[3]) if guard1 >= n or guard2 > n-guard1:    print(-1)else:    print(pos+1, guard1, n-guard1)
