import java.io.*;import java.util.*;public class b{    /*		FASTREADER	*/    static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new                    InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }         /*DEFINED BY ME         */        int[] readArray(int n) {             int arr[] = new int[n];             for (int i = 0; i < n; i++)                arr[i] = nextInt();             return arr;        }         void sort(int arr[]) {            ArrayList<Integer> l = new ArrayList<>();            for (int i : arr)                l.add(i);            Collections.sort(l);            for (int i = 0; i < arr.length; i++)                arr[i] = l.get(i);        }    }    // ----------------End of Bismatemplate --------------------------     public static void main(String[] args) {        FastReader in = new FastReader();        int ruby = in.nextInt();        for(int i =1 ; i <= 4;i ++){            int a = in.nextInt(); int b = in.nextInt() ; int c = in.nextInt(); int d = in.nextInt();            if(ruby - Integer.min(a,b) >= Integer.min(c,d)){                System.out.print(i + " ");                System.out.print(Integer.min(a,b) + " ");                int res = Integer.min(a,b);                res = ruby - res;                System.out.println(res);                return;            }        }        System.out.println(-1);     } }
