import java.util.Scanner; public class CarrotCakes {    private static final Scanner SCANNER = new Scanner(System.in);     public static void main(String[] args) {        int n = SCANNER.nextInt();        int t = SCANNER.nextInt();        int k = SCANNER.nextInt();        int d = SCANNER.nextInt();         int g = (n + k - 1) / k ;        int o1 = 0 , o2 = d;        for (int i = 0; i < g; i++) {            if(o1 <= o2)                o1+=t;            else                o2+=t;        }         System.out.println(Math.max(o1, o2) < g * t ? "YES": "NO");    }}
