from sys import stdin n,m = [int(x) for x in stdin.readline().split()] graph = [set([y for y in range(n)]) for x in range(n)]graph2 = [set() for x in range(n)] for x in range(n):    graph[x].remove(x) for edge in range(m):    a,b = [int(x) for x in stdin.readline().split()]    a -= 1    b -= 1     graph[a].remove(b)    graph[b].remove(a)    graph2[a].add(b)    graph2[b].add(a) notVisited = set([x for x in range(n)]) val = [-1 for x in range(n)] for x in range(n):    if not graph[x]:        val[x] = 'b'        notVisited.remove(x) valid = True while notVisited:    for x in notVisited:        q = [(x,'a')]        break    cur = 0    while q:        nxt,char = q.pop()         if char == 'a':            antC = 'c'        else:            antC = 'a'                if nxt in notVisited:            notVisited.remove(nxt)            val[nxt] = char            for x in graph[nxt]:                if x in notVisited:                    q.append((x,antC))                else:                    if val[x] == char:                        valid = False                        break            for x in graph2[nxt]:                if val[x] == antC:                    valid = Falseif valid:    print('Yes')    print(''.join(val))else:    print('No')
