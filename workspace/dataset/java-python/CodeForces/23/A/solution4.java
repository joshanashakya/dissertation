import java.util.HashMap;import java.util.Map;import java.util.Scanner; /** * @author fancy * @date 2020/11/21 21:16 */public class Main {    static Map<String,Integer> map = new HashMap<>();    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        String s = sc.nextLine();        for(int i = 0;i < s.length();i++){            for(int j = 1;j < s.length();j++){                if(i+j <= s.length()) {                    String t = s.substring(i, i + j);                    if(!map.containsKey(t))                        map.put(t,1);                    else                        map.put(t,map.get(t)+1);                }            }        }        int ans = 0;        for(Map.Entry<String,Integer> entry : map.entrySet()){            int k = entry.getValue();            String t = entry.getKey();            if(k >= 2 && t.length() > ans) ans = t.length();        }        System.out.println(ans);    }}
