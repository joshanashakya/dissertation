from collections import Counter  def solve(s):    c = Counter(s)    a = sorted(c.keys())    n = len(a)    if n == 3 and ord(a[0]) + 1 == ord(a[1]):        a.reverse()    res = []    for i in range(n):        if i % 2 == 0:            ch = a[n // 2 + i // 2]        else:            ch = a[i // 2]        res.append(ch * c[ch])    return ''.join(res)  t = int(input())for _ in range(t):    res = solve(input())    if all(abs(ord(x) - ord(y)) != 1 for x, y in zip(res, res[1:])):        print(res)    else:        print("No answer")
