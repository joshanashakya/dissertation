import java.util.*; public class A {    public static void main(String[] args) {        List<String> pre = new ArrayList<>();        List<String> post = new ArrayList<>();        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        for (int i = 0; i < n; i++) {            pre.add(sc.next());            post.add(sc.next());        }/*        if(pre.get(3).equals(post.get(0))){            System.out.println("true!!1");        }else {            System.out.println("false");        }*/        //有一样的则移除，和所对的交换，最后输出即可        int cnt = 0;        Map<String, String> map = new HashMap<>();        for (int i = 0; i < pre.size();i++) {            if (pre.get(i).equals(post.get(i))) {                pre.remove(i);                post.remove(i);                i--;                continue;            }            for (int j = 0; j < post.size(); j++) {                if (pre.get(i).equals(post.get(j))) {                    //System.out.println("哈哈哈哈：" + pre.get(i));                    post.set(j, post.get(i));                    pre.remove(i);                    post.remove(i);                    i--;                }            }        }        System.out.println(pre.size());        for (int i = 0; i < pre.size(); i++) {            System.out.println(pre.get(i) + " " + post.get(i));         }      }}  				 	 	  	 	   						 			
