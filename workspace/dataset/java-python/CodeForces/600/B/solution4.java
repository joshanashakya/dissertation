import java.util.ArrayList;import java.util.Scanner; public class InterestingDrink { 	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int m = sc.nextInt();		ArrayList<Integer> inputA = new ArrayList<>();		for (int i = 0; i < n; i++) {			int x = sc.nextInt();			inputA.add(x);		}		inputA.sort((e1, e2) -> e1.compareTo(e2));				for (int i = 0; i < m; i++) {			int x = sc.nextInt();			System.out.print(calc(inputA, x)+" ");		}	} 	public static int calc(ArrayList<Integer> input, int value) {		int answer = -1;		int l = 0, r = input.size() - 1, mid;		while (l <= r) {			mid = (l + r) / 2;			if (input.get(mid) <= value) {				answer = mid;				l = mid + 1;			} else {				r = mid - 1;			}		} 		return answer + 1;	}}
