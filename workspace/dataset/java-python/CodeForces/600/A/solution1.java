import java.io.*;import java.util.*;  public class Main {    public static Scanner in = new Scanner(new BufferedReader(new InputStreamReader(System.in), 640000));    public static PrintWriter out = new PrintWriter(System.out);      public static void solve() {        String s = in.next();        s += ';';          boolean isNumber = true;        boolean first = true;        boolean leadingZero = false;        StringBuilder part = new StringBuilder();  // ""        ArrayList<String> a = new ArrayList<>();        ArrayList<String> b = new ArrayList<>();         for (int i = 0; i < s.length(); ++i) {            char currChar = s.charAt(i);            if (currChar == ';' || currChar == ',') {                String sol = part.toString();                if (sol.length() < 1)                    isNumber = false;                 if (isNumber) {                    a.add(sol);                } else {                    b.add(sol);                }                 part = new StringBuilder();                isNumber = true;                first = true;                leadingZero = false;             } else {                if (currChar == '-' || currChar == '.' || currChar > '9' || (currChar <= '9' && currChar >= '0' && leadingZero))                    isNumber = false;                 if (currChar == '0' && first)                    leadingZero = true;                  first = false;                 part.append(currChar);             }         }          if (a.size() < 1) {            out.println('-');        } else {            first = true;            out.print('"');            for (int i = 0; i < a.size(); ++i) {                if (!first)                    out.print(',');                 out.print(a.get(i));                first = false;             }            out.println('"');        }             if (b.size() < 1) {            out.println('-');          } else {             first = true;            out.print('"');            for (int i = 0; i < b.size(); ++i) {                if (!first)                    out.print(',');                 out.print(b.get(i));                first = false;             }            out.println('"');        }     }     public static void main(String args[]) {        solve();          out.close();    }}     
