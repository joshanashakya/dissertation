import java.util.*; public class test {	public static void main(String []agrs){		int n;		String s;		Scanner scan=new Scanner(System.in);		n=scan.nextInt();		s=scan.nextLine(); //filter the Enter key		s=scan.nextLine();		s=s+" ";		int ans=0,x=0;		for(int i=0;i<=n;++i)		{			if(s.charAt(i)>=65&&s.charAt(i)<=90)			{				x++;			}			if(s.charAt(i)==' ')			{				if(x>ans)					ans=x;				x=0;			}		}		System.out.println(ans);	}}
