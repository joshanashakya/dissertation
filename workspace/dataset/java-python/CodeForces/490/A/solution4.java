import java.util.ArrayList;import java.util.List;import java.util.Scanner; public class A490 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = scanner.nextInt();        List<Integer> p = new ArrayList<>();        List<Integer> m = new ArrayList<>();        List<Integer> e = new ArrayList<>();        for (int i = 0; i < n; i++) {            int t = scanner.nextInt();            if (t == 1) {                p.add(i + 1);            } else if (t == 2) {                m.add(i + 1);            } else e.add(i + 1);        }        int min = Math.min(Math.min(p.size(), m.size()), e.size());        System.out.println(min);        for (int i = 0; i < min; i++) {            System.out.println(p.get(i) + " " + m.get(i) + " " + e.get(i));        }    }}
