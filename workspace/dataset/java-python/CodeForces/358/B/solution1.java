import java.io.*;import java.util.*; public class Codeforces {    public static void main(String[] args) throws IOException {        Scanner in = new Scanner(System.in);        PrintWriter out = new PrintWriter(System.out);         int n = in.nextInt();        StringBuilder st = new StringBuilder();        st.append("<3");        for (int i = 0; i < n; i++) {            st.append(in.next());            st.append("<3");        }        String all = st.toString();        String s = in.next();        int i = 0;        for (Character c : s.toCharArray()) {            if (i < all.length() && c.equals(all.charAt(i))) {                i++;            }        }        if (i == all.length()) {            out.println("yes");        } else {            out.println("no");        }        out.flush();    }     static class Pair {        int x, y;         Pair(int a, int b) {            x = a;            y = b;        }    }     static class Scanner {        StringTokenizer st;        BufferedReader br;         public Scanner(InputStream s) {            br = new BufferedReader(new InputStreamReader(s));        }         public String next() throws IOException {            while (st == null || !st.hasMoreTokens())                st = new StringTokenizer(br.readLine());            return st.nextToken();        }         public int nextInt() throws IOException {            return Integer.parseInt(next());        }         public long nextLong() throws IOException {            return Long.parseLong(next());        }         public String nextLine() throws IOException {            return br.readLine();        }         public boolean ready() throws IOException {            return br.ready();        }    }} 
