import java.util.Scanner; public class SemiCircles {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();        Integer[] point = new Integer[n];         if (n > 3){            point[0] = scanner.nextInt();            point[1] = scanner.nextInt();            point[2] = scanner.nextInt();             for (int i = 3; i < n; i++) {                point[i] = scanner.nextInt();                 for (int j = 1; j < i - 1; j++) {                    int a = Math.max(point[i], point[i - 1]);                    int b = Math.min(point[i], point[i - 1]);                    int c = Math.max(point[j], point[j - 1]);                    int d = Math.min(point[j], point[j - 1]);                     if((b > d && b < c && a > c) || (d > b && d < a && c > a)) {                        System.out.println("yes");                        return;                    }                }            }        }        System.out.println("no");    }}
