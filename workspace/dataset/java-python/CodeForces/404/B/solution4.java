import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.Scanner;import java.util.StringTokenizer;import java.util.*;public class Main{  static ArrayList<Integer> a[];  static boolean vis[];  static int last=0;    static class FastReader    {        BufferedReader br;        StringTokenizer st;         public FastReader()        {            br = new BufferedReader(new                     InputStreamReader(System.in));        }         String next()        {            while (st == null || !st.hasMoreElements())            {                try                {                    st = new StringTokenizer(br.readLine());                }                catch (IOException  e)                {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {            return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine()        {            String str = "";            try            {                str = br.readLine();            }            catch (IOException e)            {                e.printStackTrace();            }            return str;        }    }     public static void main(String[] args)    {        FastReader sc=new FastReader();        double side=sc.nextDouble();        double repeat=sc.nextDouble();        StringBuilder sb=new StringBuilder();        int num=sc.nextInt();        for( int i=1 ; i <= num; i++ )        {          double time=repeat*i;          double dist=time%(4*side);          if(dist==0)          {            sb.append(0+" "+0);          }          else          if(dist<side)          {            sb.append(dist+" "+0);          }          else if(dist>=side&&dist<2*side)          {            sb.append(side+" "+(dist-side));          }          else if(dist>=2*side&&dist<3*side)          {            sb.append((side-(dist-2*side))+" "+side);          }          else if(dist>=3*side&&dist<4*side)          {            sb.append(0+" "+(side-(dist-3*side)));          }sb.append("\n");        }        System.out.println(sb.toString());     } }
