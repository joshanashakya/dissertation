import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}				static int gcd(int a, int b) {						if(b == 0) {				return a;			}						return gcd(b, a % b);		}			public static void main(String[] args) throws IOException {				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = io.nextInt();		char matrix[][] = new char[n][n];				for(int i = 0; i < n; i++) {			String str = io.next();			for(int j = 0; j < n; j++) {				matrix[i][j] = str.charAt(j);			}		}				if(isX(matrix)) {			System.out.println("YES");		}		else {			System.out.println("NO");		}	}		static boolean isX(char matrix[][]) {				int n = matrix.length;				char c = matrix[n / 2][n / 2];				char c2 = 'a';				if(n > 1) {			c2 = matrix[0][1];			if(c2 == c) {				return false;			}		}		for(int i = 0; i < n; i++) {						for(int j = 0; j < n; j++) {								if((i == j || i + j == n - 1)) {					if(matrix[i][j] != c) {						return false;					}				}				else if(matrix[i][j] != c2){					return false;				}			}					}				return true;	}}
