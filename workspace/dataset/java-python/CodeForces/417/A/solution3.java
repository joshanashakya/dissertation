import java.util.*;import java.io.*; public class CJ2 { 	public static void main(String[] args) throws Exception {		// TODO Auto-generated method stub		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		String inp[] = br.readLine().split(" ");		int c = Integer.parseInt(inp[0]);		int d = Integer.parseInt(inp[1]);		inp=br.readLine().split(" ");		int n = Integer.parseInt(inp[0]);		int m = Integer.parseInt(inp[1]);		int k= Integer.parseInt(br.readLine());		int minfinalists=n*m-k;		int x=0; //rounds main, c problems		int y=0; //rounds additional, d problems		int ymax=minfinalists;		int minscore=Integer.MAX_VALUE;		if (k>=n*m)		{			minscore=0;			minfinalists=0;			ymax=-1;		}		for (int i=0;i<ymax+1;i++)		{			y=i;			x=(int)Math.ceil((float)(minfinalists-y)/n);			minscore=Math.min(x*c + y*d,  minscore);		}		System.out.println(minscore);	} }
