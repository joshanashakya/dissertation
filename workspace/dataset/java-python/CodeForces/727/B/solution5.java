import java.util.Locale;import java.util.Scanner; public class Project{    public static void main(String[] args)    {        Scanner sc = new Scanner(System.in);        String[] str = sc.next().replaceFirst("[a-z]+", "").split("[a-z]+", -1);        int len = str.length;         long gold = 0;        long coins = 0;         for (int i = 0; i < len; i++) {            int lastPoint = str[i].lastIndexOf('.');             if (str[i].substring(lastPoint + 1).length() == 2) {                gold += Integer.parseInt(str[i].substring(0, lastPoint).replaceAll("\\.", ""));                coins += Integer.parseInt(str[i].substring(lastPoint + 1));            } else {                gold += Integer.parseInt(str[i].replaceAll("\\.", ""));            }         }         gold += coins / 100;        coins %= 100;         if (gold == 0) {            System.out.printf(Locale.GERMAN, "0.%02d", coins);        } else if (coins == 0) {            System.out.printf(Locale.GERMAN, "%,d", gold);        } else {            System.out.printf(Locale.GERMAN, "%,d.%02d", gold, coins);        }    }}
