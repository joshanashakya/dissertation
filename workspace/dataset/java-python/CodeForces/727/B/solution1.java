import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.text.DecimalFormat;import java.util.Locale; public class Checksum {    public static void main(String[] args) throws IOException {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        String s = reader.readLine();        double checksum = 0;        for (int i = 0; i < s.length(); i++) {            int z = i;            if (Character.isDigit(s.charAt(i))) {                StringBuilder currentPrice = new StringBuilder();                for (int j = z; j < s.length(); j++) {                    char currentSymbol = s.charAt(j);                    i = j;                    if (Character.isDigit(currentSymbol) || Character.toString(currentSymbol).equals(".")) {                        currentPrice.append(Character.toString(currentSymbol));                    } else break;                }                String[] k = currentPrice.toString().split("\\.");                currentPrice = new StringBuilder();                for (int j = 0; j < k.length; j++) {                    if (k.length>1&&j+1==k.length && k[j].length()==2){                        currentPrice.append(".").append(k[j]);                    }else currentPrice.append(k[j]);                }                checksum += Double.parseDouble(currentPrice.toString());            }        }        s = String.format("%,.2f",checksum);        s = s.replaceAll(",",".");        if (s.substring(s.length()-2).equals("00"))s = s.substring(0,s.length()-3);        System.out.println(s);    }}
