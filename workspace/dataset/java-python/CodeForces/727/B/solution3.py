s = input() prices = [] cur = ""for i in s:    if i.isdigit() or i == ".":        cur += i    else:        if cur != "":            prices += [cur]            cur = "" if cur != "":    prices += [cur] total = 0 for price in prices:    if price.rfind(".") == len(price)-1-3:        price = price.replace(".", "")        total += round(float(price), 2)    elif price.rfind(".") == len(price)-1-2:        #print(price)        price = list(price)        pr = ""        for x in range(len(price)):            if price[x] == "." and x != len(price) - 3:                continue            else:                pr += price[x]        total += round(float("".join(pr)), 2)    else:        total += round(float(price), 2) total = round(float(total), 2) pr = str(total) p = pr.split(".") count = 0px = ""for j in range(len(p[0])-1, -1, -1):    i = p[0][j]    px = i + px    count += 1    if count % 3 == 0:        px = "." + pxif px[0] == ".":    px = px[1:] if p[1].replace("0", "") == "":    print(px)else:    p[1] = p[1]+"00"    print(px+"."+p[1][:2])
