import java.math.BigInteger;import java.util.*;import java.lang.*;import java.util.Arrays;   public class geek {      public static void main(String[] args) {        Scanner s=new Scanner(System.in);        try{                 StringBuffer sb=new StringBuffer();                 StringBuffer sbb=new StringBuffer();                int x=s.nextInt();                int y=s.nextInt();                int m=1;                boolean flag=true;                List<Integer> l=new ArrayList<>();                l.add(y);                while (x<y){                    if(y%2==0){                        y/=2;                       l.add(y);                    }else if(y%10==1){                        y/=10;                        l.add(y);                    }else{                        flag=false;                        break;                    }                }                for(int i=l.size()-1;i>=0;i--){                    sbb.append(l.get(i)+" ");                }                if(flag&&x==y){                    sb.append("YES\n"+l.size()+"\n"+sbb+"\n");                }else sb.append("NO\n");             System.out.println(sb);                   //solve.show(a);            //  solve.show(b);             }catch (Exception e){            System.out.println(e.getMessage());        }     } }          
