import java.lang.*;import java.util.*;import java.io.*; public class XXXXX {	static List<Integer> ans;	public static boolean isWin(int n, int m, List<Integer> temp)	{		if(m<n)return false;				if(n==m)		{			ans = new ArrayList<Integer>(temp);			return true;		}				boolean ans=false;		if(m%2==0)		{			temp.add(m/2);			ans |= isWin(n,m/2, temp);			temp.remove(temp.size()-1);		}		else if(m%10==1)		{			temp.add((m-1)/10);			ans |= isWin(n,(m-1)/10 ,temp);			temp.remove(temp.size()-1);		}				return ans;			}	public static void main(String[] args) {				Scanner sc = new Scanner(System.in);					int n  = sc.nextInt();			int m  = sc.nextInt();			ans = new ArrayList<Integer>();			List<Integer> temp = new ArrayList<Integer>();			temp.add(m);			boolean win = isWin(n,m , temp);			if(win)			{			System.out.println("YES");			System.out.println(ans.size());			for(int i=ans.size()-1;i>=0;i--)System.out.print(ans.get(i)+" ");			}			else			{				System.out.println("NO");			}						}}
