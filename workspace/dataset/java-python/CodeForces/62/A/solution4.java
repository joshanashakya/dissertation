import java.util.*;import java.io.*; public class  Tournament{	public static void main(String[] args) throws IOException 	{		BufferedReader brs=new BufferedReader(new InputStreamReader(System.in));		PrintWriter pw=new PrintWriter(System.out); 		int t=1; 		while(t-->0)		{ 						String str[]=brs.readLine().trim().split(" ");			int gl=Integer.parseInt(str[0]);			int gr=Integer.parseInt(str[1]);			str=brs.readLine().trim().split(" ");			int bl=Integer.parseInt(str[0]);			int br=Integer.parseInt(str[1]);						if(check(gl,br)||check(gr,bl))				pw.println("YES");			else				pw.println("NO");		}		pw.flush();	} 	public static boolean check(int g,int b)	{		if(b<g-1)			return false; 		b=b-2*(g-1);		int x=b/2;		b-=x; 		if(b<3 && x<3)			return true;		else			return false;	}	}
