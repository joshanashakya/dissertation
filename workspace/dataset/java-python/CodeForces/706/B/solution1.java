import java.io.FileNotFoundException;import java.io.PrintWriter;import java.math.BigInteger;import java.util.Arrays;import java.util.Comparator;import java.util.Scanner; public class Main {      public static void main(String[] args) throws FileNotFoundException {        Scanner sc = new Scanner(System.in);        PrintWriter pw = new PrintWriter(System.out);        int n = sc.nextInt();        int[] a = new int[n];        for (int i = 0; i < n; i++) {            a[i] = sc.nextInt();        }        int k = sc.nextInt();        int[][] b = new int[k][2];        for (int i = 0; i < k; i++) {            b[i][0] = sc.nextInt();            b[i][1] = i;        }        Arrays.sort(a);        Arrays.sort(b, new Comparator<int[]>() {            @Override            public int compare(int[] o1, int[] o2) {                if (o1[0] == o2[0]) return Integer.compare(o1[1], o2[1]);                return Integer.compare(o1[0], o2[0]);            }        });        int[] ans = new int[k];        int id = 0;        for (int i = 0; i < k; i++) {            while (id < a.length && b[i][0] >= a[id]) {                id++;            }            ans[b[i][1]] = id;        }        for (int i = 0; i < ans.length; i++) {            System.out.println(ans[i]);        }        pw.close();    }}
