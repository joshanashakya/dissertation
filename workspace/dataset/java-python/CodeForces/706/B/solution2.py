import math def binarySearch(itemArr:list,item:int):    low=0    mid=0    high=len(itemArr)-1    if item >= itemArr[high]:        return high+1    if item < itemArr[0]:        return 0    index=0     while low<=high:        mid=math.floor((low+high)/2)        if item==itemArr[mid]:           index=mid           low=mid+1        elif item > itemArr[mid]:            if item < itemArr[mid+1]:                return mid+1            else:                low=mid+1        elif item< itemArr[mid]:                        if item > itemArr[mid-1]:                return mid            else:                high=mid-1    return index+1   if __name__=='__main__':    import math    shop=int(input())    BottlePricePerShop = [int(x) for x in input().split()]    days = int(input())    coinPerDays=[]    for i in range(days):        coinPerDays.append(int(input()))    BottlePricePerShop.sort()     for i in coinPerDays:        print(binarySearch(BottlePricePerShop,i),end='\n')
