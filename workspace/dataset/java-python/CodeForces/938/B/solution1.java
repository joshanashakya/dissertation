import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class Bouncing  { 	public static void main(String[] args) throws IOException {		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int[] ar = new int[n];		for(int i=0;i<n;i++)ar[i]=sc.nextInt();				int lasti=1,lastj=1000000,min=0,i=0,j=n-1;		while(i<=j)		{			if(ar[i]-lasti <= lastj-ar[j])			{				lastj -= ar[i]-lasti;				min += ar[i]-lasti;				lasti = ar[i];				i++;			}			else 			{				lasti += lastj - ar[j];				min += lastj - ar[j];				lastj = ar[j];				j--;			}			//System.out.println(min);		}		System.out.println(min);				}}
