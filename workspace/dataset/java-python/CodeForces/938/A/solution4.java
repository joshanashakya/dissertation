import java.io.BufferedReader;import java.io.BufferedWriter;import java.io.InputStreamReader;import java.io.OutputStreamWriter; public class Main {     public static void main(String[] args) throws Exception {        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));        BufferedWriter writer = new BufferedWriter(new OutputStreamWriter(System.out));        /*        int n = Integer.parseInt(reader.readLine());                for (int i = 0; i < n; i++) {            String word = reader.readLine();            if(word.length() <= 10){                writer.write(word);            } else{                writer.write("" + word.charAt(0) + (word.length() - 2) + word.charAt(word.length()-1));            }            writer.newLine();        }        writer.flush();         */        int n = Integer.parseInt(reader.readLine());        String v = "aouiey";        String word = reader.readLine();        int count = 0;        for (int i = 0; i < word.length(); i++) {             if (v.contains("" + word.charAt(i))) {                count++; // 0                if (count <= 1) {                    writer.write("" + word.charAt(i));                }            } else {                count = 0;                writer.write("" + word.charAt(i));            }        }        writer.newLine();        writer.flush();    } }/*4wordlocalizationinternationalizationpneumonoultramicroscopicsilicovolcanoconiosis  */
