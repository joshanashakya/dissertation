import java.util.Scanner; public class MusketeerSelecter {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);         int n = scanner.nextInt();         boolean[][] graph = new boolean[n][n];        int[] sum = new int[n];         int m = scanner.nextInt();         for (int i = 0; i < m; i++) {            int x = scanner.nextInt() - 1;            int y = scanner.nextInt() - 1;             graph[x][y] = true;            graph[y][x] = true;             sum[x]++;            sum[y]++;        }         int min = 16000001;         for (int i = 0; i < n; i++) {            for (int j = i + 1; j < n; j++) {                if(graph[i][j]) {                    for (int k = j + 1; k < n; k++) {                        if(graph[j][k] && graph[i][k]){                            min = Math.min(sum[i] + sum[j] + sum[k], min);                        }                    }                }            }        }         if(min == 16000001) min = 5;        System.out.println(min - 6);    }}
