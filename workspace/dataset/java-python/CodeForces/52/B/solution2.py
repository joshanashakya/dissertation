import sys data = []line1 = Trueasterisks = []cols = []for line in sys.stdin:    if line1:        line1 = False        dim = line[:-1].split(" ")        for i in range(int(dim[1])):            cols.append(0)    else:        row = []        for i, c in enumerate(line):            if c == "*":                row.append(i+1)                cols[i] += 1        asterisks.append(row) sum = 0for row in asterisks:    if len(row) >= 2:        vertices = []        for i, item in enumerate(row):            sum += (cols[item-1] - 1) * (len(row) - 1)print(sum)
