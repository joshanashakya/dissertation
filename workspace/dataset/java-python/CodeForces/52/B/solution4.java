import java.io.BufferedReader;import java.io.IOException;import java.io.InputStream;import java.io.InputStreamReader;import java.io.PrintWriter;import java.util.StringTokenizer; public class Main {	public static void main(String[] args) throws IOException {		//MScanner sc = new MScanner(System.in);		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));		PrintWriter pw=new PrintWriter(System.out);		StringTokenizer st=new StringTokenizer(br.readLine());		int n=Integer.parseInt(st.nextToken());int m=Integer.parseInt(st.nextToken());		char[][]in=new char[n][m];		for(int i=0;i<n;i++) {			in[i]=br.readLine().toCharArray();		}		int[]rows=new int[n];		int[]columns=new int[m];		for(int i=0;i<n;i++) {			int c=0;			for(int j=0;j<m;j++) {				if(in[i][j]=='*')c++;			}			rows[i]=c;		}		for(int i=0;i<m;i++) {			int c=0;			for(int j=0;j<n;j++) {				if(in[j][i]=='*')c++;			}			columns[i]=c;		}		long ans=0;		for(int i=0;i<n;i++) {			for(int j=0;j<m;j++) {				if(in[i][j]=='*') {					long r=rows[i]-1;long c=columns[j]-1;					ans+=r*c;				}			}		}		pw.println(ans);		pw.close();		pw.flush(); 	} }
