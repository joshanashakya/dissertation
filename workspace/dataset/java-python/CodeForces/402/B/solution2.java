import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader; import java.util.*;import javafx.util.Pair; public class Main{     public static void main(String args[])    {        FastScanner input = new FastScanner();       int n = input.nextInt();      int k = input.nextInt();      int a[] = new int[n];        for (int i = 0; i < n; i++) {            a[i] = input.nextInt();        }                int anscount=Integer.MAX_VALUE;       StringBuilder result = new StringBuilder();                for (int j = 1; j <=1000; j++) {            int sum = 0;            int count=0;             StringBuilder ans = new StringBuilder();                                  for (int i = 0; i < n; i++) {                         if(a[i]<(j+(i*k)))            {              count++;                ans.append("+ "+(i+1)+" "+Math.abs(a[i]-(j+(i*k)))+"\n");            }             else if(a[i]>(j+(i*k)))            {                count++;                                ans.append("- "+(i+1)+" "+Math.abs(a[i]-(j+(i*k)))+"\n");             }                                 }            if(anscount>count)            {                anscount = count;                result = ans;                            }        }                System.out.println(anscount);        System.out.println(result);            }     static class FastScanner    {         BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringTokenizer st = new StringTokenizer("");         String next()        {            while (!st.hasMoreTokens()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt()        {            return Integer.parseInt(next());        }         long nextLong()        {             return Long.parseLong(next());        }         double nextDouble()        {            return Double.parseDouble(next());        }         String nextLine() throws IOException        {            return br.readLine();        }    } }
