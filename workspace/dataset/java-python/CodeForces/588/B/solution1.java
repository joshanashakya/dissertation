import java.util.*; public class DuffInLove {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        Queue<Long> bigger = new ArrayDeque<>();        Stack<Long> smaller = new Stack<>();        long n = scanner.nextLong();         List<Long> squares = new ArrayList<>((int) Math.sqrt(n));        for (long i = 1; i * i <= n; i++) {            squares.add(i * i);            if (n % i == 0) {                bigger.add(n / i);                smaller.push(i);            }        }         while (!bigger.isEmpty()) {            long t = bigger.poll();            boolean flag = false;            for (int i = 1; i < squares.size() && t >= squares.get(i); i++) {                if (t % squares.get(i) == 0) {                    flag = true;                    break;                }            }            if (!flag) {                System.out.println(t);                return;            }        }         while (!smaller.isEmpty()) {            long t = smaller.pop();            boolean flag = false;            for (int i = 1; i < squares.size() && t >= squares.get(i); i++) {                if (t % squares.get(i) == 0) {                    flag = true;                    break;                }            }            if (!flag) {                System.out.println(t);                return;            }        }    }}
