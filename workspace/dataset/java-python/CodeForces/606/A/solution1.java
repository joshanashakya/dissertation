import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(double a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            double temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }     public static void main(String[] args) throws IOException    {         Scanner input = new Scanner(System.in);        long extraA=0,extraB=0,extraC=0,NeedA=0,NeedB=0,NeedC=0;        long a  =input.nextLong();        long b = input.nextLong();        long c = input.nextLong();        long x = input.nextLong();        long y = input.nextLong();        long z = input.nextLong();        if((a+b+c)<(x+y+z))        {            System.out.println("NO");        }        else        {            if(a>x)            {                extraA=a-x;                extraA/=2;            }            if(b>y)            {                extraB = b-y;                extraB/=2;            }            if(c>z)            {                extraC = c-z;                extraC/=2;            }            if(a<x)            {                NeedA = x-a;            }            if(b<y)            {                NeedB = y-b;            }            if(c<z)            {                NeedC = z-c;                            }//            System.out.println((NeedA+NeedB+NeedC)+" "+(extraA+extraB+extraC));             if((NeedA+NeedB+NeedC)<=(extraA+extraB+extraC))             {                 System.out.println("YES");             }             else                 System.out.println("NO");                    }            } }
