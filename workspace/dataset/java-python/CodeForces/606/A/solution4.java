import java.io.*;import java.util.*; import static java.lang.Integer.*;import static java.lang.System.*; public class A {     public static void main(String[] args) throws IOException {        BufferedReader in = new BufferedReader(new InputStreamReader(System.in));        PrintWriter pw = new PrintWriter(System.out);        StringTokenizer st;        st = new StringTokenizer(in.readLine());        int a = parseInt(st.nextToken()), b = parseInt(st.nextToken()), c = parseInt(st.nextToken());        st = new StringTokenizer(in.readLine());        int x = parseInt(st.nextToken()), y = parseInt(st.nextToken()), z = parseInt(st.nextToken());        pw.println(solve(a, b, c, x, y, z));        pw.close();    }     static String solve(int a, int b, int c, int x, int y, int z) {        if (a >= x) {            a -= x;            x = 0;            // out.println(a);        } else {            x -= a;            a = 0;        }        if (b >= y) {            b -= y;            y = 0;        } else {            y -= b;            b = 0;        }        if (c >= z) {            c -= z;            z = 0;        } else {            z -= c;            c = 0;        }        //out.println((a + b + c) + " " + (x + y + z));        if (((a / 2) + (b / 2) + (c / 2)) >= (x + y + z)) return "Yes";        return "No";    }}
