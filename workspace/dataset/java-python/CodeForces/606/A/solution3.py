a = [int(i) for i in input().split()]need = [int(i) for i in input().split()]for i in range(3):    k = min(a[i], need[i])    a[i] -= k    need[i] -= kfor i in range(3):    for j in range(3):        if i == j or a[i] < 2 or need[j] == 0:            continue        k = need[j] * 2        k = min(a[i] // 2 * 2, k)        a[i] -= k        need[j] -= k // 2# print(need)print("Yes" if need == [0, 0, 0] else "No")
