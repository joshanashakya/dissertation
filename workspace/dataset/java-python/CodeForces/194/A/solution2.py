import sysimport mathfrom collections import Counter n, k = list(map(int, input().split())) near = k / nif near.is_integer():    print(n) if near == 2 else print(0)    sys.exit() possibles = list(map(int, (n * f'{math.floor(near)} ').split())) for i, number in enumerate(possibles):    possibles[i] = possibles[i] + 1    if sum(possibles) > k:        possibles[i] = possibles[i] - 1        break r = dict(Counter(possibles))print(r.get(2, 0))  		  		 		 	  		 	    	    	 	
