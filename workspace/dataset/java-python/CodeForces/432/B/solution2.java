import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}		public static void main(String[] args){				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int n = io.nextInt();				int x[] = new int[n + 1];		int y[] = new int[n + 1];				for(int i = 1; i <= n; i++) {			int a = io.nextInt();			x[i] = a;			int b = io.nextInt();			y[i] = b;		}				int count[] = new int[(int)1e5 + 1];				for(int i = 1; i < n + 1; i++) {			count[x[i]]++;		}				int hkc[] = new int[n + 1];		int okc[] = new int[n + 1];				for(int i = 1; i < n + 1; i++) {						hkc[i] = n - 1;			hkc[i] += count[y[i]];						okc[i] = 2 * (n - 1) - hkc[i];		}				for(int i = 1; i <= n; i++) {			System.out.println(hkc[i] + " " + okc[i]);		}			}}
