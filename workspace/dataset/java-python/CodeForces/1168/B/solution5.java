import java.util.*;import java.util.Arrays; public class Main {	static int inf = 1000000000;	static int MOD = 1000000007;	public static void main(String ards[])	{		Scanner cin = new Scanner(System.in);        String s1 = cin.next();        StringBuilder str = new StringBuilder(" ");        str.append(s1);        int len = str.length();        String s = str.toString();        int p = 0;        long ans = 0;        for(int i = 3; i < len; i++){            for(int j = p+1; j <= i-2; j++){                if((i-j)%2 == 0 && s.charAt(i) == s.charAt(j) && s.charAt(i) == s.charAt((i+j)/2)) p = j;            }            ans+=p;        }                 System.out.println(ans);	}}	    			   	 	 	   		   	 		 		
