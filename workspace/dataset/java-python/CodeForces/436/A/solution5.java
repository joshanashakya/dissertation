import java.util.*; public class Main {    int n, x;     public static void main(String... args) {        new Main().calculate();    }     void calculate() {        Scanner in = new Scanner(System.in);        n = in.nextInt();        x = in.nextInt();        ArrayList<Candy> Arr = new ArrayList<>();        for (int i = 0; i < n; i++)            Arr.add(new Candy(in.nextInt(), in.nextInt(), in.nextInt()));        System.out.print(Math.max(maxC(Arr, 0, x), maxC(Arr, 1, x)));    }     int maxC(ArrayList<Candy> cs, int t, int x) {        ArrayList<Candy> arrayList = new ArrayList<>(cs);        arrayList.removeIf((c) -> c.type != t || c.height > x);        if (arrayList.isEmpty())            return 0;        Collections.sort(arrayList, Comparator.comparing(Candy::getMass));        ArrayList<Candy> arr1 = new ArrayList<>(cs);        Candy c = arrayList.get(arrayList.size()-1);        arr1.remove(c);        return maxC(arr1, 1-t, x+c.mass)+1;    }} class Candy {    int type, height, mass;    public Candy(int t, int h, int m) {        this.type = t;        this.height = h;        this.mass = m;    }    public int getMass() {        return mass;    }}
