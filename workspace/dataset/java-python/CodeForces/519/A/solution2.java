import java.util.*; public class Test {    public static void main(String[] args) {         Scanner input = new Scanner(System.in);        int w = 0, b = 0;        for (int a = 0; a < 8; a++) {            char[] s = input.next().toCharArray();            for (int i = 0; i < 8; i++) {                switch (s[i]) {                    case 'P': w++; break;                    case 'B': w+=3; break;                    case 'N': w+=3; break;                    case 'R': w+=5; break;                    case 'Q': w+=9; break;                    case 'p': b++; break;                    case 'b': b+=3; break;                    case 'n': b+=3; break;                    case 'r': b+=5; break;                    case 'q': b+=9;                }            }               }        if (w > b) {            System.out.println("White");        } else if (w < b) {            System.out.println("Black");        } else System.out.println("Draw");    }}
