import sys input = sys.stdin.readline  def gcd(a, b):    if a == 0:        return b    return gcd(b % a, a)  def lcm(a, b):    return (a * b) / gcd(a, b) def main():    a,ta=map(int, input().split())    b,tb=map(int, input().split())    hh,mm=map(int, input().split(':'))    secs=hh*60+mm-(5*60-1)    curr=0    i=0    ans=0    x1=hh*60+mm    y1=x1+ta    for x2 in range(5*60,24*60,b):        y2=x2+tb        x=max(x1,x2)        y=min(y1,y2)        if (x<y):            ans+=1    print(ans)         return if __name__=="__main__":    main() 
