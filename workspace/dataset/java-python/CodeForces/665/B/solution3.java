import java.util.Scanner;import java.util.LinkedList; public class p3{	public static void main(String[] args) {		Scanner scan = new Scanner(System.in);		LinkedList <Integer> lista = new LinkedList <Integer>(); 		int n = scan.nextInt();		int m = scan.nextInt();		int k = scan.nextInt();		int suma = 0; 		for(int i = 0; i < k; i++){ 			lista.addLast(scan.nextInt()); 		} 		for(int i = 0; i < n; i++){ 			for(int j = 0; j < m; j++){ 				int aux = scan.nextInt();				int l = 0;				while(aux != lista.get(l)){ 					l++; 				} 				for(int p = 0; p < l; p++){ 					lista.add(l, lista.removeFirst()); 				} 				suma += l+1; 			} 		} 		System.out.println(suma); 	}}  		  			   		 				 		       	
