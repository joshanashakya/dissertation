import java.util.*; public class Main {    public static void main(String[] args) {        Scanner in = new Scanner(System.in);        int t = in.nextInt();        for (int i = 0; i < t; i++) {            ArrayList<Integer> rounds = new ArrayList<>();            int tenth = 0;            int n = in.nextInt();            while (n > 0) {                int x = (n % 10) * (int) Math.pow(10, tenth++);                if (x > 0)                    rounds.add(x);                n /= 10;            }            String s = "";            for (int num : rounds) {                s = s + num + " ";            }            s = s.substring(0, s.length() - 1);            System.out.println(rounds.size());            System.out.println(s);        }     }  }
