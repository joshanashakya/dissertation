/* package codechef; // don't place package name! */ import java.util.*;import java.lang.*;import java.io.*; /* Name of the class has to be "Main" only if the class is public. */public class Main{	public static void main (String[] args) throws java.lang.Exception	{		// your code goes here		Scanner sc=new Scanner(System.in);		int t=sc.nextInt();		while(t-->0) {		    int n=sc.nextInt();		    int a[]=new int[n];		    for(int i=0;i<n;i++)		    a[i]=sc.nextInt();		    boolean flag=true;		    int pre=a[0];		    int min=a[0];		    int sum=0;		    int prevsum=0;		    for(int i=1;i<n;i++) {		        if(a[i]>pre) 		        prevsum+=a[i]-pre;		      //  min=Math.min(min,a[i]);		        		       		        		        if(a[i]<prevsum)		        flag=false;		        pre=a[i];		        		    }		    if(flag)		    System.out.println("YES");		    else 		    System.out.println("NO");		}	}}
