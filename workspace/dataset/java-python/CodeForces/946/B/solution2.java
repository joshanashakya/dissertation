import java.util.Scanner;import java.util.Arrays;import java.util.Vector;import java.util.Collections;   public class Problem_Solving1 {           public static void main(String[] args) {        // TODO code application logic here        Scanner input = new Scanner(System.in);        long a , b ;        a = input.nextLong();        b = input.nextLong();                while (true) {                     if (a == 0 || b == 0)                break;            else if (a >= 2 * b)                a = a % (2 * b);            else if (b >= 2 * a)                b = b % (2 * a);            else break;                    }        System.out.println(a + " " + b);            }}
