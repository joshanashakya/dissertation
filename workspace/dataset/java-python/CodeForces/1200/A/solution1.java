/*input8LLRL1RL1*/ import java.io.*;import java.math.*;import java.util.*; public class Main {	public static void main(String[] args) throws IOException {		FastReader scan = new FastReader();		PrintWriter out = new PrintWriter(System.out);		Task solver = new Task();		ans = new StringBuilder();		int T = 1;		for (int tt = 0; tt < T; tt++)			solver.solve(tt, scan, out);		out.close();	} 	static StringBuilder ans; 	static class Task {		private String[] split; 		public void solve(int testNumber, FastReader scan, PrintWriter out) {			int n = scan.nextInt();			String s = scan.next();			int a[] = new int[10];			int l = 0, r = 9;			for (char c : s.toCharArray()) {				if (c == 'L') {					for (int i = 0; i < 10; i++) {						if (a[i] == 0) {							a[i] = 1;							break;						}					}				} else if (c == 'R') {					for (int i = 9; i >= 0; i--) {						if (a[i] == 0) {							a[i] = 1;							break;						}					}				} else					a[(c - 48)] = 0;			}			for (int x : a)				out.print(x);		}	} 	static class FastReader {		BufferedReader br;		StringTokenizer st; 		public FastReader() {			br = new BufferedReader(new InputStreamReader(System.in));		} 		public FastReader(String s) throws FileNotFoundException {			br = new BufferedReader(new FileReader(new File(s)));		} 		String next() {			while (st == null || !st.hasMoreElements()) {				try {					st = new StringTokenizer(br.readLine());				} catch (IOException e) {					e.printStackTrace();				}			}			return st.nextToken();		} 		int nextInt() {			return Integer.parseInt(next());		} 		long nextLong() {			return Long.parseLong(next());		} 		double nextDouble() {			return Double.parseDouble(next());		} 		String nextLine() {			String str = "";			try {				str = br.readLine();			} catch (IOException e) {				e.printStackTrace();			}			return str;		}	}}
