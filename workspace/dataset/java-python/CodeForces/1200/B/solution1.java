import java.lang.*;import java.util.*;import java.util.stream.Collectors;import java.io.*; public class absolute {	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner sc = new Scanner(System.in);		int T = sc.nextInt();		while(T-->0)		{			int n = sc.nextInt();			int m = sc.nextInt();			int k = sc.nextInt();			int[] ar = new int[n];			for(int i=0;i<n;i++)ar[i]=sc.nextInt();			System.out.println(isPossible(ar,k,m)?"YES":"NO");					}		}	public static boolean isPossible(int[] ar, int k,int m)	{		int cur=m;		for(int i=0;i<ar.length-1;i++)		{			//System.out.println(i+" "+cur);			if(ar[i] == ar[i+1])			{				cur += Math.min(ar[i], k);			}			else if(ar[i]>ar[i+1])			{				cur += (ar[i]-ar[i+1]);				ar[i]=ar[i+1];				cur += (Math.min(ar[i], k));			}			else			{				int j = ar[i+1]-ar[i];				if(j<=k)				{					cur += Math.min(ar[i],(k-j));				}				else if(j-k> cur)return false;				else				{					j-=k;					cur-=j;				}			}		}		return true;	}	}
