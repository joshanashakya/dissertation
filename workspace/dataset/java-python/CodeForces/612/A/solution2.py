sn = list(map(int,input().split()))n = sn[0]p = sn[1]q = sn[2] s = input() lim = 0flag = Falseflag2 = Falsestrings = []while(lim <= n):    if((n - lim)%p == 0):        flag = True        break    elif((n - lim)%q == 0):        flag = True        break    lim += p lim = 0if(flag):    # keep with p    while((n - lim)%q != 0 and n - lim >= 0):        strings.append(s[lim:lim+p])        lim+=p        # now with q    while(n - lim >= 0):        strings.append(s[lim:lim+q])        lim+=q        flag2 = True        if(flag2):        lim-=q    else:        lim-=p        if(n - lim != 0):        print(-1)    else:        print(len(strings) - 1)        for i in strings:            print(i)else:    print(-1)
