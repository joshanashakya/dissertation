n, xs, ys = map(int,input().split())s = input()for x in range(n//xs+1):    if (n-x*xs)%ys == 0:        y = (n-x*xs)//ys        print(x+y)        for i in range(x):            print(s[i*xs:(i+1)*xs])        s = s[x*xs:len(s)]        for j in range(y):            print(s[j * ys:(j + 1) * ys])        exit()print(-1)   # O(n) solution above #O(n^2) will also work contraints are small. '''import sysn, p, q = list(map(int, input().split()))s = input()for a in range(n + 1):	for b in range(n + 1):		if a * p + b * q == n:			print(a + b)			for i in range(a):				print(s[i * p : i * p + p])			s = s[p * a:]			p = q			a = b			for i in range(a):				print(s[i * p : i * p + p])			sys.exit(0)			print("-1")  '''
