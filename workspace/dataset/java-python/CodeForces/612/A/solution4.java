import java.util.Scanner; public class ER4A { 	public static void main(String[] args) {		// TODO Auto-generated method stub		Scanner scn = new Scanner(System.in);		int n = scn.nextInt();		int a = scn.nextInt();		int b = scn.nextInt();		String s = scn.next();		int x = (n / a) * a;		int y = 0;		int c2 = (n / a);		int len = s.length() - x;		y = (len / b) * b;		int c = (len / b);		boolean flag = false;		while (x >= 0 && y <= s.length()) { 			if (x + y == n) {				flag = true;				System.out.println(c2 + c);				len -= y;				int ptr = 0;				while (c2 > 0) {					System.out.println(s.substring(ptr, ptr + a));					ptr = ptr + a;					c2--;				}				while (ptr < s.length()) {					System.out.println(s.substring(ptr, ptr + b));					ptr += b;				}				break;			}			int prevx = x;			int prevy = y;			c2--;			x -= a;			c2 = Math.max(c2, 0);			x = Math.max(x, 0);			len = s.length() - x;			y = (len / b) * b;			c = (len / b);			if(y==prevy && x==prevx) {				break;			} 		}		if (!flag)			System.out.println(-1);	} }
