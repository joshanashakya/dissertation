import java.util.Arrays;import java.util.Scanner; public class Candies {	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		int testcases = sc.nextInt();		sc.nextLine(); 		for (int i = 0; i < testcases; i++) {			String array = sc.nextLine();			int[] stream = Arrays.stream(array.split(" ")).mapToInt(Integer::parseInt).toArray();			System.out.println(output(stream));		}	} 	private static int output(int[] stream) {		int out = 0;				int a = stream[0]/ stream[1];				int left = stream[0] - (stream[1] * a);				if (left > stream[1]/2) {			left = stream[1]/2;		}				return stream[1]*a + left;					}	}
