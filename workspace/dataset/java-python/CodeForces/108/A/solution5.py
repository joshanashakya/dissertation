timeNow = input() # HH:MM hour = timeNow[:2]minuteNow = int(timeNow[3:]) reverse = lambda x: x[::-1] reversedHour = reverse(hour) if int(reversedHour) > 59 or not int(reversedHour) > minuteNow:	hour = str(int(hour) + 1)	if len(hour) < 2:		hour = f"0{hour}"		reversedHour = reverse(hour) 	while (int(reversedHour) > 59):		hour = str(int(hour) + 1)		if len(hour) < 2:			hour = f"0{hour}" 		reversedHour = reverse(hour) if int(hour) > 23:	hour = 0 hour = str(hour)if len(hour) == 1:	hour = f"0{hour}" print(f"{hour}:{reverse(hour)}")
