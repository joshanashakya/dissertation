#include<bits/stdc++.h>#define it register int#define ct const int#define il inlineusing namespace std;typedef long long ll;#define rll register ll#define cll const ll #define P 998244353const int N=1000005;int n,a[N],ans;multiset<int> S;namespace io{	il char nc(){		static char buf[100000],*p1=buf,*p2=buf;		return p1==p2&&(p2=(p1=buf)+fread(buf,1,100000,stdin),p1==p2)?EOF:*p1++; 	}	template <class I> 	il void fr(I &num){		num=0;register char c=nc();it p=1;		while(c<'0'||c>'9') c=='-'?p=-1,c=nc():c=nc();		while(c>='0'&&c<='9') num=num*10+c-'0',c=nc();		num*=p;	}}using namespace io;map<int,int> cn;int main(){ 	fr(n);it i;	for(i=1;i<=n;++i) fr(a[i]),++cn[a[i]],cn[a[i]]>ans?ans=cn[a[i]]:0;//S.insert(a[i]);	//for(i=1;i<=n;++i)		//*S.rbegin()<=a[i]?S.erase(S.begin()):(++ans,S.erase(std::upper_bound(S.begin(),S.end(),a[i])));		//*S.rbegin()<=a[i]?a[i]=*S.begin(),S.erase(S.begin()):(a[i]=*std::upper_bound(S.begin(),S.end(),a[i]),++ans,S.erase(S.find(a[i])));	printf("%d",n-ans);	return 0;}
