import java.util.Scanner;public class Main {	public static void main(String[] args) {		try(Scanner input = new Scanner(System.in)){			int format =  input.nextInt();			input.nextLine();			char[] time = input.nextLine().toCharArray();			System.out.println(displayCorrectTime(time,format));		}	} 	private static String displayCorrectTime(char[] time, int format) {		StringBuilder hhmm = new StringBuilder();		String[] newTime = new String[5];		newTime[2] = ":";		newTime[4] = String.valueOf(time[4]); 		int firstHourDigit = Integer.parseInt(String.valueOf(time[0]));		int secondHourDigit = Integer.parseInt(String.valueOf(time[1]));		int firstMinuteDigit = Integer.parseInt(String.valueOf(time[3]));		if(firstMinuteDigit>=6) {			newTime[3] = "0";		}else {			newTime[3] = String.valueOf(time[3]);		}		if(format==12) {			if(firstHourDigit>1 && secondHourDigit>2) {				newTime[0] = "0";				newTime[1] = String.valueOf(secondHourDigit);			}else if(firstHourDigit>1 && secondHourDigit<=2) {				newTime[0] = "1";				newTime[1] = String.valueOf(secondHourDigit);			}else if(firstHourDigit==1 && secondHourDigit>2) {				newTime[0] = String.valueOf(firstHourDigit);				newTime[1] = "0";			}else if(firstHourDigit==0 && secondHourDigit==0) {				newTime[0] = String.valueOf(firstHourDigit);				newTime[1] = "1";			}else if(firstHourDigit<=1) {				newTime[0] = String.valueOf(firstHourDigit);				newTime[1] = String.valueOf(secondHourDigit);			}		}else {			if(firstHourDigit>2) {				newTime[0] = "1";				newTime[1] = String.valueOf(secondHourDigit);			}else if (firstHourDigit==2 && secondHourDigit>=4){				newTime[0] = String.valueOf(firstHourDigit);				newTime[1] = "3";			}else{				newTime[0] = String.valueOf(firstHourDigit);				newTime[1] = String.valueOf(secondHourDigit);			}		}		for(String newDigit:newTime) {			hhmm.append(newDigit);		}		return hhmm.toString();	}}			   			 			 	    	 		   		 	
