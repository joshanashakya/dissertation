import java.io.*;import java.util.*; public class Main{     static BufferedReader br;    static StringTokenizer st;    static PrintWriter pr=new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));        public static class AL<T> extends ArrayList<T> {};    public static class HM<T,Integer> extends HashMap<T,T> {};    public static class HS<T> extends HashSet<T> {};        static final List<Character> lst=Arrays.asList('a','e','i','o','u','y');        public static void main(String[] args) throws IOException 	{		br=new BufferedReader(new InputStreamReader(System.in));        st=new StringTokenizer(br.readLine());        int n=nxtInt(),a[]=new int[n];                st=new StringTokenizer(br.readLine());        for(int i=0;i<n;i++) a[i]=nxtInt();                HashSet<Character> vowel=new HashSet<>(lst);                boolean flg=true;    	for (int i=0;i<n;i++)     	{    		char[] s=nxtLn().toCharArray();    		int v=0;    		for(int j=0;j<s.length;j++) if(vowel.contains(s[j])) v++;    		if(v!=a[i])     		{    		    flg=false;    		    break;    		}    	}        pr.print(flg?"YES":"NO"); 		pr.flush();		pr.close();    }        static int nxtInt(){        return Integer.parseInt(nxt());    }        static long nxtLong(){        return Long.parseLong(nxt());    }        static double nxtDoub(){        return Double.parseDouble(nxt());    }        static String nxt(){        return st.nextToken();    }        static String nxtLn() throws IOException{        return br.readLine();    }}
