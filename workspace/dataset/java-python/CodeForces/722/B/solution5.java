import java.io.*;import java.util.*;  public class Main{     public static void main(String args[]){         Read input  = new Read();        int n = input.nextInt(), num[] = new int[n];        for (int i=0;i<n;i++) num[i] = input.nextInt();         boolean ok = true;         for (int i=0;i<n;i++){            String lin = input.line();            int cur = 0;            for (int j =0 ;j<lin.length();j++){                char x = lin.charAt(j);                if (x=='a' || x=='e' || x=='i' || x=='o' || x=='u' || x=='y')                    cur++;            }             if (cur!=num[i]){                ok = false;                break;            }        }         if (ok) System.out.print("YES"); else  System.out.print("NO");     }     private static class Read{        BufferedReader br;        StringTokenizer tk;         public Read(){            br = new BufferedReader(new InputStreamReader(System.in));        }         String next(){            while (tk==null || !tk.hasMoreElements()){                try {                    tk = new StringTokenizer(br.readLine());                } catch (IOException e){                    e.printStackTrace();                }            }            return tk.nextToken();        }         double nextDouble(){return Double.parseDouble(next());}        int nextInt(){return Integer.parseInt(next());}        long nextLong(){return Long.parseLong(next());}        String line(){            String ans ="";            try {                ans = br.readLine();            } catch (IOException e){                e.printStackTrace();            }            return ans;        }     }   }
