import java.io.IOException;import java.util.*;import java.util.Arrays;import java.util.Scanner; public class Bit_year{ 	public static void main(String[] args) throws IOException {				Scanner sc = new Scanner(System.in);		String s = sc.next();		char[] ch = s.toCharArray();		List<Integer> heavy = new ArrayList<Integer>();		List<Integer> metal = new ArrayList<Integer>();		for(int i=0;i<ch.length;i++)		{			if(i+5 > s.length())break;			String t = s.substring(i,i+5);			//System.out.println(t);			if(t.equals("heavy")) {				heavy.add(i);				i+=4;			}			else if(t.equals("metal")) {				metal.add(i);				i+=4;			}		}		long count=0;		int lastj=0;		//System.out.println("Heavy "+heavy.toString());		//System.out.println("Metal "+metal.toString());		for(int i=0;i<heavy.size();i++)		{			for(int j=lastj;j<metal.size();j++)			{				if(metal.get(j)>heavy.get(i)) {					count += metal.size()-j;					lastj=j;					break;				}			}		}		System.out.println((long)count);	}}
