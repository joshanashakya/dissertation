import java.util.*;import java.io.*; public class Main2 {	static class FastReader     {         BufferedReader br;         StringTokenizer st;          public FastReader()         {             br = new BufferedReader(new                     InputStreamReader(System.in));         }          String next()         {             while (st == null || !st.hasMoreElements())             {                 try                {                     st = new StringTokenizer(br.readLine());                 }                 catch (IOException  e)                 {                     e.printStackTrace();                 }             }             return st.nextToken();         }          int nextInt()         {             return Integer.parseInt(next());         }          long nextLong()         {             return Long.parseLong(next());         }          double nextDouble()         {             return Double.parseDouble(next());         }          String nextLine()         {             String str = "";             try            {                 str = br.readLine();             }             catch (IOException e)             {                 e.printStackTrace();             }             return str;         }     } 	static FastReader sc = new FastReader();	public static void main (String[] args) {		PrintWriter out = new PrintWriter(System.out);		int t = 1;//    	t = sc.nextInt();    	 while(t-->0) {       		int n = sc.nextInt();       		int d = sc.nextInt();       		int l = sc.nextInt();       		int a[] = new int[n];       		a[0] = d;       		boolean flag = false;       		for(int i=0;i<n-1;i++) {       			if(i==n-2 && (a[i]>=l || a[i]<=-l)) {       				flag = true;       				break;       			}       			else if(a[i]>=l) {       				a[i+1] = -(a[i]-l);       				a[i] = l;       			}       			else if(a[i]<0) {       				a[i+1] = (1 - a[i]);       				a[i] = 1;       			}       			else {       				a[i+1] = 1;       				a[i] = a[i]+1;       			}       		}       		if(flag)       			out.write("-1");       		else {       			for(int i=0;i<n;i++)       				out.write(a[i]+" ");       		}       	}		out.close();	}	    private static long pow(int i, int x) {		long ans = 1;		while(x>0) {			if(x%2==0) {				i*=i;				x/=2;			}			else {				ans*=i;				x--;			}		}//		System.out.println(ans+" ans");		return ans;	}}
