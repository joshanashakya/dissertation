import java.io.*; public class Main {    static String str;    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));     public static void main(String[] args) throws IOException {        str = br.readLine();        StringBuilder sb = new StringBuilder();        for (int i = 0, j; i < str.length(); ++i) {            if (str.charAt(i) == '/') {                j = i;                while (j < str.length() && str.charAt(j) == '/') {                    ++j;                }                sb.append(str.charAt(i));                i = j - 1;            } else {                sb.append(str.charAt(i));            }        }        int k = sb.length() - 1;        bw.write(sb.length() > 1 && sb.charAt(k) == '/' ? sb.substring(0, k) : sb.toString());        bw.close();    }}
