	import java.io.File;	import java.io.FileNotFoundException;	import java.io.PrintWriter;	import java.util.Arrays;	import java.util.Scanner;	import java.util.Stack;		public class test {			public static void main(String[] args)  {				Scanner sc = new Scanner(System.in);			String str = sc.next();						String output = "";			boolean open = false, f = false;			for(int i =0 ; i < str.length() ; i++){			    if(str.charAt(i) == '/'){			    	open = true;			    }else{			    	if(open){			    		open= false;			    		output += '/';			    	}			    	f = true;			    	output += str.charAt(i);			    }			}			if(open && !f)				output += '/';			System.out.println(output);		}		}
