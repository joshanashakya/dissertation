#codigo BIT++ by:David Bautista n=int(input(""))     #un input para ingresar el numero de codigos Bit a insertar. x=0                   #se estabelce el valor de x segun dice el ejercicio.   for i in range(n):     #un rango para generar n inouts para insertar los codigos   op=input("")         #se genera el input   if op[1]=="+":        #condicionales para que se sume o se reste por unidad     x+=1                  if op[1]=="-":               x-=1             print(x)   
