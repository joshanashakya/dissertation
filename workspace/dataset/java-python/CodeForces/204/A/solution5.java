import java.util.Scanner; public class A {     public static int MOD = 1_000_000_007;    public static void main(String[] args) {         Scanner scanner = new Scanner(System.in);        long l = scanner.nextLong();        long r = scanner.nextLong();        long L = Interval(l - 1);        long R = Interval(r);//        System.out.println(Interval(r) - Interval(l - 1));        System.out.println(R - L);/*     int res = 0;     for(int i = 1; i <= 1024; ++i){         if (i < 10) ++res;         else {             int a = i % 10;             int x = i;             while(x >= 10){                 x /= 10;             }             if (a == x) ++res;         }     }         System.out.println(res);*/    }         public static long Interval(long n){        if (n < 10) return n;        long ans = 9;        int d = digits(n);        for (int i = 2; i < d; ++i){            ans += 9 * (long)Math.pow(10, i - 2);        }        ans += lastDigit(n);        return ans;    }     public static int digits(long n){        int ans = 0;        while(n > 0){            ++ans;            n /= 10;        }        return ans;    }     public static long lastDigit(long n){         long m = n;        long r = m % 10;        long l = 0;         while (m >= 10){            m /= 10;        }         l = m;         long ans = (l - 1) * (long)Math.pow(10, digits(n) - 2);        long k = Long.parseLong(Long.toString(n).substring(1)) / 10;        if (l > r){          return k + ans;        }else {            return k + ans + 1;        }    }}
