import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = Integer.parseInt(scanner.nextLine());        for (int i = 0; i < t; i++) {            int n = scanner.nextInt();            int m = scanner.nextInt();            scanner.nextLine();            int sum = 0;            String[] s = scanner.nextLine().split(" ");            for (int j = 0; j<n; j++) {                sum += Integer.parseInt(s[j]);            }             System.out.println(sum>=m? m:sum);         }      }}
