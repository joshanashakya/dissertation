# ================================================== Q1# t = int(input())## for _ in range(t):#     n,x = tuple(map(int,input().split()))#     a = list(map(int,input().split()))##     dp = [0]*101#     for i in a:#         dp[i] = 1##     sm = 0#     i = 1#     while i<len(dp) and x>0:#         if dp[i] == 0:#             x-=1#         i+=1##     if x > 0:#         i-=1#         i += x#     else:#         while i<len(dp) and dp[i] == 1:#             i+=1#         i-=1#     print(i)   # ================================================ Q2 t = int(input()) for _ in range(t):    n = int(input())    a = list(map(int,input().split()))    mx = max(a)    ans = set()    used = [0]*(mx+1)    l1 = -1     for i in range(len(a)):        if used[a[i]] == 1:            l1 = i            break        else:            used[a[i]] = 1     if l1!=-1 and sorted(a[:l1]) == list(range(1,l1+1)) and sorted(a[l1:]) == list(range(1,len(a)-l1+1)):        ans.add((l1,len(a)-l1))     used = [0]*(mx+1)    l1 = -1    for i in range(len(a)-1,-1,-1):        if used[a[i]] == 1:            l1 = i            break        else:            used[a[i]] = 1     if l1!=-1 and sorted(a[l1+1:]) == list(range(1,len(a)-l1)) and sorted(a[:l1+1]) == list(range(1,l1+2)):        ans.add((l1+1,len(a)-l1-1))      print(len(ans))    if len(ans) > 0:        for i,j in ans:            print(i,j) 
