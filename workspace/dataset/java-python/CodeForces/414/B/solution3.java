import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.OutputStreamWriter;import java.io.PrintWriter;import java.io.StreamTokenizer;import java.util.Arrays; public class Main {		public static void main(String[] args) throws IOException {		StreamTokenizer st = new StreamTokenizer(new BufferedReader(new InputStreamReader(System.in)));		PrintWriter pw = new PrintWriter(new OutputStreamWriter(System.out));		int MOD = 1000000007;		st.nextToken();		int n = (int) st.nval;		st.nextToken();		int k = (int) st.nval;		int[] dp = new int[n+1];		int[] tmp = new int[n+1];		Arrays.fill(dp, 1);		for (int i = 2; i <= k; i++) {			Arrays.fill(tmp, 0);			for (int j = 1; j <= n; j++) {				for (int t = 1; t*j <= n; t++) {					tmp[t*j] = (tmp[t*j] + dp[j]) % MOD;				}			}			for (int j = 1; j <= n; j++) {				dp[j] = tmp[j];			}		}		int ans = 0;		for (int i = 1; i <= n; i++) {			ans = (ans + dp[i]) % MOD;		}		pw.print(ans % MOD);		pw.flush();	} }
