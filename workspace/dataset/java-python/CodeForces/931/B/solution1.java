import java.util.Scanner;  public class WorldCup { 	/**	 * @param args	 */	public static void main(String[] args) {		// TODO Auto-generated method stub 		Scanner sc = new Scanner(System.in);		int n = sc.nextInt();		int a = sc.nextInt();		int b = sc.nextInt();		int first  = (a+1)/2;		int second = (b+1)/2;		int  count = 1;		int round = 1;		for(int i=1;i<n;i++){			if(Math.pow(2, i) >= n)				break;			round++;		}//		System.out.println(round);		while(first != second){			first = (first+1)/2;			second = (second+1)/2;			count++;//			if(first == second)//				break;		}		if(round == count)			System.out.println("Final!");		else		System.out.println(count);			} }
