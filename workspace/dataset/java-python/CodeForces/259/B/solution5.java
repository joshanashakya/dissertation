import java.util.*;import java.lang.*;import java.io.*; 	public class FastIO{					BufferedReader br;		StringTokenizer st;				public FastIO(){ // constructor						br = new BufferedReader(new InputStreamReader(System.in));		}				String next(){						while (st == null || !st.hasMoreElements()){				try{					st = new StringTokenizer(br.readLine());				}								catch (IOException e){					e.printStackTrace();				}			}			return st.nextToken();		}				int nextInt(){						return Integer.parseInt(next());		}				long nextLong(){						return Long.parseLong(next());		}				double nextDouble(){			return Double.parseDouble(next());		} 		String nextLine(){						String str = "";			try{								str = br.readLine();			}			catch (IOException e){								e.printStackTrace();			}			return str;		}		public static void main(String[] args){				FastIO io = new FastIO();				// int n = io.nextInt(); -> for int		// double d = io.nextDouble(); -> for double		// String ch = io.next(); -> for one word		// String str = io.nextLine(); -> for the entire line				int matrix[][] = new int[3][3];				for(int i = 0; i < 3; i++) {			for(int j= 0; j < 3; j++) {				matrix[i][j] = io.nextInt();			}		}				int r1 = 0, r2 = 0, r3 = 0;				for(int x : matrix[0]) {			r1 += x;		}				for(int x : matrix[1]) {			r2 += x;		}				for(int x : matrix[2]) {			r3 += x;		}				int a = (r3 + (r2 - r1)) / 2;				int b = a - (r2 - r1);				int c = r1 - b;				matrix[0][0] = a;		matrix[1][1] = b;		matrix[2][2] = c;				for(int arr[] : matrix) {			for(int x : arr) {				System.out.print(x + " ");			}			System.out.println();		}	}}
