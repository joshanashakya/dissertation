// Created by Shora#include <bits/stdc++.h>#define print(_v) for (auto &_i : _v) { cout << _i << ' '; } cout << '\n';#define pii pair<int, int>#define X first#define Y secondusing ll = long long;using namespace std;const int INF = 0x3f3f3f3f; void solve() {    int n, m, q; cin >> n >> m >> q;    vector<vector<int>> a(q, vector<int>(4)), res(n, vector<int>(m));    for (int i = 0; i < q; ++i) {        cin >> a[i][0];        if (a[i][0] <= 2) {            cin >> a[i][1]; a[i][1]--;        } else {            cin >> a[i][1] >> a[i][2] >> a[i][3];            a[i][1]--; a[i][2]--;        }    }    reverse(a.begin(), a.end());    for (int i = 0; i < q; ++i) {        if (a[i][0] <= 2) {            if (a[i][0] == 1) {                // xoay hang ri sang phai                int r = a[i][1];                for (int i = m-1; i > 0; --i) swap(res[r][i], res[r][i-1]);            } else /* if (a[i] == 2) */ {                // xoay cot ci xuong duoi                int c = a[i][1];                for (int i = n-1; i > 0; --i) swap(res[i][c], res[i-1][c]);            }        } else res[a[i][1]][a[i][2]] = a[i][3];    }    for (int i = 0; i < n; ++i) {        print(res[i]);    }} int main() {    cin.tie(0)->sync_with_stdio(0);    // int t; cin >> t; while (t--)    solve();    return 0;}
