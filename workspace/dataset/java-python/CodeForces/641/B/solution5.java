import java.util.Scanner; public class LittleArtemandMatrix { 	public static void main(String[] args){		Scanner scan = new Scanner(System.in);		int n = scan.nextInt(), m = scan.nextInt(), q = scan.nextInt();		int[][] a = new int[n][m];		int[][] c = new int[q][4];		for(int i = 0 ; i < q ; i++){			int cc = scan.nextInt();			c[i][0] = cc;			if(cc < 3) c[i][1] = scan.nextInt();			else for(int j = 1 ; j <= 3 ; j++)c[i][j] = scan.nextInt();		}		for(int i = q-1 ; i >= 0 ; i--){			if(c[i][0] < 3){				if(c[i][0] == 1){ // row 1st becomes last					int row = c[i][1]-1, last = a[row][m-1];					for(int j = m-1 ; j > 0 ; j--) a[row][j] = a[row][j-1];					a[row][0] = last;				}				if(c[i][0] == 2){ // column 1st becomes last					int column = c[i][1]-1, last = a[n-1][column];					for(int j = n-1 ; j > 0 ; j--) a[j][column] = a[j-1][column];					a[0][column] = last;				}			}			else{				a[c[i][1]-1][c[i][2]-1] = c[i][3];			}		}		for(int i = 0 ; i < n ; i++){			for(int j = 0 ; j < m ; j++) System.out.print(a[i][j] + " ");			System.out.println();		}	}}
