import com.sun.org.apache.bcel.internal.generic.AALOAD;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.io.PrintWriter;import java.math.BigInteger;import java.util.*;import java.util.stream.IntStream;import javafx.util.Pair; public class Main{     static void sort(int a[])    {        Random ran = new Random();        for (int i = 0; i < a.length; i++) {            int r = ran.nextInt(a.length);            int temp = a[r];            a[r] = a[i];            a[i] = temp;        }         Arrays.sort(a);    }     public static void main(String[] args) throws IOException    {         Scanner input = new Scanner(System.in);         int n = input.nextInt();        String s = input.next();        int a[] = new int[n];        for (int i = 0; i < n; i++) {            a[i] = input.nextInt();        }        int count=0;        for (int i = 0; i < n; ) {            if(count>n)            {                System.out.println("INFINITE");                return;            }            count++;            if(s.charAt(i)=='>')            {                if((i+1+a[i])>n)                {                    System.out.println("FINITE");                    return;                }                else                {                    i=(i+a[i]);                }                            }            else if(s.charAt(i)=='<')            {                if(((i+1)-a[i])<=0)                {                    System.out.println("FINITE");                    return;                }                else                {                    i= i-a[i];                }            }        }        System.out.println("INFINITE");    } }
