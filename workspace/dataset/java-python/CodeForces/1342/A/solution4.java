import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        long t = sc.nextInt();        while (t-- > 0) {            long x = sc.nextLong(), y = sc.nextLong(), a = sc.nextLong(), b = sc.nextLong();            long c = Math.min(b,a+a);            long ans = (Math.max(x,y) - Math.min(x,y)) * a + (Math.min(x,y) * c);            System.out.println(ans);        }    }}
