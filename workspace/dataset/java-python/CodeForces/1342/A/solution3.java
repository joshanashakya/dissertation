import java.util.Scanner; public class _0601RoadToZero { 	static long RoadToZero(long x,long y,long a,long b) {				long diff=Math.abs(x-y);		return(Math.min((diff*a+(Math.min(x, y)*b)),(x+y)*a));			}		public static void main(String[] args) {				Scanner sc = new Scanner(System.in);		int cases=sc.nextInt();		while(cases>0) {			long x=sc.nextLong();			long y=sc.nextLong();			long a=sc.nextLong();			long b=sc.nextLong();			System.out.println(RoadToZero(x, y, a, b));			cases--;		} 			} }
