import java.util.ArrayList;import java.util.Scanner; public class problem53 { 	public static void main(String[] args) {		// TODO Auto-generated method stub        Scanner sc = new Scanner(System.in);        ArrayList<Integer> al = new ArrayList<>();        int n = sc.nextInt();        int ar[] = new int[n+1];        int seats=0;        int m=0;        int k=0;                for(int i=1;i<=n;i++)        {        	ar[i] = sc.nextInt();        	seats = seats+ar[i];        }        if(ar[1]>seats/2)        {        	al.add(1);        	k++;        	m=1;        }        else         {        	int total=ar[1];        	al.add(1);        	k=1;        	        	for(int j=2;j<=n;j++)        	{                 	        		if((total<=seats/2)&&(ar[1]>=2*ar[j]))        		{        			//m=1;        			total = total+ar[j];        			al.add(j);        			k++;        		}        		if(total>seats/2)        			m=1;        	        }	}        if(m==0)        {        	System.out.println("0");        }        else        {        System.out.println(k);        for(int i=0;i<al.size();i++)        	System.out.print(al.get(i)+" ");	}}}
