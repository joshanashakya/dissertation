//package com.company;import java.io.*;import java.util.*; public class Main {     // Use greedy ONLY when locally optimum result yields globally optimum result.     public static void main(String[] args) throws IOException {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        PrintWriter pw = new PrintWriter(System.out);        /*int t = Integer.parseInt(br.readLine());        while(t-->0){        StringTokenizer st = new StringTokenizer(br.readLine());        }*/        String s = br.readLine();        long ow=0,w=0;        long ans=0;        for(int i=s.length()-1;i>0;i--){            if(s.charAt(i)=='v'&&s.charAt(i-1)=='v'){                w++;                ans+=ow;            }else if(s.charAt(i)=='o'){                ow+=w;            }        }        pw.println(ans);        pw.flush();        pw.close();    }}
