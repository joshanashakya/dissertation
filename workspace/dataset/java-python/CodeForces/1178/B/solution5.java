import java.io.*;import java.util.*;import java.lang.*; public class c1 {    public static FScanner scan;    public static PrintWriter out;    public static void main(String[] args) {        scan=new FScanner();        out=new PrintWriter(new BufferedOutputStream(System.out));         int T=1;//        int T=scan.nextInt();        while(T-->0) {             String s=scan.nextLine();            int n=s.length();            int[] pre=new int[n],suf=new int[n];            for(int i=1;i<n;i++) {                if(s.charAt(i)=='v'&&s.charAt(i-1)=='v') pre[i]++;                pre[i]+=pre[i-1];            } for(int i=n-2;i>=0;i--) {                if(s.charAt(i)=='v'&&s.charAt(i+1)=='v') suf[i]++;                suf[i]+=suf[i+1];            } long ans=0;            for(int i=0;i<n;i++) if(s.charAt(i)=='o') ans+=(long)(pre[i])*(long)(suf[i]);            out.println(ans);         } out.close();    }       //------------------------------------------------------------------------------------------------------------------     //scanner    public static class FScanner {        BufferedReader br;        StringTokenizer st;         public FScanner() {            br = new BufferedReader(new InputStreamReader(System.in));        }         String next() {            while (st == null || !st.hasMoreElements()) {                try {                    st = new StringTokenizer(br.readLine());                } catch (IOException e) {                    e.printStackTrace();                }            }            return st.nextToken();        }         int nextInt() {            return Integer.parseInt(next());        }         long nextLong() {            return Long.parseLong(next());        }         double nextDouble() {            return Double.parseDouble(next());        }         String nextLine() {            String str = "";            try {                str = br.readLine();            } catch (IOException e) {                e.printStackTrace();            }            return str;        }    }}
