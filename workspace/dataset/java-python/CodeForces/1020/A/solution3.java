import java.util.Collections;   import java.util.*;import java.lang.*;import java.io.*;import java.util.Arrays; public class CodeChef {	public static void merge(int arr[], int l, int m, int r)     {         // Find sizes of two subarrays to be merged         int n1 = m - l + 1;         int n2 = r - m;           /* Create temp arrays */        int L[] = new int[n1];         int R[] = new int[n2];           /*Copy data to temp arrays*/        for (int i = 0; i < n1; ++i)             L[i] = arr[l + i];         for (int j = 0; j < n2; ++j)             R[j] = arr[m + 1 + j];           /* Merge the temp arrays */          // Initial indexes of first and second subarrays         int i = 0, j = 0;           // Initial index of merged subarry array         int k = l;         while (i < n1 && j < n2) {             if (L[i] <= R[j]) {                 arr[k] = L[i];                 i++;             }             else {                 arr[k] = R[j];                 j++;             }             k++;         }           /* Copy remaining elements of L[] if any */        while (i < n1) {             arr[k] = L[i];             i++;             k++;         }           /* Copy remaining elements of R[] if any */        while (j < n2) {             arr[k] = R[j];             j++;             k++;         }     }       // Main function that sorts arr[l..r] using     // merge()    public static  void sort(int arr[], int l, int r)     {         if (l < r) {             // Find the middle point             int m = (l + r) / 2;               // Sort first and second halves             sort(arr, l, m);             sort(arr, m + 1, r);               // Merge the sorted halves             merge(arr, l, m, r);         }     }  	    public static void main(String args[] ) throws Exception {                  BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringBuilder sb=new StringBuilder();            		String[] as=br.readLine().split(" ");   int n=Integer.parseInt(as[0]);int h=Integer.parseInt(as[1]); int a=Integer.parseInt(as[2]);int b=Integer.parseInt(as[3]);int k=Integer.parseInt(as[4]); for(int i=0;i<k;i++){String[] sr=br.readLine().split(" ");int t1=Integer.parseInt(sr[0]);int t2=Integer.parseInt(sr[2]);int f1=Integer.parseInt(sr[1]);int f2=Integer.parseInt(sr[3]);  	  if(t1==t2){System.out.println(Math.abs(f1-f2));	}else if((f1>b)&&(f2>b)){System.out.println(f1+f2-2*b+Math.abs(t1-t2));	}else if((f1<a)&&(f2<a)){System.out.println(2*a-f1-f2+Math.abs(t1-t2));}else{	System.out.println(Math.abs(f1-f2)+Math.abs(t1-t2));} 	  	}      }} 							  
